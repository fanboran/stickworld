# 组织系统深化：进度与交接

> **用途**：跨会话交接的单一入口。新会话恢复本任务时**从本文件开始读**，不重新摸底。
> **真相源**：[`docs/设计/系统/01-组织系统.md`](../../设计/系统/01-组织系统.md)（设计基线：通用项目管理系统，五层级框架，游戏最核心系统）；[`docs/项目/路线图.md`](../路线图.md) 阶段 1 第 5 项（本任务的路线图排期依据）。本文件只记进度与下一步。

---

## 快速恢复（新会话从这里开始）

- **分支**：`agent/organization-deepening`（基于 main `73b17f05` 建，任务唯一分支）
- **工作区**：`.temp/organization-deepening`（独立 worktree；主工作区留给 main，禁止在主工作区 checkout 本分支）
- **自检**：`bash stick-world/tools/check_godot_errors.sh`（退出码 1 必须修复）；测试 `bash stick-world/tests/run_all.sh`
- **当前阶段**：**立项，未开工**。首会话 = 设计先行——通读设计文档全文 + 摸底 `modules/organization/` 与既有消费方现状，产出批次拆分（Pro/Flash 分级）回填本档，经创始人确认范围后再动手实现。
- **任务分级调度**：沿用项目约定——**Pro** = 需设计判断 / 跨模块契约改动 / 算法建模；**Flash** = 规格明确、组件与模式现成、单模块机械实现。
- **通用验收（两级同规）**：`check_godot_errors` 干净 + `run_all.sh` 全绿（含既有测试零回归）+ 完成后回填本档「进度记录」+ 中文提交。
- **新会话开场白**：「继续 组织系统深化 批次 N」——该会话先读本档 + 设计文档指定章节，再动手。

## 任务边界（立项输入，范围待首会话裁决）

> 以下是立项时已知的输入，**哪些进本任务范围由首会话设计定稿**，不作为实施依据：

- 路线图阶段 1 排期项：`load_preset`/`export_as_preset` stub 补全、树状层级 UI、逐层指挥 AI。
- 既有底座（现状摸底起点）：
  - `modules/organization/`——api.gd 三信号（org_created/org_restructured/org_disbanded，**当前零消费方**）+ organization_manager.gd（集中制：状态注册 WorldState 容器，存档由 WorldState 统一序列化）；
  - 编制预设 `config/formations/presets.tres`（战斗班/建造队/劳工队/运输队，Tag.LOGISTICS）+ FormationPanel 编制管理窗口；
  - 指挥官任命链（`commander_assigned`）已被战斗侧 FormationSystem 消费。
- 创始人诉求关联（取舍待裁决）：部门不预设/组织涌现（设计文档 §2.3）、头衔承认检定（§8.2）、情报可见性（§8.1）等。
- 边界外：运输队职责消费（WORK_TRANSPORT 跨城运输）挂物流系统任务；本任务只做组织本体。

## 批次拆分（首会话设计后回填）

| 批次 | 级别 | 任务 | 依赖 | 验收门 | 新会话必读 |
|---|---|---|---|---|---|
| （待回填） | | | | | |

**依赖图**：（待回填）

## 进度记录（每批完成后回填）

| 批次 | 状态 | 提交 | 备注 |
|---|---|---|---|
| （待回填） | ⬜ 未开工 | — | |

## 关键决策速查（详表随设计定稿建档）

| 决策 | 结论 |
|---|---|
| （待回填） | |
