# 存储架构设计

> 本文档定义 stick-world 项目的数据存储分层、格式选择与最佳实践。
> 关联：`docs/技术/架构/场景与战斗架构.md`、`docs/项目/待办事项.md`（地图编辑器）

---

## 一、设计原则

1. **静态/动态分离**：只读资源打包进游戏，可写数据独立存档
2. **模板/实例分离**：建筑/单位定义是模板（.tres），游戏内建造是实例（存档）
3. **版本化存档**：存档带版本号，支持升级迁移
4. **数据驱动**：平衡数据走 xlsx→.tres 流水线，地图元数据走 .tres，运行时状态走存档

---

## 二、存储分层

### 层 1：静态资源层（打包只读）

| 数据 | 格式 | 说明 |
|------|------|------|
| 地形/装饰场景 | `.tscn` | Godot 原生可视化编辑 |
| 地图元数据 | `.tres` (MapDefinition) | ground_y/ratio/bounds/出生点/建筑槽位 |
| 建筑/单位模板 | `.tres` (从 xlsx 生成) | BuildingDefinition/StickmanDefinition |
| 平衡数据 | `.tres` (从 xlsx 生成) | balance_resource.gd |
| 动画/纹理 | `.tres`/`.png` | 美术资源 |

**原则**：打包后**只读**，运行时**绝不修改**。

### 层 2：存档层（运行时读写，玩家专属）

| 数据 | 格式 | 说明 |
|------|------|------|
| 游戏内建造状态 | JSON（推荐）/ SQLite | 哪些格子被占、建筑实例数据 |
| 实体状态 | 同上 | 火柴人位置/状态/朝向 |
| 资源/科技/组织 | 同上 | 玩家进度 |
| 地图探索状态 | 同上 | 已探索区域、Chunk 激活状态 |

**原则**：独立于地图场景，换地图/多周目可保留。

### 层 3：配置层（用户偏好，独立于存档）

| 数据 | 格式 | 说明 |
|------|------|------|
| 用户设置 | `settings.json` | 音量/键位/语言 |

---

## 三、存档格式选择

### 方案对比

| 维度 | JSON | SQLite | 二进制 |
|------|------|--------|--------|
| Godot 原生支持 | ✅ JSON 类 | ❌ 需第三方库 | ✅ FileAccess |
| 可读性/调试 | ✅ 高 | ❌ 需工具查看 | ❌ 低 |
| 查询能力 | ❌ 全量加载 | ✅ SQL 查询 | ❌ 全量加载 |
| 大数据性能 | ❌ 慢 | ✅ 快 | ✅ 快 |
| 关系数据 | ❌ 需手写 | ✅ 外键/索引 | ❌ 需手写 |
| 实现成本 | ✅ 低 | ❌ 高 | ⚠️ 中 |
| 玩家可编辑 | ✅ 是 | ❌ 否 | ❌ 否 |

### 推荐：JSON（阶段 P0-P2），预留 SQLite 迁移路径

**理由**：
1. 本项目存档数据量中等（建造状态 + 实体状态 + 进度），JSON 足够
2. Godot 原生 `JSON.stringify`/`parse_string`，无第三方依赖
3. 可读性强，便于调试和玩家手动备份
4. 关系简单（建筑实例引用模板 ID，无复杂关联）

**迁移触发条件**（满足任一则迁移 SQLite）：
- 单存档 > 5MB
- 需要存档历史快照（自动存档多版本）
- 需要复杂查询（如"所有铁矿建筑"）

---

## 四、存档数据结构（JSON 示例）

```json
{
  "version": 1,
  "save_id": "slot_01",
  "timestamp": "2026-07-13T15:30:00",
  "current_map_id": "test_village",
  "maps": {
    "test_village": {
      "buildings": [
        {
          "instance_id": "b_0001",
          "def_id": "house_wood",
          "cell_x": 10,
          "cell_y": 5,
          "width": 2,
          "height": 2,
          "health": 100,
          "built_at": 120.5
        }
      ],
      "entities": [
        {
          "instance_id": "e_0001",
          "def_id": "stickman_basic",
          "pos_x": 300.0,
          "pos_y": 666.0,
          "facing": 1,
          "current_anim": "idle"
        }
      ],
      "explored_chunks": ["chunk_0_0", "chunk_1_0"]
    }
  },
  "player_progress": {
    "resources": {"wood": 100, "stone": 50},
    "tech_unlocked": ["basic_building"],
    "organization_level": 1
  }
}
```

**关键设计**：
- 建筑实例只存 `def_id` 引用模板，不复制模板数据
- 按 `map_id` 分区存储，支持多地图独立
- `version` 字段支持升级迁移

---

## 五、常见毒点排查

| 毒点 | 后果 | 正确做法 |
|------|------|---------|
| 运行时修改 .tres | 打包后只读失败，数据丢失 | .tres 只读，运行时状态写存档 |
| 建造状态存进 MapDefinition.tres | 存档与地图绑定，换地图丢失 | 建造状态存存档层 |
| JSON 存二进制数据（贴图） | 存档膨胀 10x+ | 二进制资源单独文件，JSON 只存路径 |
| 无版本号的存档 | 升级后旧存档无法加载 | 存档带 version + 迁移函数 |
| 全局单存档 | 无法多周目/多角色 | 存档分槽位（slot_01, slot_02...） |
| 建筑实例复制模板数据 | 存档膨胀，改模板不生效 | 实例只存 def_id + 运行时状态 |
| 存档与场景节点混存 | 加载顺序耦合 | 存档是纯数据，加载时实例化场景 |

---

## 六、行业最佳实践

1. **Save/Load 分离**：存档是纯数据，加载时根据数据实例化场景节点
2. **ID 引用**：实例引用模板 ID，不内联模板数据
3. **增量存档**：大地图可按 Chunk 增量保存（减少写入量）
4. **存档校验**：加载时校验 version + 数据完整性
5. **异步加载**：大存档分帧加载，避免卡顿
6. **存档隔离**：每个存档独立目录，避免交叉污染

---

## 七、实现路径（待开发）

### P0（当前）
- [ ] 定义 SaveData 结构（GDScript Dictionary + JSON 序列化）
- [ ] SaveManager 扩展：save_game(slot_id) / load_game(slot_id)
- [ ] 建筑放置时写入存档（不修改 .tres）

### P1
- [ ] 存档版本迁移框架
- [ ] 多槽位存档 UI
- [ ] Chunk 增量存档

### P2
- [ ] 评估是否迁移 SQLite（基于实际存档大小）

---

## 八、地图编辑器插件需求（待设计）

### 目标
在 Godot 编辑器内提供自定义 Dock，辅助地图设计。

### 功能需求
1. **地图参数面板**：可视化设置 ground_y/ground_ratio/map_left/map_right/ground_bottom
2. **建筑槽位编辑**：在地图上放置/移除建筑槽位（预定义可建造位置）
3. **出生点编辑**：标记玩家/实体出生点
4. **Chunk 划分**：可视化 Chunk 边界，设置 Chunk 触发器
5. **导出**：生成 `MapDefinition.tres` + 修改 `.tscn`

### 技术方案
- `@tool` 脚本 + `EditorPlugin` + 自定义 Dock
- 复用 Godot 编辑器场景编辑能力
- 输出 `.tres` 资源（静态数据）+ `.tscn`（地形）

### 不做独立编辑器程序的理由
- 维护成本高，需独立项目
- 与 Godot 资源管线脱节
- 无法复用 Godot 编辑器能力
