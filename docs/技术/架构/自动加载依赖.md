# Autoload 依赖图

> 底层架构第五阶段：6 个核心 Autoload 服务的依赖方向、初始化顺序、职责边界。
>
> **摘要已迁移至 [`../../../stick-world/modules/README.md` §10 Autoload 依赖](file:///f:/VSCode/game-2/stick-world/modules/README.md#10-autoload-依赖)**。各章保留精简摘要 + 深度细节。

---

## 一、当前 Autoload 清单

> 完整清单（含 `DebugApi` 改名备注等当前状态）见 [README §10 Autoload 依赖](file:///f:/VSCode/game-2/stick-world/modules/README.md#10-autoload-依赖)。
>
> 当前 **9 个 Autoload**（实测 project.godot）：
> EventBus / ConfigManager / SaveManager / AudioManager / DebugApi（`modules/debug_GUI/api.gd`） / **WorldState** / **TimeManager** / **BalanceConfig**。
> 注：SceneManager 曾为 autoload，2026-08 因全库零引用已移除（场景切换由 world 模块 SceneLoader 承担）。
> 历史版本中的 `_mcp_game_helper` 已移除。

---

## 二、新增 Autoload 状态（已落地 3/4）

下表四项曾列为"建议新增"，实际状态：

| 名称 | 职责 | 状态 |
|------|------|------|
| `WorldState` | 运行时世界状态集中管理（所有实体状态） | ✅ **已实现并注册**（core/autoload/world_state.gd） |
| `TimeManager` | 游戏时间流速控制、自动暂停/减速逻辑 | ✅ **已实现并注册**（core/autoload/time_manager.gd） |
| `BalanceConfig` | 热加载平衡变量（从 config/balance/ 读取） | ✅ **已实现并注册**（core/autoload/balance_config.gd） |
| `InputManager` | 统一输入映射 | ⬜ 未实现（由 player_control/input_dispatcher 承担） |

> 以下三节（2.1~2.3）的代码示例为早期设计稿，实际实现已落地，代码以 core/autoload/ 为准。

### 2.1 WorldState

```gdscript
# core/autoload/world_state.gd
# 集中管理所有运行时实体状态

var stickmen: Dictionary = {}       # {id: StickmanState}
var organizations: Dictionary = {}   # {id: OrganizationState}
var regions: Dictionary = {}         # {id: RegionState}
var battles: Dictionary = {}         # {id: BattleState}
var projects: Dictionary = {}        # {id: ProjectState}
var supply_chains: Dictionary = {}   # {id: SupplyChainState}
var game_time: float = 0.0

func get_entity(entity_type: String, entity_id: String):
    # 统一查询入口
    pass

func register_module_save_data(module_name: String, get_save_fn: Callable, load_save_fn: Callable):
    # 与 SaveManager 协作
    pass
```

### 2.2 TimeManager

```gdscript
# core/autoload/time_manager.gd

enum Speed { PAUSED, X1, X2, X4 }

var current_speed: Speed = Speed.X1
var auto_pause_conditions: Array[String] = []   # ["battle_started", "commander_died", ...]
var auto_slow_on_possess: bool = true

func set_speed(speed: Speed):
    # 发射 game_paused / game_resumed 信号
    pass

func should_update(system_name: String) -> bool:
    # 各系统调用此方法判断当前帧是否需要更新
    pass
```

### 2.3 BalanceConfig

```gdscript
# core/autoload/balance_config.gd

# 从 config/balance/variables.tres 加载所有平衡变量
# 提供热加载：编辑 .tres 后无需重启游戏

var data: Dictionary = {}

func get_value(path: String):   # 如 "combat.base_hp"
    pass

func reload():
    pass
```

---

## 三、依赖图

> 模块依赖图（高层 ASCII 版）见 [modules/README.md §2](file:///f:/VSCode/game-2/stick-world/modules/README.md#2-模块依赖图)。本节仅保留**循环依赖检查**细节。

### 循环依赖检查

**结论：无循环依赖。**

- EventBus → 零依赖 ✓
- ConfigManager → 零依赖 ✓
- WorldState → 仅依赖 EventBus ✓（EventBus 不依赖 WorldState）
- TimeManager → 仅依赖 EventBus ✓
- AudioManager → 依赖 ConfigManager ✓（ConfigManager 不依赖 AudioManager）
- SaveManager → 依赖 WorldState ✓（WorldState 不依赖 SaveManager）
- BalanceConfig → 仅依赖 EventBus ✓

---

## 四、初始化顺序

> 完整 ASCII 流程图见 [README §10 Autoload 依赖](file:///f:/VSCode/game-2/stick-world/modules/README.md#10-autoload-依赖)。
>
> 原则：
> 1. 零依赖的先初始化（EventBus）
> 2. 数据层先于服务层（WorldState 先于 SaveManager）
> 3. 服务层最后（AudioManager）

---

## 五、职责边界

> 完整职责对照表见 [README §10 Autoload 依赖](file:///f:/VSCode/game-2/stick-world/modules/README.md#10-autoload-依赖)。
>
> 核心原则：每个 Autoload 单一职责（信号/存档/场景/音频等），不互相覆盖；不存其他 Autoload 负责的数据。

---

*下一阶段：平衡性调优框架。*
