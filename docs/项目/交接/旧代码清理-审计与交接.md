# 旧代码清理——审计与交接

> **一句话**：全面 HD-2D 化（第七~九轮）后，对仓库里残留的旧 2D 时代代码做一次全量审计，按「可立即删 / 随迁移逐步退役 / 明确保留 / 待裁决」四级分类，供后续清理会话照单执行。
> 审计基准：2026-09-14 main（全融合描边+碰撞地基化之后）。**删除一律走 git rm（Git 历史可找回）**；每删一批必须跑 `bash stick-world/tools/check_godot_errors.sh` + `bash stick-world/tests/run_all.sh`（当前基线：自检干净，41 通过 / 7 套 SUSPENDED 摘出）。
> ⚠ 主工作区可能混有其他会话的未提交改动——提交时按路径精确 add，不 `git add -A` 全仓。

---

## 一、A 级：已删除（本轮已清，勿重复动手）

| 对象 | 原用途 | 替代 |
|---|---|---|
| `modules/world/scenes/maps/village_a.tscn` | 旧 2D 出生村主场景 | hd2d_street |
| `modules/world/scenes/maps/village_b.tscn` | 2D 村B | hd2d_village_b（算法村） |
| `stick-world/tools/building_pipeline/`（整目录） | v2 Godot 2D 建筑管线 | v3 = tools/blender_buildings |
| `tools/baking/render_village.gd` | 拍旧村全景 | proto_hd2d --shots |
| `tests/dev/snapshot_town.gd` | 拍旧村村民 | 同上 |
| `tests/dev/proto_hd2d/proto_hd2d_ground.tscn` | --save-scene 探索产物（9.9MB） | 运行时构建 |

## 二、B 级：可立即删（审计为零生产引用）

| 对象 | 判定依据 | 备注 |
|---|---|---|
| `tests/dev/snapshot_demo.gd/.tscn` | 独立截图工具，仅自引用 GameRoot | 拍图走 verify_hd2d_map / --shots 即可；若创始人还想留通用截图工具则改挂 hd2d_street 保留 |
| `tests/dev/snapshot_harvest.gd`（及同类 snapshot_*） | 拍 2D 村采集场景（世界已删） | 确认 preload 无 village_a 残留后删 |
| `tools/terrain_viewer/` | 旧 2D 地形查看器；worldgen（战略图）有自己的预览 | 删前 grep 一次 tools/worldgen 确认无交叉引用 |
| `tools/baking/` 中 village/manor/interior 渲染类（`render_manor.gd`、`render_interior.gd` 等） | 拍旧 2D 建筑验收图；建筑与美术升级线已收官 | **逐个 grep 确认**不引用保留的烘焙项再删；`bake_anims/extract_weapons/inject_anim_events/boot_timer` 是火柴人动画/武器烘焙 **保留** |
| `tests/dev/crowd_probe.tscn`、`bar_probe.tscn`、`weapon_probe.tscn` 的**未跟踪 .png 产物** | probe 输出图不入库 | 只删产物图；probe 场景本身是火柴人调试设施 **保留** |

## 三、C 级：随 HD-2D 化逐步退役（生产链仍引用，删除前必须有替代）

按「替代就位→切链→删」的顺序执行，**禁止直接删**：

| 对象 | 现状引用 | 退役条件 |
|---|---|---|
| `battlefield.tscn` + battlefield 脚本链 | 注册+旅行链（主街西口↔战场）；**4 套战斗测试 boot 在它上面** | 战场图 HD-2D 化 + 测试迁移 |
| `road_a_b.tscn`（road_map_generator 等） | 主街↔村B 的道路图 | 道路 HD-2D 化 |
| `siege_battlefield.tscn`（守城/波次敌军） | 注册+守城入口选项 | 守城 HD-2D 化 |
| `forest_zone.tscn` | 战场右出→森林链 | 森林 HD-2D 化或砍链 |
| `mega_interior.tscn`（室内） | 注册+TravelHandler 绑定 | 室内系统裁决（见五） |
| `l1_settlement_00~07.tscn`（8 张） | 战略图 P5/D1 聚落入口 | L1 聚落 HD-2D 化（另有任务线归属，勿单方面动） |
| 2D 地图基础设施：`village_map.gd`、`town_siege.gd`、`resource_gen.gd`、`initial_buildings_list.gd`、ChunkTrigger 2D 地形节点（TerrainLayer/GroundPolygon） | 上述 2D 图全部依赖 | 上述图退役后整链删 |
| 建造/建筑 2D 系统（ConstructionManager、Building 场景、WorkSlots 宿主侧） | 建造循环/兵营/仓库玩法；7 套 SUSPENDED 测试的重建前提 | **玩法设施 HD-2D 化**（第八轮待办①）完成后：设施搬主街→测试重建→2D 侧裁撤 |
| 2D 环境效果：`sky_decor/fireflies/sky_stars/sky_birds/pond/weather` | 2D 图脚本挂载（CanvasModulate 体系） | 对应图退役后删；EnvironmentSystem 的 game_time 时钟**永远保留**（HD-2D 昼夜数据源） |
| `recruit_manager` 的 2D 依赖路径（barracks 扫描） | 招兵玩法暂不可玩（无兵营实体） | 玩法设施 HD-2D 化时接露天兵营卡 |

## 四、D 级：挂起测试（重建而非删除）

`tests/integration/test_{town_life_harvest,town_life_worksite,recruit_flow,construction_cycle,save_roundtrip,garrison_spawner,village_map}.tscn.suspended` + 同名 .gd（gd 内有 SUSPENDED 说明）。
**处置口径**：玩法设施 HD-2D 化后，在生产图（主街/村B）上重建断言，改回 .tscn 进矩阵；**不要删 .gd**——里面的用例逻辑是重建底稿。

## 五、待创始人裁决（清理会话不要自作主张）

1. **mega_interior 室内系统**：B 类室内 4 地标提案（待定档）——是做 HD-2D 室内还是整系统退役。
2. **forest_zone / siege**：战斗链 HD-2D 化优先级，还是先砍支线聚焦主循环。
3. **l1_settlement 8 图**：归战略图任务线，HD-2D 化排期由那边定。

## 六、明确保留（易误删清单）

- `tools/blender_buildings/` 全部（v3 主力：装配器/材质/city_layout/export_city_layout）
- `tools/baking/` 的动画/武器烘焙（bake_anims/extract_weapons/inject_anim_events/boot_timer）
- `tools/worldgen/`（战略图生成端）+ `check_godot_errors.sh` + `diff_png.py`
- `assets/resources/*_paint_*` 贴图池（2D ResourceNode 在用，HD-2D 图上视觉虽隐藏但节点仍挂载）
- `assets/_raw/建筑/smithy.png`（v3 风格基准参考图）
- `tests/dev/` 的火柴人 probe（crowd/bar/weapon）、`verify_hd2d_map`
- `modules/environment/scripts/environment_system.gd`（game_time 时钟=HD-2D 昼夜数据源）

## 七、执行纪律

1. 一批一提交：`chore(cleanup): 删 <清单>`，路径精确 add。
2. 每批后自检+全量测试，绿了再进下一批。
3. 删 C 级任何一项前，先在本文件该行打勾登记（改完随批提交）。
4. 全部 C 级清完 → 本文档移入交接归档；A/B 级清完即结。
