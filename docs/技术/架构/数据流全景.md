# 数据流与存储方案

> 底层架构第三阶段：信息在各系统间的流动路径 + 数据持久化选型。
>
> **摘要已迁移至 [`../../../stick-world/modules/README.md` §7 数据流全景 + §8 存储分层](file:///f:/VSCode/game-2/stick-world/modules/README.md#7-数据流全景)**。各章保留精简摘要 + 深度细节。

---

## 一、数据流全景

> 完整数据流图（玩家→组织→战斗/科技/建设→资源→运输→扩张）见 [README §7 数据流全景](file:///f:/VSCode/game-2/stick-world/modules/README.md#7-数据流全景)。
> 三流详解（命令下发 / 经济自动调节 / 信息上报）见同节。

---

## 二、核心数据流详解

> 详见 [README §7 数据流全景](file:///f:/VSCode/game-2/stick-world/modules/README.md#7-数据流全景)：
>
> - **命令下发流**：玩家选"虎贲师" → "进攻北方行省" → 组织创建 MilitaryCampaign Project → 分解子 Project（师→团→营→连→排）→ 各层 Organization → Stickman → Battle
> - **经济自动调节流**：铁矿石需求↑ → 资源系统检测供需 → `price_changed` 信号 → 商业组织调商队路线 → 价格回落 → 循环自稳定
> - **信息上报流**：L1 排长 AI 检测前方大量敌军 → 根据 `autonomy_level` 决定（自主撤退/求援 vs 上报连长）→ `org_efficiency_changed` 反映在组织效率指标 → 玩家在帝国概览看到"虎贲师·第三团·第二连·士气下降"

---

## 三、数据存储方案

> 完整 5 层存储（静态配置 / 关卡数据 / 程序化产物 / 运行时状态 / 存档）见 [README §8 存储分层](file:///f:/VSCode/game-2/stick-world/modules/README.md#8-存储分层)。
>
> 详细存储架构（静态资源层 / 存档层 / 配置层）见 [`../存储架构设计.md`](../存储架构设计.md) §二。
> 存档格式选型（JSON vs SQLite）见 [`../存储架构设计.md`](../存储架构设计.md) §三。

### 3.5 大规模数据的性能策略

> README §8 未完整吸收此节（仅简单提"极远 LOD 3"），保留作为深度参考。

后期帝国 10000+ 火柴人的运行时处理：

| 距离玩家视口 | 模拟方式 |
|-------------|----------|
| 近（LOD 0） | 完整 AI + 完整渲染 + 每帧更新 |
| 中（LOD 1） | 简化 AI + 简化渲染 + 每 N 帧更新 |
| 远（LOD 2） | 抽象数值模拟 + 不渲染 + 每 M 帧批处理 |
| 极远（LOD 3） | 仅保留聚合统计数据，通过统计公式推算 |

**实例化策略**：当玩家缩放到某个远距离区域时，该区域的火柴人从 LOD 3 的"统计数字"**现场生成为具体实例**。特性（名字、性格、装备）在生成时随机分配——但存档中不存这些远距离单位的个体数据，只存聚合统计（人口数 + 种族比例 + 平均能力值）。

---

## 四、模块间通信规则

> 完整通信准则（同步 vs 异步选择表 + 禁止事项）见 [README §6 模块间通信准则](file:///f:/VSCode/game-2/stick-world/modules/README.md#6-模块间通信准则)。
>
> 规则要点：
> - ✅ 允许：模块 A → EventBus 信号 → 模块 B / 模块 A → `api.gd` → 模块 B（同步调用）
> - ❌ 禁止：模块 A 直接访问模块 B 的内部脚本 / 跨模块信号形成循环
> - 同步调用 vs 异步事件选择：查询用同步，多接收方级联用异步
>
> EventBus 完整 8 大类信号分类见 [README §9](file:///f:/VSCode/game-2/stick-world/modules/README.md#9-eventbus-信号分类)。
> EventBus 完整信号参数表（发射方/接收方/触发条件）见 [系统交互与EventBus.md §二](../系统交互与EventBus.md)。

---

*下一阶段：模块 API 规范。*
