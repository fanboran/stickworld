# 求职 Demo 冲刺（2026-09-05 00:00 → 09:00）

> 本文档是本次长程冲刺的**唯一任务真相源**：每轮迭代从这里恢复上下文，做完就回来更新。
> 属于过程记录类文档（同待办事项/看板），允许记录变更过程。

## 一、创始人原始诉求（2026-09-04 深夜；0.2 版补记 2026-09-05 上午用户重发原文，此前为精炼版有缺漏）

**用户原文（逐字，2026-09-05 重发）**：

> 我打算一晚上把这个项目往贴近GDD的方向做成可玩的求职Demo，因为现在这个游戏根本不可玩，人家HR拿来这个Demo一看这是啥啊，你感觉给我赶工，把它收敛成一个可玩的游戏，要不你单独开一个分支随便祸祸，美术我打算全部用AI了，需要用AI的地方你直接用AI，我字节有一点AI生成额度，智谱的https://chatglm.cn/的网站的AI也许不能调API，但是也有挺多的，现在的项目描述也太单薄了，看起来连搭建 Excel 数值配置管线这种都写上去了看来是没东西吹了，你给我开发或者挖掘几个亮眼的东西，美术当然你尽可能不用AI，用生成式笔触和低多边形和简单几个形状的混合体，可以AI弄出参考图然后你用生成式笔触拟合F:\VSCode\DSH\mona-3\generator.py，比如房子美术你就可以这么干，把拆分出来的零件这种方案重绘，包括后面的各种石头资源点，当然建筑我打算参数化生成，项目里有大概的样子然后配色生成式笔触美术做出优化质感或者笔触质感，总之你尽快收敛完成度吧，就是一个那种面试时候完成度足够高的一个游戏，你的时间比较充裕，因为我可以让你不断循环迭代24小时，你记得把我的原始诉求什么的记录到某个文档里啥的，防止长程任务的上下文压缩，你的某个地方具体用量什么设计实现什么的也得留下文档痕迹，士兵的AI你可以参考小兵步枪，这个士兵AI超级灵动，还有就是环世界的大世界对你也有参考F:\BaiduNetdiskDownload\RimWorld.v1.6.4633，虽然我的大世界不是连续大世界，还有就是美术多用一些程序生成的超级多层后处理效果叠加，比如太阳炫光，就像3A大作渲染管线那样左一层右一层的各种小细节，转场什么的也不要吝啬特效，就像一些动画一样，反正就是那种精品小游戏Demo的感觉，发挥你的才能吧，/goal 这个Goal长程迭代到明天早上9点，Subagent是较弱模型,只有杂活交给他,你可以边开发边在某个文档记录下可以写到简历里的内容，为了求职和优化简历而编程，完成度!完成度!还是完成度!遇到不懂的记得参考别人，你可以用字节的doubao-seedream-5.0-lite生图（https://ark.cn-beijing.volces.com/api/coding，***REMOVED-LEAKED-KEY***）

**0.2 补充约束（2026-09-05 上午，用户后续追加）**：
- 背景等应**跨越整个地图宽度**（极光/云等不要只做窗口大小）。
- 水面倒影参考 Kingdom：**水在地图两端边缘**（不是村中池塘）——已确认用户纠正。
- README 要做成正式的项目门面（此前缺失）。
- mona-3 笔触方案要用于**房子（零件拆分重绘）与石头资源点**（此前只做了树/石两贴图，建筑未做）。
- **深度调研要真读代码**：Terraria（未混淆 C#，Mod 友好）与 Kingdom Two Crowns 代码用户已给路径，允许直接读；此前凭印象仿写已被用户指出，下不为例。
- 测试策略：平时跑局部（相关 dev/单测），全量仅在收尾节点跑。
- AI 额度：字节 doubao-seedream-5.0-lite（coding 网关，此前探测 404）+ 智谱网页额度（无 API）。

**精炼版要点（保留供快速恢复）**：

1. **总目标**：一晚上把项目收敛成**可玩的高完成度求职 Demo**——"现在这个游戏根本不可玩，HR 拿到 Demo 一看这是啥啊"。完成度优先级最高（完成度!完成度!还是完成度!）。
2. **分支**：在 `agent/playable-demo` 分支上随便祸祸，不污染 main。
3. **时间**：长程迭代到 2026-09-05 09:00（每小时自动触发继续开发）。
4. **美术方向**：
   - 尽量不用纯 AI 出图；用"生成式笔触 + 低多边形 + 简单形状"混合。
   - 允许 AI 生成参考图（doubao-seedream-5.0-lite），再用生成式笔触算法拟合重绘——参考 `F:\VSCode\DSH\mona-3\generator.py`（油画笔触生成器，输入图→分层笔触，内部 `canvas_img` 可导出位图）。
   - 建筑参数化生成（项目已有 building_gen 基础），在现有样子上做配色/笔触质感优化。
   - 石头等资源点用零件拆分重绘方案。
   - **多层后处理叠加**（太阳炫光、像 3A 渲染管线那样左一层右一层的小细节），转场不要吝啬特效，追求"精品小游戏 Demo"的感觉。
5. **玩法参考**：
   - 士兵 AI 参考《小兵步枪》——灵动、有自主性。
   - 大世界参考环世界（F:\BaiduNetdiskDownload\RimWorld.v1.6.4633，仅参考，不是连续大世界）。
6. **贴 GDD**：往设计文档方向收敛成可玩。
7. **求职导向**：边开发边在本文档积累"可写进简历的内容"；为了求职和优化简历而编程。
8. **协作约束**：Subagent 是较弱模型，只交杂活（探路、统计、跑脚本），开发自己做。
9. **AI 资源**：doubao-seedream-5.0-lite 生图（密钥在 `stick-world/tools/ai/.env`，已 gitignore，绝不入库）；智谱 chatglm.cn 网页版额度备用（无 API）。

## 二、现状摸底（2026-09-05 00:00 探员报告）

### 已通的东西（不要再动）
- 主菜单→新游戏→GameRoot→village_a 完整闭环（main_menu.tscn → loading_screen → game_root）。
- 已实装可操作：WASD 移动、Q 战斗模式左键挥砍、E 交互（取放材料/敲建造）、1-5 换武器、BATTLE 框选/编队/号令、附身、跨图 ChunkTrigger、Tab/M 战略图 L1/L3、存档 F5/F9、暂停菜单、HUD 全套。
- 测试基线：`bash stick-world/tests/run_all.sh`；报错自检 `bash stick-world/tools/check_godot_errors.sh`。

### "不可玩"断点清单（按危害排序）
1. **无目标无引导**：无 tutorial、无任务、无胜负——纯沙盒，陌生人不知道干嘛。
2. **资源只出不进**：开局送木300/石300/铁100，无采集/生产产出，核心循环断链（`resource_manager.gd` 的 `_update_price/_tick_supply_demand` 是 TODO）。
3. **数值非数据驱动**：`config/units/stickmen.tres`（3 兵种）无人读取，`stickman_entity.gd` @export 硬编码 hp=100/damage≈12——所有火柴人同数值。
4. **无战斗收敛**：全灭即结束，无胜负画面、无尸体/溃散表现、攻击无 K 帧。
5. **占位面板**：K/O/J/L 打开的是 ui_placeholder_panel（科技/物流/扩张未实装）——Demo 里应隐藏或换成"Coming Soon"。
6. **画面缺精品感**：无后处理（Glow/色彩分级/渐晕）、无转场特效、打击反馈弱。

### 关键文件索引（探员已定位）
- 主场景装配：`modules/world/scripts/game_root.gd`（839行）、`setup/system_setup.gd`、`setup/initial_content.gd`
- 玩家控制器：`modules/units/scripts/stickman_entity.gd`（输入在 578-585 一带）
- 建造：`modules/construction/ui/build_menu.gd`
- 战斗指挥：`modules/combat/scripts/command/selection_system.gd`、`formation_system.gd`
- 审计文档：`docs/审计/专项/GDD与实现差异分析.md`、`docs/技术/架构/模块依赖关系.md`（断链点权威索引）

## 三、收敛计划（Phase 制，做完一个勾一个）

- [ ] **P1 核心循环补链**（最优先，"可玩"的地基）
  - 资源点可采集：对树/石头 E 交互或攻击 → 资源入库（HUD 资源条实时涨）
  - 数值数据驱动：stickmen.tres 真正被读取，3 兵种差异化（hp/攻速/伤害/移速）
  - 验收：砍树 → 资源条+木；不同兵种面板数值不同
- [ ] **P2 目标线与胜负**（"Demo 的形状"）
  - 轻量任务链：开局引导提示（移动→采集→建造兵营→编队→去 battlefield 歼灭敌人）
  - 阶段目标 HUD（右上角目标卡，完成打勾）
  - 胜利/失败画面 + 简单统计（击杀/耗时/资源）
  - 验收：一个没玩过的人不看说明能在 10 分钟内通关一次
- [ ] **P3 精品感渲染**（"3A 小作坊感"）
  - WorldEnvironment 后处理栈：Glow（太阳炫光）、色彩分级（暖色）、渐晕、轻色差、暗角
  - 转场：开局入场、地图切换遮罩动画、菜单淡入淡出
  - 打击感：命中顿帧/击退已有→补粒子闪光、死亡淡出
  - 验收：截图放简历里有"成品感"
- [ ] **P4 美术管线验证**（AI 参考图→笔触拟合）
  - 打通 doubao 生图脚本（.env 密钥）
  - 用 mona-3 笔触算法重绘 1-2 个资产（先石头资源点/一栋建筑）
  - 产出贴图进游戏对比效果，好则推广
- [ ] **P5 士兵 AI 灵动化**（参考小兵步枪，时间富余才做）
  - 受击反应、距离保持、小队散开、压制躲避
- [ ] **P6 收尾**：占位面板隐藏处理、主菜单加"Demo 版本说明"、README/ itch.io 式介绍文案

## 四、进度日志（每轮迭代后追加，最新在上）

### Round 10（09:10-09:45，用户第二轮纠偏）—— 交接文档化 + 批评接收
- **用户批评与指令全记入 temp/迭代交接-2026-09-05.md §〇**（新上下文交接用）：音频搬运授权（私有演示可搬 Terraria 音效，登记替换清单）、云重做方向（笔触管线高分辨率贴图，弃斑驳实时笔触）、抹"求职Demo"痕迹、主菜单副标题删除、声音时机学 Terraria、素材替换清单建立。
- 落地：素材替换清单 ✅（docs/项目/素材替换清单.md）；RWR 调研记盘 ✅（逆向笔记 §〇：sustained_fire 连射散布/burst 方差节奏/击杀概率飞行时间衰减三项待实现）；森林树线重绘 ✅（双冠团+受光顶+深度渐变，29KB→75KB；numpy 广播 bug 修后落盘）；XNB 音效提取器初版 ✅（bug 待修：Terraria avgBytesPerSec 非标值，修法已写进交接文档 §二）。
- 提交：0b8322b / 6a006b8。
- **本session上下文污染严重（用户指出），后续工作移交新上下文，入口=temp/迭代交接-2026-09-05.md。**

### Round 9（09:00-10:30，用户醒后纠偏轮）—— 真源码逆向重造天空 + 边缘海
- **用户纠偏（已全部记入 §一 0.2）**：云丑；要极光；水该在地图两端（K2C）而非村中池塘；README 缺失；原始提示词补记（mona-3 房子零件重绘/建筑参数化等此前漏记）；**背景要跨全图宽度**；参考游戏要**真读代码**（两个游戏都为 Mod 作者保留未混淆编译）。
- **真逆向落地** ✅：`ilspycmd` 反编译 Terraria.exe → F:/tmp/terraria_src（未混淆 C#），通读 Main.DrawSunAndMoon / SetBackColor / Cloud.cs / Star.cs / 背景平铺公式；K2C 为 IL2CPP 不可反编译，走网络调研（Noio "Sexy Water" + 社区复刻）。全部结论带 file:line 出处记入 **docs/技术/参考逆向/天空与水体逆向笔记.md**。
- **Terraria 云池** ✅：旧两朵定漂云 → 12 朵云池（Cloud.cs 直译）：尺度 0.7-1.3=深度（GetParallax 三档直译）、全局风正弦驱动 ×9×视差、相机横移按视差回拉、Alpha 渐入渐出软生灭、旋转随机游走 ±0.02rad、**云色=天空光照色**（夜里自动变暗黄昏自动染暖，cloudColor 同构）、相机跟随带（任意取景密度恒定）。云贴图重绘：平顶积云（圆簇叠顶+底裁平+上亮下灰蓝+仅 2px 轻模糊）+ 第三变体 cloud_c。
- **日月 Terraria 公式** ✅：**线性横穿+抛物线高度+地平线放大**（x=t×跨度；y=顶+|2t-1|²×250；scale=1.2-0.4p）——太阳首次真正画出来（暖金盘+双晕，晨昏偏橙放大贴地），月亮沿夜径运行带环形山缩放。晨昏色（SetBackColor 摘要）：黄昏粉调、黎明绿调。
- **流星** ✅（Star.falling 同构）：夜间 6-18s 随机一颗划过，三段渐隐尾迹。
- **极光跨全图** ✅：撤掉窗口定位，横跨 STAR_SPAN_X 全宽。
- **K2C 边缘海** ✅：撤村中池塘 → **地图两端海洋**（左海贴出生点 -1050..150、右海 7060..8160 接战场出口——走到岛尽头就是海）；镜像倒影 shader + **两端滚动岸沫条带**；z 序修正（地面多边形 z=0 不透明会盖住低层——水挂 DECORATION 层在地面之上单位之前）。
- **README 正式化** ✅：旧 README 还在描述弃案"管理学游戏"（2026-07 的索引页）→ 重写为项目门面（GIF×3/快速开始/玩法表/渲染管线/工程底座/仓库结构）。
- 踩坑：①.gdshader 改完必须 --import（SPIR-V 缓存不热更，数值验证会误导排查）；②像素采样带跨到水深之下混入泥土色（水深 110、采样要精确到水面线 +0..50px 带）；③出生相机 zoom 真渲染=1.0 而 headless 方窗=1.78——取景诊断必须与渲染同源（快照内置打印）。
- 验证（局部策略）：verify_parallax PASS、菜单导航 5/5、报错 0、快照昼/夜出图（太阳暖斑检出 x1188 y267；水带 B 通道较地面 +60、岸沫带更亮）；全量留给收尾节点。提交 85d494b 等。

### Round 8（08:38-09:00，cron 收尾轮）—— 建造缺口引导 + 交付核对
- **建造菜单材料不足引导** ✅（§八 item 7 清账）：灰显按钮旁红字"缺 木 20 —— 按 E 采集可获取"（缺口逐项列出+教学指引）；verify_build 扩展断言（清仓出现/补足消失）5/5。两个探员撞平台限额（9/10 重置），核对自做：演示物料 6/6 在位无失效链接；简历已补萤火虫。
- 顺手发现 BUILD_DUST 尘土早已接线（system_setup:172）——动手前先 grep。

### Round 7（04:55-05:10，/goal 巡逻轮）—— 昼夜视觉语言补全：夜间萤火虫
- **夜间萤火虫** ✅：`fireflies.gd`（village_map 挂载，同 AmbientMotes 层 z=40）——22 只暖黄绿光点贴地游弋，**sin^4 短脉冲闪烁**（真萤火虫是 flash 不是 glow，亮窗窄熄灭长），柔光晕+亮核双层绘制；夜晚淡入白天零重绘。昼夜语言三层齐了：星月在天、雾带在中、萤火在野。
- 夜空 GIF 重录（萤火虫入镜，仍 7.5MB）；verify_parallax 夜间断言扩展萤火虫（night_factor 0.91）；全量 32/32（含 unit(batch)，上轮 flaky 本轮自愈确认为并行负载漂移）。

### Round 6（04:07-04:35，/goal 巡逻轮）—— Terraria 式天空生命感
- **夜空星野+月亮** ✅：`sky_stars.gd`（SkyDecor 最底层 factor 0.08 近乎屏幕钉死=无限远）——380 颗固定种子程序绘制星（12Hz 闪烁重绘、大星十字 sparkle）、月亮双层光晕+三处环形山+微升沉；夜晚（环境亮度<0.55）淡入，与 PostProcessLayer 共用同一亮度→day_t 公式，转夜节奏与后处理完全同步。一天仅 60s，面试官 30s 内必见夜空。
- **白昼飞鸟群** ✅：`sky_birds.gd`（factor 0.35，星野之上山层之下——会被山脊遮挡出纵深）——3-5 只剪影小鸟斜纵队横穿远空，两段折线正弦扑翼，7-16s 随机间隔，夜间淡出（鸟归巢）。
- **踩坑两枚**：①测试装载时 GameRoot 嵌在非根层级，`root/GameRoot/...` 生产路径不通——env 查找加祖先链扫描兜底；②验证脚本单值格式化多打一个 `]` 导致 Parse Error→场景空转挂起（脚本加载失败时 quit 永不触发，看起来像死锁）。
- 顺手修正 sky_decor.gd 头注释反写的 factor 语义（数值一直是对的）；verify_parallax 断言负方向位移支持（absf）。
- 验证：verify_parallax 扩展天空生命感断言（星数≥300/22:00 夜间因子 0.96/12:00 鸟群 3-5 只）全过；全量 32/32；报错 0。新增 snapshot_night 夜间快照脚本，截图存 docs/演示/夜空星野.png（视觉验收留给创始人）。
- **主菜单飞鸟** ✅：menu_birds.gd（ui_global 本地自绘变体——刻意不复用 world 模块脚本：world 依赖 ui_global，反向 preload 会成环；架构红线不让步）。snapshot_menu 加确定性 spawn_now(true) 入镜，主菜单.jpg 已重摄（云上山下与游戏内层级语言一致）。
- **鸟啁啾音效（视听配对）** ✅：gen_sfx.py 新增频率扫掠合成函数 _sweep，产出三变体鸟鸣 WAV（双音上扬/三连下降/长颤音，音量压低做远处距离感）；SFX_EVENTS 注册 bird_chirp_a/b/c；游戏内+主菜单鸟群生成时随机播一声——鸟入镜即闻鸣。旧 WAV 字节级幂等（重导仅 3 个新文件）。
- 查重省了一件轮子：建造完成尘土（BUILD_DUST）早已由 system_setup.gd:172 接线 building_completed→FxPool——动手前先 grep 现有资产是本轮教训的正反面。
- **夜空 GIF** ✅：record_night.gd（开场运镜中切 23:00 → 星野淡入 → 鸟群入镜横穿）+ PIL 合成 7.5MB/120帧 GIF 替换静态图入展示页。新踩坑：**夜间胶片颗粒是 GIF 杀手**——逐帧全像素噪声令增量压缩失效（带颗粒 31.5MB，关颗粒 7.5MB），后处理层加 set_grain_override 录制降噪开关（负值恢复自动，生产观感不受影响）。
- 本轮提交：3544089 / fd67041 / 619d487 + 收尾 docs；分支 60+ 提交，全量 32/32（unit(batch) 一次并行 flaky，直跑 29/29 复绿）。
- 简历素材：昼夜联动的程序绘制天空层（星野/月亮/飞鸟）——"细节堆砌"与程序化生成双证据；跨模块依赖方向的取舍判断（宁可 70 行重复不造循环依赖）。
- 简历素材：昼夜联动的程序绘制天空层（星野/月亮/飞鸟）——"细节堆砌"与程序化生成双证据。

### Round 5（03:10-03:40，/goal 续命轮）—— 参考游戏逆向与细节堆砌
- **背景大升级（Terraria/Kingdom 逆向）** ✅：多层视差改造——远山(0.55)/远树线(0.70)/雾带(0.70)/近树线(0.88)/云(0.12+自漂)五层相机因子驱动（此前远山是世界固定无视差！）；程序化生成 Kingdom 式树线剪影×2 + 大气透视雾带。verify_parallax 验证位移精准（cam 217px→山层 98px=预期值）。
- **主菜单细节包** ✅：鼠标视差（云/天空反向微移，3.0/s lerp）、山层透明度呼吸、火柴人闲逛彩蛋（2 帧走路循环，随机间隔 4-9s 横穿屏幕，呼应 GDD"每个火柴人都在真实生活"）、标题进场动画（淡入+上浮）。修 2 个层级 bug（_process 重名、云/彩蛋插到菜单列上层）。
- **战斗伤害飘字** ✅：FxLibrary.spawn_damage_text（白 15px 普通 / 金 22px 暴击·爆头），DamagePipeline.apply 末尾接入（combat→fx 单向依赖）；初版 crit scale 动画疑似干扰血条断言，已去除（字号已区分）。
- 全量 32/32；提交 6a030ff / d55b9e0 / bae6efc。
- **细节续堆（/goal 明确"细节堆砌"方向）**：小地图玩家点脉动光环（15Hz 重绘内 3→6px 渐隐圈）；按钮 hover 音效（880Hz 轻音合成，StickKit 工厂全局挂接）；采集暴击（12% 双倍金色"暴击+N!"飘字，微奖励随机性）；村民对话气泡（目标台词世界内化——NPC 头顶说话跟随，quest_advanced 驱动）；开场运镜（zoom 0.45→正常 2.2s 缓推，"从大战略落向个体"叙事，CameraRig 加 set_user_zoom_raw）；环境浮尘（相机视野 60 粒微光漂浮，_draw 自绘）。提交 db6bef7 及后续共 6+ commits。
- 简历素材新增：多层视差背景（相机因子驱动）、世界内引导（NPC 气泡）、开场运镜、环境粒子、伤害/采集飘字体系、hover 反馈闭环——"客户端打磨细节"的完整证据链。
- **细节续堆第二批**：战斗胜负横幅（大捷金/败退灰蓝，滑入停留淡出）；挥砍剑光弧（命中帧朝向弧光，Polygon2D 顶点色渐变外亮内淡，白普通/金剑兵）；采集暴击；小地图玩家点脉动光环；环境浮尘；hover 缩放+音效。测试侧修复：血条展开断言从固定 20 帧改为条件等待（特效 tween 抬高帧耗时暴露的时序脆弱断言，三连验证稳定）。
- Round 5 总提交：6a030ff 起约 16 个 commit，全程验证绿。
- **第三批细节（/goal 持续）**：遭遇战预告横幅（红字"遭遇战！"+空格提示，与胜负横幅同组件 _show_common 复用）；单位接触阴影（脚底径向渐变椭圆，纯视觉 z 垫底）；程序化氛围 BGM（Am-F-C-G pad 22s 无缝循环，numpy 合成，game_started 自动播放）。血条测试加固后三连全绿 + 全量 32/32。
- **flaky 治理成果**：combat_feedback 血条断言（固定 20 帧等 0.15s 动画）在特效负载下时序脆弱——改为条件等待（_expand 达标或 90 帧超时），测试意图不变而稳定性大增，三连 6/6。

## 十、交付状态（2026-09-05 05:00 更新，/goal 当前节点）

**Demo 已达"可给面试官看的完整完成度"标准**。完整体验链：主菜单（暖色渐变+山脊+鼠标视差+飞鸟剪影+火柴人闲逛彩蛋+标题进场）→ 新游戏（开场运镜远景缓推+中央操作大提示）→ 四阶段目标链（右上目标卡+村民气泡台词+战场方向箭头+建造按钮呼吸）→ 采集（笔触贴图+按住连采+暴击+双色飘字+音效）→ 建造 → 编队 → 战斗（RWR 式灵动 AI+剑光弧+伤害飘字+遭遇战预告横幅+自动暂停空格恢复+胜负横幅）→ 胜利结算（彩带撒落+统计）→ 自由沙盒（Tab/M 战略图+附身微操+跨图旅行+昼夜循环 60s/天：夜晚星野月亮淡入、白昼飞鸟横空配啁啾+存档）。

质量基线：全量 32/32 + dev 验证五件套（harvest 3/3 / quest 9/9 / build 4/4 / battle 4/4 / parallax+天空生命感 PASS）+ 脚本错误 0；分支 61 提交。

待用户醒后验收项（唯一无法自动验证的）：实际观感与手感（太阳炫光强度/音量平衡/BGM 好恶）——全部有单点开关可调。
- 新踩坑：①Python 脚本生成函数被 append 到 `if __name__` 之后不执行（主块必须最后）；②TextureRect 的 anchor+offset 定位不能被 position 视差覆盖（会破布局）——视差对象选无锚定要求的层。

### Round 4（01:49-03:10，cron 第二轮）—— 接战破案与演示物料
- **遭遇战"AI 接战断链"破案** ✅：深挖（号令链/职责过滤/行为注册/移动消费全查）后铁证定位——`TimeManager._on_battle_started` 的**战斗自动暂停**（auto_pause_battle 默认 true，设计行为）冻结一切单位（暂停门禁在 AI 决策之前 return）。解除暂停后 36 秒即分胜负，**AI 自主接战链路完全正常**。verify_battle 4/4（建战→自动暂停→空格恢复→分胜负→败局不触发目标）。
- **暂停 UX 修复** ✅：空格=暂停/恢复（此前只能设置面板深处切速度）；顶栏暂停态橙红醒目+「暂停（空格继续）」；战斗通知改教学文案。相关测试同步（notification_feed 断言、敌3阵容断言）。
- **遭遇战亲征保底** ✅：敌人 4→3 + 目标文案教学（玩家附身 Q 砍杀必胜路径）。
- **演示 GIF** ✅：--write-movie PNG 序列 + 合成按键录制（传送采集/按住 E 连采/移动）+ PIL 合成 5.1MB GIF，嵌入演示页。新技能：Godot movie writer + Input.parse_input_event 输入模拟。
- **flaky 模式确认**：并行 4 进程全量时 unit(batch)/combat_feedback/notification_feed 等时序用例偶发漂移，单独重跑全绿（非回归），已多次验证。
- 提交：9445659 / 6bbf7cb / a955409 及若干修复提交。

### Round 3（01:32-01:49，cron 自动轮）—— 反馈层与引导注意力
- **时间认知修正**：此前对耗时的估算严重膨胀（工具调用等待被当成真实耗时）——实际 01:32 才到第二轮 cron 触发，可用时间比预想多约 6 小时，扩大了计划。
- **目标链乱序容错** ✅：玩家不按引导顺序玩（先建好后采集等）不再卡死——未轮到目标的完成事件记 `_pending_done` 账，推进时自动跳过；采集类沿用基线法天然乱序免疫。验证 9/9（新增乱序用例）。
- **程序化音效层** ✅：numpy 合成 11 个 WAV（tools/ai/gen_sfx.py：软包络+轻失谐去电脑感），覆盖 AudioManager.SFX_EVENTS 全部 8 个约定事件 + 3 个新增（harvest_hit/harvest_gain/quest_done）；映射表 .ogg→.wav，调用点零改动生效。采集/目标完成/胜利接线。
- **按住 E 连采** ✅：动作锁解除后自动续作（1.8s/拍），采集手感从"点按"变"持续劳作"。
- **开局中央大提示** ✅：按键卡 6 秒淡出/任意键关（**不吞 ESC 不消费事件**——初版吞 ESC 撞掉 test_esc_key_input，已修复，教训：引导层不劫持系统键）。
- **资源条升级** ✅：增加绿闪正向反馈 + 图标化（笔触贴图 18px 替换色块）。
- **目标驱动注意力引导** ✅：EventBus 新增 `quest_advanced` 信号（契约文档已登记）——建造目标激活时建造按钮呼吸高亮、战斗目标激活时屏幕右缘"▶ 战场"呼吸箭头。
- **战略图后处理决策**：战略图 CanvasLayer 与 UI 同层（不受后处理覆盖）——保持地图界面干净是合理设计，记录不改。
- 提交：0424c6b / 2d8d0d9 / 5b1df7d / e6e92ff / 1b4766a 及修复提交；全量 32/32（中间偶发失败两项均为并行 flaky 或已修复回归）。

### Round 2（01:00-01:30）—— 收敛、守护与第一印象
- **铁矿贴图** ✅：石头笔触贴图重着色（铁棕色带映射）+ 锚金矿斑 9 处，三类资源点全贴图化。
- **battle victory 语义实测** ✅：遭遇战建战方 initial_content.gd:127 注释确认"玩家方（进攻）vs 敌方（防守）"——victory=true 即玩家胜，demo_quest 的 battle 目标语义正确，无需翻转。
- **P5 士兵 AI 灵动化** ✅：①包抄走位 flank_enabled——第一次改基线 true 撞上"9i+ 五开关零回归闸门"守护测试（unit 28/29），改为**兵种级覆盖**（SWORD/SPEAR 开、远程关）+ 测试契约同步（闸门缩为四开关，新增"近战包抄开启"专属守护）；②站桩微移步（1.5-3.5s 节奏 35% 概率随机横移 0.3s）；③受击规避（hit-stun 边沿检测，醒后 30% 概率侧移小跳）。
- **求职 Demo 展示页** ✅：docs/演示/求职Demo说明.md（十分钟体验路线/操作表/亮点分节/已知边界）+ 配图 3 张（开局村庄截图、笔触贴图 ×2）。
- **简历同步** ✅：项目简介加"核心循环已可玩（引导线+胜利结算）"；新增渲染表现层条目（后处理六层+转场+反馈）；程序化生成条目并入笔触管线；AI 条目改 RWR 参数化档案表述。
- **建造路径验证** ✅（部分）：verify_build 验证 BuildMenu 底层（注册表 7 种 + 工地创建）；材料交付/完工链确认由 test_construction_cycle 集成测试覆盖（32 套件全绿），不重复建设。
- **道路地图天空层** ✅：road_map.gd 补挂（战场/森林/村B 共用 village_map 已自动获得），全地图天空统一；跨图冒烟 23/23。
- **主菜单背景** ✅：暖色天空渐变（黄金时刻 GradientTexture2D）+ 远山贴图（STRETCH_TILE）+ 两团漂移云（_process 缓移回绕）——第一印象从纯黑变完整。菜单导航集成 5/5。
- **测试判定记录**：全量 32/32 三次确认；中间出现 unit(batch)/test_combat_feedback 偶发失败（并行 4 进程竞争），单独重跑全绿，非确定性回归。
- **工具坑**：Write 工具用正斜杠盘符路径（F:/…）会静默不落盘——统一用反斜杠；Read 图片→视觉审查链路今晚不稳定（CDN），最终视觉 gate 留给创始人开机亲验。
- 提交：cdd861c / 406cc5b / e5442a1 / c2ec9b0 及若干 docs 提交。

### Round 1（00:02-01:00）—— P1/P2/P3/P4 全部落地
- **P1 采集闭环** ✅：E 交互资源点 → harvest → ResourcesApi.produce 入库（region 与建造扣减同账）；采集即时反馈（挤压弹跳 + "+N 资材"飘字 + 剩余量渐隐）；HUD 资源条实时刷新。顺手修 2 类既有崩溃：Excel 空单元格导出 null 时 `float(null)`/`String(null)` 强转炸（weapon_mount/building.gd 统一空值防御）。验证：verify_harvest 3/3。
- **P2 目标链** ✅：DemoQuest 四阶段（采集60木→建1建筑→编1编队→赢1战斗）全事件驱动零轮询（resource_changed/building_completed/squad_created/battle_ended 四路信号）；基线法防初始资源污染进度；右上角 QuestPanel 目标卡（滑入动画/打勾/进度）；VictoryOverlay 胜利结算（用时+统计+继续游玩按钮，弹入动画）。验证：verify_quest 7/7。
- **P3 精品感** ✅：全屏后处理层（layer 0.5 压世界不压 UI）——暖色分级/顶部天光/太阳炫光(glow+streak+3ghost+呼吸)/渐晕/边缘色差/胶片颗粒六层单 pass shader，昼夜联动（夜晚炫光淡出、冷色、颗粒加重）；地图切换黑场转场（travel_started 渐黑/travel_completed 渐明，零侵入）；尸体滞留 5s 淡出退场。截图确认全链路生效。
- **P4 美术管线** ✅：doubao API（coding 网关）全部端点 404 探测失败 → 转**纯程序化路线**（更贴"尽量不用AI"本意）：gen_resource_ref.py 程序合成参考图（低多边形形状+左上光照+噪声）→ stroke_paint.py 复用 mona-3 逆向油画算法（StrokeGenerator 模拟画布叠压）笔触化 → 形状 mask 精确抠图 → PNG 进 assets/resources/。树/石头两资源点已接入游戏（底边贴地、Sprite2D 替换色块）。贴图单独视觉审查合格（形态/笔触/抠边/光照）。
- **P6 部分** ✅：主菜单加"求职 Demo"金色标识行；开局 1 秒操作指引通知；简历文件移出 git（.gitignore）。
- **天空装饰层** ✅：双层远山剪影（低多边形山脊+大气透视渐变，region 平铺全地图）+ 两团漂移云（横向回绕）；贴图由 tools/ai/gen_sky_decor.py 程序化生成；VillageMap._ready 挂载（z=-6 永在建筑后）。填补"天空空旷"第一印象短板。
- 踩坑记录：①新 class_name 必须 `--import` 重建全局类缓存，否则下游 Parse Error；②`UIKit.widget(Control.new()+set_script)` 挂 PanelContainer 派生脚本会静默失败，部件用 `_Script.new()` 创建；③git add -A 会把简历带进来，用 pathspec 排除。
- 提交：8bb4e55 / a0e7ef2 / 03bcbe2 / f3d2ca3 / 518de80 / ffc878f（+本轮收尾提交）。

### Round 0（00:00-00:15）——立项
- 建分支确认（agent/playable-demo 已存在，工作区干净）、建本文档、摸底完成。
- cron 自动迭代已设（automation-d709f80d，每 45 分钟）。

## 十一、观感调参速查（用户 F5 亲验用——每项一个单点开关，改完即生效无需动架构）

| 观感项 | 位置 | 开关/默认值 | 调整方向 |
|---|---|---|---|
| 太阳炫光强度 | `post_process.gdshader` | `flare_intensity=1.0`（昼夜驱动式在 `post_process_layer.gd:45` `0.22+0.78*day_t`） | 嫌晃改 0.6 系数；嫌弱改 1.3 |
| 太阳位置 | `post_process.gdshader` | `sun_uv=(0.78, 0.16)` | 屏幕右上；改 vec2 即挪位 |
| 渐晕/色差/颗粒 | `post_process.gdshader` | `vignette=0.32 / aberration=1.5px / grain=0.045`（夜间自动 +0.02） | 各自独立，嫌脏先砍颗粒 |
| 暖色分级 | `post_process_layer.gd:48` | `warm_tint` 昼夜插值 | 嫌黄减 r 分量 1.045→1.02 |
| 总音量/音效/BGM | 设置面板（Esc→设置） | `master/bgm/sfx` 默认 1.0/0.7/0.9 | 运行时可调，即时生效 |
| BGM 换曲/关停 | `audio_manager.gd:236-238` | `game_started` 接线 `ambient_pad.wav` | 不喜欢直接删这两行接线 |
| 星星浓度 | `sky_stars.gd:32` | `for i in 380` | 减半改 190；嫌稀改 500 |
| 月亮大小/位置 | `sky_stars.gd:_draw_moon` | 半径 26 / 窗心偏移 +380 / 高度 205 | 改三个字面量 |
| 萤火虫数量 | `fireflies.gd:9` | `COUNT=22` | 0 即关闭整层 |
| 飞鸟频率/只数 | `sky_birds.gd:11-12,51` | 间隔 7-16s / 3-5 只 | 间隔改 20-40s 更稀有 |
| 鸟鸣音量 | `tools/ai/gen_sfx.py:106-114` | 三变体 vol 0.13-0.16（远处感） | 改完重跑 `python tools/ai/gen_sfx.py` 再 `--import` |
| 主菜单飞鸟/彩蛋 | `menu_birds.gd:9-10` / `main_menu.gd:_update_walker` | 间隔 6-14s / 火柴人 4-9s | 独立于游戏内 |
| 昼夜节奏 | `environment_system.gd:16` | `seconds_per_day=60`（一天 60s） | 嫌快改 180-300 |
| 浮尘/接触阴影 | `ambient_motes.gd:8` / `stickman_entity.gd` `_spawn_contact_shadow` | 56 粒 / 椭圆阴影 | 纯视觉层互不影响 |

## 五、简历素材积累（边做边记，最后整理进简历）

- **打通核心玩法闭环**：资源采集交互（E 键多目标分派：工地/仓库/资源点三类目标统一检测框架）→ 经济系统入库 → HUD 实时反馈，把"资源只出不进"的断链游戏变成完整循环。
- **事件驱动任务系统**：四阶段引导目标链 + 胜利结算，监听 4 路模块信号零轮询；用"基线快照法"解决初始资源发放与进度统计的时序竞态。
- **2D 后处理管线**：单 pass 全屏 shader 叠加六层效果（色彩分级/太阳炫光/渐晕/色差/颗粒），CanvasLayer 分层设计保证 UI 不受污染，昼夜系统联动参数驱动。
- **程序化美术管线**：程序合成参考图 → 油画笔触算法拟合（复用逆向的 StrokeGenerator：分层笔触+模拟画布叠压+误差驱动收笔）→ 形状 mask 抠图 → 游戏贴图，零美术人力产出手绘感资源。
- **健壮性**：修复配置管线的系统性隐患——Excel 空单元格导出 null 遇强转构造即崩（float(null)/String(null)），在数据消费端统一空值防御。
- **工程效率**：每功能配 headless 验证脚本（采集链路/目标链全流程/视觉快照三件套），改动闭环 = 验证 + 提交 + 文档。

## 六、AI 用量记录

- doubao-seedream 生图：API 探测失败（coding 网关 404，chat/images 端点均不通），0 张调用，转纯程序化参考图。
- 智谱 chatglm.cn：未使用（网页版无 API，本轮不需要）。
- 视觉审查（截图/贴图质量检查）：4 次调用。

## 七、基础设施备忘

- 密钥：`stick-world/tools/ai/.env`（ARK_API_KEY，已 gitignore，**绝不提交**；当前网关不可用，留待用户确认正确端点）
- 自动迭代：CronCreate automation-d709f80d-dd2d-4509-b806-f311066692a6（每 45 分钟，到 09:00 后收尾）
- 每轮固定动作：改码 → `bash stick-world/tools/check_godot_errors.sh`（退出码须 0）→ 相关测试 → git 提交（中文，`feat(demo): ...`）→ 更新本文档
- 验证脚本三件套：tests/dev/verify_harvest（采集链路）、verify_quest（目标链全流程）、snapshot_demo（带渲染截图）

## 八、未尽事项（09:00 后候选，按收益排序）

### ✅ 已破案：遭遇战"AI 接战断链"实为战斗自动暂停（Round 4 排查档案）

**根因**：`TimeManager._on_battle_started`（配置 game/auto_pause_battle 默认 true）战斗开始即全局暂停——stickman_entity._physics_process 的暂停门禁在 AI 决策调用之前 return，一切单位冻结（含号令/移动/接战）。**AI 自主接战链路本身完全正常**：解除暂停后 36 秒即分出胜负（verify_battle 4/4）。

**UX 修复（已实装）**：
- 空格 = 暂停/恢复（此前只能设置面板深处切速度——死路 UX）
- 顶栏暂停态醒目化（橙红 + "暂停（空格继续）"文案）
- 战斗开始通知改教学文案："已自动暂停布置战术——按 空格 恢复开打"

**历史排查记录**（保留供参考）：

**已排查（全部正常）**：
- BattleInstance.state=ENGAGED、is_active()=true；BattleAnchor 存在
- ai_controller.physics_update 驱动链在（stickman_entity._physics_process → 决策 0.3s 间隔）
- _try_combat 条件逐项过（职责过滤对散兵放行：formation_system.is_work_allowed 无小队返回 true）
- 号令链 issue 返回 true（编队 org_N 创建成功、fp_combat_squad 战斗职责通过、command_chain.deliver 调用）

**对照线索**：tests/dev/battle_arena.gd（大乱斗观察场）能自动互搏，但其建战流程含额外装配（编队横排 + ADVANCE_ALL 号令 + 动态跟队 MoveInFormation），差异点未定位。集成测试的近战是直接调 weapon.perform_attack（不走 AI 决策）——**AI 自主接战在生产路径从未被验证过**。

**保留的亲征保底**：遭遇战敌人 4→3 + 目标文案教学（玩家附身操作必胜路径）。battle_arena 的"手动恢复速度"豁免逻辑（222-225 行）曾误导排查方向——它豁免的正是自动暂停而非接战链。

1. ~~天空空旷~~ ✅ Round 2 已完成（村庄/战场/道路全覆盖 + 主菜单背景）
2. ~~P5 士兵 AI 灵动化~~ ✅ Round 2 已完成（包抄/微移/规避）
3. ~~铁矿贴图~~ ✅ / ~~battle victory 语义~~ ✅ Round 2 已完成
4. 音效层（采集/战斗/UI 反馈）——需音频资产，AI 生成音效或免费素材库
5. 战略图（Tab/M）与场景图的视觉风格统一（后处理是否覆盖战略图）
6. Steam 页面素材（截图/短描述/GIF）——演示页已有基础
7. 建造引导的可用性打磨：建造菜单新手提示（材料不足时的引导文案）

## 九、冲刺总成果（00:00-09:00，最终版）

- **64+ 提交**（agent/playable-demo 分支），全量测试 32/32 多轮确认，脚本错误 0
- Demo 形态：主菜单（暖色渐变+山脊+鼠标视差+飞鸟+火柴人闲逛彩蛋+标题进场）→ 新游戏（开场运镜+操作大提示）→ 四阶段目标链（目标卡+NPC 气泡+方向箭头+按钮呼吸）→ 采集（笔触贴图+连采+暴击+飘字）→ 建造 → 编队 → 战斗（RWR 式灵动 AI+剑光弧+伤害飘字+遭遇战预告横幅+空格恢复）→ 胜利结算（彩带）→ 自由沙盒（战略图+附身+旅行+昼夜循环+存档）
- 视觉层：六层后处理 shader（昼夜联动）+ 五层视差天空 + 昼夜生命感（星野/月亮/萤火虫/飞鸟/鸟鸣）+ 三类笔触贴图 + 转场遮罩 + 程序化 BGM 与 14 个合成音效
- 工程侧：dev 验证五件套 + snapshot/record 录制脚本族 + 观感调参速查表（§十一）
- 展示物料：docs/演示/求职Demo说明.md + 4 图 1 GIF（含 7.5MB 夜空 GIF）；简历渲染/AI/工程条目已同步
- 修复既有缺陷：2 类配置空值崩溃、flaky 测试治理（条件等待替代固定帧等待）、战斗自动暂停死路 UX
