# 待办事项

> **⏳ 当前进行中**：兵种 AI 复刻批次，**计划与执行口径见 [`AI复刻执行计划.md`](AI复刻执行计划.md)**。2026-09-03 P5/P6/P7 完成（P7 = 祭司 Meric 兵种+治疗），**覆盖率 53%→55%（含近似 57%→59%）**，详见计划 §三。同日三条并行开发线收线合并回 main：配置线（BalanceConfig 自装载）、战略图线（聚落 tooltip/粒度指示/视图互斥）、UI 线（设置项真实生效+通知堆叠 feed），合并后全量测试绿。下一项 P8（批次 7a 巨人）；观察场缺陷余 9h/9j/9k/9s，见计划 §二 P1。

> **已完成条目不留在本档**：完成即迁入 [`待办事项-已完成.md`](待办事项-已完成.md)（历史完成记录已批量迁出至此）。

### 建筑动态长度：三段式拼接卡管线（2026-09-16 创始人立项，独立批次）

> 背景：现行卡片为离散宽度档（4/8/12/16，v3 立项当天 early spec，从未重审），接不上 2D 时代"玩家造任意格数、建筑动态拉伸"的建造操作（placement_grid/build_menu/Building.rebuild_exterior 那套）；且国家级宏伟建筑愿景 = **半屏宽（≈40 格=960px）、两三层高**，现 w16 上限连一半都不到。创始人拍板走三段式拼接卡，并预警：不限三段，特殊结构需特型段，整体复杂度高，必须独立批次。

- [ ] 烘焙端：装配器拆"端头 + 可重复中段"双段出卡（山墙/角部细节进端头，重复开间进中段；中段天然对接现有 bay 开间体系）；特型建筑（塔/穹顶/山墙正立面等）允许特型段表
- [ ] 引擎端：hd2d_world 拼卡器——按玩家造的格数（4 的倍数，上限 40 格=半屏）以"左端头 + N×中段 + 右端头"拼 quad 条带，檐口/基线对齐
- [ ] 建造端：build_menu/placement 接动态宽度（拆除 2D 卡渲染路径与 3D 拼卡的断层，玩家在 HD-2D 街上造出的就是 v3 卡建筑）
- [ ] 宏伟档：palace/cathedral/abbey/town_hall 等加 w32/w40 半屏档（两层~三层横躺式巨构，宽度纪律仍 4 的倍数）
- [ ] 试验体：abbey（大修道院，左塔右翼楼）先行三段化验证，再铺开
- [ ] **每 def 模数规格**（创始人 2026-09-16 补充）：动态宽度 = 各自模数 N 的整数倍，N 按建筑结构特性与拆分单元大小定（中段 = 一个完整结构单元：一榀屋架 + 一组开间窗/扶壁距，如村舍 N=4、仓库 N=6、修道院翼楼 N=8）；宽度区间 [min, max] 按种类定——上限防止"村舍拉成宫殿"的种类失真（村舍 max≈12、仓库 max≈32、修道院/宫殿 max≈40 半屏），min 沿带门下限≥6；规格落 def 数据表，建造菜单按 [min..max] 步进 N 出选项
- [ ] **动态层高**（创始人 2026-09-16 补充）：结构强度允许即可做 2~3 层（层数也是 def 规格字段，受结构/种类约束）；层数联动**建筑属性与材料消耗**（面积/层高/层数 → 造价、维护、容量、强度），属性与材料消耗系统与动态尺寸同一批次一起做

### 道路生成单测存量债（main 侧既有，2026-09-11 合并征服分支时发现，非合并回归）

> 证据：`test_road_walk.gd` 与 `road_map_generator.gd` 在 main、conquest-loop 分支、合并树三处逐字节一致，失败在纯 main 上同样复现。

- [ ] `test_road_walk` 结构用例（EntityHost/道路本体带/出口触发器缺失）+ seed 确定性零断言：生成器代码字面创建这些节点，实例化后丢失——疑似主侧架构装配收敛（四层结构/MapBase 改动）破坏了道路场景模板的子节点序列化（pack owner 或实例期重构），需架构会话排查
- [x] 宽度断言已对齐 MIN_WIDTH=2500 下限（本次修）
- [x] `test_fx_damage_text` 断言已对齐 c6919002 减半字号（本次修）

### 全量测试假失败与稳定失败（2026-09-14 审计实测 41/49：7 假失败 + 1 真失败）

> 审计快照：[`../审计/代码审计_2026-09-14.md`](../审计/代码审计_2026-09-14.md)。7 个 integration 套件 `.tscn` 已悬置为 `.tscn.suspended`（设施类待 HD-2D 重建），`run_all.sh` 清单仍登记 `.tscn` 且不识别悬置后缀——空跑加载失败计 FAIL，每次全量固定产出 7 个假失败，淹没真信号。真失败仅 `test_cross_map_travel` 2 断言（「村落B: HD-2D 村实例」「反向旅行: 玩家在右侧入口」）。

- [ ] `run_all.sh` 识别悬置后缀（跳过缺 `.tscn` 或 `.suspended` 套件并输出 `[SKIP]`）+ `tests/README.md` 补悬置条款（审计 §三 运行器项）
- [ ] `run_all.sh` bash 进程级挂死排查（2026-09-16 发现）：`-Changed` 在起 Godot 前即卡死、进程树无 godot 子进程，多日不退（本机存有 9/14 18:13、9/15 14:54、9/15 17:37 三个未退出实例，多会话并行下还在堆积）。临时规避 = 按套件直跑：`"$GODOT" --headless --path <工程> res://tests/…tscn -- --fresh-start`
- [ ] `test_cross_map_travel` 2 断言修复（「村落B: HD-2D 村实例」「反向旅行: 玩家在右侧入口」，归属 HD-2D 线收口）
- [ ] `test_melee_combat` 配剑挂载不稳定（2026-09-17 排查，非改速回归——stash 基线复现）：headless fresh-start 下「配剑: 右手挂载占位剑」断言同树连跑结果轮转（一次过、一次败；败时连带「挥砍: 武器旋转变化」失败），疑加载时序相关；新鲜 worktree（无 `.godot` 缓存）上两次均 180s+ 超时零断言输出（挂死形态，疑与 unit(batch) 同根因）
- [ ] `test_selection_formation` 框选 3~4 断言失败（2026-09-17 排查，stash 基线复现非本批回归）：「矩形内单位被选中 / 追加选择 / 单击最近单位（/ 阵营过滤，轮转）」在基线与改动树同败

### unit(batch) 批跑失败集轮转（2026-09-16 排查，非合并回归）

> 现象：同一份代码多次运行 `run_all.sh -Match unit(batch)`，`[BATCH-FAIL]` 集在两次运行间轮转——主工作区报 `test_suppression`（伴随 `-s` 编译错误 `Identifier not found: TimeManager`，指向 arrow_projectile.gd:121；该引用来自 main 既有提交 08eca519，且 TimeManager 注册合法、tscn 全量套件同引用正常），合并前后双环境交叉复测时又分别轮换成 `political_data/political_mesh/townlife_save_persistence`、`map_camera_clamp/river_parse/settlement_entry`。子套件断言层全绿（3/3、7/7、13/13…），失败发生在脚本加载/编译层。已排除：性能批次（agent/perf-pareto）合并回归——失败集与代码版本无关、只与环境/时序相关；编辑器启动模拟报错自检 EXIT=0。

- [ ] batch_runner.gd 载入机制排查：`load()` 链式编译下 autoload 标识符（TimeManager 等）在 `-s` 无实例环境的解析条件——为何同一测试在并行 3 不同槽位/缓存状态下结果轮转（疑与 `.godot` 全局类缓存/uid 缓存的热态有关）
- [ ] 并行 3 的实例隔离：3 个 Godot 实例是否竞争写 `.godot` 缓存与 `user://` 目录（候选修复：每实例独立 `--user-data-dir`，或批内串行化）
- [ ] 修复后回归验证口径：同一提交连续 3 次 `-Match unit(batch)` 失败集为空，再并入全量门禁

### 火柴人视觉修复（遗留可选项，已完成批次详见交接档）

- [ ] **创始人观感验收**后收线：合并回 main + 归档交接档
- [ ] **描边对比度微调（风格决策，可选）**：描边-身体对比 ~1.5:1 属"亮边/轮光"意图；若要更强形体边界，描边微降暖灰（~0.92）或身体微降（~0.76）提至 ~1.8:1
- [ ] （远期）极端 >15× 缩放机位若实装，头部多边形提边数（`_make_circle_at` 边数一行改动）
### UI 运行时架构三项优化（2026-09-11 立项设计，方案详见 [`../技术/架构/UI运行时架构优化方案.md`](../技术/架构/UI运行时架构优化方案.md)）

> 三批独立可合入可回退；A→B→C 三批全部实施完毕（分支链 `agent/ui-pause-primitive` → `agent/ui-hud-zones` → `agent/ui-button-variants`，链尾统一合 main），观感验收待创始人。

- [x] **A 暂停原语化**（2026-09-11 实施完毕，观感验收待创始人）：引擎 `SceneTree.paused` + process_mode 分层做总闸；TimeManager 退为速度控制（`sim_delta` 单一入口，顺修倍速不作用于昼夜）；删各系统 is_paused 自查。实施要点：暂停期快捷键经 game_root.tscn 内 ShortcutGate（ALWAYS）转发；分层声明表与三个「暂停期仍要跑」例外节点见方案档 §二
- [x] **B HUD 布局收权**（2026-09-11 实施完毕，观感验收待创始人）：UIRoot zone 注册表（`hud_zone_layout.gd`，锚定+偏移自适应，fill/dock/stack 三模式）+ `place_in_zone`；六角部件（minimap/资源条/任务卡/时钟组/缩放条/通知流）全部迁移，`UIAPI.HUD_*` 布局常量退役，部件内自算定位清零；debug 构建画 zone 合同框；`top_left_stack` 堆叠区即多任务线落点（双验收卡实测逐件下移无重叠）；三档分辨率自适应实测过
- [x] **C 按钮变体系统**（2026-09-11 实施完毕，观感验收待创始人）：SketchStyle 静态变体表六档（DARK/PAPER/PRIMARY/ACCENT/DANGER/ICON_SQUARE）+ icon_mode；SketchButton 一次查表应用（`_apply_font_colors` 条件分支删除）；逐屏切换（主菜单 PAPER/PRIMARY、设置/模板选中态 ACCENT、HUD 顶栏七钮切 SketchButton）；调用点按钮视觉 override 全库清零；bg_alpha 并入变体字段

### 昼夜氛围与任务 UI 演进（2026-09-11 创始人反馈，昼夜已调 10 分钟/天）

- [ ] **建筑夜晚自发光**：窗户/门头夜间点亮（贴图 swap 或 modulate 曲线，挂 environment_system 昼夜相位）；铁匠铺火焰自发光（火焰贴图提亮 + PointLight2D 补光，参考既有 _ensure_camp_light）
- [ ] **NPC 昼夜节律**：日程驱动（白天劳作/入屋/夜晚归巢），接 town_life 职业系统与建筑室内锚点
- [ ] **任务卡多任务线扩展**：QuestPanel 现为单卡（左上，顶栏下方），多任务线时改左侧可滚动任务列表（按线分组）；DemoQuest 的 show_quest/mark_done 接口需扩为多线并行

### 出征与领地循环（P0 可玩循环收口，2026-09-07 立项）

> C1~C7 全部完成（批次 1~6 收线，循环已由 `test_conquest_e2e` 等套件锁死），完成记录迁 [`待办事项-已完成.md`](待办事项-已完成.md)；交接档 [`交接/出征与领地循环-进度与交接.md`](交接/出征与领地循环-进度与交接.md)。

- [ ] （P1 预留）据点反扑→守城战接线（territories.retaliation）/ 邻接解锁树 / 敌将增兵成长 / 战略图双击出征

### 守城战 Demo

> 本节「首版闭环 / 地平线优化 / 合并回 main」三条已完成，迁 [`待办事项-已完成.md`](待办事项-已完成.md)。
- [ ] **Demo 包体瘦身到 300M 以下**（2026-09-17 打包实测立项）：Windows 导出首测 **1010M**，其中 `temp/` 验收临时产物 365M、`docs/`（符号链接把验收图当资源）31M 被误打进包、`.godot/imported` 孤儿缓存 356M——已修（exclude_filter 补 `temp/*`、`docs/*` + 清孤儿），现 **613M**（exe 105M + pck 506M）。pck 剩余大头：战略图 `config/strategic_map` ≈334M（l1_packs 贴图 185M + l2_packs 61M + 烘焙 bin 113M）、`modules/hd2d` 贴图 96M、音频 47.5M、字体 17.6M。瘦身路线【提案】：① 战略图 l1/l2 贴图导入改 lossy WebP/VRAM 压缩；② Demo 只带所需区域的区域 pack（l1 30 份/l2 9 份现为全量）；③ 烘焙 bin 上 zstd；④ hd2d 贴图同①；⑤ Demo 曲目裁剪 + 字体子集化。做全估算 pck 180~250M，叠加 exe 压线进 300M（再走 zip/安装器分发则稳）。
- [ ] **后续打磨**：波次报幕（battle_banner 泛化后接"第 N 波"）、守城胜负结算（撑波数战绩）、路带加车辙/草斑、墙垛口贴图三变体混排、敌人攻门行为（现有门洞纯视觉）、弓箭手箭矢抛物线越垛口
### AI 复刻覆盖率审计 → 新批次（2026-09-01 审计立项，方案详表见 [`AI复刻执行计划.md §三`](AI复刻执行计划.md)，按 P1~P13 顺序执行）

> 本节已完成批次记录（11b / P5 / P6 / P7 / P9 / battle_sim 治疗动画修复）迁 [`待办事项-已完成.md`](待办事项-已完成.md)；P9 物品栏系统的未完成余项如下：
  - [ ] **武器 stats 数据化应用**：ItemDef.stats 的 attack_mult/speed_mult/armor_penetration 只入数据未应用（战斗数值由 BalanceConfig 校准管，避免打破 battle_sim），待 P5 数据化批次接线
  - [ ] **拾取/掉落**：战利品实体 + 地面物品 + 拾取交互
  - [ ] **商店购买**：原版 InGameShop 形态，经济系统接线时做
  - [ ] **背包存档接线**：to_dict/from_dict 已就绪，SaveManager 玩家数据段挂链验证
  - [ ] **背包拖拽交互 + 整理按钮**：UI 打磨批次
- [ ] **11e 玩法依赖簇挂总账缓**：ZombieAi（Pounce/Kai 召唤）、StatueAi/BarricadeAi、CastleArcher 簇、MinerAi 路障/互殴——依赖对应玩法实装
- [ ] **9 余项**（见计划 §二 P1）：9h 卡死复验 / 9j 脱战回血（MOBA 延迟血条）/ 9k 群体避让+空挥保真（含 9s）
- [ ] **P7 祭司翻译缺口与已知限制**（2026-09-03 登记）：
  - [ ] **MericHeal / MericDeath 特效资源名**（P10 消费）：本批治疗表现 = 动画 + 血条回升，特效资源名待 P10 特效扩容时补
  - [ ] **meric-staff 贴图裁剪工具链**：`extract_weapons.gd` 未覆盖 meric 图集 → 首版复用 Magicstaff 贴图占位，待裁剪工具链扩展后换真贴图
  - [ ] **祭司 wiki 面板值补真值**：P5 同源渠道 namu wiki Normal 面板值，一次性校准 `heal_amount`/`heal_cooldown`/`heal_range`/HP/价格/站位参数（当前全为语义推断初值，标注待实测校准）
  - **已知限制（非缺陷）**：bone3~6 装饰骨保持默认姿势（L2 降级无 Spine 源数据，维持不映射）；治疗冷却与 HOT 现实秒混源（暂停期继续流动，与既有 DOT 同源特性）

### SWL 全模块对账 → 补充立项（2026-09-01，全景统计见 [`AI复刻执行计划.md §四`](AI复刻执行计划.md)）

> SWL 游戏代码 363 类型 / 35 命名空间，游戏性模块 14 个。除 AI（§三）外的对账结论与立项：

- [ ] **12 特效扩容**（新批次，~1 轮）：fx 模块对照原版 Effects 16 类扩容——**DirectionalBlood 方向血溅 / ExplosionScorch 爆炸焦痕（兼召唤/爆炸地面标记）/ GroundSlam 震地 / Rain+Cloud 天气**；战斗观感大头
- [ ] **Spell 基类先行**（并入计划 4）：原版 15 法术 = Spell 基类（冷却/耗金/施放动画/效果统一结构）+ 14 具体法术；ArrowVolley/HealSpell 挂基类实现，其余按需
- [ ] **InGameShop/HudInventory 并入批次 10 物品栏**：原版战场商店+物品条 UI 形态参考
- [ ] **模式变体低成本低挂**（Alpha 后）：EndlessDeads 无尽尸潮 / SuperDeathMatch 超级乱斗 → 观察场变体
- [ ] **缺失兵种实体决策**：CastleArchers（城堡弓手）/ Statue+MainStatue（雕像=SWL 胜利目标，可抽象为"战争目标建筑"）/ King / GiantBoss / ConversionChannel——依赖大世界玩法定位，需创始人拍板
- [ ] **不复刻**：平台服务（IAP/广告/云同步/成就/AB 测试 ≈40 类型）、Levels 关卡制（战略图路线替代）、UserInterface 159 类（UX 流程按需参考）

> **项目级待办追踪**。按优先级排序，完成即删除。
>
> **唯一待办源声明**：本文档是项目级待办的**唯一入口**——需要创始人决策的事项、
> 工程流程决策、跨模块协调都在这里记；各专项总账（审计、缺口对账等）追踪各自领域的
> 明细工作项，经下方职责边界表挂载到这里，不另设平级待办文档。

---

## 职责边界（与其他文档解耦）

本文档只记录**项目级**待办：需要创始人决策的设计待定项、工程流程决策、跨模块协调。
**不重复**以下文档已追踪的内容，仅指向：

| 职责 | 文档位置 | 内容 |
|------|---------|------|
| 代码级技术债（bug/隐患） | [`技术/架构/场景与战斗架构.md`](../技术/架构/场景与战斗架构.md) §六 | P0-1~P0-11（PossessionInterface 装配、Chunk 空壳、EnterTrigger 缺失等） |
| **模块依赖/实现状态真相** | [`技术/架构/模块依赖关系.md`](../技术/架构/模块依赖关系.md) | GDD↔代码对照、断链点索引、战斗链路特写 |
| 架构级待补充（设计/数值/实现） | 同上 §五 | 兵种/村落/建筑/数值/存档等待补全项 |
| P0 阶段实施顺序与完成状态 | 同上 §四 | 阶段 0.1~0.9 的子任务清单 |
| 战略图工作项 | [`技术/架构/世界地图数据流.md`](../技术/架构/世界地图数据流.md) §八 | 生成端/导入端/消费端待实现清单 |
| **复刻缺口总账**（专项总账挂载） | [`审计/总账/翻译缺口总账.md`](../审计/总账/翻译缺口总账.md) | 动画/骨骼/事件/皮肤四层对账 + 机制/操作层缺口，新缺口先补总账再立项 |
| 阶段里程碑规划 | [`项目/路线图.md`](路线图.md) | 阶段 0~4 的目标与验收标准 |
| 开发进度追踪 | [`项目/项目进度.md`](项目进度.md) | 当前完成到哪 |

---

## 当前状态

- **2026-09-04 待办全量对账**：对全部未勾选条目逐条 grep/文件核对——勾销 15 条（T1-T13 批次修复回写：存档 SQL 参数化与 schema 版本化、combat⇄units 契约化、texture_gen 回归测试、战略图 UI 三件套、存档面板/读档兜底等；实测过时：HealthComponent 出生即死、数据存储方案选择题、科技状态字段漂移；世界生成 L1 链与 0.9a 产物核实已落地）；Excel 导出管线条目刷新为现状（9 文件 15 sheet 已映射、导出器已硬化）；其余条目按"仍有效/待创始人决策"保持 `[ ]`。

- **2026-09-09 待办对账（挂账待办 + 低优先级/封印项专项）**：4 路并行对代码逐条核对——勾销/结项 10 条（守城战合并 main、0.9b 聚落生成器、政权/聚落生成简化版、战场垂直跨度、出城机制重构、横躺 90° 结案、图标接入 UI、尸体处理、天体云遮挡拍板、游戏内快捷栏接线），改写 20 余条（各条带「2026-09-09 对账」注）；新发现 project.godot:63 乱码键吞 `[rendering]` 段头（msaa/渲染器配置从未生效，见性能审计节 P3）。

- **全大陆 L1 地块蒙版已实现**（程序化世界生成后续步骤；⚠ 历史方案，已被 `l1_world_split_v2`/`city_split_v2` 取代，**现行真相 = 69 老 L1 + 1040 城**，见 `战略图架构.md`/总体设计 §七口径）：`tools/worldgen/l1/l1_world_split.py` 曾在 L3 层把整块大陆划分为 389 个 L1 地块（城市点 = 均匀网格 + 细微扰动，每岛至少 1 点；岛×L2 分组细胞质膨胀，细胞不跨海/不跨湾/不跨 L2、半岛不共地块，边缘城市向陆内膨胀不吃亏；面积下限合并 + 极小岛豁免；同 L2 地区相似色蒙版），M 键 L3 战略图按 V 键可叠加查看（`l1_overlay.gd` + `test_l1_overlay` 集成测试绿），详见 [`08-程序化世界生成.md §二十一 L3-L`](../设计/系统/08-程序化世界生成.md)。
- **城市细分层已实现**（⚠ 历史方案，已被 city_split_v2 取代，现行 = 1040 城）：`tools/worldgen/l1/city_split.py` 曾细分出 3203 个城市（城市点 jittered grid + 每 L1≥1 点；按 L1 分组细胞质膨胀，城市不跨 L1/海/湾；面积下限合并；同 L1 相似色），产物 `output/l1/city_*`，详见 [`08-程序化世界生成.md §二十一 L3-M`](../设计/系统/08-程序化世界生成.md)。
- **出生 L1 已定案**：游戏内 L3 地图 `#28bd72` 色（L2 地区 13）内最东侧 L1 地块 = **老 L1 label 69**（region_013 的 3 号老 L1 地块）；Tab 战略图数据源 = 出生 L1 的城市划分（8 城市 / MST 道路，城市暂无可玩地图）。双击城市进入已打通（0.9b 以离线生成器落地 8 张聚落图，见下文 P0 新 0.9 节）。详见 [`08-程序化世界生成.md §L3-O`](../设计/系统/08-程序化世界生成.md)。

P0 原型阶段：阶段 0.1~0.8 已全部完成，P0 收口执行计划已执行完毕（数据清理/EnterTrigger/资源接入/数据驱动）。
- **队伍类型编制系统已实现**（原型循环"编队"环节阻塞解除）：编制预设（战斗班/建造队/劳工队/运输队）+ 职责范围可调整 + 编制管理窗口（FormationPanel），详见 [`CHANGELOG.md`](../CHANGELOG.md)
- **带队出征已实现**（原型循环"战斗"环节阻塞解除）：跨图携带编队 + 战场重建 + 遭遇战队伍参战（`test_squad_travel` 4 用例绿），详见 [`CHANGELOG.md`](../CHANGELOG.md)
- P0 测试链稳定化（S1-4）+ 战斗链路确认（S2-1）+ 测试体系重设计（T0，数字标号全清除）+ 文档校准（T1）完成（`bash tests/run_all.sh` 一键运行）
- S1-4 修复：02/03/07 挂死（脚本解析错误 + TestHelpers class_name 未注册）、存档污染（`save_0.db` 自动读档）、resource_node 信号参数、03 AI 移动抖动、06 HBox/Label 类型错
- S2-1：`TacticalOrders→CommandChain→AIController.set_order` 链路确认已装配
- 技术债清理：P0-3 废弃 WorldMapController 删除、P0-4 IK 日志噪音、P0-1 Chunk stub 防静默告警
- T1 文档校准：架构 §四/§六（P0-1~P0-17 统一编号）、自动加载依赖、路线图、GDD差异分析、归档声明 全部对齐实测
- **P0 范围**：0.1~0.8 + 新 0.9 基础战略图（L1 单层）——8 城邦 + 随机地块边界 + 道路 + 卫星图底图 + 边界线着色（P 社索引图机制），点击聚落进出场景图。聚落模型见 [08-程序化世界生成.md §0.19](../设计/系统/08-程序化世界生成.md)（1 个 L1 地块 = 1 个聚落或空聚落；1 聚落 = 1 张主场景图）。室内系统归阶段 1/2
- **待创始人亲自跑一遍原型循环**（建造→编队→任命→下令→战斗→附身→跨图→战略图）确认"层级管理->好玩"假设成立后，从路线图阶段 1 启动 Alpha
- 详见 [`场景与战斗架构.md`](../技术/架构/场景与战斗架构.md) §四、§六

---

## 高优先级（需尽快处理）

### UI 设计语言审计行动项（2026-09-08 审计 → 当日整改完并合入 main）
> 审计快照档已按「审计/快照类用完即删」约定删除（整改内容见 git 历史 `f82a80b4`..`b771a028` 与下方各条完成记录）；整改后备选图入 README「UI 备选图」。

> 本节已完成四条（主菜单迁 SKETCH / 面板对比度 / 图标接线 / 设计语言文档回写）迁 [`待办事项-已完成.md`](待办事项-已完成.md)。
- [ ] 设计语言新增「地图 UI」章（R8 验收后回写，蓝本=世界地图返工方案档 §四 R8）

### 世界地图观感返工（2026-09-08 创始人验收反馈立项，R 系列）
> 单一真相源：[`世界地图观感返工-诉求与方案.md`](世界地图观感返工-诉求与方案.md)（诉求 18 条 + 四份算法调研 + R1-R9 方案 + 分期批次）。R0 弹窗 bug 已修。批 1~4（R1~R9）已全部实施并合并 main（`bd51d74f`，合并后全量 47/47、报错自检干净），完成记录迁 [`待办事项-已完成.md`](待办事项-已完成.md)。**待创始人观感验收。**

- [ ] 裁决余 1 项：F8 缩略窗底图（并入 R9 定）；其余 4 项已裁决回填（方案档 §六）

### 世界视觉表现（HD-2D 口径，2026-09-06 统筹立项后随载体迁移改写）

> 视觉载体已定稿为 **HD-2D**：自然资源 = 自然物 PBR 卡（3D 离线渲染出卡，类型→卡池轮转，分布走 `resource_gen` 程序化算法——群落散布+林区梯度），地面 = 分段体系（城心石板→夯土→野地三级渐变），天空/昼夜 = 泰拉瑞亚夜空复刻系（极光已落地，见已完成档）。原 2D 笔触方案（TreePainting/TreeYarnBall 毛线团树、RockPainting、20 张变体贴图、笔触脚本）已随旧世界清退与死代码清理根除，不再作为视觉载体。

- [ ] **砍树反馈表现**（行为层诉求保留）：树木被砍空时的消失/倒伏反馈在自然物 PBR 卡载体上重新设计（原 2D 树桩替换/倒塌动画方案随载体退役）
- [x] **背景元素归属裁决**：大世界云（SketchCloud）已接入 HD-2D 场景背景（`hd2d_world.gd`）与主菜单（2026-09-17 对账刷新；原 road_map 等 2D 宿主已随旧图清退删除，裁决事实落地）
- [ ] **旧 2D 图清退后续接回项（2026-09-16 全量清退产生，裁决均走 HD-2D/v3，规格考古走 Git 历史）**：
  - [ ] 守城战 HD-2D 重建（城墙阵地+波次敌军走出征系统；出城选项框现役形态 `hd2d_gate_prompt.gd`）
  - [ ] 行军道路图 HD-2D 重做（跨图步行现以直达替代，主街西缘/战场右缘步行出口已随链关闭）
  - [ ] 星野/天气演出 HD-2D 重做（HD-2D 现有天幕+太阳+云+光照档，星野日月极光流星/萤火虫/降雨/浮尘/水面无对应物）
  - [ ] `test_settlement_entry` 重建（unit——按 HD-2D 聚落薄实例 layout_name/city_tier 契约重写断言，归聚落等级链收口）
- [ ] **HD-2D 代码结构拆分（从头重做，2026-09-16 裁决扔弃旧尝试）**：`hd2d_world.gd` 瘦身为编排门面 + 职责组件（ground/street_placer/walls 等）、街道/战场宿主公共代码上提 `Hd2dMapBase` 基类——旧尝试（批次②+②b）基于迁址前代码写就且半步搁置，重做按当前 main 起步；需考古时 bundle `F:\VSCode\stickworld-pre-rewrite-20260916.bundle` 可找回旧实现

### 河流/水体系统重做（2026-09-05 封印，用户指示）
- [ ] **水体全线封印已落地**：`MapBase.enable_water` 默认 false（battlefield 显式关闭），Pond（K2C 镜像倒影+滚动白沫）在所有地图不再挂载
- [ ] **重做方向（用户已定调）**：K2C 布局需要**地面抬高**让水带落在画面底部（battlefield ground_y=432 时水带会横在屏幕中央），且必须适配缩放（zoom 0.5~2 下水线锚定不漂移）；地面构图定稿后按 docs/技术/参考逆向/天空与水体逆向笔记.md §六 重做
- [ ] 诊断脚本 `tests/dev/diag_pond.gd` 保留可复用

### 光影系统（2026-09-05 用户需求登记）
- [ ] **方向**：类似**八方旅人（Octopath Traveler）**的 HD-2D 光影质感 + **泰拉瑞亚部分 mod** 的光照风格（参考：tModLoader 的 Lighting 权重光照/阴影系统）
- [ ] 现有基础：单 pass 六层后处理（暖色分级/太阳炫光/渐晕/色差/颗粒）+ CanvasModulate 昼夜染色；炫光已在暂停（菜单）时淡出（post_process_layer.gd `_flare_fade`）
- [ ] 候选路径：Light2D/PointLight2D 逐光源（营地光 CampLight 雏形已有）→ 2D 光照 buffer（CanvasItemMaterial normal_map 或 CompositorEffect）→ 昼夜色 LUT

### 战斗性能后续（2026-09-05 一轮优化后，96v96 模拟已满速）

> 渲染瓶颈已单独立项并收官：分支 `perf/battle-30fps` 已合并（换帧级联修复/LOD/MultiMesh 批渲染等落地；96 大军 4.5fps 未达可玩线，D 刀整链迁移暂缓、重开条件见交接档），完成记录见 [`待办事项-已完成.md`](待办事项-已完成.md)「战斗规模化战役收官」节。

- [ ] 基准工具 `tests/dev/battle_perf.gd`（渲染 FPS + headless tick 速率 + 瓶颈二分开关 --anim-off/--rig-hidden/--bodies-ghost/--freeze-entities）；同机对比：改动前 1.4 FPS → 优化后 16.4 FPS（安静负载窗口）
- [ ] **天空小项**（村庄背景视觉验收 7/10 遗留）：云在可视区占比偏疏（可调云池密度）；树线层顶部树尖齐平（Terraria 贴图固有，重提取裁切可解）

### 代码阻塞项
- [ ] **建筑内部 z_index 从未落地**（2026-09-13 建筑管线 v3 室内结构规范发现，详见 [`../技术/架构/建筑管线/建筑室内结构.md`](../技术/架构/建筑管线/建筑室内结构.md)）：建筑内部各层（BackLayer / Floor / Modules_Slot / FrontLayer / Roof）实测无任何 `z_index`，绘制顺序全靠节点树序。后果：内景层会糊住前墙，是「窗外透内景」与「前墙遮挡火柴人」两个需求的**共同前置**。修法：按规范文档的 z 序落地（BackLayer 0 / Floor 4 / Modules 5 / FrontLayer 10 / Roof 11 / ForegroundLayer 10）
- （无）P0 阻塞项已全部清除

### 架构审计修复（2026-08，全量只读审计后）

> 本节「本轮已修复」叙事与已完成条目迁 [`待办事项-已完成.md`](待办事项-已完成.md)。

**遗留（转入本节跟踪）**：
- [ ] units→player_control 的 api.gd 级反向边（`stickman_entity.gd::_find_input_dispatcher` 经 `PlayerControlAPI` 查 InputDispatcher）：api.gd 注释现为中性表述「经本 api 获取（替代 group 反查）」（2026-09-09 对账：原「有意妥协」字样已不存在，反向边本身仍在），待阶段 1 输入系统重构时评估是否改为注入
- [ ] 代码建 UI 迁移：PossessPanel/SavePanel/SettingsMenuPanel/ResourceBar/ZoomBar/DebugOverlay 从 `Control.new()` 迁回 `.tscn`（场景是布局唯一真相源）（2026-09-04 对账刷新：五面板实测已无 `Control.new()` 根——代码建根改走 UIKit `full_rect()`/`widget()` 合规出口路线，`.tscn` 迁移被助手方案取代；余 `debug_overlay.gd` 4 处 `Control.new()` 根待迁）
- [ ] EventBus 死信号二次清理（2026-09-09 对账刷新：travel_completed（订户 map_transition_overlay）、strategic_map_opened（订户 l3_map_controller）已活，从死信号清单划掉）。仍死：chunk_loaded/chunk_unloaded 零订户（scene_loader 双发无订阅）、map_unloaded 仅测试订户、balance_changed 有发射零生产订户（balance_config.gd 自注「热重载预留」）；模块 api 状态信号 building_started/resource_not_enough/org_created·restructured·disbanded（已迁各模块 api.gd，非 EventBus）仍零消费方
- [ ] `stickman_entity.gd`（1415 行，2026-09-09 复核，较 09-04 的 1332 仍在增长）按职责拆分（状态/渲染/武器/AI 接口）（2026-08-30 审计长期治理；2026-09-04 已做协议级收口——IncomingThreatLedger 账本外置，全量拆分仍有效）
- [ ] `construction_manager` 拆上帝对象 + 32/390/96 等魔法数收编到 PlacementGrid/地图常量（2026-09-09 对账：已拆成本/进度条子组件（a7339fb7，686 行降至 700 以下）；`PlacementGrid.CELL_SIZE=32` 已存在并被 construction_project 消费；余 manager 内 32.0/390.0 裸数与 96.0 回退默认待收编）
- [ ] texture_gen/reference 下残留 `.import` 源路径仍指向 `res://modules/building_gen/materials/...` 的历史文件，下次资源清理时重导或删除
- [ ] 测试进程退出泄漏（103 ObjectDB / 13 resources）统一 teardown
- [ ] `test_menu_navigation` 疑似既有失败（"调试区含地图选择按钮"/"动态生成目的地含步行出口" 2 断言）——2026-09-09 对账存疑：该文件自 2026-08-30「适配战略图懒加载……全量 27/27 恢复全绿」后零改动，与「既有失败」记录矛盾，需复跑一次定论

### 架构审计整改（2026-08-15，第二轮全量审计后）

#### PLACEHOLDER 素材替换清单（Demo 占位 → 自制资产）

| # | 文件 | 现状 | 替换方向 |
|---|---|---|---|
| P1 | `modules/fx/scripts/fx_library.gd` | 运行时生成的软圆点贴图 + 手调粒子参数 | 手绘粒子贴图（尘土蓬松/木屑形状）、逐效果调参 |
| P2 | `environment_system.gd::_ensure_camp_light` | GradientTexture2D 径向光斑跟随视口 | 逐光源布点：火把/建筑窗光挂 PointLight2D 到场景节点，本光退化为调试用 |
| P3 | `thatch.gdshader` | 程序化草束（参数可用级） | 美术调参或换手绘屋顶贴图混合程序化细节 |
| P4 | `barracks.gd/_post_build` | 几何军旗+八边形盾饰 | 军旗手绘贴图+摆动动画、纹章盾徽 |
| P5 | `warehouse.gd/_post_build` | 几何货箱堆+椭圆麻袋 | 木板纹货箱贴图、麻袋手绘、货物随库存变化 |
| P6 | `audio_manager.gd::SFX_EVENTS` | 表已全映射真实文件（34 个 wav 入库：程序化合成 SFX 33 + 氛围 BGM 1，1eedf83e/cdf158d5）；音质/风格待美术复核 | 换录制/采购成品直接替换，调用点零改动 |
| P7 | `modules/fx/assets/demo/`（水晶簇/岩壁带/墙纹/接触阴影） | 从药工解包资源与实机截屏提取的**本地演示占位**（fx_gif authentic 舞台用，游戏内资源点未引用） | 自制手绘水晶簇与岩壁纹理替换；发布导出须排除该目录 |

**遗留（转入本节跟踪）**：
- [ ] 装配根越界（C2 剩余）：`system_setup.gd` 仍 42 处装配 preload 跨模块内部脚本（audit_deps 豁免口径：composition root 需按具体类型注入）并直挂 `_root`；world 持 ConstructionManager/ResourceManager/OrganizationManager 强引用——内部类装配收敛到各模块自建出入口（阶段 1 装配重构）。注：C1 已拆 `battle_director→WorldAPI` 硬引用并文档标注 world⇄combat 为有意环
- [ ] CI 未启用（ci.yml.disabled 模板）；`AGENTS.md` 与 `.trae/rules/rule.md` 双规则文件漂移（2026-09-09 实测约 110 行差异）

### 需创始人决策
- [ ] **建筑比例遗留三项**（2026-09-13 建筑管线 v3 比例审计发现）：① 20° 俯角把"进深×sin20°"计入屏幕高度（house12 屋顶屏占≈墙高 79%），"头重脚轻"主因是取景放大器——降俯角/压屋脊/接受现状三选；② **门宽规范自相矛盾**（§8.7 写 0.9~1.0m，§8.3 写 45~60px≈0.6~0.8m，实测 0.60~0.76m）待统一；③ 窗宽实测 0.53~1.15m 待 WINDOW_SPEC 统一。详见建筑管线交接档 §二 落差清单
- [ ] **零偏航 vs 回退建筑侧翼**（2026-09-13 HD-2D 原型发现）：建筑站位回退后"侧翼可见"在零偏航平面卡下几何不可实现——接受无侧墙，或只为回退建筑放开 ±几度偏航（会打破"纯正面"铁律）。详见 `docs/技术/架构/建筑管线/2.5D与HD-2D可行性.md` §八
- [ ] **窗外透内景：常驻还是仅交互区**（2026-09-13 建筑管线 v3 提出）：现 `地图与场景图.md` §5.2.1 把 `Interior.visible` 与前墙 alpha 同门控，导致"平时从窗户看进去是空房"，与创始人「要真透明、能透视到室内的玻璃」的诉求冲突。建议口径：**内景底图常驻**（窗后始终有房间），交互区只控制前墙 alpha；待创始人确认
- [ ] **B 类「可进入室内」地标清单与规模**（2026-09-13 提案/待定）：室内结构规范提案只给 4 个地标（cathedral / grand_hall / tower / lighthouse），需创始人确认清单与规模（详见 [`../技术/架构/建筑管线/建筑室内结构.md`](../技术/架构/建筑管线/建筑室内结构.md)）

---

## 中优先级（P0 后期 / P1 前期）

### 阶段 1/2 指挥系统深化（从 P0-1 附身后移）

> **已完成（2026-08）**：队伍类型编制系统（编制预设/职责范围/编制管理窗口 FormationPanel）——原型循环"编队"环节阻塞解除，详见 CHANGELOG。

- [ ] **运输队职责消费（WORK_TRANSPORT）**：编制预设已有"运输队"（fp_transport_convoy，LOGISTICS 标签），但跨城市运输/物资运往前线的实际行为待物流系统（[`../设计/系统/06-物流系统.md`](../设计/系统/06-物流系统.md)）实装时接入；场景图小队的"运输队"与组织层级模板的 L2"运输队"（`config/formations/presets.tres` tier_transport_convoy）为同一概念的两层投影，挂接关系届时一并设计（2026-09-09 对账：`config/logistics/` carriers/roads/vehicles.tres 已导出但零代码消费，modules/logistics 未建——定义层就位、行为层零实现）

- [ ] **附身系统完整版**：多层级指挥官附身 / 附身时 AI 协奏 / 指挥链延迟=0 / 自主决策权限切换
      - 定位：附身是指挥系统的"亲自下场"模式（详见 [`../设计/系统/02-战斗系统.md`](../设计/系统/02-战斗系统.md) §1、[`../技术/架构/场景与战斗/战斗与AI.md`](../技术/架构/场景与战斗/战斗与AI.md) §7.5/§8.5）
      - 现状（2026-09-09 对账）：基础单实体附身可用（`possession_interface.gd` possess/release/慢速时间 + battle_panel 附身按钮）；AI 跳过被附身单位已做（formation_system.gd:706），前队全灭转自主决策有队级近似（:640/884/902）；全无：多层级附身 / AI 协奏 / 指挥链延迟=0（阻塞于任务依赖图 ui_possess_full）
      - 阶段：阶段 1 "核心系统（Alpha）"或阶段 2 "深度扩展（Beta）·完整指挥链设计"子任务，详见 [`路线图.md`](路线图.md)

- [ ] **传输层跨图距离接口 + 组织驻地语义**（组织系统批次 3-F2 登记的开放收口，架构文档 组织系统架构.md §4.2.2/§七）：`region_distance_provider` 当前恒 -1 走 fallback 常数（balance var_command_cross_map_distance=2000px），待 world_map 侧立「两区域（定居点/地图根）世界坐标距离」取数接口后换真值；同批收口 `org.location` 字段语义源（当前除默认 "" 无赋值方，跨图分支按"同驻地=0"退化）——需先定义区域标识来源，再做「组织驻地随实体移动更新」。同图战斗延迟不受影响：全链路走指挥官实体坐标直线距离（中间层指挥官也是真实实体）

### 工程流程决策
- [x] **WorldState 容器决策（集中 vs 模块自持）→ 已决策 A（集中制），2026-08 organization 先行落地**：OrganizationManager 值升级为 OrganizationState 对象、经 set_world 同步注册 WorldState 容器、序列化格式与 WorldState 统一（存档由 WorldState 统一落盘，模块不再自注册 SaveManager）；单测含容器同步用例（15 用例全绿）。**剩余**：resources 的 ResourceState 是"品类定义"（价格/重量）而非"库存台账"（区域嵌套 stocks），需先扩展状态类设计（或新增 ResourceLedgerState）再迁移；technology 为空壳，重建时按 A 方案实施
- [ ] **地图编辑器方案决策**：三种方案待选（直接改 .tscn / 独立编辑器 / Godot 插件），见下方"地图编辑器方案探讨"

### 行为树演进（详见 [P0收口执行计划-2026-08.md](归档/P0收口执行计划-2026-08.md) §13）
- [ ] [短期] **寻路避障系统**：火柴人直线移动遇障碍硬弹回导致卡死，需实现局部避障或 A* 网格寻路（调研报告见 [`../技术/架构/寻路避障调研.md`](../技术/架构/寻路避障调研.md)）（2026-09-09 对账：仍直线移动（behavior_move.gd 头注 P0 不做 A*）；已有 unit-unit 群体分离推挤（stickman_entity.gd:207）非障碍寻路；全工程无 AStar/NavigationAgent）
- [ ] [中期] WorkCrewAssigner 扩展为任务板（搬运/建造任务分发），决策与执行解耦（参考监狱建造师/环世界 Job 系统）
- [ ] [中期] AIController 决策优先级数据驱动化（.tres 配置），把 `_make_decision` 硬编码优先级抽成资源
- [ ] [长期] 可编辑行为树评估：LimboAI（Godot 成熟 BT 插件）vs 自研 HBT，`BehaviorBase` 适配为 BTNode

### 代码债治理（2026-09-03 系统性审计挂载）

- [ ] **胖文件拆分·其余**：formation_system 1029 / game_root 926 / system_setup 916 行（2026-09-09 复核，均在增长）；construction_manager 已降至 686（拆出成本/进度子组件）——内部已分区，随改随拆
- [ ] **Excel 导出管线映射收尾**（2026-09-04 对账刷新）：元数据行机制（首行 `#output_dir: <目录>` + sheet 英文名）已铺开——12 张 Excel 中 11 张带映射，其中 9 张文件 15 个 sheet 命中现有英文名产物（平衡→balance/variables、建筑→buildings、战斗战术→combat/tactics、扩张×3→expansion、资源→resources、单位×3→units（stickmen/weapons/armors）、制造→manufacturing、法律→law、运输×3→logistics）；导出器已硬化（导出保留目标 .tres uid 头 / 空 id 行硬错误阻断导出 / `--yes` 非交互确认 / 强制 LF 幂等）。**剩余**（2026-09-09 对账大幅缩围）：① 编制预设.xlsx 有 sheet（presets）但无产物映射；config/formations 双产物归属实际已明——formation_presets.tres 被 formation_system 消费、presets.tres 为无引用孤儿（待清 + 补映射）；② 法术科技/管理科技已按 spell_techs/admin_techs 双表导出落地（config/tech/ 各有导出器 _meta 产物）——仅剩科技模块重建后接线消费（归并科技条目），旧手写 techs.tres 并存待清；③ ~~4 表陈旧格式 / 真实导出冻结~~ 已解除（7d3fcbed 四表按当前导出器重导、字节级幂等）

### 静态审计登记（2026-09-08 第二轮：帕累托性能优化 + 顺手捉虫，修一国记录一国）

> 本轮已修项见该分支提交（`agent/perf-pareto`）；以下为**核验属实但本轮未动**的登记，按"修法是否涉及设计决策"分流。

### 无头基准轮登记（2026-09-08 第三轮：4 并行代理无头剖析，基准脚本入库 tests/dev/bench_*）

> 本轮已修（SQLite 批包/堆寻路/L2 批解码/能力位缓存等）见分支提交；以下为实测后**明确不动**的项，证据数字在基准里可复跑。

- [ ] **RigHost 渲染子树 ≈ 群体帧 57%**（bench_units_main `--diag-only`：117ms/帧中 ~75ms 是 AnimationTree/AnimationPlayer/Skeleton2D IK 引擎侧解算）——脚本层已无可动空间，下一步唯一大头是全局动画 LOD/屏外降频/烘焙，属表现取舍待拍板
- [ ] **map.query_neighbors 每次调用分配返回数组**（6.5µs/次，200 单位群体帧占 ~1.4ms/1.2%）——实现与网格缓存在 `modules/world/scripts/map/map_base.gd`，本轮各代理范围外交汇处，可改 out 参数复用数组
- [ ] **formation_system._process 218~333µs/次**（光环逐成员 restore_morale + 死员清理扫描）——节流会改变移除/回士气时序语义，留战斗线决策
- [ ] **L3 JSON 回退路径已坏**：`l3_world_data.gd:227` `_compact_dict`/`_to_vec2` 对真实 l3_world.json 报 `Nonexistent 'float' constructor`（多边形嵌套形态未覆盖）——生产恒走 bin 不触发，但回退路径等于不存在，要么修要么删
- [ ] **SettlementBlob.generate_outline ~100µs/城**（L3 T2+ 468 城 ~45ms）——与生成端 blob_bake.py"逐位同公式"是确定性锚点，重构漂移风险 > 收益，除非两端同步改
- [ ] **存档剩余 ~50ms**：十余次独立提交（各表 DELETE/WorldState 双语句/maps、resource_nodes 表）——进一步压需整段 game_saving 包外层事务，但 insert_rows 自带事务会嵌套冲突（save_manager 内有 2026-08-15 注释记录此坑），需全线回退逐行+外层事务，收益边际小
- [ ] **战斗模拟宏观 8.4tps（192 单位饱和）其余大头**：rig 动画/物理/fx（本轮范围 splitting 后脚本侧已挤干，见上 RigHost 条）；模拟存在 4 类墙钟依赖使默认不可复现（举盾窗口/攻击者新鲜度/TeamAi 投射物窗/环境特效吃全局 RNG），bench_battle_sim 内已冻结可作回归护栏，行为线改动跑它防退化
- [ ] **AStar2D 仍比堆版 Dijkstra 快 ~200×**（合成网格 50 对 1.6~2.6ms vs 398ms）——路网涨到 4k+ 节点且可接受平局路径变化时再切；当前堆版在千级节点 8ms/对已够

- [ ] **建筑损毁失联（含 grid 占用泄漏）**：`building_gen/scripts/building.gd:313` `demolished` 信号全项目零订阅——`take_damage`→`demolish()` 后 ConstructionManager 的 `_buildings`/`_building_to_id`/PlacementGrid 占用都不知道建筑已毁（唯一注销路径是 `demolish_building`）。后果：注册表残留 DESTROYED 引用、grid 占用不释放。本轮已在 `get_nearest_warehouse` 加 DESTROYED 实时过滤兜底（搬运工不再认废墟取货），注册表/grid 根治需 construction 订阅 `demolished` 注销
- [ ] **军旗强调色死配置**：`building_gen/buildings/barracks.gd:22` `pal.get("C_ACCENT", …)`——调色板契约（`building_exterior._get_palette`）只产 7 键无 `C_ACCENT`，军旗永远走硬编码默认红。修法 = 先拍板调色板键位（视觉决策）
- [ ] **工地工作位不重排**：`construction_project.gd:127-144` `unassign_worker` 删人后不重算 `_worker_slot_map`，后派工人与剩余工人 slot_index 冲突（同位站两人）。修法 = unassign 后重排或取 slot 时挑空位
- [ ] **派工不看距离**：`work_crew_assigner.gd:104-117` `try_assign` 按插入序派工，NPC 可能跨图跑工地；manager 已有 `get_nearest_project` 距离逻辑可复用
- [ ] **加速档市场节奏不变**：`resources/api.gd` `_process` 供需 tick 按现实秒累计，X2/X4 时间流速下市场节奏不随加速——设计口径待定（要不要跟时间倍率）。⚠ 同文件 `resource_changed` 第二参 consume 发操作前总量/produce 发操作后总量，是有测试断言的现行契约（`test_resources_api.gd:58`），统一语义需连测试一起改
- [ ] **建造进度追踪潜伏泄漏**：`build_progress_tracker.gd:35-46` untrack 不 disconnect `progress_changed`，cancel 路径无人 untrack（`project.cancel()` 当前零调用方，未爆雷；接取消玩法时必修）
- [ ] **聚落 tooltip 状态不刷新**：`world_map/ui/settlement_tooltip.gd:79-83` 悬停同地块期间早退，战斗开始/到访状态变化时旅行提示停留旧文案
- [ ] **建筑无修复回路径**：`building.gd` `take_damage` 进 DAMAGED 后无 repair 接口，半血建筑永久红 tint（设计缺口，接修理玩法时定）
- [ ] **石砖门洞越界脆弱**：`texture_gen/scripts/stone_brick_gen.gd:64-71` `make_gate` opening 超图幅时 set_pixel 报错刷屏（默认参数不触发）；另 `siege_wall.gd:148` 运行时兜底重生成 512² 是主线程尖峰，宜保证烘焙图必在或兜底改异步
- [ ] **茅草笔触负坐标截断**：`texture_gen/materials/thatch/scripts/thatch_applier.gd:215-216` `int()` 应 `floori`（负坐标 1px 错位；当前仅预览场景引用）
- [ ] **crystal_sparkles 基础层错相失效**：`crystal_sparkles.gd:448` `preprocess = lifetime × (layer % 6)/6`，layer=0 恒 0，注释宣称的"按层错相"对基础层不生效（轻微视觉）
- [ ] **命名卫生**：`world_map/ui/l3_zoom_indicator.gd` 声明 `class_name MapHUD`（文件名/节点名/HUD 实职三方错位）；l2/l3 渲染器 `BORDER_WIDTH()` 是方法却用常量命名（触发 GDScript 命名告警）
- [ ] **travel_planner 手写 Dijkstra 升级点**：线性扫 frontier O(V²)，当前 8 节点规模合理（代码注释自知）；跨 L1 路网（~10³ 节点）接入时换二叉堆或 Godot `AStar2D`，查询频繁再加缓存
- [ ] **量级可控暂不动**（记录防重审）：stats_screen 打开期 0.25s 拆重建列表；item_slot_widget 0.12s wobble 重绘（设计内"手绘沸腾"感）；build_menu 选址模式每帧全地图格扫描 + ghost 无条件重绘（仅选址期）；formation_system 每帧小队清扫+士气光环（节拍化会微调士气恢复时序，留战斗线）；texture_gen 逐像素生成已用静态缓存覆盖大头，单次生成本身可接受

### 设计待定（GDD 标注 [待补充]，详见架构文档 §十六）

> 2026-09-09 对账注：架构侧清单（场景与战斗架构.md §四）已将兵种/宫殿/随机事件/战场两端四项标结项，与本节 `[ ]` 及设计文档 `[待补充]` 标记存在两处口径冲突，待对齐后统一勾销/改写；`VillageDefinition.tres` 全仓无此文件（config/villages 目录不存在）。
- [ ] 兵种类型与武器属性（影响 Hitbox、伤害计算）
- [ ] 各村落 `VillageDefinition.tres` 内容
- [ ] 大建筑（宫殿）内部场景设计
- [ ] 道路上随机事件机制
- [ ] 战场地图两端建筑模式（预设阵地 vs 现场部署）
- [ ] 加工品/奢侈品具体资源列表（GDD §2.2 `[待补充]`）

### 科技系统（阶段 1 重建）
- [ ] **科技模块重建**：原 modules/technology 空壳（研究制契约）已删除；阶段 1 按 [04-科技系统.md](../设计/系统/04-科技系统.md)「征服获得」策略重建——征服解锁 + 事件获取，获得即拥有，无研究状态机；重建时同步 modules/README 速查表、模块API契约.md 与 EventBus 信号（按当时契约声明，不预先占位）（2026-09-09 对账：config/tech/ 三表已导出（techs 旧手写 + admin_techs/spell_techs 导出器产物）零代码消费；存档侧 `region_state.tech_unlocks` 字段与序列化已就位——重建时可直接接线）

### 火柴人个体系统（创始人确认，参考《世界盒子》）
- [ ] **个体属性标签系统**：每个火柴人有独立属性（力量/智力/敏捷/工艺/指挥），影响行为树和适合的工作（2026-09-09 对账：出生掷骰五维 7~13 已生成（stickman_entity.gd `attributes`）、统计屏已展示；缺：行为/工作适性消费、入存档（stickman_state 无字段））
- [ ] **装备系统**：武器/护甲/工具影响属性和行为——**创始人 2026-09-06 扩充需求：每个火柴人独立的特定记录 + 个人背包 + 穿戴装备栏**（个体级库存，区别于仓库级经济/物流），需求存档与开放问题见 [`../设计/系统/10-武器装备与个人库存.md`](../设计/系统/10-武器装备与个人库存.md)，**架构设计已定稿（该文档 §九模块结构规划 modules/equipment/、§十分期 E-0~E-4、§十一开放问题裁决速查）——待分期实施（E-0 起步）**；2026-09-09 对账：个体级库存代码未动（modules/inventory 为玩家专属，`stickman_state.equipment` 仍预留字段）
- [ ] **迭代式复杂化**：P0 先做基础属性 + 简单行为，后续逐步增加（世界盒子至今仍在完善）
- [ ] 详见 [战斗与AI.md §7.2.1/§7.2.2](../技术/架构/场景与战斗/战斗与AI.md) + [竞品分析.md §4.12](../商业/竞品分析.md)

### 地图系统（创始人确认）
- [ ] **城镇朝向决定道路方向**：大世界地图上城镇的随机朝向决定从场景图左/右边界走出分别到达哪些道路（依赖战略图完成）
- [ ] **道路遭遇战**：行军途中遭遇敌方触发战斗（低自由度全面战争式行军，依赖战略图 + 行军系统）（2026-09-09 对账：场景图遭遇战链已做——game_root 遭遇战战场注册 + 敌军 spawn；剩战略图行军遇敌）
- [ ] **城镇地面贴图差异化**：不同类型城镇使用不同硬化地面贴图（矿业/农业/军事等）
- [ ] 详见 [地图与场景图.md §5.5.5-§5.5.8](../技术/架构/场景与战斗/地图与场景图.md)

### 地图系统架构重构（创始人原始诉求，待重构）
- [ ] **城镇动态扩张**：地图大小随城镇规模增长，`map_left`/`map_right` 可变
- [ ] **城墙改为 Building 类型**：可建造/破坏/穿越，不在地图边界，可能有多层（旧城墙保留+新城墙新建）
- [ ] **城外环境**：城墙外约 100 格的自然环境（森林/草原/绿洲），城外地面贴图与城内不同
- [ ] **战场类型扩展**：遭遇战/攻城战/守城战三种，攻城守城利用城墙阵地（2026-09-09 对账：遭遇战+守城战已上线（game_root 注册循环）；攻城战骨架在 `battle_director.start_siege_battle`（按 siege_side 找墙布阵，反向布阵待扩展）；工程战未动）
- [ ] **战场 spawn 动态**：我方位置根据行军方向决定（从南往北走 -> 右侧/南面），不固定左边
- [ ] **战场持续性**：玩家走回撤退点返回城镇后战场战斗不停止，由 AI 自动指挥（2026-09-09 对账：mark_player_absent/present 标记 + 战斗继续 tick 已做（battle_director.gd:146，P0 简化）；缺完整 AI 自动指挥，P1+）
- [ ] **工程战/攻城战**：我方阵地变为城墙（如果有城墙），需攻破城门入城
- [ ] 详见 [地图与场景图.md §5.6](../技术/架构/场景与战斗/地图与场景图.md) + P0计划地图架构重构需求说明
- [ ] **后续**：基于以上诉求重新设计地图系统架构（方案待定）（2026-09-09 对账注：八诉求中出城重构/战场类型/战场持续性已随守城战 Demo 零散落地，立项重构时按现状缩围）

### P1 工程
- [ ] **UI 做做样子项清理（2026-08 系统审计）**：① 通知 feed 仍是 GlobalHUD 单条 Label（04 篇规划堆叠 feed 未做）；② 游戏内快捷栏缺失（仅 hud_template 演示，未接模式/指令）；③ 设置项部分"存了不生效"——video/window_mode、audio 音量、video/show_fps 写了 ConfigManager 但无消费方应用；④ 小地图/缩放条样式未统一（未走 StickTheme）。模板场景（template_*/workspace/gallery）为演示性质，不在正式范围（2026-09-04 对账刷新：①③ 已完成——通知升级左下堆叠 feed（`notification_feed.gd` + `test_notification_feed`）、设置项真实生效（音量接 AudioServer 总线/FPS 计数修复/重启恢复 round-trip + `test_settings_apply`）；② 已完成（2026-09-09 对账：游戏内 Hotbar 已存在且接真实指令——`modules/inventory/ui/hotbar.gd` 点击/数字键经 InventoryService 用物品/开关背包/统计屏，信号驱动非轮询，system_setup 挂 HudOverlay 槽；"仅 hud_template 演示"表述过时）；④ 部分推进（ZoomBar 已走 sketch 皮肤自绘刻度（SketchHSlider），Minimap 仍独立硬编码色板未统一））
- [ ] **UI 体系落地（部分完成，2026-08）**：主菜单/载入屏/暂停菜单/设置面板（分类切换+落盘）/存档面板已接入；分层重构完成（ModalOverlay 盖住全部 UI、模态暂停+遮罩消费鼠标+相机输入兜底、ESC 统一 `_handle_escape`）；4 种弹窗行为模板（StickScreen=MODAL / StickWindow=FLOATING/DOCK/POPOVER，编制菜单已归 FLOATING）；空面板通达（暂停菜单帝国功能 + K/O/J/L 快捷键）；战略图装配步骤表分帧（启动不再整图一次性实例化）+ 世界加载覆盖（无死灰屏）；统一层号 `LayerOrder` + `SystemOverlay` 系统层；顶栏/小地图/缩放条/时钟布局调整。**剩余**（2026-09-09 对账刷新）：已迁模板——InventoryScreen/StatsScreen/SavePanel/PauseMenu/SettingsMenu（StickScreen）、FormationPanel（StickWindow·FLOATING）、world_map 三件（travel_dialog/settlement_tooltip/map_legend 挂 StickTheme）；未迁——BattlePanel/BuildMenu/PossessPanel/ContextPanel/ModePanel/DebugToolsPanel（裸 Control+StickKit）；DOCK/POPOVER 行为已实现暂无业务面板使用。详见 [`docs/设计/UI/README.md`](../设计/UI/README.md)
- [ ] 读档完整性（2026-09-09 对账刷新覆盖面）：已恢复——建筑+建造项目（血量/状态/城墙 tier/门/ID 计数器续算，building_persistence.gd）、工地进度/材料/障碍物/派工池重注册、资源点、实体位置/朝向/附身（save_handler.gd）。缺——faction_id 存了不读（阵营未恢复）、背包/装备零持久化、编队/编制无存档代码、world_state 表空壳

### UI 系统技术债（2026-08 审计遗留，见 modules/ui_global 重构记录）
- [ ] **PossessPanel 每帧刷新改信号驱动**：`_process` 每帧字符串拼接 + 反射查询 HP/士气/冷却，改订阅单位属性变化事件（当前收益低，P1 随战斗 UI 深化做）
- [ ] **hover_indicator 悬停检测移出 `_process`**：每帧全量遍历实体做包围盒检测，可改 `_unhandled_input` 鼠标移动事件驱动（2026-09-09 对账：已加 10Hz 节流 + 暂停期免重绘 + 无悬停零重绘，仍 `_process` 遍历；事件驱动改造仍有效、收益已降）

### P1 系统设计（2026-09-09 对账刷新）
- [ ] 确定完整科技树结构（策略已定稿：04-科技系统.md「征服获得」；缺完整树结构）
- [ ] 确定法律分支树（GDD §七 已有框架：法律树/道德分支/废除代价；缺完整分支树）
- [ ] 各文明的初始条件和差异化（civ_differences.tres 有表零消费、无文档细化）
- [ ] 主角力量来源设定（GDD 仍 `[待补充]`）
- [ ] 两个帮手设定（GDD 仍"可有可无/取决于剧情"未定）

---

## 低优先级（P2+）

- [ ] **战斗结束判定（战斗收敛）**（2026-09-09 对账缩围）：尸体处理已完成（死亡 0.9s 禁用 Collider 防挡路 + 4s 淡出移除，stickman_entity.gd:459）；溃散状态已有（`is_routed` + behavior_retreat 横向游走 + 士气自然恢复），但收敛判定仍只看死亡（battle_instance `_check_victory` 计 is_dead，全员溃逃不结束）——剩 rout 收敛判定/超时/溃兵回场策略。P0 战斗基础已可用（全灭即结束）
- [ ] 成就图章完整列表（07-成就系统.md 已有设计文档 + 占位面板入口；缺逐枚清单）
- [ ] 定价策略确定（docs/商业/商业模式.md 框架已建，定价数值 `[待补充]`）
- [ ] 宣发计划细化（框架已建：docs/宣发/ 总计划 + Steam 文案 + 社区运营 + 视频规划 + 素材清单；待细化排期）
- [ ] 音效/音乐方案（SFX 已有程序化合成 33 枚入库且 SFX_EVENTS 全映射；音乐与外包方案未定）
- [ ] 实体周边可能性调研（仅 07-成就系统.md 一处 `[待补充]` 挂名，无推进）

---

---

## 地图编辑器方案探讨（待决策）

> 触发原因：当前地图用 Godot 场景（.tscn）存储，手动编辑效率低，需要专门工具。
> 关联架构文档：[`场景与战斗架构.md`](../技术/架构/场景与战斗架构.md) §三 地图系统

**核心问题：地图用什么格式存储？编辑器如何工作？**

### 待选方案

1. **直接修改场景文件**（当前方式）
   - 优点：Godot 原生支持，所见即所得
   - 缺点：手动编辑繁琐，无法批量生成，易出错

2. **独立编辑器程序**（@tool 脚本 / 独立 Godot 项目）
   - 编辑器输出结构化数据（JSON/自定义格式），游戏运行时转译为场景
   - 优点：可控性强，支持程序化生成参数
   - 缺点：开发成本高，需维护转译层

3. **Godot 编辑器插件**（@tool + EditorPlugin）
   - 在 Godot 编辑器内提供自定义 Dock，辅助放置地形/装饰/建筑点位
   - 输出 .tscn 或 .json
   - 优点：复用 Godot 编辑器能力，无需独立项目
   - 缺点：插件 API 学习成本

### 待决策问题

- 地图数据是否应该从 .tscn 迁移到 JSON/Resource 驱动（与 xlsx 数据驱动一致）？
- 地形/装饰/建筑点位如何参数化存储？
- Chunk 流式加载（阶段 0.8，实际为地图间切换）与编辑器输出格式的兼容性？
- 是否需要支持程序化生成（如噪波地形）与手工编辑混合？

### 决策时机

P0 阶段现有 6 张场景图（village_a / village_b / road_a_b / battlefield / forest_zone / mega_interior），直接编辑 .tscn 足够。
当地图数量超过 10 张或需要程序化生成时，再启动编辑器方案设计。**不阻塞当前开发**。

---

## 输入/战斗表现小问题（2026-08-30 演练场验收时发现，均低优先）

- [ ] **全套测试偶发 26/28**：并行池下集成测试偶发一例抖动，重跑即恢复（本轮观察到一次，
  未定位到具体用例，连续两次重跑均 27/28）。若复现频率上升再排查。
- [ ] **unit(batch) 稳定失败于 test_suppression**：批量输出末尾报 `[BATCH-FAIL]
  res://tests/unit/test_suppression.gd`（批内汇总 4/4 通过后仍标失败，疑似
  test_ai_enhance 同款批量挂死/看门狗家族）。2026-09-16 大乱斗观察场修复会话实锤：
  带改与 stash 基线各跑一次均复现，与本批改动无关。待排查。
## 测试演练场（2026-08-30 新增，入口：主菜单「测试场景」→ 仅 debug 构建显示）

- 已有 4 项（main_menu.gd `TEST_SCENES`）：大乱斗观察场（tests/dev/battle_arena.tscn，HD-2D 战场大绿场，
  预设 1/2/3 = 双方各 16/48/96 人、3~4 小队按武器分排编队推进互殴；地平线钉上 1/3 线地面占 2/3、
  缩放条 100% 档；ESC 返回/R 重开/1·2·3 换预设/空格暂停）、单位动作画廊（unit_action_gallery）、
  手绘皮肤对比（sketch_compare）、手绘云画廊（sketch_cloud_gallery）
- 可扩展（加场景只需在 main_menu.gd 的 TEST_SCENES 清单登记一行）：
  - 单挑观察场（1v1 各武器对位，看站姿/挥砍细节）
  - 编队配置化（当前编队硬编码 COMPOSITION 常量，可改 UI 调兵）
  - 箭雨/法术演示场（依赖法术系统立项）

## 夜空复刻验收遗留（2026-09-06 视觉验收后，均不阻塞）

- [ ] **水面夜间倒影补偿**（潜伏问题）：`Pond`（village_map `enable_water`，当前全图
  默认 false 未挂载）的水面输出整体会被 CanvasModulate 乘暗——深夜倒影（反射的是
  未染色的清屏色天空）会一起被压到近黑，呈"死水"。启用水面时需在
  pond_reflection.gdshader 给倒影项乘 1/cm 补偿矩阵（与 `EnvironmentAPI.unmodulate`
  同构；水体底色/岸沫仍保留 CM 染色）。
- [ ] **夜空"露出真天空"的取舍**（待决策）：清屏色夜空只在山脊上缘的窄条可见，
  大部分"天空"实为背景山体贴图的剪影（BIOME_SETS 各层 `top` 值把贴图铺满了
  天空区）。若想要泰拉瑞亚式大视野天空，需整体下调各层 `top`——会改变
  已定稿的日间构图，需用户拍板后再动。

## 运行时性能审计（2026-09-09，PerfProbe 临时探针 + battle_perf 差分全套）

健康项（本轮实测确认，无需动作）：场景切换对象数正常回落（world→battle→world
对象 5.3k→19k→5.4k，无明显泄漏）；显存 ~95MB 低位；SaveManager SQLite 开关频率
低（日志无刷屏）；物理 30Hz 下 phys 耗时 <3ms。

- [ ] **P1 夜间极光/光柱视觉过强复核**：强度随时间梯度明显——黄昏（19:05 截图
  world_idle_b.png）极光横带铺半个天空、山体可辨，偏强但可读；深夜（01:55 截图
  stratmap_l1.png）极光自月亮呈放射状洋红-青色光柱铺满天空、山体剪影完全被洗掉，
  已病态；白天完全干净（排除常驻渲染异常）。大概率是极光夜间浓度/形状参数或
  光柱节点与 CanvasModulate 的叠加关系问题，需人工复核。【提案/待定】
- [ ] **P2 战略图 L1/L2/L3 性能未测**：Tab 地图默认展开已实装（`system_setup._open_tab_map_default`），可重建 PerfProbe 探针补测（实现协议见 2026-09-09 审计会话：临时 autoload + 文件命令通道 + 每秒 CSV + sysoff 差分；`tests/dev/perf_bench_smap.gd` headless 分相基准探针已存在，可作起点）。
- [ ] **P2 战斗单位渲染规模化课题（P0 修复后暴露的真实成本）**：UI 风暴消除后，
  48v48 战斗 proc 71ms + draws 4178（fps 7），96v96 proc 161ms + draws 7236 +
  5.5 万对象（fps ~2）。大头是火柴人骨架每只 30+ 部件的节点渲染与逐单位
  GDScript 模拟。方向：骨架部件合并/贴图烘焙、MultiMesh 批渲染、远处/大量
  单位 LOD、AI 分帧摊销。属独立优化课题，建议单独立项规划。

- [ ] **main 侧遗留：tests/unit/test_ai_enhance.gd 直接运行挂死**（batch_runner 看门狗杀掉后报 FAIL，伪装成并行 flake）——与 perf 分支无关（stash 对照 + main 复现），疑似测试线程内 add_child 进树的死锁，待 main 处置。（自「战斗规模化战役收官」节迁入，该节完成记录见已完成归档）


---

## main 既有测试失败登记（组织深化会话 2026-09-11 摸底）——已全部修复（2026-09-11 测试稳定性批）

以下三项登记已于同日修复（`agent/test-stability` 并入 main，全量 run_all **44/44 全绿**）：
- [x] ~~**test_road_walk 3/7**~~：根因 = RoadMapGenerator 的 `pack()` 前未补设 owner——运行时 add_child
  节点 owner 为空，产物仅剩根节点（地形/装饰/出口触发器全丢，步行旅行进图即空壳的**产品级 bug**）；
  修复 = pack 前递归传播 owner + 宽度断言对齐 MIN_WIDTH=2500 钳制语义（原 2400/1600 期望自创建起即错）。
- [x] ~~**test_fx_damage_text 1/5**~~：根因 = c6919002 字号基准减半（观感修复）后断言未同步——
  断言对齐新参数（12/17、钳制 [11,28]、描边 clampi(px/4,4,12)）。
- [x] ~~**test_melee_combat 4/5**~~：根因 = headless 下 process_frame 快于动画推进，6 帧等待只推进
  几毫秒——改按真实时间轮询（同 _test_cooldown 口径）。
- [x] 另修（同批）：loading 批次引入的 **headless 启动协程挂起**（`game_root._yield_frame` +
  `initial_content` 建筑链/村民链两处 `await frame_post_draw` 短路）——曾致全部集成/冒烟测试崩
  （装配未执行），为上列 9+ 套件失败的共同根因；main 基线一度 16/25，修复后 44/44。

## 架构收敛 AR 系列（2026-09-11 立项，状态内联本节）

> 实测数据与证据：[`../审计/架构审计_2026-09-11.md`](../审计/架构审计_2026-09-11.md)（临时审计快照，AR 全部完成后删除）。
> 方案标注【提案/待定】；架构深化单开分支专项讨论（见末条），届时定文档形态与排期。

- [ ] **AR-1 装配下沉（大，长期增量）**：`system_setup.gd` 42 处装配 preload 跨模块内部脚本（audit_deps 豁免口径）并直挂 `_root`。方向【提案】：各模块自建 `setup.gd::assemble()` 自持实例化、system_setup 收缩为纯编排；新模块必走新规、存量随改随迁、preload 总数只降不升。
- [ ] **AR-2 拆 world⇄world_map 环（小，Flash）**：唯一编译期环，根因 = WorldAPI 持共享词汇被 world_map 反向消费（审计 §1.2）。方向【提案】：共享枚举/路径常量下沉 `core/shared/`；执行时同步刷新 modules/README §2 与 模块依赖关系.md §一 两张依赖图（均缺 inventory、world 层级过时）。
- [ ] **AR-3 胖文件治理（中，任务分支进行中）**：**不固定行数阈值**，按职责内聚与行业最佳实践判断拆分点，拆分只在任务分支进行。实测登记（行数随改浮动）——\>1200 行：formation_system 1770 / stickman_entity 1625 / weapon_mount 1328 / system_setup 1246 / ai_controller 1221（本批分支 `agent/fat-files-docs` 拆除前 4 个；system_setup 因并发编辑保护挂起、随改随拆）；1000~1200 行：game_root 1179 / crowd_renderer 1153 / team_ai 1146 / map_renderer 1132 / org_panel 1020 / squad_card 1001（本批一并评估拆分）。
- [ ] **AR-4 信号与探针卫生（小）**：PerfProbe 已退出 project.godot autoload（实测 autoload 共 9 个，PerfProbe 保留为 dev 场景工具）✅；待做：死信号处置（team_ai_stance_changed / chunk_loaded / chunk_unloaded 死发射、settlement_updated 死订阅，审计 §1.4）；新信号须在 EventBus 参数表登记发射方与首个消费方。
- [x] ~~AR-5 分支/worktree 卫生~~ → **搁置**（创始人指示：先不用管；重启时重测再裁决）。
- [ ] **AR-6 装配守护（小）**：audit_deps.py 增红线检查（system_setup 深路径 preload = FAIL），AR-1 首批迁移后接线。
- [ ] **架构深化单开分支讨论**（创始人指示：AR 执行排期届时定；文档形态讨论时以下为既定事实——架构目录已有 README 地图页、总纲形态文档已试写并撤销（与既有文档定位重复），增量已并入 AGENTS 原则第 4 条/待办本节/模块依赖关系.md）。

## HD-2D 街景内容批次（2026-09-14 创始人反馈立项）

> 背景：主街 HD-2D 化的渲染/相机/碰撞修复已收口（`6af0b0fd`→`736182a4`），修复轮定稿口径——3D 相机与 2D 逐像素 1:1（32px/格）；行走带 688→1294（前端=3D 可见近沿），ground_bottom/地面蓝线/屏幕底沿/可行走边界四点合一；构图契约=地面占屏幕下 1/3、天际线基线压 1/3 线。

- [x] ~~**城墙+城门传送**~~（2026-09-14 完成，二轮定稿）：±67 格两端 3D 石墙板转角收边（墙顶垛口；正交视角下呈窄竖条读法）；门洞开在路面纵深带（z 8~14 ↔ 行走带 y 944~1136），门柱/叠涩/横梁深色石材读作门楼；**墙体碰撞整带封死，过墙唯一方式=城门传送带**（创始人口径"到门口就传送，门外也得传送过去"）——每端内外各一条 Area2D，方向判定（朝墙走才触发）+ 600ms 防弹跳，collision_mask=2（角色本体位）；出口 ChunkTrigger 随地图边界扩到 ±95 格自动落到墙外带外端（西出 road_a_b、东出 battlefield 不变）。设计师注：若门楼观感仍不够"门"，备选=补烘带穿洞的城市门楼卡（city_gate，alpha 拱洞）。
- [x] ~~**城门外树林带**~~（2026-09-14 完成，二轮定稿）：资源分布**接回 resource_gen 程序化算法**（群落散布+林区梯度，不手摆；创始人：算法就在那）——算法宿主 duck 对接（decoration_layer 属性 + get_terrain_type_at_cell 城内硬化），梯度档按 28 格墙外带压缩（净空 3 格+渐密 12 格），实测 124 点全落墙外、近墙稀远端密（兼作城中心→边缘密度渐变）；卡随点落（类型→自然物卡池轮转）。采集 AI 断点已修（--watch 探针实锤）：自然物卡实体碰撞把村民卡死在资源点 24px 阈值外（自然物全部去碰撞，同 2D 时代）+ 门缝夹角楔人（墙整带封死）；村民采集遇墙由 gate_router 组协议引导到门口，进传送带跨墙，实测穿墙劳作/换点/往返全链活。
- [x] ~~**战场 HD-2D 化**~~（2026-09-14 完成，创始人裁决修场景本体、不摘旅行链）：战场图（battlefield）重建为 **HD-2D 城郊战场**——`hd2d_battlefield.tscn` + `Hd2dBattlefieldMap`（继承主街宿主全套：碰撞映射/昼夜/相机镜像/角色进 3D），3D 层走 proto_hd2d **战场模式**（`battlefield=true`：无墙无街无楼群，开阔野地 + 战痕散布 BF_PROPS/BF_NATURE_SPOTS，资源点仍走 resource_gen 算法）。摆布口径：西半（含两缘入口带）标记硬化区=算法净空带不开树（战场开阔可列阵），东半渐密成林线（"往森林去"的方向感），密度 0.12；旅行链左出回主街（东缘落，穿东门进城）、右出去 forest_zone（原 战场↔森林 链保留）；进图不自动刷敌（出征与领地架构 §4.3，dev 验证走 verify_battle 直达）；旧 2D 战场以 `battlefield_2d` 独立 id 保留注册为 dev 空旷演练场（战斗/AI 测试开机图——测试依赖 2D 空旷图 + 秒级开机，不进旅行链）。随批修正：主街东出口进战场从其 LEFT 缘落（原 RIGHT 把人扔到战场最东端背对全部内容）；清除 game_root 里与场景触发器打架的死出口注册；proto 高清 src/ 贴图缺失改静默软探测（回退游戏档，不再污染报错自检）。验证：`tests/dev/verify_hd2d_battlefield.tscn` 全断言过 + 报错自检干净。诊断补充（2026-09-14）：全量矩阵中 test_cross_map_travel 两条断言（村B实例/反向入口位置）在 HEAD（`9641bb87`）上即已失败、与本批次无关；其余 7 个失败套件均为 `.tscn.suspended` 挂起套件（设施类待 HD-2D 重建）。
- [x] ~~**城镇规模+建筑种类**~~（2026-09-14 完成）：前排 9→11 栋（新增西门塔 tower_w6、风车 windmill_w6，谷仓/酒馆/东门塔重排让位）；背景层三段池扩到 26 种卡全量入池（教堂/法师塔/图书馆/兵营/炼金坊/联排/马厩/草棚/smithy2-4 各归西教区/中市集/东作坊三带），bg1 缝距收紧 1.2~2.6 格保全量可见；背景层限界随城墙收口 ±104→±65（墙外是野外天空）；灯距收中 ±48（越近墙越暗）；地面三级渐变（城心石板 ±48 → 夯土过渡 → 墙外野地）+ 台面近墙段换 v1 变体再暗半档。后排"推挤修重叠"逻辑已随 `9641bb87` 进布局驱动模式（算法村）。
- [x] ~~**F3 碰撞箱与实际碰撞不一致**~~（2026-09-14 修）：蓝箱/建筑箱/道具箱此前按 2D 原始 y 直绘，角色实际渲染在 3D 投影域——绘制统一走 remap_fx_pos 重映射（物理本就自洽，纯可视化错位）。
- [x] ~~**HD-2D 正式化迁址**~~（2026-09-15 完成）：3D 世界场景/脚本/char_sprite_3d/5 shader/63MB 随包 tex 镜像从 `tests/dev/proto_hd2d/` 迁入正式模块 `modules/hd2d/`（api.gd 契约 + scenes/scripts/shaders/assets），场景更名 `hd2d_world.tscn`；贴图解析优先级不变（烘卡机 temp/ 优先→随包镜像兜底，新机器 clone 即玩）；宿主 preload 换新路径，行为与公共 API 零变化。验证：双探针全绿（worktree 无 temp/ 纯镜像跑通）+ 自检干净。
- [ ] **HD-2D 职责拆分 v2**（缓期，2026-09-15 裁决）：world 根编排 + cards/placer/ground/walls/char_stage 组件化 + Hd2dMapBase 宿主基类提炼。当日 main 上 HD-2D 代码被并行会话演进 ~1000 行/20 新函数，试合并实证拆分冲突面过大 → 挂起；成果存档分支 `agent/hd2d-split-wip`（仅供结构参考），组件映射表 v2 与恢复指引见 `docs/项目/交接/HD-2D架构正式化-进度与交接.md`。触发条件：城镇生成器/投影收线批次收口、函数面稳定。
- [x] ~~**ResourceNode 2D 笔触视觉根除**~~（2026-09-14 完成，创始人令）：程序化树/岩块/贴图池/树冠闪绿光（2026-09-06 逆向特效）全删——树根闪光即 2D 树在 HD-2D 图上残留所致；采集飘字改走 fx_pos_remapper。
- [ ] **测试效率优化**（创始人 2026-09-15：测试太慢，先别跑）：全量矩阵现状 = boot 模拟 + integration/smoke 进程级并行，单轮数分钟且 dev 探针（dev_playtest --watch 等）每次都要完整装配主街。优化方向【提案】：① run_all 剔除/跳过悬置套件（消 7 假失败）；② 布局 JSON 等重资产探针常驻缓存或 `--fast` 档（跳过存档/音效/加载屏阶段）；③ watch 类采样探针复用单次 boot 出多组数据；④ unit 批量runner 并行度复核。
- [ ] **绿草地贴图换 AI 手绘源**（2026-09-16 创始人裁决：挂起，以后再换；当前程序化提亮版作过渡保留在库）。调研结论（当日 web 调研）：值噪声程序化路线画不出"草"——高质量风格化草地贴图皆由大量独立草叶笔触构成（暗底→中间调→亮边，3×3 偏移网格上作画保无缝，参考 ArtStation ZugZug 风格化草地教程/Reddit gamedev 无缝贴图经验谈）；项目内现成合格源图 = `stick-world/assets/environment/grassland.png`（1024，创始人认可的"原AI贴图"）与 `stick-world/assets/_raw/地面/grassland0~3.jpg/png`（2048，更亮，均色 0.50~0.53/0.69/0.22~0.30）。既定方案【提案】：源图→半偏移+羽化交叉混合做无缝（现边缘差 4%~7%）→ 派生 512 高清档/128 游戏档/亮度场法线/粗糙度/旋转变体 → 按管线既有文件名落 `temp/ground_tiles/` + 同步 `modules/hd2d/assets/tex/ground_tiles/`（消费端零改动，`_add_ground_plane_at` 已优先 512 档）。工具落 `tools/blender_buildings/`（纯 Python 即可，无需 Blender）。
- [ ] **过渡带/段落草色与草地贴图脱节**（同日发现）：`ground_tiles.py` 砖草过渡（tr_brick_grass）与段落/散料层的草色仍是旧深绿 `(0.300,0.430,0.145)/(0.155,0.252,0.078)` 与 `_pal("grass")` 旧档——当前运行时不消费这些图，但接进场景前须与新草地（提亮版或将来 AI 源）对齐，避免拼缝两侧色差。
