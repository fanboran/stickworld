# 火柴人视觉修复：进度与交接

> **用途**：跨会话交接单一入口。新会话恢复本任务从本文件开始读，不重新摸底。
> **任务**：修火柴人两大视觉问题——①胳膊与身体的描边融为一体；②缩放视野到很小时抗锯齿很差（创始人多次修复未果）。
> **愿景锚点**：[`docs/设计/系统/12-小镇生活与美术.md`](../../设计/系统/12-小镇生活与美术.md) §六。
> **工作区约定**：worktree `.temp/stickman-visual`（分支 `agent/stickman-visual`，已建好），会话在 worktree 内干活。**首跑测试前先 `godot --headless --path stick-world --import` 连跑两遍**（首遍只建文件系统索引，第二遍才导完资源；只跑一遍 fx 类单测假红）。

---

## 侦察结论（2026-09-09 已完成，勿重复摸底）

### 实现现状
- 火柴人**纯矢量绘制零贴图**：`modules/units/scripts/rig/stickman_skeleton.gd` `_build_limb`（:231-260）每段肢体 = 容器 Node2D + 两层圆头 Line2D——`stroke` 描边层（宽=肢体厚 23×scale + OUTLINE_WIDTH×2，z_index=-1）+ `fill` 填充层（宽=23×scale，z=0）；头部两层 40 边 Polygon2D。`OUTLINE_WIDTH=2.0`（:25）。
- **全局两遍渲染**（:227-230 注释自述）：所有肢体 stroke 全部 z=-1 压底、所有 fill 全部 z=0 置顶 → 全身填充无缝融合、描边只在整体剪影外缘露一圈。
- `_make_line`（:263-273）**显式不开 Line2D.antialiased**，注释声称"项目已开 msaa_2d"——该前提已失效（见下）。
- 相机：`modules/world/scripts/camera/camera_rig.gd` 用 Camera2D.zoom，`user_zoom ∈ [0.5, 2.0]`；**线宽是世界像素常量，无屏幕像素补偿**（units 模块 grep zoom 零命中；对照：world_map 有 b443c26a「描边改固定屏幕像素」先例）。

### 根因排序（已抓到实锤）
1. **project.godot 的 [rendering] 段被摧毁**：`stick-world/project.godot:63` 现为一行乱码键——4.7.2 解析器把段头前的中文注释吞行+乱码合并（同类事故两次：85a2f3c0、2c2865f7），`[rendering]` 段头连同 `msaa_2d=2(4x)` 一起落进了 [physics] 段 → **运行时 MSAA 2D 实际关闭，全项目零抗锯齿**。缩到 0.5 zoom 时描边 27 世界像素只剩 ~13.5 屏幕像素，锯齿必然刺眼。修复成本最低收益最大。
2. **全局两遍渲染结构性抹掉臂-躯干内描边**：任何 fill 都盖住一切 stroke，臂/躯干交界处的分隔线被躯干填充盖掉 → 读作融合体。这不是 bug 而是 14b6c67c 回退链式分层的**设计后果**（当年 e8eb6b3e 链式分层实现过分隔线，因接缝问题被整体放弃）。
3. 缩小时描边随 zoom 线性变细无下限 + 白描边对 0.82 灰身体对比度低 → 糊/融观感放大器。
4. 头部 40 段 Polygon2D 在 0.5 zoom 下每段 4.5°，无 AA 时有棱角（小项）。

### 历史修复线（勿重走弯路）
`97e2b8e0` 矢量化 → `936b28c5` 两遍渲染 → `e8eb6b3e` 链式分层（五条链 z 递增+肩髋胶囊补丁，**曾实现分隔线**）→ `835e1296`/`22310e87` 接缝补丁 → `c27d6900` SWL 槽位序 → `14b6c67c` 回退全局两遍（现状）。AA 线：`d6a24531`→`03e4aae0`→`208a1a5f`→`85a2f3c0`→`2c2865f7`（配置事故）。

---

## 任务分级调度（Pro / Flash × 会话批次）

> 通用验收（两级同规）：`check_godot_errors` 干净 + `run_all.sh` 全绿（涉 units 组必跑）+ 完成后回填本档 + 中文提交。
> **视觉验收一律派有视觉的 Subagent**（Agent 工具，读渲染 PNG；主会话无视觉只做指挥与代码审查）——多 zoom 档位（0.5/1.0/2.0）× 多姿态（站立/奔跑/攻击/死亡）截图，对照「线条干净、肢体可读、剪影完整」三判据。
> 新会话开场白：「继续 火柴人视觉修复 批次 N」。

| 批次 | 级别 | 任务 | 依赖 | 验收门 | 新会话必读 |
|---|---|---|---|---|---|
| **1** | Flash | **修 project.godot rendering 段**：修复 :63 乱码键、恢复独立 `[rendering]` 段与 `msaa_2d=2`（4x）等原值；**段头附近不放任何注释**（4.7.2 解析器吞行前科×2，如需说明移到本文档）；补一个启动期自检（可选：`ProjectSettings.get_setting("rendering/anti_aliasing/quality/msaa_2d")` 断言值>0，防再次静默失效） | 无 | check_godot_errors 干净 + run_all 全绿 + 视觉 Subagent 截图对比（修前/修后同场景同 zoom）确认锯齿改善 | 本档侦察结论；`project.godot:60-70` |
| **2** | Pro | **描边屏幕像素恒定 + 对比度**：线宽随 camera zoom 补偿（参考 world_map b443c26a 先例；StickmanRig 侧监听 zoom 或每帧读相机），设屏幕像素下限；评估描边色/身体色对比度微调 | 批次 1 | 0.5 zoom 下描边仍清晰可读（视觉 Subagent 判定）+ run_all 全绿 | `stickman_skeleton.gd:231-273`；`camera_rig.gd:423-436` |
| **3** | Pro | **臂-躯干描边分隔**：重做链式分层（e8eb6b3e 思路：五条链各描边压底/填充置顶、链间 z 递增）**并解当年接缝问题**（肩/髋交界补丁方案已两次失败，需新思路——候选：肢根局部暗色窄带/关节小圆点/剪影内关键线）；或整体剪影屏幕空间描边方案评估 | 批次 1（2 可并行） | 多姿态截图肢体分隔可读、无可见接缝 artifacts（视觉 Subagent 判定）+ run_all 全绿（test_ai_behaviors 等动画相关套件重点） | 本档历史修复线全部提交；`stickman_skeleton.gd` 全文 |
| **4** | Flash | **头部棱角小项**：批次 1 后评估是否仍可见，必要时 Polygon2D 边数/antialiased 属性处理 | 1/3 | 视觉 Subagent 判定 | — |

**依赖图**：1 → (2 ∥ 3) → 4。批次 1 是全局收益（全游戏 2D 抗锯齿恢复），**最优先**。

## 进度记录（每批完成后回填）

| 批次 | 状态 | 提交 | 备注 |
|---|---|---|---|
| 1 | ✅ 完成 | `2f02cc1d` | rendering 段修复 + 看门狗单测 + 视觉验收 pass；详情见"批次 1 记录" |
| 2 | ✅ 完成 | `5c7325f6`（经 `cd3be521` 合入主分支） | zoom 补偿落地；对比度评估后未改色，微调决策项移交创始人（见待办） |
| 3 | ✅ 完成 | `b5abe2b9` | 链式分层 + U 形开口描边，视觉 judge pass；详情见"批次 3 记录" |
| 4 | ✅ 完成 | 无提交 | 评估结论：棱角亚像素级不可见，无需改码；judge 目视复核 pass；详情见"批次 4 记录" |
| 4b | ✅ 完成（合并后补丁） | `565c347f` | 观感复验补丁：创始人质疑后跑修前/修后同机位对比（diag_stickman_shots，before=合并前基线 checkout），judge 像素级评审——AA/臂躯分隔/剪影三目标全达成，但抓到 1× 机位 0.65× 小体型单位描边过粗发白（补偿只按画布缩放、未除 rig 自身 scale）；修 outline_world_width(s×rig_scale)+rig_scale 变化帧重补偿，复验 pass（小/大单位描边实宽比 0.9×≈1，1.0× 零回退），units 组回归绿 |
| 4c | ✅ 完成（合并后补丁） | `395d08c1` | 创始人二次反馈修正：躯干-腿分离线（髋部）+肩部身体-胳膊分界线。根因=五链 z 递增（腿在躯干下层→躯干描边压腿填充；远侧臂在躯干上层→描边横在身上）+565c347f 修缩放后描边屏宽 1→2px 放大一切线。改为三层纸娃娃：身体层（腿+躯干+头同 z 融合）→远侧臂藏后→近侧臂保留分隔；OUTLINE_SCREEN_PX 回校准 1.0。judge 复验四项全过，近侧臂分隔不回退 |
| 4d | ✅ 完成（合并后补丁） | `a047bdee` | 创始人三次反馈：肩圈未融合+两腿应胯下才分界。骨架几何探针（probe_rig，留存 dev）实测定位——旧 U 形开口描边的多边形闭合边横在肩关节上；腿并身体层后两腿无界。修正=三件套真开口（两侧线+端弧，OPEN_ROOT_GAP=30 留隙肩圈）+显式描边/填充双 z 表（躯干描边沉两腿填充下=髋无线；躯干填充盖腿描边=胯融合；内腿描边压外腿填充=两腿分界）。judge 五项全过 |
| 4e | ✅ 完成（合并后补丁） | 见 git log | 创始人四次反馈：v4 两臂都开了根隙，远侧臂背侧肩描边被误伤（历史 AI 同款 bug）。OPEN_ROOT_LIMBS 收窄为 [1]（仅近侧臂），远侧臂恢复闭合描边。judge 四项全过 |
| 4f | ✅ 完成（合并后补丁） | `194262b5` | 创始人五次反馈点破：开口不应两侧对称——臂=长条，内侧线（贴身侧）肩圈留隙 30 融合，外侧线（背侧肩轮廓）从根部全程保留。OPEN_ROOT_GAP 改 Vector2 双侧独立；撤销误判的远臂补全件；diag 增肩部 4× 特写机位。judge 4× 复验四项全过 |
| 4g | ✅ 完成（合并后补丁） | `db4cda4e` | 创始人六次反馈三条：①线不进脑袋——静息留隙对手臂摆动失准（v4 缺口/v7 进头是同一根因的孪生症状），加运行时头圆动态裁剪（每帧侧线起点钳到圆外）；②腿上端开口 40（胯部融合，分界自胯下起）；③远臂上端开口 30。OPEN_ROOT 扩至 [1,3,11,14]。judge 四组全过 |
| 4h | ✅ 完成 | `51b9ae3f` | 标准站姿场景修通（链方向摆姿语义，塌块根因=±90° 绝对对准证伪）+IK 栈不解算实锤+缺陷清单 D1~D11 落档；详情见"进行中工作流"节 |
| 4i | ✅ 批次 0 完成 | 本提交 | 逆向重建启动（创始人拍板解包真实数据）：真相源从 APK 重提（zombie.txt=核心骨架 56 骨 93 动画）+覆盖率方案落档+旧提取器五件套删除+IK 死重清理；批次 A~D 待做，总纲见覆盖率方案文档 |
| 4i-A | ✅ 完成 | 见 git log | 新导入器 `tools/spine_importer.py`：56 骨全量表 + 93 动画全通道精确曲线 + 事件元数据 + skins 表 + 覆盖矩阵（coverage_ok=True）；验证全绿（详见"批次 A 记录"） |
| 4i-B | ✅ 完成 | `eec7313d` | 渲染重标落地：56 骨真值骨架 + 附件几何矢量肢体 + 动画切真 Spine 数据；unit 47/47、check_godot_errors 干净、受影响集成套件串行全过、视觉 judge 两轮后 PASS（D1~D11 实质清零）；详见"批次 B 记录" |

**四批全部完成，全部合入 `agent/stickman-visual`**（批次子分支 batch2/3/4 已验证并入并删除；最终改动以 git log 为准）。创始人委托主会话统一验收（逐批代码审查 + 视觉 judge 全过），终态全量 run_all **38/38 绿**（含 4 个已知 flaky 重套件）。**唯一剩余项 = 合并回 main**。

### 合并回 main 注意事项（2026-09-09 核对）

- main 已由 `c029c705` **独立修复了同一 project.godot 事故**（乱码键吞 [rendering] 段头），恢复值与本分支批次 1 完全一致——无实质冲突，唯一差异：**main 删除了 `[godot_ai]` 段、本分支保留**。合并时 project.godot 建议整体取 main 侧（连带删 [godot_ai]）。
- `docs/项目/待办事项.md` 在 main 被 `4cfde92d` 全量对账重写过；本分支顶部新增的「火柴人视觉修复」段（含对比度微调、远期提边数两项挂账）届时需手工并入新版待办。
- 本分支落后 main 27 个提交（含主菜单整改、UI 主题性能修复等无关改动），建议合并前先在本分支 `git merge main` 解完冲突再跑一遍全量，或直接在 main 侧解。

## 批次 1 记录（2026-09-09）

- **修复**：`project.godot` 删除 :63 乱码键，恢复独立 `[rendering]` 段（`rendering_device/driver.windows="d3d12"` / `renderer/rendering_method.mobile` / `msaa_2d=2` / `msaa_3d=2`），段头附近零注释。中文注释不回填文件（纪律），说明只在提交历史与本档。
- **看门狗单测**：`tests/unit/test_project_render_settings.gd`（已注册 batch_runner，unit 批量 41→42 套）——断言 `msaa_2d==2` / `msaa_3d==2` / `driver.windows=="d3d12"` / `physics_ticks_per_second==30`。段头再被吞时键路径失效、get_setting 落回默认值，测试立刻红，不再静默。
- **截图对比**：新增 `tests/dev/diag_stickman_shots.tscn`（挂 unit_action_gallery 站姿全景，三档机位 zoom 1.0/0.5/2.0，`--outdir/--prefix` 参数化）。修前（stash 事故版）修后各三张，视觉 Subagent 判定 **pass**：痛点场景 zoom_050 改善明显（修前轮廓碎裂成散点→修后线条连贯剪影扎实），zoom_100/200 轻微改善，无渲染回归。截图在 worktree 外 `F:/VSCode/game-2/.temp/shots_stickman/`（不入 git）。
- **验证**：worktree 首跑 import ×2 后 check_godot_errors 干净；unit 批量 42 套全过；全量首跑 4 项失败（melee_combat/esc_key_input/battle_retreat/new_game_smoke）为 4 并行跑重套件的负载超时 flaky——带改动单独重跑全过，与本次改动无关（改动仅涉渲染配置与新增测试文件）。
- **批次 4 前置观察**：视觉判定 zoom_200 特写头部轮廓"阶梯感减弱、更均匀"，棱角小项是否还需处理待批次 3 后再评估。

## 批次 2 记录（2026-09-09）

- **方案**：`stickman_skeleton.gd` 新增 `OUTLINE_SCREEN_PX=2.0`（屏幕像素下限）与 `outline_world_width(s)=max(OUTLINE_WIDTH, OUTLINE_SCREEN_PX/s)`（思路同 world_map `b443c26a`），`apply_outline_zoom` 批量刷描边几何；`stickman_rig.gd` `_process` 每帧仅一次画布缩放浮点比较、变化才批量刷新（缩放=滚轮/改窗低频），`_do_rebuild` 后强制重应用，编辑器内不补偿。zoom≥1 区间行为与改前完全一致（下限生效），默认/特写观感零变化。
- **视觉验收 pass**：zoom 0.5 修前单位读作灰团→修后白描边剪影分明（含 0.65× 小护卫）；zoom 1.0/2.0 与修前完全一致；多姿态（idle/run/attack/dead）× 三机位无回归。
- **对比度评估（未改色）**：描边可读性实际由描边-背景对比（白 vs 近黑 ~19:1）提供，描边-身体 ~1.5:1 属"亮边/轮光"设计意图；zoom 补偿落地后拉远已可读。**微调备选留给创始人**：描边微降暖灰（~0.92）或身体微降（~0.76）可提至 ~1.8:1（已登记待办）。
- 新增多姿态截图 driver `tests/dev/diag_stickman_pose_shots.tscn`。

## 批次 3 记录（2026-09-09）

- **方案**：恢复五链 z 分层 `CHAIN_STROKE_Z`（外腿 -3 → 内腿 -1 → 躯干+头 +1 → 内臂 +3 → 外臂 +5，填充=描边+1），链间高链描边压低链填充 = 分隔线可见、链内保持融合。**接缝新思路 = 前臂根部 U 形开口描边**（`OPEN_ROOT_LIMBS` + `_make_open_root_stroke`：两侧边+末端半圆、根端平口无端帽弧）——臂根方向不产生环绕弧线，填充根帽同色融合进躯干，从几何上消灭"缝"的产生，弃用历史三次失败的"盖补丁"路线。z 压缩 [-3,+6]（单位带 EntityHost z=3 → 全局 0~9 < 地图前景层 10），规避 e8eb6b3e 当年未处理的室内遮挡隐患。武器/盾相对 z 0/1→7 盖全身肢体。
- **视觉验收 pass**：修前臂-躯干融合团 → 修后各姿态（idle/run/挥砍三帧/dead）分隔连续可读；肩部微距平口融合无裂缝/环绕缝/色块；剪影完整、七兵种全景无回归。
- **已接受小项**：武器/盾全局 z=10 与前景层同值，武器尖角过前景支柱时可能轻微盖柱（身体遮挡不受影响）。
- 新增多姿态截图 driver `tests/dev/diag_stickman_poses.tscn`。

## 批次 4 记录（2026-09-09）

- **结论：无需改码**。三重证据：①理论——40 边形弦高 ≈0.065 逻辑 px，现实缩放（1~3×）下亚像素；②定量——头环圆拟合残差 RMS 0.241px = 像素量化底噪、频谱无 40 边形相干峰；③目视——6× 放大头弧平滑无棱角。视觉 judge 复核 pass。
- 唯一残留：极端 >15× 缩放（游戏内无此机位）棱角理论上达 1px，届时改 `_make_circle_at` 的边数一行即可（已登记待办远期项）。

## 合并与收线记录（2026-09-09，主会话）

- 批次 2/3 并行于独立 worktree（各拉分支），合并自动成功（常量区插入点错开无冲突）。合并暴露交互问题：批次 3 的 U 形前臂描边（Polygon2D+Line2D）不匹配批次 2 补偿逻辑的两个分支、被静默跳过——已修（`_open_root_pts` 顶点生成抽出共用 + `open_root_half_len` meta 识别），提交 `7ca7197f`。
- 合并后验证：check_godot_errors 干净 + unit 批量 42 套全过 + 视觉 judge 三机位确认（U 形补偿生效 / 分隔保持 / 无渲染异常）。
- 批次 worktree `stickman-batch2/3/4` 已清理；主分支待创始人观感验收后合并回 main。



---

## 进行中工作流：标准站姿 + 观感缺陷狩猎（批次 4h，2026-09-10 更新，**阻塞已解除，待创始人选重构路线**）

> 背景：4b~4g 六轮描边修补全部提交且测试绿，但创始人仍不满观感，并连续点破根因方向：
> ①要求建**标准站姿独立测试场景**（一个火柴人、空手、无动画——动画摆动让静态留隙
> 时准时不准）；②**授权底层重构**（原话："可以从底层彻底重构，我们这是烘焙出来的
> 火柴人，直接尝试用矢量图开始重构都可以"）。

### 标准站姿场景（已定稿）：`tests/dev/stickman_relaxed_pose.gd/.tscn`

- **裸 rig**：直接实例化 `stickman_test.tscn` 并 `set_script(null)` 剥离 legacy 驱动；
  rig 置 (960,735) scale 0.5；5 机位截图 full(1.2×)/head(5×)/torso(3×)/legs(3×)/shoulder(6×)。
- `--pose=idle|hang|rest`：idle=播 idle 冻结 t=0（**游戏基线站姿**，默认）；hang=双腿
  双臂按链方向直摆（对称垂放诊断档，暴露肩点前伸/双腿重叠）；rest=纯 bind 姿态。
- `--ik=on|off`（默认 off）、`--verbose`（结算后逐骨 dump grot/gpos）。
- 程序化叠加层恒关（`ProceduralOverlay.ENABLED=false` 静态开关）；截图输出 `--outdir=`。

### 阻塞根因（已解除，两件事叠加，勿回退）

1. **`_aim`±90° 绝对对准语义证伪**：本骨架 rest 旋转全 0、肢体方向烤在子骨偏移里
   （Spine 导入遗留）。正确语义：**链方向 = 子骨偏移角 + 骨骼全局旋转**；摆姿=按此
   语义做小增量（场景 `_pose_hang`/`HANG_CHAIN` 即正确示范）。
2. 裁剪/覆写干扰：IK 栈延迟一帧启用、ProceduralOverlay 每帧绝对复位——场景里
   等 2 帧后复位 rest 再摆姿 + overlay 恒关后即完全确定。

### 批次 4h 新实锤（一手证据，修正旧记录）

1. **IK 修改器栈不参与解算**：裸 rig 实测——结算 6 帧后骨骼值与 idle t=0 动画值
   **逐度相等**；两套不同 IK 目标 Marker 输出完全一致。4 个 TwoBoneIK 修改器
   enabled 但从不移动骨骼。游戏内同代码路径（推断，未单独实测）→ tscn 的 4 个
   Marker 目标、overlay 的手目标抖动、legacy 驱动的 Marker 同步**全是死重**。
   **limb 姿态 100% = rest 几何 + 动画绝对旋转**。（清理死重=独立待办，需创始人确认）
2. **idle ≈ rest**：idle 动画对骨骼只有 <3° 旋转微调（唯一 position 轨在 hip），
   idle t=0 骨骼全局位置与 rest 几何几乎重合。旧记录"rest 驼背、idle 才立直"**证伪**
   ——源于塌块截图误读 + 1× 小图轮廓错觉（4× 特写里画廊单位同样含胸前倾，
   对照 `.tmp_shots/v8_zoom_400_shoulder.png` vs `relax_shoulder.png` 几乎同姿态）。
3. 旧谜团"bone21 被覆写回 0.02"破案：0.02 就是 idle 动画的轨道值（AnimationPlayer
   正常播动画，非异常）；ProceduralOverlay 每帧绝对复位 hip/torso/head 是另一处
   "覆写"，均为设计行为。
4. 骨架命名错位（几何混乱的源头之一）：骨骼名="到子骨的段"，故 sprite_1（名为
   forearm_outer）实际画的是**上臂**（肩→肘段，根端帽在肩点=头圆高度）；大腿骨
   type=-1 无渲染，可见"大腿"实为 sprite_3（shin_outer 的 sprite 画在 thigh 骨上）。

### 缺陷狩猎清单（idle 基线，主 agent 亲眼判读 2026-09-10，截图 `.tmp_shots/`）

结构性（=SKELETON_DATA 几何比例，重构对象）：
- D1 无脖+头吞肩：neck 骨无渲染段，头圆心距肩点 11px、r19 圆覆盖肩关节，头下直接是躯干。
- D5 肩点前伸：臂/颈同挂 (10.4,-29.2) 偏移，肩点 x=17.9 前伸；直臂垂放悬在躯干前（hang 档实证）。
- D6 躯干短细：髋→肩仅 90.6px（≈头直径 2.4 倍），躯干线厚 23px。
- D7 背线不连贯+含胸 12°：躯干 3 段偏移角 3.3°/10.6°/10.6° 逐段前倾，剪影读作驼背。
- D9 无骨盆：双腿从髋部单点发散，上端剪影锥形入胯。
- D10 脚掌仅 11px：圆头帽后读作半圆突起，"没有脚"。
- D11 整体含胸站姿（D1/D5/D7 的合成观感）。

描边/机制性（几何修正后可大幅缓解）：
- D3 近臂双侧描边线横穿躯干填充区读作"白色缝线"——臂根在头圆高度所致。
- D4 远臂上段与背部融合成厚板——z 分层设计后果+臂根过高。

已修好保持（4d~4g 成果，基线上验证无回退）：胯部融合+两腿分界自胯下起笔+近臂线
不进脑袋（运行时头圆裁剪生效中）。

### 重构路线（建议 a，待创始人拍板）

- **路线 a（推荐）：换 rest 几何**。可行性升级的两块基石：动画只写**绝对 rotation**
  （idle 全轨验证）+ IK 不参与解算 → **只改 SKELETON_DATA 的 x/y/length，不动骨名/
  动画/描边机制**即可整体换几何；新几何下动画旋转轨道仍成立（链方向=偏移角+旋转，
  动画增量照常叠加），只需画廊逐条目检各动画姿态观感 + 重标 OPEN_ROOT_GAP 等留隙
  常量。工作量≈一张数据表+全动画目检。
- 路线 b：平行纯矢量渲染器，动画系统后续重烘焙（大工程，仅当 a 的动画观感校验
  失败率过高才值得）。
- 新几何设计要点（缺陷清单反推）：头在肩上+真脖颈段；真大腿渲染（thigh type 补
  ROUND_SEG）；臂根回肩点正上方；脚加长；骨盆/肩宽微张；躯干加长立直。

### 下一步

1. 创始人选路线（本档已附推荐与理由）。
2. 路线 a：新几何比例表设计先行（本文档规范：属 AI 提案，标「提案/待定」）→
   改 SKELETON_DATA → 画廊逐动画目检 → 留隙常量重标 → 缺陷清单逐项复验。
3. IK 死重清理（独立小项，可与重构并行或先行）：删 tscn 修改器栈+Marker、overlay
   手目标抖动、驱动 Marker 同步——先与创始人确认再动手。
4. 收尾：归档截图结论进本档（已完成）、清理 probe_rig 遗留（probe_rig.gd.uid 待入库）。

### 关键决策速查（4h 增补）

| 决策 | 结论 |
|---|---|
| 摆姿语义 | 铂方向=子骨偏移角+骨骼全局旋转（rest 旋转全 0）；±90° 绝对对准=放倒 |
| IK 修改器栈 | 实测不参与解算（裸 rig 结算值=动画值逐度相等）；Marker/overlay 手抖动=死重 |
| 标准站姿定义 | idle t=0 冖帧≈rest；玩家实际看到的就是这个含胸姿态 |
| 重构路线 | 创始人拍板：**从火柴人战争解包真实骨架数据重建**（非自行设计），方案见覆盖率方案文档 |

---

## 批次 4i：逆向重建启动（2026-09-10，创始人拍板后批次 0 完成）

> 创始人指令：把旧提取器彻底删掉、从零逆向吃透原理、对比两版 SWL 角色模型、给出完整覆盖率方案。
> **总纲文档：[`docs/设计/系统/火柴人逆向重建与覆盖率方案.md`](../../设计/系统/火柴人逆向重建与覆盖率方案.md)——接手必读，本节只记执行状态。**

### 真相源恢复（关键解锁）

- 旧真相源 `game-2-aux/external/decompiled/...` 已被清空（目录为空），**已从 APK 重新提取**：
  `tools/swl_extract_spine.py`（UnityPy）→ `external/decompiled/legacy/spine_raw/`
  （规范名 `[skeleton].txt`/`[universal.atlas].txt`/`textures/universal.png` 已恢复；不进 git，丢了重跑脚本即可）。
- 核心单位骨架 = APK 内部资产 **`zombie.txt`**（开发者占位名）：56 骨 / 93 槽位 / 22 皮肤 / 93 动画 /
  事件 5 类（Hit/Sound/Drawn/Mine/Cast）；IK 约束仅 2 条且**核心动画 0 使用**。

### 关键实锤（旧管线为什么烂，全部一手验证）

1. 真骨是标准 Spine 语义：骨长沿轴、带 setup 旋转（躯干三段 30.5/29.7/28 沿轴；大腿 79.5 有真长度；
   脚 27 骨长+51px 附件；脖/头 32.4 长 -39.6°）；旧 SKELETON_DATA 把 setup 旋转全丢成 0、长度全丢、偏移错位
   ——缺陷 D1/D5/D6/D7/D10/D11 全部对上。
2. 旧动画只译 rotate（translate/scale 全丢——Stand1 里 17 骨全带 translate）、bezier 曲线全按直线插、
   17/56 骨子集映射、还有 KEYFRAME_FIXES/wash_anims 洗稿污染。
3. `.nodes` 2021 units-pack（`external/swl_extracted/`）：无名私有二进制（UnityPy 零对象、无 ASCII 名、
   无动画），无对账价值，弃用；APK 菜单小骨架（menuarcher 8 骨等）仅展示用，不采用。

### 批次 0 已执行（本批次提交）

- **删除旧提取器五件套**：`spine_import.*`/`bake_anims.*`/`wash_anims.*`/`verify_anims.*`/`inject_anim_events.*`
  （现役 .tres 保留不动，批次 A 重烘后自然替换）；保留 `dump_rig_pose`（验证链 Godot 侧）、`extract_weapons`（武器贴图独立管线）。
- **IK 死重清理**（创始人批准）：`stickman_test.tscn` 删修改器栈；`stickman_rig.gd` 删 `_init_ik`/`_enable_ik_stack`；
  `procedural_overlay.gd` 删手目标抖动/攻受弹簧（notify_attack/notify_hit 本就无调用方），保留呼吸/惯性/微抖；
  Marker 保留（实体 foot_offset 仍消费）；全库注释引用更新。
- 新增工具：`tools/analyze_spine_skeleton.py`（骨架全量分析器）、`tools/render_spine_bones.py`（真值骨骼线渲染）、
  `tools/swl_extract_spine.py`（真相源再提取）。
- 验证：check_godot_errors 干净；unit 批量 46 套全过；relax 场景渲染与清理前逐帧一致（死代码无视觉贡献）；
  全量 run_all 另行跑（见进度记录表）。

### 下一步（批次 A~D，接手从覆盖率方案文档开始）

1. ✅ **批次 A 完成**（见下方"批次 A 记录"）。
2. ✅ **批次 B 完成**（见下方"批次 B 记录"）。
3. 批次 C：逐动画 RMS 数值验收——真值侧 `dump_spine_pose.py` 需先升级（现全按 linear 近似求值、不含 translate/scale 通道、未做 Godot 域映射；批次 B 实测其躯干位置与游戏渲染偏差 ~10 单位即因此），参照导入器求值器重写；`tools/baking/dump_rig_pose.gd` 已适配骨名键。
4. 批次 D：画廊逐动画目检 + D1~D11 缺陷复验（批次 B 已按标准站姿预清一轮）+ 修前/修后对比，judge 终验。
5. 批次 B 遗留（可并入 C/D 或单独小批）：装备贴图件（helm/bag/Quiver/盾）——几何已入 `SpineRenderData.EQUIP`、承接节点 `attach_<槽>` 已建，缺图集裁切与贴图渲染管线；兵种步态仍用通用人形组（SPINE_MAP 全局，Spearton 原版无 Walk 只有 Run）；武器几何从 skin 表直读（§5.1.1）未接，现役武器 tscn 照旧挂 pickaxe1/Arrow1。

### 批次 A 记录（2026-09-10）

- **新导入器** `tools/spine_importer.py`（Python，真相源路径自动回退主 checkout——external/ 不进 git）。产出四件：
  - `modules/units/scripts/rig/spine_skeleton_data.gd`：56 骨全量表（Spine 原值域 y-up/逆时针/度，消费端转换 pos=(x,-y)、rot=-弧度），每骨带 render 初判（core/attach/skip+理由，批次 B 重标）。
  - `modules/units/animations/spine/*.tres` × 93：**新目录，现役旧 .tres 未动**（新轨道路径指向 56 骨新树，批次 B 消费端切换后再删旧目录）。rotate/translate/scale 全部 bezier 轨道精确表达（bezier 段数学等价、stepped 段 ε 折叠 1ms 陡坡、linear 段零 handle）；rotate 值=-(setup+解缠绕 delta) 转弧度；translate 映射 (setup.x+tx, -(setup.y+ty))、root 骨 x 丢弃防漂移（--root-tx keep 可保留）；scale=setup·倍率。
  - `modules/units/data/spine_skins.json`：22 皮肤附件几何（批次 B 武器/装备直读）。
  - `docs/设计/系统/覆盖矩阵.json`：逐动画通道/键/事件/loop 对账，`coverage_ok=True` 自动断言（rotate 1837 / translate 1803 / scale 1776 全对账、事件 52/52、shear 6 轨道弃用登记、deform 34 动画与 drawOrder 5 动画弃用登记、attachment NULL 43 键→visible 轨道 73 键）。
- **Godot 4.7 bezier 轨道语义实锤**（决定性实验 `tests/dev/bezier_*_probe.gd`，已落方案文档 §2.1.1）：handle x=**绝对秒偏移**（非比例！此前对称短段实验有盲点误判）、y=绝对值偏移；换算 `out=(x1·Δt, y1·Δv)`、`in=((x2−1)·Δt, (y2−1)·Δv)`；.tres points 每键 5 floats+times 独立数组、keys dict 非末项必须有逗号。本数据曲线字段是 Spine 4.x 字段风格（curve=x1/c2=y1/c3=x2/c4=y2）。
- **循环标记**：资源侧忠实原版——Stand/Walk/Run/Idle/Hold/Pushup/Sit/Zombie-Crawl → LOOP_LINEAR（33 个），其余一次性（Miner-Attack1 忠实为一次性，游戏 build 循环语义由批次 B 映射表处理，防攻击播完不回切）。
- **看门狗单测** `tests/unit/test_spine_import_assets.gd`（已注册 batch_runner，47 套）：93 tres 加载+轨道非空、事件 52+Hit@1.0、循环标记、bezier spot check（3 轨道 15 点，≤0.5°）、骨架表 56 骨/setup 基准。
- **验证**：单测 5/5；随机 30 动画 708 采样点对账 0 超阈（max_err≈1.1e-5，`tests/dev/importer_sampling_verify.gd`，期望由导入器独立求值器生成）；check_godot_errors 干净；unit 批量 47/47。踩坑修复：rotate 轨道一度直写度数（Godot 要弧度）、bezier handle 一度按比例写（Godot 4.7 是绝对秒）——均由 spot check 抓获回修。
- **遗留到批次 B**：`attach_<槽位>` 节点未建（visible 轨道播放前需建，否则日志报 track path 警告）；visible/事件消费路径已就绪（`anim.has_meta("anim_events")` 机制）。

## 批次 B 记录（2026-09-10，渲染重标）

### 架构决策（三件套，接手必读）

1. **数据 1:1 保真，不做任何烘焙缩放**。56 骨表中 x/y/len/rot 保持 Spine 原值；
   原因：93 个动画的 translate/scale 轨道是**绝对值**，烘进骨架的任何缩放都会
   与动画脱节（56 根骨全带 translate 通道，实测）。缩放一律走节点 `scale`。
2. **合成锚点骨 `RigRoot`**（无任何轨道）：承载"髋骨对齐 rig 原点"的平移
   （`SpineRenderData.RIG_ANCHOR = (4, 183.02)`，Godot 局部系）。锚点不能烘到
   root 骨 setup 位置——root 有 position:y 轨道（躯干起伏），每帧会覆写回 0。
   所有动画轨道路径因此带 `RigRoot/` 前缀（导入器 `bone_track_paths` 统一生成）。
3. **渲染缩放锚定「髋→脚底屏幕距离不变」**：真骨架髋→脚底 188.86 骨单位
   （旧手搓骨架 131），`BASE_SCALE 0.5 → 0.3468`（代码默认 + `balance.variables.
   var_base_scale` 同步改），实体场景判定盒授权值统一 ×1.4418 补偿 →
   **foot_offset(65.5px)、碰撞/受击/悬停盒世界像素逐值不变，玩法判定零变化**。
   整体身高约 +9%（真数据腿更长、头更小），比例更正。

### 批次 B 实锤（一手验证，接手勿重查）

- **Bone2D 动画轨道路径必须是全相对路径**（`RigRoot/root/bone/...:rotation`）：
  AnimationPlayer root 指向 Skeleton2D、Bone2D 是嵌套 Node2D，扁平骨名
  （`bone3:rotation`）解析不到、骨骼纹丝不动。证据探针留存
  `tests/dev/bone_path_probe.gd`（批次 A 产出的扁平路径已由导入器全量重生成修正）。
- **mesh 附件顶点格式**（Spine 3.8 加权 mesh）：`vertices` = 每顶点
  `[影响数 n, (骨索引, x, y, 权重)×n]`，x/y 在**该骨局部系**；加权平均得世界点。
  躯干 mesh 41 数值 → 14 顶点，三角形集合提取"只出现一次的边"即得有序边界环
  （10 顶点，已烘进 `SpineRenderData.TORSO_POLY`，minertorso1 局部、Spine y-up）。
  躯干按静态多边形渲染（mesh 无 deform 动画；骨权重形变未做，属已知简化）。
- **肢体几何全部从附件表直读**（`SpineRenderData.CORE_ATTACH`，勿手调比例）：
  肢体 = 附件 w×h 圆头胶囊（臂 30×98 / 腿 30×114，跨关节超出量即原版关节融合），
  脚 = 51×30 横条，头 = 130×130×0.6215 圆（r≈40.3）。渲染端按 (w,h,rot) 反算
  胶囊两端点，根端 = 骨局部 x 较小端。
- **`.tscn 节点体末尾的中文注释会吞掉其后的段头`**（4.7.2 解析器，与
  project.godot 事故同源；实测 `stickman_entity.tscn` 注释后 RigHost+Collider
  两节点直接消失）。纪律：**tscn 注释只能写在属性之间**，不能写在节点体末尾。
- **Miner-Mine 的原生命中事件是 `Mine@1.2` 不是 Hit**：`StickmanAnims.
  WEAPON_HIT_EVENT`（镐=Mine）+ `weapon_mount` 结算/宽限口径按武器取事件名。
- **动画库键 = 游戏动作名**（`SPINE_MAP`：动作名→Spine 原名，资源在
  `animations/spine/`），状态机/变体池/事件订阅/命中帧机制**零改动**；
  旧 `animations/*.tres` 目录与 `tools/animation/generate_walk_carry.gd` 已删。

### 渲染分层（链带 z 表，批次 B 重标）

远臂(-7/-6) → 躯干描边(-5) → 远腿(-4/-3) → 近腿(-2/-1) → **头描边(0)** →
躯干填充(1) → 头填充(3) → 近臂(4/5)；全局 -4~8 < 前景层 10。
头部要点：**头描边沉到躯干填充之下**——头圆底弧压在颈/躯干填充上会读作"白色
缝线横穿肩颈"（视觉评审第一轮 FAIL 的唯一必修项），沉下去后由躯干剪影自然裁掉
底弧，头轮廓到肩线为止；头填充仍在躯干填充之上盖住颈茬。
开口描边（两侧线+端弧、根部留隙）重标到新骨名：近臂上段 30（肩）、臂/腿下段
34/36（肘/膝=上段超出量）、腿上段 40（胯）；近臂双线保留运行时头圆动态裁剪。

### 验收

- `check_godot_errors` 干净；unit 批量 **47/47**。
- 受影响集成套件串行全过：melee_combat 5/5、anim_finished 2/2、combat_feedback
  6/6、ai_behaviors 9/9、possession 15/15、placement_grid_units 11/11、
  combat_control 4/4、battle_retreat 4/4、recruit_flow 5/5、town_life_worksite
  3/3、battle_lifecycle 7/7、squad_travel 4/4。
- 视觉 judge 两轮：第一轮 FAIL（唯一必修项=头底白弧横穿颈肩，D1~D11 其余全消），
  修头描边 z 后第二轮 **PASS**（遗留软建议：膝/裆交叠轻微分界线、肩部 6× 下
  颈侧描边终点短划，不阻塞）。截图 `F:/VSCode/game-2/.temp/shots_bB/`
  （idle/hang 两档 × 5 机位 + 真值骨骼线参考，不入 git）。

### 环境实锤（本轮排障副产物，影响所有线的全量跑）

- **遗留 `run_all.sh` 僵尸编排进程吃满 CPU** → 全量并跑级联超时（一次 21/41 失败）。
  排查：`Get-CimInstance Win32_Process -Filter "Name='bash.exe'"`，杀掉
  CommandLine 含 run_all.sh 的遗留进程后恢复。套件超时由 run_all 的
  `kill -9 $pid` 不杀干净 Windows 子进程导致，属测试基建已知缺口。
- **同机基线对照证明无性能回归**：headless `battle_perf --headless-measure`，
  批次 B 前基线（8958d62b）= 4.8 tps（speed_ratio 0.16），本分支 = 7.8 tps
  （0.26）。带渲染 96v96 的 1 FPS/超时全部是机器负载（MuMu 模拟器/msedge 等
  常驻吃满 CPU），与骨架重建无关。
- 预存在问题（非本批次引入，登记待办）：`tests/unit/test_road_walk.gd:83` 调用
  不存在的 `PackedVector2Array.is_equal_approx` → 套件加载失败被批量**静默跳过**
  （批量仍报 47/47）；顺手修 API 后暴露更深的道路生成器/测试契约脱节
  （ChunkTriggers 节点缺失），已回滚保持批次边界，待道路线处理。

## 关键决策速查

| 决策 | 结论 |
|---|---|
| AA 第一根因 | project.godot [rendering] 段被解析事故摧毁，msaa_2d 静默失效（修它=最大收益） |
| 描边融合 | 全局两遍渲染的设计后果，非意外；修复需重解 e8eb6b3e 时代的接缝问题 |
| project.godot 编辑纪律 | 段头附近禁注释（4.7.2 吞行前科×2） |
| 视觉验收 | 主会话无视觉，一律派视觉 Subagent 读截图评审 |
