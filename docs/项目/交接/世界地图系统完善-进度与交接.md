# 世界地图系统完善：进度与交接

> **用途**：跨会话交接的单一入口。新会话恢复本任务时**从本文件开始读**，不重新摸底。
> **真相源**：[`世界地图系统完善总体设计.md`](../世界地图系统完善总体设计.md)（docs/项目/ 下）——设计/决策/技术方案全在那边，本文件只记进度与下一步。

---

## 快速恢复（新会话从这里开始）

- **分支**：`agent/world-map-revamp`（**地图任务唯一分支**——pro/flash 双分支 2026-09-06 已合并并删除（P2/B3/P3.5/P4 blob/P5 进城/F5 道路渲染/F8 全景全部并入），合并结果已进 main；此后地图相关任务全部在本分支直接处理，不再开子分支）→ **2026-09-08 观感返工起任务迁至 `agent/world-map-rework`**（变基后旧分支名退役；旧哈希 97e22cc8/a6d694c4 等在 main 上对应新哈希）
- **工作区**：`.temp/world-map-rework`（2026-09-08 建，基于 main `0e80f973`；worldgen 再生产物已复制：heightmap/河湖 mask/regions/l3_view；l1_v2 旧标签与 l2_packs 大中间件未复制——world_origin 已在包内、P7 有免复制路径，需要时再从主工作区搬）
- **必读**：总体设计文档 §二（创始人已拍板决策）、§四（分期 A-F）、§五（各期技术方案）
- **自检**：`bash stick-world/tools/check_godot_errors.sh`（退出码 1 必须修复）；测试 `bash stick-world/tests/run_all.sh`
- **当前阶段**：**2026-09-08 创始人观感验收：多项否决，反馈 18 条已立案返工（R 系列）**——诉求清单/根因/算法调研/修复方案/技术栈检讨全在 [`../世界地图观感返工-诉求与方案.md`](../世界地图观感返工-诉求与方案.md)（本轮返工单一真相源）；**R 系列全部实施完毕并已合 main（`ed815a25`，含两批观感反馈修复），创始人 2026-09-10 第三批反馈 4 条未修、见下方「创始人第三批观感反馈」专节（交接下一会话）**：R0 弹窗 ✅、R1/R2（`d4e7c809`）、R3（`c98d5bf8`）、R9 烘焙管线（`e02625b9`/`3a09f78c`）、R8 层1 HUD 换肤（`a6ed3a66`）、R6 道路生成端（`537a8685`）、R4 三模式+R6 渲染端（`9f4bed34`/`3d3ee322`）、R5 建成区生成端（`f7f0c815`）、R7 政权 80 国+ID mask/LUT（`f45a3128`/`cec85931`/`15a1bb71`/`4c0be3ff`）、R5 运行时三档接入（`377a7a04`）、R8 层2 线条 SketchDraw 同源（`8f1fe999`）、R8 层3 标注/星标（`c5a1a541`）；各批验收图见 `tools/worldgen/output/` 的 `r*_*.png` 系列。此前记录：P7 政权简化版 ✅ 已实施（unit 38/38 + 全量 32/32，观感验收未过→转 R7）；装备系统架构设计 ✅；F6 ✅ 道路场景+步行旅行已实施（观感验收未过→道路渲染转 R6）；下一步 = 批2（R9 L1 烘焙管线基建）
## 创始人第三批观感反馈（2026-09-10，**待下一会话处理**）

> 创始人审查 L3 政治全景截图（feedback2_l3_country.png 与 main 实机）后的反馈，**原话语义忠实记录，未修**。前两批反馈已修（RGBA8 掉色/去抖动/描边字号/城邦色/无归属洞等，见 main `ed815a25`）。

1. **「边界线上的白色你看不到吗」**——L3 政治图上的**白色国界实线样式本身不被接受**（上一批只修掉了「位置错乱的乱白线」，白色界线仍留，深蓝底上扎眼）。修前须与创始人确认期望样式（制图选项：深色勾边/邻国色加深/更细暗线等），不要自作主张选。白色定义在 `stick-world/modules/world_map/scripts/map_tokens.gd` 的 LINE_NATIONAL_COLOR，界线绘制在 `l3_map_renderer.gd` 的 `_build_political_borders`（城块邻接提取，几何已是单一来源）。
2. **都城星标要有开关**——「为什么要显示首都的位置？显示也应该是有个开关啊」。现状恒显无开关（标注层 `map_label_layer.gd` / 渲染器星标段）。开关放哪、默认开/关，与创始人确认。
3. **视口上下没满屏**——`OceanBackground` 上一批补了 z_index=-2，但截图上下仍露灰边：ColorRect 尺寸/锚点没铺满视口（只盖方形画布）。须真机确认游戏内是否同样没满屏（还是仅截图脚本环境）。
4. **截图直接出画布矩形**——r8_label_capture.gd 输出带四周空边，应裁成画布矩形再交付审查。

前两批已修背景：L3 政治模式现状＝ID mask（RGBA8）+ PoliticalLut 查表上色 + 国界白实线（城块邻接单一几何源）/地区界长虚线（同源）+ 253 自由城邦灰 + 8 城邦 CONTENT_PALETTE 族色。验证截图工具 `stick-world/tests/dev/r8_label_capture.tscn`（真实渲染非 headless）。

## 任务清点（2026-09-06 双分支合并后）

| 状态 | 任务 |
|------|------|
| ✅ 已完成 | Phase A 全部（观感修复+UI 三件套+M 全屏化+Tab 三态）｜B1 群系+超自然炎热大陆（region_008）+种族地域锚点 §5.11.1（雪山=region_007）｜B2 程序着色底图（TERRAIN 默认=真实地形，观感已验收）｜B3 河流矢量进视图包+三层叠加（硬验收双指标过，**观感待验收**）｜B4/F1 地图模式系统｜E1 道路生成+P3.5 贴地形重做 1107 条（**特写三图待观感验收**）｜F2/C1 玩家图钉+动态接线（观感待验收）｜F3/C3 population_score（含每局扰动）｜P4/C2 城市 blob 生长模型（`97e22cc8`，**观感待验收**）｜F5 道路渲染分级（土路虚线/官道实线，**观感待验收**）｜F7 §七文档口径裁决｜F8 缩略窗全景 preview 导出（切换待创始人裁决）｜P5 进城闭环（8 城邦可进出，`a6d694c4`，观感待验收）｜**P6 快速旅行（观感/手感待验收）**｜**P7 政权简化版（本提交，观感待验收）**｜世界观 §六大陆王国+§七主线三幕｜story-lab Demo 剧情 v6（三工单形态，`.temp/story-lab` 独立 git）｜装备系统需求档（10 号档，待架构） |
| ✅ Pro 队列清空 | **装备系统架构设计 ✅**（10 号档扩充为正式设计，见已完成表末行）；敌占区阻断数据消费**不排期**（P6 `set_block_filter` 钩子已留，政权归属数据已就位，待外交/敌对系统立项后另开任务） |
| ✅ Flash 队列清空 | **F6 ✅ 道路场景+步行旅行（本提交，观感/手感待验收）**——P5/P6 依赖已消费，WALK 直达改为道路场景流程 |
| 👁 2026-09-08 已验收 | **创始人观感验收已回收：B2 地形底图通过；B3 河流/F2 图钉/F5 道路虚线/P4 blob/35 国/M 图圆点/L1 模式相同等多项否决，P5/P6/F6 因弹窗 bug 未测完**——全量反馈与返工方案见 [`../世界地图观感返工-诉求与方案.md`](../世界地图观感返工-诉求与方案.md)（诉求 C1-C18 → 方案 R1-R9 批次）；story-lab 剧情 v6（`.temp/story-lab/STORY-DEMO.md`）与世界观国名/主线走向仍在待审 |
## 已完成

| 提交 | 内容 |
|------|------|
| `34d99b1` | 总体设计文档定稿：六期规划（A 观感 / B 地形图+河流 / C 玩家位置+团块 / D 进城闭环 / E 道路+旅行 / F 政权），四项创始人决策入档 |
| `5c2d661` | **Phase A1 ✅** 像素感根因修复：5 处 `draw_arc` 补 `antialiased=true` + 统一 48 段（城市圆圈/建造涟漪/选中环/加载转圈/滚屏指示），`msaa_2d` 2x→4x |
| `e3ead8e` | 团块模型定稿：地形调制加性生长（`r(θ)=base(θ)+capacity(θ)×g(s)`，非等比、贴地形、参数化） |
| `fcf2991` | **Phase A2 ✅** 地图 UI 设计语言落地：新增左上视图名牌 `map_title_bar.gd`（层级徽标+视图名+概览，三视图接线）与右下图例 `map_legend.gd`（色块条目数据驱动 `set_entries`，即 Phase B 模式换内容入口；L1 填 8 城邦政权色，与地图同色源）；三件套 token 违规修整（间距 10→12、separation 2→4、字面量→token）；**修复 A1 遗留**：`msaa_2d` 误写枚举界外值 4（正确 4X=2，原值运行时被拒实际禁用） |
| `38a3dac` | **顺带修（非地图任务）**：读档进城崩——Excel 空单元格导出 null 引爆 `String()/float()` 等 Variant 构造（`Nonexistent constructor`，波及 building/weapon_mount）；装载侧统一消毒 `BalanceResource.sanitized_rows()`（BalanceConfig/BuildingCatalog 两处接线）+ `building.apply_building_def` null 安全纵深；消费约定入 `docs/技术/教程/Excel数据管线.md` §2.4 |
| `240300b` | **Phase A3 ✅** M 全屏 + Tab 空心化 + 顶部双小地图：L3 全屏海洋底（`OceanBackground` 全屏 ColorRect，显隐跟 M 会话、下钻 L2 保留）+ 初始缩放按视口适配（锁 `min_zoom`）+ `map_camera.gd` 视口限位（上下 STRICT/左右 LOOSE，默认关不影响 L1/L2）+ MapHUD 滑块下限对齐相机 min_zoom；L1 邻居灰块空心化（只描边不填充）+ context 外缘黑线"纸张边框"；Tab 三态循环（关闭→顶部双小地图→L1 大图→关闭，`system_setup.gd` 状态机）+ 新增 `l1_thumbnail.gd` 缩略窗（`l1_base.png` 同源静态图，120² 贴 Minimap 右侧，点击开大图）+ 边界自动触发直开大图（信号加 full_map 参数）；单测 `test_map_camera_clamp.gd`（8 用例）入 batch；L2/L3 集成测试断言适配新语义；定标见总体设计 §5.12.3、架构文档 §6.1b |
| `f6416b1` | A3 收尾：M（L3/L2）会话期间忽略 Tab/边界开图请求（防三态机在海洋层下脱节、M 关后 L1 意外弹出） |
| `443bad7` | **A3 创始人验收反馈修复**：① L3 垂直"向内裁切"——限位语义做反，修正为**初始整图可见**（fit=(vp_h−128)/map，上下各留 64px 海洋带"向外放海洋"）+ 限位=视野不越出"地图+外扩海洋带"矩形（§5.12.3 定标表已重写）② **当前位置标记**（C1 静态部分提前，§5.12.4）：新 `flow_outline.gd` 流动光工具（重采样缓存+正弦亮度波）+ Tab 缩略窗出生 L1 天蓝描边+城市中心白点脉冲环（`set_map_data` 由装配层喂）+ M 大世界所在老 L1 青蓝描边（`player_l1_label`，`set_player_l1` 留 C 期动态跟踪）；L3 集成测试补"初始整图可见"断言并修装配缺口（`_test_load` 补 `set_data`，此前渲染器无数据跑全部用例） |
| `3a2cb14` | **A3 验收二轮反馈修复**（§5.12.4 修正）：① M 描边目标改**整个 L2 地区**（出生=region_013 全部 land_polygons，含 5 块陆地；`player_region_label`/`set_player_region`）② 描边**双色不透明**（`flow_outline.draw_flow` 改双色参数：亮青蓝↔深蓝色调往返流动，废透明度衰减——创始人定标）③ **Tab 大图（L1）补当前城市地块描边**（`map_renderer.gd`：`_current_tile_id` 默认出生聚落所在块，`set_current_tile` 留 C 期；4px 屏幕像素固定，画在纸边框内层） |
| `8c19640` | **F1 ✅（B4 地图模式系统，Flash 级）**：新 `map_mode_manager.gd`——模式全局静态共享三视图，**默认 TERRAIN**（§5.5）；每视图 Content 挂实例，数字键 1/2 切换（视图关闭门控）+ `mode_changed` 广播；MapHUD 加地形/政治双选模式条（ButtonGroup，L1/L2/L3 通用，与 L3 细分按钮并列）；三渲染器加 `map_mode`+`set_map_mode` 钩子（**地形底图层 B2/政权叠加层 F 期数据落地前两模式回退现状着色**，届时分层即插）；L1 图例按模式换内容走 `MapLegend.set_title/set_entries`（TERRAIN=海洋/湖泊，B2 后追加群系色条目；POLITICAL=8 城邦政权色）；api.gd 暴露 `set/get_map_mode`；单测 `test_map_mode_manager`（5 用例）入 batch + `test_strategic_map_ui` 图例断言适配；资源/人口/战线模式枚举预留（战略图架构 §八） |
| `055315a` | **F1 验收反馈修复**：① **点击穿透**——控制器 `_input` 先于 GUI 处理，点 HUD 按钮同时点选底下地块；三控制器鼠标分支加 `gui_get_hovered_control` 门控 + L3 `OceanBackground` mouse_filter STOP→IGNORE（设计本意不挡输入，写错且门控后会误伤）② **HUD 可见外壳 + 不可穿透区**——根改 STOP + `window_panel_light` 玻璃横条（地图拖拽/滚轮走 `MapCamera._input` 先于 GUI，不受影响）③ **资源/物流置灰预留按钮**（创始人要求；枚举 `Mode.RESOURCE/LOGISTICS` 已留，tooltip 标注未开放）④ **对齐**——细分按钮 32→26，模式条全排统一高度 |
| `5e4ea3a` | **Tab 语义修订（创始人反馈）**：当前城市 Minimap **常驻恒显**，Tab 三态不再影响——`_setup_minimap` 进游戏即显示；`_set_top_minimaps_visible` → `_set_l1_thumbnail_visible`（三态唯一作用对象 = L1 缩略窗）；总体设计 §5.12.1/§5.12.3 定标已同步回写 |
| 本提交 | **P1/B1 ✅ 群系生成**：新 `tools/worldgen/l3/biome_generate.py` + 参数外置 `biome_params.json`——温度=纬度梯度（y=0 北冷）+fBm−海拔递减；降水=fBm+离海 EDT 加成−**副热带干旱带**（哈德莱环流，物理正确的荒漠成因）−西风雨影（窗口 max 相对高差）；Whittaker 映射七群系（标签 uint8：0海洋/1平原/2森林/3荒漠/4冰原/5源流/6火山）；火山独立放置（高海拔∧低频热点∧距出生点 300px 下限，贪心撒 6 个边缘概率蚀变圆）；源流=河流/湖泊 mask 1px 沿岸带；硬验收内置（region_013 陆地平原众数≥55%，`--search N` 自动换 seed）；温湿场高斯平滑 σ3 消阈值椒盐斑。产出 `biome_labels_2048.npy` + 彩色预览（群系色×高度明度+出生红圈/火山橙圈+图例占比）。定标结果：平原 47%/冰原 17.6%/森林 16.8%/荒漠 7.9%（y 质心 0.72 正对干旱带）/源流 10.5%/火山 0.1%；**出生地区平原 91.1% PASS**——预览 `output/biome_preview_2048.png` 分布自然度**已创始人验收通过** |
| 本提交 | **B1 增补：超自然炎热大陆**（创始人追加）——L2 地块大小（5.6 万 px² ≈ region_013 同级）圆形热区，温度 +0.45 中心平台/边缘 smoothstep 衰减，选址避开独有地形（冰原/荒漠带≤15%、平均海拔≤0.3 避主山脉、距出生≥500px、距火山≥150px），落位普通平原/森林区（圆内平原55%+森林30%）；核心温度中位 0.80（稳超 t_hot），群系按炎热重判但平原/森林标签不变（温带草原→热带草原同标签，语义保留）；**暖色偏移进预览**（R+42/B−18 随 falloff 渐变）+ 独立输出 `biome_hot_zone_2048.png`（falloff 0~255 mask，B2 着色炎热色调直接乘）；圆心写回 params 复现（位置开发期定死）。火山放置拆「选址/应用」两步以支持热区选址避让 |
| `9edb49b` | **热区修订（创始人反馈）+ 种族地域设定入档**：① 圆形热区 → **region_008 整块 L2 地块**炎热化（温度 +0.45 边界羽化 16px，region 内平原 51%/森林 32%/荒漠 4%/冰原 0%，温度中位 0.77；params 改 `region_id` 驱动，圆形选址逻辑删除）；② 总体设计新增 **§5.11.1 文化圈地域锚点**（创始人 2026-09-05 口述）：火焰火柴人=region_008、水火柴人=region_011 左下群岛、极地=北部冰原带 region_001/002/003/009、沙漠火柴人=南部荒漠带（region_010 荒漠 75%+region_006 南部）、森林火柴人=region_004 赤道东侧、**雪山火柴人（高山巨人）=region_007**（与炎热大陆同陆块北部，平均海拔 0.202 全图最高档+冰原 31%，创始人两轮确认排除孤岛方案）；③ 世界观设定 §2.2 补 4 新种族行（沙漠/森林=文化区，火焰/水=元素） |
| `7215416` | **F7 ✅ §七 文档口径裁决 5 项**：① 素材库路线作废——《世界地图数据流》管线图/§5.1-5.4/checklist/决策表全量重写为程序化着色管线（biome_generate+terrain_render，零 strategic_kit 残留）；② 聚落表现改 blob——《战略图架构》§4.6 重写（动态建筑群→覆盖团块，建筑群仅场景图；settlement_renderer→settlement_blob 归 C2）；③ 8 城邦口径无残留（已一致）；④ 待办事项 389 L1/3203 城旧方案标注「已被 v2 取代，现行 69 老 L1+1040 城」；⑤ 两架构文档分辨率节统一为「L3 程序着色 2048 起/L2 同管线裁切/L1 无底图纯矢量」 |
| `11b6d79` | **F3 ✅ C3 population_score 补齐**：生成端新 `tools/worldgen/l1/population_score.py` 就地补字段（l1_world.json 8 城按级别带+同级别面积分位：T1 0.15-0.35/T2 0.35-0.55/T3 0.55-0.80；l3_city.json 1040 城按 2048 归一面积分位派生级别 p55/p85→T1 572/T2 312/T3 156 再映射分数带；json 格式对齐原 indent=1，纯增量 diff）；**重跑 l_world_bake.gd 刷 bin**（86 个）；运行时 `WorldState.run_seed`（新开局 GameRoot 调 start_new_run 随机、存档 get_save_data/load_save_data 回传）+ `SettlementRef.jitter_population_score` 静态方法（hash(id)+run_seed 确定性 ±15%，出生聚落免疫、基准≤0 不扰动）+ l1_world_data 装配接线；单测 `test_population_jitter`（6 用例）入 batch |
| `7920337` | **F2 ✅ C1 玩家图钉 + 动态接线**：map_renderer 新琥珀色图钉（程序化绘制：圆头+尾针+白点+「你在这里」标注，set_data 默认锚出生聚落，set_player_pin/clear_player_pin 入口）；api 新 `set_player_map(map_id)`（反查当前 L1 聚落→更新图钉+set_current_tile；未命中保持出生默认）；system_setup 订阅 `scene_loader.map_loaded` → api.set_player_map（战略图懒加载未初始化时跳过，语义仍正确）。跨 L1 的 set_player_l1/set_player_region 接线依赖 l1_packs 的 map_id 回填，留 D 期（api docstring 已标注）；Tab 大图打开 L1 图可见图钉（创始人观感验收项） |
| 本提交 | **P2/B2 ✅ 程序着色底图**：新 `tools/worldgen/l3/terrain_render.py` + 参数外置 `terrain_params.json`（hillshade/高度明度/岩石雪线/海洋渐变/海岸暗线/热区暖调全可调）——8192 原生着色（分块合成控内存），产出 `l3_terrain.png`（2048，3MB）+ L2 每地区 `l2_terrain.png`（8192 级 context 裁切共 13 张 ~60MB，虚空区 RGBA 透明透出渲染器海洋背景，13 地区与 l2_world.json context_size 全对齐）；**河流宽度口径**：river_mask 掩码本身就是「四次根流量×6px」画好的宽度，直接继承（river_paths.json 无流量字段）；远海终色 = 渲染器 OCEAN_COLOR(30,55,95) 无缝衔接。运行时接线：L3 渲染器 TERRAIN 分支异步加载底图（照 city_preview 三件套）+ L2 同（load() 走 .import）+ L2 地形模式跳过湖 mesh/邻居灰影（纹理已含）；L1 图例 TERRAIN 分支追加 6 群系色条目（`MapRenderer.BIOME_LEGEND` 与生成端色板同源）。数值自检六项全过（群系色带/hillshade std 33.8/热区 R-B 比 1.89/河流色差/海岸暗线 −15%/远海无缝）；**观感已创始人验收通过**（游戏内 M 键 L3 / 下钻 L2，TERRAIN 默认模式即地形图） |
| 本提交 | **B2 附：Thread join 段错误修复**：L3 渲染器三个后台加载线程（l1_index/city_preview/terrain）未 `wait_to_finish` 销毁在 Windows 上**直接段错误**（既有隐患被新 terrain 线程放大，`test_strategic_map_ui` 稳定崩溃）——`_exit_tree()` 统一 join 三个线程；测试断言随图例 8 条目更新 |
| 本提交 | **B2 附：Demo 展示素材入库**（创始人指示）：`output/l3_terrain.png` + `output/l2_preview_region_008/013.png`（1600² 缩版预览）作 Demo 工作量展示，gitignore 白名单入库，登记在 `tools/worldgen/README.md` §Demo 工作量展示素材 |
| 本提交 | **P3/B3 ✅ 河流矢量进视图包 + 各层级叠加渲染**：**数据源裁定**——`locked/river_paths.json` 是废弃实验管线（river_step4.py，C 加速器+Catmull-Rom）产物，与 river_mask 底图河流重合率仅 7.2%，弃用；新 `tools/worldgen/l2_export/river_export.py` 从**现役 `fractal_river_mask_8192.png` 骨架化提取**（skimage skeletonize → 端点/交点拆段图追踪 → DP 抽稀 tol 0.5px），宽度 = EDT×2 实测河宽（mask 宽度即「四次根流量×6px」，流量语义保留），与底图河流像素级同源。**注入**：就地 patch L1 70 份（出生+69 包，`rivers` context [x,y] + `world_origin` 世界原点重算自 legacy 蒙版 bbox+json side，防御=l1_polygon 界内校验）+ L2 13 份（`rivers` context [y,x]，窗口=bbox−tiles_offset），不动其他字段（与道路会话的 roads 字段干净共存）；**L2 geom 重烘**（l2_bake 湖色 (28,50,82)→(72,116,158) 对齐 B2 底图）。**渲染**：L1 map_renderer 拆 base mesh 为 tiles/lakes 两张、河流 draw_polyline 画层间（城市块→河→湖，河入湖被湖面覆盖）+ LAKE_COLOR 对齐；L2 POLITICAL 模式矢量叠加（TERRAIN 底图已含不重复画），河色统一 (46,102,140)；L1WorldData/L2WorldData 加 rivers 归一化（bin/json 同构）。**硬验收**：忠实度双指标=命中率 1.000（折线点全在 mask+1px 内，阈值 0.97）/覆盖率 0.953（mask 中心带距折线 ≤2px，阈值 0.95）；全量测试 32/32 + 单测 `test_river_parse`（5 用例）入 batch；重跑 `l_world_bake.gd` 刷 86 bin |
| 本提交 | **P3.5 ✅ E1 道路贴地形重做**（接手 Flash F4 骨架，只换搜路核心）：**新 cost 模型**——窗口有向 Dijkstra on 4096 降采样高度场（长路窗超限自动降 2048 粗搜，两级同噪声场），边权 = 步长 × (1 + **方向性坡度**（梯度沿行进方向分量 ×k_up/k_dn，上坡贵下坡廉——沿等高线不付坡度账，穿山 ∫坡度 ≥ Δh 自动走鞍口，废显式山口豁免）+ **谷脊 TPI**（h−邻域均值 ×k_tpi，河谷吸引山脊回避）+ 河湖/群系/低频噪声（seed 固定，平原蜿蜒主力）)；调参 7 轮定标 **amp3.5/λ180/2八度/decay0.55 低频主导**（关键认知：走廊平移弯靠低频分量，高频只给毛刺且被 DP 收掉；平原路物理上限 ~10-25px 弓形，出生中路 dev 25-46px、短路 3-14px、山地大绕 166px、穿峰 0.55-0.67 的路绕行仅 1.04-1.09=走鞍口）。全量 1107 条（回退 2/跨海丢 103/绕行 p50 1.02 p90 1.07；12 条 >2.0 离群查实为隔海湾/群岛绕行——无桥世界诚实数据）；**`--only 69 --closeup` 调参循环**（免回写，内存渲特写 + 每路 detour/dev 指标）；bin 已刷、全量测试 32/32、报错自检干净 |
| 本提交 | **Flash worktree 基线对齐 + 数据流契约文档刷新（Flash 级）**：`agent/world-map-flash` 自 main 快进合并 pro 基线（至 `6482ae9`，含 B1/B2/B3/道路契约，合并后 32/32 测试全过）；《世界地图数据流.md》对齐实装现状——头部门户状态行刷新、§3.3 数据容器改实装的 `L1/L2/L3WorldData`（废 StrategicMapData 单一 Resource 旧述）、§5.2 CLI 补河流注入/道路生成步骤与刷 bin 提醒、§7.1 启动流程改实际视图包流（Tab 三态/M 下钻/travel_requested，废 manifest.tres+continent.tres 旧框架描述）、§7.2 L1 包按需加载口径、§7.3 Tab 行对齐三态机、§8.3 打包器标记已实装、§8.4 补模式系统与 L1 划分完成项 + E2 道路渲染待接、§8.1 补 P3.5 道路贴地形重做工作项 |
| 本提交 | **旧 .tres 导入管线口径作废（数据流契约 + 生成端文档双清）**：《世界地图数据流》§2.1 三份桥接 JSON（continent_data/regions_data/tiles_data）标记已作废（实装由视图包承载，political_data.json 保留待 P7）、§四 管线图改实际工具链（region_split/city_split_v2 → biome/terrain/river/road → l2_export+l_world_bake → L1/L2/L3WorldData）、§5.5 索引图改实装文件表（l3_partition_2048/l2_tiles_index/l1_mask，均已核实 RGB）、§5.7 打包器改视图包要点（废 import_to_godot.py/manifest.tres）、§6.2 加「类未采用、字段映射留作参考」注、§7.2 加载时机对齐代码（L3=M 键按需 `system_setup.gd:625`、L2=单击下钻 `l3_map_controller.gd` 头注）；《08-程序化世界生成》§12.1 [5.5] 风格化底图改程序化着色（零 strategic_kit 残留）、[5.6] 改 l2_export 视图包管线，实装现状注更新 |
| `97e22cc8` | **P4 ✅ C2 城市 blob 生长模型**（pro 分支，观感待验收）：**生成端** 新 `tools/worldgen/l1/blob_bake.py` + 参数表 `config/strategic_map/blob_params.json`（两端同源）——1040 城沿 16 方向射线烘焙地形容量（**探测段=该级生长段 base→base+g_max**，防远端河海误伤；slope_ref 定标 0.004≈梯度 p97：山向卡死 5%/水向 0.15/海向 0，capacity 均值 0.249）；就地注入 L1 70 份 settlements `blob_capacity[16]` + `l3_city.json` tiles `anchor`（世界锚点；centroid 是地块质心与聚落锚差数十 px 不可直接用）+ L2 13 份新 `cities` 数组（城市 group=老L1 → pack tiles global_l1_label，[y,x] 惯例）；顺带回填 69 包 population_score（null→l3_city 值；出生包 8 城统一全大陆分位尺，test_population_jitter 断言随之改动态基准）。**运行时** 新 `settlement_blob.gd` 纯函数轮廓：`r(θᵢ)=base(θᵢ)+capacity(θᵢ)×g(s)`，谐波 base 相位=DJB2 哈希**两端逐位同源**（预览即游戏内形状），周期 Catmull-Rom 16→72 点；L1 矢量绘制（单城缓存、画在城界描边之下防与行政区混淆、T4 白/T5 金描边代码就绪）+ L2 ×0.3 / L3 ×0.12（T2+）合并 ArrayMesh 每帧 2 draw；三容器 population_score 同 jitter 口径（出生免疫经出生包顶层 spawn_id 静态缓存 `L2WorldData._birth_spawn_id()`）；`EventBus.settlement_updated(id, score)` + api 订阅单城重算（发射方建设系统待接）；图例两模式加「城镇建成区」；`SettlementRef.get_footprint_radius()` 升级真值（原硬编码 16）。单测 `test_settlement_blob`（10 用例，含 **DJB2/半径跨端锚点回归**）入 batch；bin 重刷 86；全量 32/32 + 报错自检干净。**观感验收素材**：`output/blob_closeup.png`（出生城+容量差最大三城特写，s=0.2/0.5/0.9 三档轮廓叠加+白色容量辐射条）+ `output/blob_preview_2048.png`（156 个 T3 blob 叠地形底图）；游戏内 Tab L1 / M L3 直接看 |
| `63eb7983` | **F5 ✅ E2 道路渲染分级（Flash 级）**：`L1WorldData.roads` 升级结构化 `[{"pts","tier","length_px"}]`（原起终点直线对丢 tier/polyline——bin 的 polyline 完好只需改运行时解析；无 polyline/旧包回退 from/to 聚落直线，§5.9 向后兼容口径）；`map_renderer.gd` 新道路层（城市块之上、河流之下）：DIRT 土路虚线（dash/gap 弧长切段 `draw_multiline`，相位跨顶点连续）/ PAVED 官道实线（`draw_polyline`），色对齐生成端特写 `road_closeup.py` STYLE 同源，宽 = context 比例（地图单位，与河流同策略）；图例两模式各追加「土路（虚线）/官道（实线）」条目（`MapRenderer.ROAD_LEGEND`）；单测 `test_road_parse`（5 用例）入 batch，`test_strategic_map_ui` 图例条目断言适配；json/bin 数据零改动（不刷 bin） |
| `2e97bf72` | **F8 ✅ 缩略窗全景 preview 导出（Flash 级，可选任务）**：`export_l1_view_context.py` 新 `--panorama` 模式——L1 世界全景（69 块+1040 城政权色+湖，配色规则与 `l1_base.png` 同源）8192 LUT 查表上色 → 陆地 bbox 正方形裁切（外扩 5%，实测铺满 8192 全幅）→ LANCZOS 缩 2048；产出 `output/l1_panorama_preview.png` + 同名 sidecar json（world_bbox 映射参数：全景像素→世界坐标 = px×side/out_size+(x0,y0)），gitignore 白名单入库；**缩略窗底图切换留待创始人裁决**（切换需改 `l1_thumbnail.gd` 的 `BASE_TEXTURE_PATH` + `set_map_data` 坐标映射 context→全景） |
| 本提交 | **P6 语义裁决入档（开工前确认项关闭）**：创始人 2026-09-06 拍板快速旅行语义 = **已到访 ∧ 路网连通 ∧ 未被不可通过区切断**；调试期传送免费即时，时间消耗/交通工具暂缓（正式版预留按日行程限距 + 交通工具扩范围）。总体设计 §5.10/§四 E3/验收行/模块边界表/风险表 + 本档（当前阶段/Pro 队列/关键决策速查）全部按此重写；P6 归 Pro 实施，Flash 曾误接手已全部回滚未留代码 |
| `a6d694c4` | **P5 ✅ D 进城闭环**（D1+D2，观感待验收）：**生成端** settlement_mapgen.py 三处修复后首次成功实跑——① 路径 bug（`../..` 少一级，产物全落 `tools/stick-world/` 幽灵路径，工具此前从未跑通）② def_id 去 `bld_` 臆造前缀对齐 building_gen 注册表（placeholder/wall_gate/wall_tier1/2/warehouse/barracks）③ 出口语义改**边界回图**（删 ExitRight 环形直连下一城——玩家顶到地图边界 3 秒由 MapBoundaryDetector 开 L1 大图回战略图选下一站，ChunkTriggers 留空容器对齐 village_a 结构）+ **map_id 就地回填** l1_world.json 8 处（幂等读-改-写）；产出 8 张 .tscn（T3=6144/T2=4096 沿用工具现值，VillageMap 结构：PlacementGrid/TerrainLayer/InitialBuildingsList[defs_json_path]/WalkBarrier/BattleAnchor）+ 6 份建筑 JSON 重建（01/06 与历史产物一致零 diff；实际级别分布 **6×T3+2×T2 无 T1**，城市8=00/城市7=06 是 T2）+ bin 重刷 86 + headless --import。**运行时** game_root `_L1_SETTLEMENT_SCENES` const 数组预载 8 图 + `_register_default_maps` 循环注册（MapType.VILLAGE，不配 register_map_exit）；进出链路全部现成接线——双击聚落（`strategic_map_controller` 双击判定已有）→ `api.enter_settlement`（map_id 非空发射 `EventBus.travel_requested`）→ `scene_loader.travel_to_map` 实例化进城；`_on_map_loaded` 通用回调自动注入 MapBoundaryDetector/相机/小地图/ConstructionManager；tooltip map_id 非空自动显示「双击进入」（「未开放进入」分支留创始人验收后删）。单测 `test_settlement_entry`（5 用例：map_id 回填唯一/tscn 结构与出口语义/def_id 合法/布局匹配级别/game_root 预载）入 batch；`test_strategic_map_p0` 进入用例为自适应写法，map_id 回填后自动切换到「发射 travel_requested」正向分支，零改动适配。全量 35/35 + 报错自检干净 |
| 本提交 | **P6 ✅ E3 快速旅行**（观感/手感待验收）：**语义落地** = 已到访 ∧ 路网连通 ∧ 未被不可通过区切断（创始人 2026-09-06 拍板），调试期免费即时。**新 `data/travel_planner.gd`**（纯逻辑：roads from/to 建无向图（重复边取短）→ 单源 Dijkstra（线性选最小，出生 L1 规模够用）→ `find_path` 途经聚落序+途经道路（高亮层消费）；blocked 集合 = P7 敌占区阻断注入点）。**数据层** `L1WorldData._roads_from` 透传 from/to（F5 时代曾丢弃端点，路网图依赖它——回归锚入单测；改解析不改 json 不刷 bin）；`WorldState.visited_settlements` 到访表（start_new_run 重置、get/load_save_data 存取、set_player_map 命中即写入=进城即到访；出生聚落恒已到访不依赖开局时序）。**交互** 双击分流（controller `_handle_settlement_activation`）：NO_SCENE 不进（保持旧口径）/ SELF 直接进城 / 其余开新 `ui/travel_dialog.gd`（挂 CanvasLayer 直下自管理遮罩——战略图层号 100 高于 UIRoot 的 1，不能走 UIModalStack；OK=三按钮、不可达=走过去+置灰原因、BATTLE=仅取消；ESC 先关弹窗）；「走过去」WALK 调试期同直达（F6 接管后走道路场景）；「快速旅行」→ map_renderer 新路由高亮层（途经道路琥珀加粗+节点白圈，画在水系之上）0.8s → `api.fast_travel_to`（二次校验）。**契约** `EventBus.travel_requested(map_id, travel_mode)` 加 mode 参数（scene_loader 透传 travel_to_map；WALK 给 F6 用）；tooltip 进入行按可达性分文案（当前位置/双击出发/走过去+原因）。**测试** 单测 `test_travel_planner`（8 用例：建图/Dijkstra/路径/目标阻断/中间切断绕行/真数据 MST 全连通/from-to 透传回归锚）+ P0 集成 2 用例（可达性状态机含战斗门控与阻断钩子/fast_travel 执行与 mode 透传+弹窗挂载）；全量 32/32 + 报错自检干净 |
| 本提交 | **P7 ✅ F 政权简化版**（观感待验收）：**生成端** 新 `tools/worldgen/l3/state_expand_lite.py` + 参数表 `state_params.json`——文化圈 = 分 culture MCP 测地场 argmin（锚点种子 + region 越界罚×30 + 坡度/河/群系 cost，种子落海防御）；都城 = 圈内 T3 按 population_score 降序 + 自适应间距；扩张 = 城图多源 Dijkstra（认领过滤=国界贴文化圈 + 均衡系数平滑均分 + 容量 cap + 三轮兜底，出生 8 城邦节点预占移除）；命名 = 世界观 §6.1 王国名 + 圈内按城数降序（主名归主导国）。**产出** `political_data.json`（真相源，35 国/1040 城主）+ 注入 l3_city.json（state_id+states）+ l2 packs ×13 + `l3_political.png` + `l2_political_preview.png` ×13 + 验收预览两张；bin 重刷 86 + headless --import。**运行时** L3WorldData/L2WorldData 加 states 装载；L3 渲染器 political_texture 异步加载（第 4 张底图线程）+ POLITICAL 分支贴图；L2 同步加载 + 政治模式优先贴政权图；L1 出生 8 邦不动。政权色 HSL 黄金角（出生 8 邦原色）。**测试** 单测 `test_political_data`（7 用例：全量覆盖/归属合法/出生邦原样/l3_city 注入一致/L2 注入一致/碎度/L3 装载）入 batch；全量 32/32 + unit 38/38 + 报错自检干净。13 region 主导文化全部锚定正确（region_005 金穗 27:14 击退游牧侵入经三轮罚值定标） |
| 本提交 | **装备系统架构设计 ✅**（Pro 队列收尾，纯文档）：`docs/设计/系统/10-武器装备与个人库存.md` 需求存档扩充为正式设计——三核心决策（ItemDef 模板/实例分离照 BuildingDef 先例、装备实例无全局 id 值语义、战斗动画层零改动走 `weapon_type` 映射 weapon_mount 六型）；**7 开放问题全裁决**（六槽位模型=3 实装+3 预留、畸变差异走 def 数据过滤/8 格堆叠背包不进物流网络/耐久有词条无品质三档/个体档案=诨名+战绩+慢性伤病/万体规模 entities 表 per-row 增量+惰性 resolve/UI 双入口=选中侧栏+附身面板/剧情三挂点=生产链+教学清单+QUEST 任务栏）；**验收口径 4 条对照**（Loadout 双宿主读写+存档迁移/AttributeResolver 加法制派生公式/2MB 上限增量写/经济物流「仓库 vs 口袋」边界）；分期 E-0 数据层..E-4 经济闭环，Demo 需 E-0..E-3。⚠️ 需求档引用的「世界观 §3.2 描述法命名规范」不存在（编号漂移），命名生成器接口先行、词库待规范成文（E-1 外部依赖）。《核心实体与状态机》§一 `equipment`→`loadout`、Organization `equipment_template`→编制配额语义同步 |
| 本提交 | **F6 ✅ E4+E5 道路场景+步行旅行**（Flash 级，观感/手感待验收）：**生成端** 新 `tools/worldgen/l1/road_biome_export.py`——polyline 逐弧长（12px）采样 `biome_labels_2048.npy`（世界 8192=本地+world_origin → /4，坐标链 P4/B3 先例验证 spawn (1466,813)@2048 吻合），biomes 数组注入 roads 条目（70 包 904 条有折线路；分布：平原 47.5%/森林 17.4%/冰原 15.6%/源流 8.7%/荒漠 8.3%/海洋 2.4%=贴海岸段）；bin 重刷 86。**E4 场景侧** 新 `modules/world/scripts/travel/road_map_generator.gd`（RoadMapGenerator.build(leg)→PackedScene，结构对齐手工 road_a_b.tscn 模板）：宽度=length_px×tier 系数（DIRT×24/PAVED×16「大路好走」，clamp 2500..40000）+ 沿线群系色带（BiomeBand 分段，色板与 map_renderer.BIOME_LEGEND 同源，无 biomes 旧包单色兜底）+ 道路本体带（tier 色）+ 装饰层（road_id.hash() 种子确定性，密度按 tier）+ 左右 ChunkTrigger（target 留空走出口配置）；scene_loader.travel_to_map 未注册时 `_ensure_road_registered` 查 WorldState.walk_legs 懒生成注册（会话内缓存不落盘）。**E5 步行链**：api 新 `get_walk_status`（连通∧未阻断∧非战斗，**不查已到访**——走过去正是解锁手段）/`walk_to`（组装 `WorldState.walk_legs` 逐段队列 + travel_requested(第一段 road_id, WALK)）；travel_dialog「走过去」按钮改 walk_status_fn 复核（UNVISITED 亮/UNREACHABLE·BLOCKED 置灰+原因，走过去不再无脑可用）；GameRoot `_consume_walk_state`（map_loaded 通用钩子）：道路场景按队列位置刷左右出口 register_map_exit（正向=下一段/终点城、反向=上一段/出发城，ChunkTrigger 空目标分支自动消费），进城/折返即清队列；存档防御=步行中（current_map 为 road_*）跳过整个存档回调 + 读档 road_* 异常回退 l1_settlement_00（道路场景不入档，读档恒回聚落）。**测试** 新 `test_road_walk`（7 用例：宽度公式/场景结构与出口 target 空/seed 确定性/色带分段与兜底/biomes 透传回归锚/walk_to 队列组装+travel_requested 捕获/拒绝分支）入 batch；unit 39 套 + 全量 32/32 + 报错自检干净。P6 弹窗「走过去」WALK 直达语义由本任务接管，调试期直达口径作废 |
| `e02625b9`/`3a09f78c` | **R9 ✅ L1 静态层烘焙化基建**（批2）：生成端 `l1_terrain_bake.py`（import 复用 terrain_render 单一真相源，参数继承 terrain_params.json）70 包各产 `l1_terrain.png`（8192 原生 1:1，B2 同管线分层合成+群系基色小 sigma 柔化消 2048×4 台阶）；运行时 map_renderer TERRAIN 分支异步线程贴图（l3 三线程样板+join 防段错误），POLITICAL 保持运行时矢量（政权色不烘焙裁决）；L1WorldData 加 base_dir 运行时字段不改 json；决策：纯场合成不做 4x 超采样（批3 矢量层预留） |
| `a6ed3a66` | **R8 层1 ✅ HUD 换肤回 SKETCH**（批2）：删 4 处玻璃 StyleBox override（map_legend/map_title_bar/l3_zoom_indicator 内壳/travel_dialog），回退 StickTheme 自带 SketchStyle 九宫格手绘贴图，与 granularity_indicator/settlement_tooltip 同一皮肤语言（根因 A12） |
| `537a8685` | **R6 生成端 ✅ 道路自然化**（批3）：`road_naturalize.py` 971 条折线就地加工——等弧长重采样+法向噪声位移（世界坐标采样 value-noise，低频 160/80px+高频 32px，幅度按等级×坡度×端点 fade）+窗口3滑动平均禁样条+之字锐折角锚点；两个自检修正（位移场去 DC 防整体平移、超短路幅度衰减）；seed=DJB2 两遍逐位一致；长度变化 p90 +0.76%、端点 0 位移 |
| `9f4bed34`/`3d3ee322` | **R4 ✅ 三模式语义分离 + R6 渲染端 ✅**（批3）：MapModeManager 加 TRAFFIC（数字键 1/2/3）；map_renderer 三模式真分支——地形=terrain 贴图+建成区（仅此模式）/政治=运行时矢量政权色/交通=travel 贴图（含 casing 道路）；`l1_travel.png` 70 包（casing 双层实线 DIRT1.2/PAVED3px@8192+hillshade 贴地形+端部收尖+之字折角，道路层 2x 超采样）；`road_writeback.py` 978 条对账写回（biomes/tier 保留、length_px 更新、直线渡线透传）；图例随模式换、路由高亮改琥珀虚线（UI 语义正确位置）；废 F5 虚线 |
| `f7f0c815` | **R5 生成端 ✅ 建成区场叠加管线**（批4）：`blob_v2_generate.py` 13 轮调参——U=a·核场+b·道路串珠条带+卫星种子+fBm，排除层硬否决，τ 分位数反解目标面积（偏差<5%）；**三档同场不同 τ 严格嵌套**（low⊆mid⊆high=随人口增长切档的正确语义）；find_contours 多环+洞+飞地+shapely 修 invalid；贫瘠城合法输出空（2 城无建成区）；D_b 0.95~1.02（游戏尺度物理上限，观感目标达成） |
| `f45a3128`/`cec85931`/`15a1bb71`/`4c0be3ff` | **R7 ✅ 政权扩容 35→80 + ID mask/运行时 LUT**（批4）：城市种子（pop top-N+四叉树间距，圈内配额最大余数法）+随机 expansionism+面积反馈 4 轮（Zipf 斜率 -1.00）+山脊/过河罚（边界贴河脊）+normalize 消飞地 152 城；文化圈硬锚点 region 零越界；政权色 CONTENT_PALETTE 7 族×明度梯派生（相邻冲突 0 对，废 HSL 黄金角，出生 8 邦原色）；`l3_political_id_8192.png`+L2 ID mask+PoliticalLut 共享纹理+colorize shader——改 LUT 即全图换色三重自证；命名可插拔接口（state_names_placeholder.json 占位名全部「提案/待定」） |
| `377a7a04` | **R5 运行时 ✅ 三档接入**（批4）：70 包×3 张透明贴图（blob_low/mid/high.png 卫星感填充全分层：群系派生底色+屋顶噪点+路网线+晕圈，2x 超采样，43MB）+`blob_v2_geo.bin`（三档环顶点量化）；运行时按扰动后 population_score 切档（0.35/0.65 分界）、单城小贴图补丁分帧生成、R2 当前城描边改 mid 档轮廓；旧径向 blob 退役（settlement_blob.gd 径向模型全删）；顺带修上游 npz start 字段错位 bug |
| `8f1fe999` | **R8 层2 ✅ 线条 SketchDraw 同源 + token 化 + 界线三级**（批5）：`MapTokens`（线宽三级 3/2/1px+语义色槽：交互=BORDER_STRONG/操作=ACCENT 唯一合法/玩家=玩家国色）+`MapSketch`（wobble 同公式：静态线固定 seed 不沸腾/动态线 boiling 0.12s 与血条同拍）；三渲染器字面量清零（琥珀查漏归位 3 处）；L3 政治国界 3px 实线（states 邻接 3.7 万边提取）+地区 2px 长虚线、L2 地区/地块 2px/1px 虚线分级；Python 端 wobble 同源基座 `r8_sketch_line.py`（道路 casing 几何已自然化免重烘） |
| `c5a1a541` | **R8 层3 ✅ 标注体系 + 都城星标**（批5）：三级标注（国名 18Bold 字距 15%/地区 14/城市 12 首都 13Bold，白 halo 1/6~1/5 字号）+缩放显隐阈值（L3 国名/下钻 L2 地区/L1 城市）+防压盖（碰撞盒优先级贪心，首都兜底强显）；都城星标 Ø8px 金色 80 国（l3_city centroid 是 [x,y] 序已对拍）；StickHand 零 fallback（F3 调试标签一并归正）；附带修 `_roads_from` 对 length_px=null 容忍 |
| `d4e7c809` | **R1+R2 ✅ 观感返工批1运行时**（待观感验收）：**R1** 删 L3/L2 渲染器城市 blob 团块层（`_bake_blob_meshes`/`_mesh_from_arrays`/draw 分支整删，C1「M 图不需要城镇小圆点」）——MapLegend 只挂 L1（L3/L2 无图例），L1 blob 保留（R4/R5 处理）；**R2** map_renderer 玩家标记重做 GPS 范式（§7.3-6 规范表）：中心 4px 玩家国色点（白描边）+12px 静态细环+1.5s 周期脉冲扩散环（12→36px alpha 0.5→0，玩家国色），废拟物图钉+「你在这里」文字（C9/C13）；玩家色=所在地块政权色（内容语义，api.set_player_map→set_player_pin 接口保留，查询失败保留旧色）；当前城流动描边几何改该城 blob 轮廓（FlowOutline.resample_closed，与建成区图形逐像素重合，废整地块多边形描边；invalidate_blob 单城重算时同步重采样）；_process 动画推进含脉冲相位。战略图 4 集成套件 PASS |
| `c98d5bf8` | **R3 ✅ 地块边缘平滑数据级根治**（待观感验收）：mesh_extract 新 `extract_smooth_mesh`——per-label bbox 裁剪（**lp 系算 bbox**，块不贴 field 边）+ skimage find_contours(0.5) 亚像素等值线（坐标=像素中心系 **+0.5 对齐角点系**；外环 shoelace 正/洞负）→ Visvalingam 有效面积抽稀（阈值 0.02px²，deleted 三态防锚点误删）→ Chaikin×2 → 二次 Visvalingam 回收插值点（顶点数 -65%，面积偏差 <0.4%）；**共享弧缓存**：相邻地块同界只平滑一次（key=首尾点+点数，反向取缓存反转），两侧逐点一致无缝；**T 形点焊**：三岔交界 3 个双共享点（相距 ≤√0.5px）BFS 聚类合并质心，重合点强制段边界（防锚点沉入段中被抽稀删掉），缝隙三角闭合+相邻重复点去重；L1 城市/legacy **合成蒙版一次提取**（legacy +10000 命名空间，city 优先覆盖）——跨蒙版交界共享弧缓存（分两次提取各自平滑出楔形缝）；`export_l1_view_context.py`/`export_l2_view_packs.py` 加 **--polys-only** 就地 patch（只换 tiles polygon/polygons/holes+neighbors+lakes+l1_polygon，rivers/roads/blob_capacity/states/cities/map_id 后注入字段全保留；L1 窗口读包内 world_origin+context_size 规避 margin 坑；跳过索引图/底图重写；L2 bake 从 merged world 重烘 l2_geom.bin）；70 L1 包+13 L2 包 patch+86 bin 重刷；新 `smooth_closeup.py` 特写对比图 3 张入库（gitignore 白名单+README §Demo 登记）。全量 33/33 + 报错自检干净 |

## 下一步：Phase B 任务分解（地形图默认模式 + 河流）

1. ~~**B1** 群系生成~~ **✅ 已完成并验收通过**——预览 `tools/worldgen/output/biome_preview_2048.png`；调参入口 `tools/worldgen/l3/biome_params.json`（阈值/干旱带/雨影全外置）重跑
2. ~~**B2** 程序着色底图~~ **✅ 已完成并验收通过**——观感调优入口 `tools/worldgen/l3/terrain_params.json`（hillshade 强度/明度曲线/岩石雪线阈值/海洋渐变/海岸暗线/热区暖调全外置），重跑 `terrain_render.py --install`；游戏内 M 键（L3）/下钻 L2 直接看 TERRAIN 模式
3. ~~**B3** 河流矢量进视图包~~ **✅ 已完成（待创始人观感验收）**——重跑入口 `python tools/worldgen/l2_export/river_export.py`（矢量缓存 `output/river_vectors.json`，改提取参数加 `--force-extract`）；游戏内 Tab 看 L1 河流、M 下钻 L2 政治模式看矢量河流
4. ~~**B4** MapModeManager~~ **✅ 已完成（F1 `8c19640` + B2 接线）**——TERRAIN 分支绘制底图纹理（L3/L2 已接）+ L1 图例 TERRAIN 分支群系色条目（已加）

## 任务分级调度（Pro / Flash）

> 用途：剩余任务按模型能力分级派发（**Pro** = 强推理模型；**Flash** = 轻量快速模型），创始人先清 Pro 级。
> **分级标准**：Pro = 需设计判断 / 算法数学建模 / 跨模块契约改动 / 视觉质量调校 / 总体设计风险表点名的项；Flash = 规格明确、组件与模式现成、单模块机械实现的项。
> **通用验收（两级同规）**：`check_godot_errors` 干净 + `run_all.sh` 全过 + 视觉类过创始人观感；完成后回填本档 + 中文提交号。

### Pro 级（顺序 = 依赖顺序，从 P1 开始）

| # | 任务 | 为什么是 Pro | 关键锚点 |
|---|------|--------------|----------|
| P1 | ~~**B1 群系生成**~~ **✅ 已完成（含炎热大陆，B1/B2 观感创始人已验收）** | Whittaker 温湿矩阵参数定标 = 分布自然度（风险表点名项，彩色预览须交创始人验收循环）；出生地区平原硬验收 | §5.2；先读 `tools/worldgen/l3/fractal_continent.py`（numpy 接口现成） |
| P2 | ~~**B2 程序着色底图**~~ **✅ 已完成（pro 分支，观感已验收）** | hillshade/海岸线/水体着色 = 全游戏地图观感底座，算法 + 审美双密集；产 `l3_terrain.png` + L2 裁切 | §5.3 |
| P3 | ~~**B3 河流**进视图包 + 三层叠加渲染~~ **✅ 已完成** | ~~`river_paths.json` 现成~~ **该文件已裁定弃用**（废弃实验管线产物，与 river_mask 不同批河）——实际从 river_mask 骨架化提取，详见已完成表 | §5.4 |
| P3.5 | ~~**E1 道路贴地形重做**（接手 Flash F4 骨架）~~ **✅ 已完成（待创始人观感验收）** | 搜路核心已重写（方向性坡度 Dijkstra + TPI + 噪声，详见已完成表）；调参入口 `road_params.json` + `road_generate.py --no-write --only 69 --closeup` 快速循环 | §5.9 |
| P4 | ~~**C2 城市 blob** 生长模型~~ **✅ 已完成（观感待验收）** | 调参入口 `config/strategic_map/blob_params.json`（两端同源，改烘焙参数须重跑 `blob_bake.py` + 刷 bin）；预览验收图 `output/blob_closeup.png`（三档 s 轮廓叠加） | §5.7 |
| P5 | ~~**D 进城闭环**（D1 mapgen 补完 + D2 进出接线）~~ **✅ 已完成（观感待验收）** | 双击→enter_settlement→travel_requested→travel_to_map 链路本就通（P0 自适应测试覆盖），实际工作是生成端三修复+回填+注册，详见已完成表；城内初始建筑 JSON 按 T3/T2 差异化 | §5.8 |
| P6 | ~~**E3 快速旅行**~~ **✅ 已完成（观感/手感待验收）** | 语义创始人拍板（已到访∧连通∧未阻断）已落地：TravelPlanner 路网图 + 到访解锁 + 弹窗 + 路由高亮 + travel_mode 契约；阻断钩子 `api.set_block_filter` 留给 P7 敌占区 | §5.10 |
| P7 | ~~**F 政权简化版**（F1 + F2）~~ **✅ 已完成（观感待验收）** | 调参入口 `l3/state_params.json`（越界罚/扩张系数/命名表全外置）重跑 `state_expand_lite.py`；预览验收图 `output/political_preview_2048.png` | §5.11 + §5.11.2 定标 |

### Flash 级（规格明确，可小会话批量派发；可穿插在 Pro 项间隙）

| # | 任务 | 说明 | 关键锚点 |
|---|------|------|----------|
| F1 | ~~**B4 MapModeManager** 实装~~ **✅ 已完成（`8c19640`）** | TERRAIN（默认）/POLITICAL 切换 + HUD 模式条 + 数字键；图例换内容走 `MapLegend.set_title/set_entries`（A2 已备入口），键位接线照 `system_setup` 现有 pattern | §5.5 |
| F2 | ~~**C1 剩余**~~ **✅ 已完成（`7920337`）** | 图钉 + api.set_player_map + map_loaded 订阅落地；跨 L1 的 set_player_l1/set_player_region 接线留 D 期（依赖 map_id 回填）；图钉观感待创始人验收 | §5.6 / §5.12.4 |
| F3 | ~~**C3 population_score 补齐**~~ **✅ 已完成（`11b6d79`）** | 生成端工具 + JSON/bin 已写入 + run_seed 每局扰动（出生免疫）+ 单测入 batch | §5.7 |
| F4 | ~~**E1 道路生成**~~ **⤴ 升级 Pro（P3.5）** | Flash 已落地骨架（flash 分支 `853dc09`/`7ab04c1`），贴地形观感未过创始人验收，属算法建模不再由 Flash 调参——详见 P3.5 行 | §5.9 |
| F5 | ~~**E2 道路渲染分级**~~ **✅ 已完成（`63eb7983`）** | 土路虚线/官道实线 polyline 叠加已落地（`map_renderer.gd` 道路层 + 数据层结构化 + 图例），观感待创始人验收 | §5.9 |
| F6 | ~~**E4 + E5 道路场景 + 步行旅行**~~ **✅ 已完成（本提交，观感/手感待验收）** | 生成器 `road_map_generator.gd` + 步行队列（WorldState.walk_legs）+ api.walk_to + 群系预采样工具 `road_biome_export.py`，详见已完成表 | §5.10；依赖 P5/P6（已满足） |
| F7 | ~~**§七 文档口径裁决 5 项**~~ **✅ 已完成（`7215416`）** | 五项全落地，两架构文档零残留 | 总体设计 §七 |
| F8 | ~~（可选）**缩略窗全景 preview 导出**~~ **✅ 已完成（`2e97bf72`）** | `export_l1_view_context.py --panorama` 已产出全景图+sidecar；缩略窗底图是否切换留创始人裁决 | §5.12.3 定标表 |

### 派发约定

> **2026-09-05 起 Pro/Flash 并行**（创始人指示）：从 `agent/world-map-revamp` 拉出两条分支建双工作区——
> **Pro → `.temp/world-map-pro`（分支 `agent/world-map-pro`）**；**Flash → `.temp/world-map-flash`（分支 `agent/world-map-flash`）**。
> 本工作区（`.temp/world-map-revamp`）保留作集成分支。新工作区跑 worldgen 工具前先从主工作区或
> 本工作区复制被 gitignore 的再生产物（高度场 npy/湖河 mask/regions//biome_labels 等，见踩坑记录）。

- Pro 项开工提示词必须带：总体设计对应 § 小节、本交接文档路径、所在分支与 worktree 路径、开头一句「继续 世界地图系统完善」恢复上下文。
- Flash 项提示词附对应 § + 现成入口/参照文件（上表"说明"列）；同样跑全量测试；视觉类（F2 图钉 / F5 道路）完成后仍须创始人观感验收。
- P1/P2 的预览图每步交创始人验收后再进下一步；P7 开工前重读 §5.11 + §5.11.1 锚点表 + 世界观 §6.1 王国表
- **文化圈地图 + 世界观编撰并行**（创始人 2026-09-05 指示）：P7 文化圈 flood-fill 落地时同步采用世界观设定 §6.1 王国表（命名/立场已入档，可改）；世界观 §六/§七（大陆王国 + 主线故事）已编撰为提案稿，待创始人审订

## 关键决策速查（详见总体设计 §二，勿重新问）

- **全程序化着色**，strategic_kit 素材库路线作废；仅玩家图钉等 UI 标记可用图标
- **政权简化版先行**（文化圈 flood-fill，字段按完整版预留）；出生 L1 保持 8 城邦
- **步行+快速旅行都做**。快速旅行语义（创始人 2026-09-06 拍板）= **已到访 ∧ 路网连通 ∧ 未被不可通过区（敌占区等）切断**；调试期传送免费即时（时间消耗/交通工具暂缓，正式版预留「按游戏内日行程限距 + 交通工具扩范围」方向）。战斗中禁用 + 途经路径高亮保留。原话「只有相邻地区能传送」已被本裁决取代，勿再追问
- **城市团块**=卫星图式覆盖，**非等比加性生长**（§5.7），建筑群只在场景图内
- **边界毛刺不做烘焙大修**（创始人裁定）；已完成参数级 AA 修复
- 默认地图模式 = TERRAIN 地形图

## 实施注意事项（踩坑记录）

- **坐标序陷阱**：L1 顶点 `[x,y]`，L2/L3 顶点 `[y,x]`（`l1_world_data.gd:219` 注释）——动数据管道必查
- **`msaa_2d` 枚举值**：project.godot 里写的是枚举原始值（0=关 1=2X **2=4X** 3=8X），写 4 运行时被拒并回落禁用（`set_msaa_2d` 报 out of bounds 但不致命）——改 AA 档位勿想当然写"x 倍数"
- **worktree 全局类缓存**：新增带 `class_name` 的脚本后测试可能全挂（连老类型 `StickmanState` 都找不到）——跑一次 `godot --headless --path . --import` 重建 `global_script_class_cache.cfg` 即恢复
- **MapLegend 同帧重复填充**：`set_entries` 用 `queue_free`（延迟删除），同一帧多次 open 会叠计数——已改为先 `remove_child` 再释放，改回时注意
- **生成管线位置**：Python 工具在**仓库根** `tools/worldgen/`（l3/ l1/ l2_export/），不在 `stick-world/tools/`（那里只有 `l_world_bake.gd`）
- `l*_world.json` 有同名 `.bin`（bin 优先加载）：**改 JSON 后必须重跑 `l_world_bake.gd` 刷 bin**，否则运行时看不到变更
- `settlement_mapgen.py` 已产 8 份建筑 JSON（`config/strategic_map/buildings/l1_settlement_00..07.json`），.tscn 产出 / map_id 回填 / game_root 注册三步断点待补（Phase D）
- 河流矢量现成：`tools/worldgen/output/locked/river_paths.json`（2048 坐标折线）
- 高度场现成：`tools/worldgen/output/fractal_heightmap_8192.npy`（群系/道路/blob 容量都从它采样）
- 运行时战略图数据全部来自 `config/strategic_map/` 预生成包，**禁止运行时程序化生成世界数据**
- **headless 测试视口 = 1920×1920**（project.godot 1920×1080 但 headless 实测正方形），凡断言涉及视口适配值的测试，用 `get_viewport().get_visible_rect().size` 现算期望，勿硬编码
- **战略图行为类断言易过时**：A3 改 open() 初始缩放（固定 0.36→动态适配）与缩放下限（可任意缩→锁 min_zoom），L2/L3 集成测试两处硬编码断言随之失败——改相机/HUD 语义时同步排查 `tests/integration/test_l2/l3_strategic_map.gd`
- **地图点击穿透坑（F1 验收发现）**：控制器 `_input()` 先于 GUI 处理——悬停在 HUD 控件上点鼠标，地图同时收到点击（点按钮=点选底下地块）。凡地图悬停点选/下钻的鼠标分支必须加 `get_viewport().gui_get_hovered_control() != null` 门控（三控制器已加，新视图照抄）；全屏背景 ColorRect 的 `mouse_filter` 必须 IGNORE（枚举 0=STOP/1=PASS/2=IGNORE，L3 OceanBackground 曾误写 STOP，门控后会全屏挡死地图点击）
- **headless 下 `is_visible_in_tree()` 恒 false**（L2 控制器注释先例）：非 CanvasItem 节点做"视图是否打开"门控时，沿父链找第一个 CanvasItem 读 `visible`（`MapModeManager._is_view_open()` 现成样板）
- **静态模式跨套件污染**：`MapModeManager.current_mode` 是全局静态，batch unit 单进程跑全部套件——模式类测试用例前后必须重置回 TERRAIN（`test_map_mode_manager` 已按此写）
- **L3 集成测试曾整场无渲染器数据跑过**：`_test_load` 漏 `renderer.set_data()`，open() 的 map_size 回退默认 2048（真实 8192）——旧断言不依赖 map_size 故从未暴露；新增"初始整图可见"断言时才现形。写涉及视角/适配的断言时先确认装配与 `system_setup` 等价（set_data 不可少）
- **worldgen Python 环境**：PATH 首位是 Inkscape 自带 python（无 scipy），跑 `tools/worldgen/` 工具须用完整路径 `C:\Users\fanbo\AppData\Local\Programs\Python\Python312\python.exe`
- **worktree 的 `tools/worldgen/output/` 只有 locked/**：其余再生产物（高度场 npy/湖河 mask/regions/ 等）被 .gitignore 排除不入库——新工具跑之前先从主工作区 `F:\VSCode\game-2\tools\worldgen\output\` 复制所需输入
- **出生聚落世界坐标反推**（B1 用）：`l1_world.json` 的 `position_px` 是 798 本地 **[x,y]** 序，世界 8192 = 本地 + (x0,y0)；x0,y0 由 legacy L1 蒙版（主工作区 `output/l1_v2/legacy_l1_labels_8192.npy`）label=69 的 bbox 中心 −399 得出。出生聚落 ≈ (1466,813)@2048
- **region polygon 数据质量参差**（B1 踩坑）：`region_data.json` 的 polygon 画 mask 面积与官方 `area_px` 对不上——island 类（region_009/010/011）只有官方值 0.1~4%，region_003/005 也严重缺失；**continent 主体 region（001/002/004/006/007/008/012/013）polygon 可靠**。凡按 region 做统计/蒙版，island 与 003/005 必须用 `output/regions/region_labels.npy` 标签图（2048 int32，label=n → region_n），不能信 polygon
- **Thread 未 join 直接销毁 = Windows 段错误**（B2 踩坑）：后台加载线程（FileAccess+PNG 解码）在节点销毁时若未 `wait_to_finish()`，Godot 4 退出阶段**直接段错误**（不是警告完事）——测试表现为 TIMEOUT。凡 `Thread.new().start(...)` 必须配 `_exit_tree()` 里 `wait_to_finish()`（`l3_map_renderer.gd` 三线程统一 join 是样板）
- **Git Bash 双引号 + Windows 反斜杠路径**：`"...\\$d"` 会把 `\$` 转义成字面 `$d`，循环建目录全建到错误路径且 cp 静默失败——从主工作区复制文件到 worktree 用**正斜杠路径** `/f/VSCode/...` + 单变量展开
- **新 PNG 入 config/ 后必须 headless --import**：运行时 `ResourceLoader.exists()` 依赖 `.import` 元数据，缺了不是报错而是**静默返回 null**（L2 地形图不显示无任何日志）；L3 走 `FileAccess` 直读不受影响
- **river_paths.json 是废弃管线产物**（B3 踩坑）：`output/locked/river_paths.json` 出自 `experiments/river_step4.py`（C 加速器 paths.bin + Catmull-Rom），与 `fractal_continent.py` 产的现役 `fractal_river_mask_8192.png` **不是同一批河**（折线采样落 mask+2px 内仅 7.2%）——文档旧口径「river_paths 现成可直接用」已废，河流矢量一律从 river_mask 骨架化提取（`river_export.py`）。顺带：该文件坐标是 8192 级非 2048
- **矢量提取质量别用像素 IoU 验收**（B3 踩坑）：折线按宽度重栅格化 vs 原 mask 的 IoU 只有 ~0.66——PIL `draw.line` 宽度是方形足迹，斜线重建恒膨胀 ~12%，属栅格化伪影不是提取跑偏。改用**忠实度双指标**：折线采样点落 mask+1px 内比率（跑偏检测）+ mask 中心带像素距折线 ≤2px 比率（漏河检测），阈值 0.97/0.95
- **L1 context 的 margin 不统一**（B3 踩坑）：出生包当年用 `--margin 15` 导出（side 798），批量 69 包用默认 45（如 label 69 side 1038）——**重算 L1 窗口别假设 margin**，side 直接读 json 的 `context_size`，原点按 legacy bbox 中心 ± side/2 重算 + l1_polygon 界内校验；今后 `world_origin` 字段已在包内可直接用
- **并行会话就地 patch 同一批 json 的共存验证**：道路（roads 字段）与河流（rivers/world_origin）两会话先后就地 patch 同 70 份 `l1_world.json`——读-改-写天然共存，但**json 改后必须重跑 `l_world_bake.gd`**（bin 不刷则运行时看不到），且提交前用 `git diff --stat` 核对改动清单是否只有自己那部分
- **道路噪声调参：走廊平移弯靠低频，高频只给毛刺**（P3.5 踩坑）：路的「弓形/绕行」由噪声**低频分量**的横向梯度驱动（octaves 加多 + decay 加大 → 低频占比被归一化摊薄，路反而更直）；高频分量只会产生沿路小毛刺且被 DP 收掉。平原区（坡度/TPI≈0）物理上限 ~10-25px 弓形，别指望短路（<150px）扭出大弯——真实邻居城镇间的乡道本来就接近直的
- **路网绕行度离群（detour >2）先查地形再怀疑算法**（P3.5）：隔海湾/群岛对的最短路绕 3-5.6× 是无桥世界的诚实数据（直线走廊采样高度 ≈ −0.09 即海床），不是搜路 bug；E3 旅行按 length_px 计价正好体现「绕海湾很远」
- **settlement_id 编号不补零陷阱**（P4 踩坑）：l1 packs 的 id 是 `settlement_city_001`（三位补零），l3_city 的 label 是整数——**跨数据源拼 id 一律走 `label_to_sid` 映射**（anchors 键反查），自行 `"%d"` 拼接会静默 miss（回写段用原文 id 幸存、注入段全 skip，不报错只在统计对不上时现形）
- **l3_city 的 centroid 是地块质心非聚落锚点**（P4）：与 L1 position_px+world_origin 反推的世界坐标差 16-60px——blob 烘焙/渲染锚点统一走 L1 锚（`anchor` 字段已注入 l3_city），centroid 只作 L3 无 anchor 时的回退；另 centroid 是 [x,y] 序（与 l3 polygons [y,x] 不同，city_split_v2 就这样）
- **blob 容量探测段必须等于生长段**（P4）：固定探 260px 会把远处无关河海算成阻挡（90% 方向误撞水、容量虚低）——从 `lv_base` 探到 `lv_base+g_max` 才是「生长增量的阻挡」语义；slope_ref 按高度场梯度分位定标（p97≈0.004），别拍脑袋（0.012 时山不卡、0.002 时 33% 方向被单陡像素误杀）
- **bin 的折线字段进 PackedVector2Array 后是 float32**（F5 踩坑）：json 是 float64，测试做「运行时 vs json 原文」坐标对照必须用距离容差（<0.01），`%f` 字符串比较会因精度差假失败
- **新增结构化字段不用刷 bin**（F5 顺带确认）：bin 是 json 原样序列化（LWDB），P3.5 写入的 roads polyline/tier 在 bin 里完好——运行时解析升级（`_roads_from`）即可读到，改解析不改 json 就无需重跑 `l_world_bake.gd`
- **settlement_mapgen.py 从未跑通过**（P5 踩坑）：① 相对路径 `../..` 少一级（worldgen/l1 上两级=tools/ 而非仓库根），产物全写进 `tools/stick-world/` 幽灵目录静默"成功"（os.makedirs 建出来的）——历史 8 份建筑 JSON 是更早版本产物，与当前 l1_world.json 的 level 分布**不匹配**（旧的 00 号是 T1 三民居布局，实际 city_1033 是 T2）；② def_id 曾臆造 `bld_` 前缀，合法值以 `building_gen/api.gd` 的 `_BUILDING_SCENE_PATHS` 为准（文件名=def_id，无前缀）；③ 出生 8 城实际 **6×T3+2×T2 无 T1**（城市8=00/城市7=06），LEVEL_CONFIG 的 T1 分支/部落布局为死代码
- **城内出口语义 = 边界回图**（P5 定标）：聚落城**不配** ChunkTrigger/register_map_exit 直连邻城——MapBoundaryDetector 顶到左右边界持续 3 秒发 `open_world_map_requested(true)` 开 L1 大图，玩家在战略图双击下一城再进（§5.8「回战略图选择下一站」；ChunkTrigger 是被 boundary detector 替代的旧机制，village_a 上仍在用但新地图不再加）
- **Godot 信号→lambda 连接不允许参数少于信号参数**（P6 踩坑）：给 `travel_requested` 加第二参后，旧测试的单参 lambda `func(m: String)` 在 emit 时报 "Method expected 1 argument(s), but called with 2"——错误不崩溃但 **lambda 不被调用**（信号看似发了、监听方收到 0 次，断言静默挂）。命名方法带默认值参数可兼容（`_on_travel_requested(map_id, mode := X)` 没问题），**lambda 不行**——给信号加参数必须排查全部 connect 点的 lambda 签名
- **战略图弹窗不能走 UIModalStack/SystemOverlay**（P6 定标）：战略图 CanvasLayer 层号 100 高于 UIRoot（层 1）与 SystemOverlay（z=90 在层 1 内），挂那边会被地图整个盖住——地图内弹窗（travel_dialog）挂战略图 CanvasLayer 直下自管理遮罩，ESC 由地图 controller.handle_escape 分流（弹窗开→先关弹窗）
- **l3_city.json 坐标是 8192 级**（P7 踩坑）：anchor/centroid/polygons 全部 8192（city_split_v2 --res 8192 产物）——当 2048 用会越界崩溃；栅格采样须 /4，蒙版光栅化直接同级画
- **MCP 种子落海 = 整圈扩散失败**（P7 踩坑）：群岛 region 的城 anchor 质心常落海（MCP 海洋 cost 1e6），种子必须 snap 到陆地（最近城 anchor）——症状是某文化圈城数为 0 且 no_cul 兜底污染全局分布
- **文化圈贴合锚点地块靠"region 越界罚"而非地形 cost**（P7 定标）：分 culture 各自 MCP 场 + 逐像素 argmin（不许单 cost 场混跑——任一 culture 合法即免罚会削弱锚定）；罚值 10 不足以挡游牧侵入 region_005（nomad 31 城），30 才确立 golden 27:14 主导
- **桥接边会被距离过滤悄悄丢掉**（P7 跳坑两轮）：桥接/孤立城补救边若超 max_edge，权重循环 `if dist > max_edge: continue` 会滤掉——补救边集合要显式豁免；且"孤立城连最近城"会连到同分量同伴（最近≠异分量），统一交给分量桥接函数处理
- **多源 Dijkstra 的链式竞速有先手优势**（P7 认知）：同圈多国"遇他国已占即停"会让 pop 最高都城的地盘反被邻居圈死（主名国 1 城）——解法 = 均衡系数（边权×(1+eps×规模)）+ 命名按城数降序（主名跟地盘走）；黄金角有限项最小色相 gap ~7°，须随机明度带兜底
- **legacy 标签图与 json 有版本漂移**（P7 踩坑）：`city_labels_2048.npy` 是 1038 城旧版（json 已 1040）——蒙版上色一律从 l3_city.json polygons 直接光栅化（与数据同源），不借旧标签图
- **find_contours 管线四坑**（R3 踩坑）：① skimage 用**像素中心系**（labels 像素 (r,c) 中心=(r,c)），extract_mesh 顶点是像素角点——换算恒 +0.5，漏加则整体错位半像素；② per-label bbox 必须在 **pad 后的 lp 系**算（labels 系 slice 切 lp 会错位 1，且块贴 field 边时等值线被边缘截断面积崩坏）；③ 二值场等值线的「直边」点全是共线点（面积 0），Visvalingam 阈值必须 << 特征面积 0.06px²（0.5px 步长的台阶三角形），取 0.02；④ 三岔交界逐 label 独立提取天然裂出 0.5px 缝隙三角（三个双共享点不重合，旧整数角点管线天然重合无缝）——T 形点焊（≤0.75px BFS 聚类合并质心）+重合点强制段边界，缺一不可：只焊不强制段边界，锚点会沉入合并段被抽稀删掉，跨地块缓存 key 失配缝隙回归
- **跨蒙版交界也要共享弧**（R3）：L1 的城市块（city_labels）与邻居块（legacy_l1_labels）是两张蒙版——分开跑 extract_smooth_mesh 则交界两侧各自平滑出楔形缝（旧管线整数角点无平滑位移故无此问题）；合成一张标签图（legacy+10000 命名空间，city 优先覆盖，**只填 legacy>0** 防海洋变 label）一次提取即共享
- **导出工具重跑会抹掉后注入字段**（R3 定标）：rivers/roads/blob_capacity/states/cities/map_id/population_score 全是导出后的就地 patch 注入——重跑导出工具全丢；改几何一律走 `--polys-only`（读现有 json 只换几何字段）
- **Python 输出重定向文件是全缓冲**：`print` 进度在进程结束前不落盘，长批量任务查进度用产物计数（ls 目录/日志 grep）而非 tail 日志
- **L2 政治贴图裁切免复制 746MB 中间产物**（P7 技巧）：政权蒙版 8192 光栅化后按各 region `info.json bbox_8192 + l2_world.json context_size/tiles_offset` 窗口 NEAREST 采样（`map_coordinates` order=0）——只需 13 个小 info.json，不必搬 tiles_8192.npy