# 看板工业级增强计划（AI 调度塔台）——持久工作日志

> **本文件是长时间自主工作的持久化记忆**：上下文被压缩/新会话接续时，读此文件即恢复全部上下文。
> Goal（创始人 2026-09-04 00:55 下达）：把任务依赖图看板升级为**机场调度/火车运行图级的工业调度软件**，
> 支持策划 AI 自发拆解任务、执行 AI 自发认领任务、超多并行 AI 调度可视化。
> **停止条件：次日（2026-09-04）早 9:00**。期间无限循环：实现→浏览器验收→截图→提交→在本文件记录 Round 日志。
> 服务：`python -m http.server 8124 -d F:/VSCode/game-2`（看板 http://127.0.0.1:8124/docs/项目/任务依赖图.html）
> 验收命令：`python tools/deptask/gen.py --check`（须 0E）+ `python tools/deptask/gen.py`（重生成 HTML）+ 浏览器 IAB 截图。

## 核心概念（已拍板设计）

1. **认领机制（调度核心）**：txt 新字段 `认领=AI名`。认领≠状态——认领后卡片渲染特殊标识（琥珀边框脉冲/AI 名角标），
   状态语义 = "已认领待开工"。校验：认领任务必须前置全齐（可开工）。
2. **CLI 是 AI 的接口**（执行 AI 自发认领的机制）：
   - `python tools/deptask/gen.py claim <id> --by <AI名>`：原子认领（校验可开工→写 txt 认领字段→追加调度日志）
   - `python tools/deptask/gen.py release <id> [--by <AI名>]`：释放认领
   - `python tools/deptask/gen.py done <id> --by <AI名>`：标记完成+日志
   - `python tools/deptask/gen.py sim --agents N --rounds M`：多 AI 调度模拟器（生成模拟调度日志 jsonl，供运行图渲染/压力测试）
   - 调度日志：`docs/项目/调度日志.jsonl`（事件：claim/release/done/new，字段：ts/agent/action/task/lane）
3. **底部 IDE 面板**（LOGIC-8 trace-wrap/Blender 底栏式，占下方 ~40% 可折叠）：
   - Tab A **运行图**（火车运行图式 canvas：x=虚拟时间轮次，y=泳道，任务认领→完成画横条双线，运行中=琥珀蚂蚁线，完成=实线绿）
   - Tab B **调度日志**（事件流列表，mono，最新在顶，agent 色标）
   - 可整体折叠到只剩 tab 条。
4. **动效系统**（对标专业调度软件）：
   - 认领瞬间：卡片琥珀边框脉冲 ×3 + flyTo 镜头 + 底栏日志滚动
   - 进行中：卡片顶部呼吸微光
   - 完成：绿色确认闪 + kv 燃尽数字跳动
   - 关键路径/运行中链：琥珀蚂蚁线（已有，扩展到认领链）
5. **压力测试**：sim 生成 500+ 节点数据验证渲染帧率（draw 每帧全量，必要时增量绘制/离屏缓存）。

## Round 日志（新在上）

### Round 12-14 ✅（02:15–02:30）agent 筛选/拆解辅助/帮助同步
- Round 12：运行图数据源切换（vbar：全部/真实/SIM）+ agent 筛选条（认领者色点过滤）——含此前丢失的数据源切换补齐
- Round 13：✂拆解辅助——右键任务→拆解为子任务，表单预填前置=父任务、id 自动编号（策划 AI 拆解接口）
- Round 14：帮助弹窗同步全部新交互（认领 CLI/塔台/底栏/vbar/拆解/回写）
- 教训追加：**放弃 heredoc 改大段文本**——锚点脆+引号地狱+虚假提交；统一 Read+Edit 或 Write 脚本文件


### Round 11 ✅（02:05–02:13）导出字段丢失修复
- exportTxt 补 认领=/级= 字段输出（看板编辑导出曾丢这两个字段——数据丢失 bug）


### Round 6-9 ✅（01:40–02:20）压测/联动/AI负载/塔台巡航（提交 c7f6877/6947472/81cab1d）
- 压测 354 节点/501 边 **76fps**（>60 目标）；wave_levels 重写 Kahn（60s 超时→0.26s）
- 运行图/日志/主图**双向 hover 联动**；制作人页 👥AI 负载卡（认领中+per-agent 统计）
- ✈塔台巡航（3.2s/站轮巡已认领任务+详情联动）
- **🔴 关键教训（Round 9 引入、Round 10 抓获）**：注入 JS 后只 grep 字符串未跑 `node --check`，
  toggleTower 缺 `}` 导致**整页 JS 瘫痪**，后续多轮浏览器"验证"全被旧缓存掩盖失效。
  **铁律：每轮改 JS 必须 node --check + 强制 cb 参数加载 + evaluate 断言（grep 字符串≠验证）**
- 域类型统一教训同上：heredoc 注入大段 JS 后必跑 node --check

### Round 10 ✅（02:20–02:35）抓获并修复整页瘫痪语法错 + 全功能重验
- toggleTower 缺闭合 `}` 修复；16 项全功能冒烟断言全绿（vbar 16钮/kv 6格/dock 4tab/2 认领/6 函数/134 事件）


### Round 5 ✅（02:30）认领可视化
- agent 色稳定散列边框+呼吸辉光+◉角标；工位占用边；详情认领者；常驻动画判定扩展
- 实数据：exec-alpha→p8_giant、exec-beta→region_entity（真实认领保留）
- flyTo 镜头特写实测（列表点击/跳转/双击均走缓动镜头）


### Round 1-4 ✅（01:00–02:10）CLI 认领 + 模拟器 + 底栏 dock + 运行图渲染器（提交待办）
- **CLI 全链路实测通过**：claim（防双认领/前置未完成拦截）→ release → done；字段写回重写为 split/join（根治正则吞分隔符）；调度日志 jsonl 落盘
- **sim 模拟器**：6 agents×60 轮 → 124 事件/62 完成，专长线优先+速度差异，主图零污染
- **底栏 dock**（38vh 可折叠/Tab 键）：▦运行图（火车运行图 canvas：泳道带/任务条/认领描边=agent 色/运行中琥珀光标/滚轮缩放/拖拽平移/点条跳主图）+ ≡调度日志（事件流行式）
- **数据合并注入**：真实调度日志 + sim 日志双源（src 标记），HTML DATA.simlog 注入
- 修复：gen_html root 参数缺失、heredoc 断裂字符串 ×4、setView 启动幂等守卫吞初始化（curView 初始 null+force）——**此 bug 曾致初始加载侧栏/工具条/底栏全不显示**
- 遗留到 Round 5：认领卡片脉冲动效、进行中呼吸光、完成确认闪


### Round 0（00:55–01:00）计划立盘 ✅
- 本文件创建。当前基线：`e0cf2b5`（219 节点/0E0W/就绪 63），看板已有：多视图页签/三栏 IDE/带状布局v2/flyTo/vbar 筛选/编辑闭环/LOGIC-8 主题。
- 已知技术债：无阻塞；screenshot 偶发超时（IAB guest 端，重开 tab 绕过）。

### Round 1（01:00–）CLI 认领机制
- 计划：parse 认领= 字段 → FIELDS/渲染；subcommand claim/release/done（argparse 或首参分发）；
  claim 校验：存在/未完成/前置全齐（排除自环）/未被认领 → 写回 txt（就地改该行）→ jsonl 日志；
  状态联动：认领不改状态字段（状态仍"可开工"），渲染层把"认领中+可开工"显示为已认领。
- 注意：txt 写回要保持其它行字节不动（按行替换）。

### Round 2（计划）模拟器
- sim：解析图→循环轮次：每 agent 从就绪集（未认领）按关键路径优先认领 1 任务→执行 rounds 轮→完成→日志。
- 输出 .temp/sim_log.jsonl + 统计（吞吐/并行度/关键路径压缩）。

### Round 3–5（计划）底栏/运行图/动效（见核心概念）。

### Round 6+（计划）压力测试、文档同步（维护规范/工具索引/CHANGELOG）、迭代打磨。

## 验收清单（9 点前自检）
- [ ] claim→看板标识→done 全链路（CLI+浏览器双端）
- [ ] sim 多 AI 模拟运行图有真实感数据
- [ ] 底栏运行图/日志面板可用且折叠顺畅
- [ ] 认领/完成动效专业（对标机场调度）
- [ ] 500 节点压力下交互不卡
- [ ] --check 0E 全程保持；CHANGELOG/规范同步
