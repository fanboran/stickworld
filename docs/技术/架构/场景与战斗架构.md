# 场景与战斗架构

> **P0 原型阶段的工程架构方案。**
> 解决火柴人行为 AI、小队级战斗、定居点建设三大高度耦合系统的解耦设计。
> 最后更新：2026-07-13（v3：建筑三层架构 + 通行障碍系统 + 调试覆盖层 + 山坡地图独立化）
> 关联文档：[`../../设计/GDD.md`](../../设计/GDD.md)、[`../../设计/系统/战斗系统.md`](../../设计/系统/战斗系统.md)、[`../../设计/系统/组织系统.md`](../../设计/系统/组织系统.md)、[`../../设计/核心循环.md`](../../设计/核心循环.md)

---

## 〇、文档目的

GDD 定义了"做什么"，本文档定义"怎么搭才不变屎山"。

P0 阶段三件事必须同时上线：
1. 火柴人行为 AI（特别是战斗 AI）
2. 小队级战斗（10-30 人）
3. 定居点建设基础逻辑

这三者高度耦合：工人建造时被袭击要切战斗 AI、战斗需要建筑作为掩体、建设需要建筑网格、战斗的编队直接复用组织系统的 L1 节点。本文档定义统一的场景骨架与模块边界，让三者各自开发但接口对齐。

---

## 一、设计原则（三条铁律）

### 1.1 逻辑层 ≠ 表现层

所有系统状态放在 `core/entities/` 的 State 对象里，**场景节点只是 State 的视图**。

- 火柴人 HP/位置/动画状态 → `StickmanState`
- 建筑血量/等级/占地 → `BuildingState`（新建）
- 战斗进度/参战双方 → `BattleState`（已有）
- 编制关系/指挥链 → `OrganizationState`（已有）

**场景卸载时 State 不丢**，挂载到新场景就能继续。这是支持"城镇就地变战场"、"快速旅行后状态延续"的基础。

### 1.2 常驻宿主 + 动态子场景

`GameRoot` 主场景常驻，村落/战场/室内都是动态加载的子场景。

- 环境、相机、UI、时间、输入分发跨场景保持
- 地图切换 = 替换 `WorldChunkHost` 的子节点

### 1.3 接口契约

模块间通信一律走 `api.gd` 或 EventBus。**任何 `get_node("../...")` 跨模块引用视为 Bug**。

依赖方向严格单向（见 §12 模块依赖图）。

---

## 二、GameRoot 主场景骨架

### 2.1 节点结构

```
GameRoot.tscn (常驻)
├── WorldClock              ← 接管 TimeManager，时间流速/暂停
├── EnvironmentSystem       ← 天空/天气/光照/震动（跨场景保持）
│   ├── SkyLayer            (极光、星星、太阳月亮 — Shader)
│   ├── WeatherLayer        (雨/雪/沙尘暴 — GPU 粒子)
│   ├── LightingLayer       (Environment + CanvasModulate)
│   └── GroundShakeRig      (整个世界父节点抖动)
├── CameraRig               ← 跟随玩家/小队/战略俯瞰
├── SceneLoader             ← chunk 流式加载/卸载
├── InputDispatcher         ← 模式路由（探索/建设/战斗/附身）
├── WorldChunkHost          ← 当前激活的地图实例挂载点
├── UIRoot                  ← 三层 UI 容器（见 §10）
└── BattleDirector          ← 多战场调度（城镇被袭→激活战斗实例）
```

### 2.2 各节点职责

| 节点 | 职责 | 关键接口 |
|------|------|---------|
| `WorldClock` | 时间流速、暂停、自动暂停触发 | `set_speed(multiplier)`、`pause()`、`resume()` |
| `EnvironmentSystem` | 天空/天气/光照/震动的全局参数 | `set_weather(id)`、`set_time_of_day(t)`、`shake(intensity)` |
| `CameraRig` | 水平卷轴跟随、手动控制、缩放、震屏（详见 §2.4） | `set_follow_target(node)`、`set_ground_y(y)`、`set_map_bounds(l,r)`、`set_zoom(level)`、`set_centered_mode(bool)`、`shake(...)` |
| `SceneLoader` | 加载/卸载地图与 Chunk | `load_map(map_id)`、`load_chunk(idx)`、`unload_chunk(idx)` |
| `InputDispatcher` | 根据模式路由输入 | `set_mode(mode)`、`register_handler(mode, handler)` |
| `WorldChunkHost` | 持有当前 Map 实例 | `current_map`、`spawn_entity(...)` |
| `UIRoot` | 三层 UI 容器 + 小地图（详见 §10） | 见 §10 |
| `BattleDirector` | 多战场并行管理 | `start_battle(...)`、`get_active_battles()` |

### 2.3 启动流程

```
1. 项目入口 → SceneManager 加载 GameRoot.tscn
2. GameRoot._ready() 初始化所有常驻子系统
3. SceneLoader.load_map("village_start") 加载初始村落
4. 地图加载完成回调：
   a. spawn 玩家 StickmanEntity，注入 ground_y/map_left/map_right
   b. CameraRig.set_ground_y(map.ground_y)
   c. CameraRig.set_map_bounds(map.map_left, map.map_right)
   d. CameraRig.set_follow_target(player_entity)
5. InputDispatcher.set_mode(EXPLORE)
6. WorldClock.set_speed(1x)
```

### 2.4 CameraRig 详细设计（水平卷轴 + 自由镜头）

#### 2.4.1 设计原则

- **水平卷轴**：地图沿 X 轴展开，相机只在水平方向跟随角色
- **垂直显示范围限定**：相机 Y 不跟随角色 Y，而是由 `ground_y` 和 `ground_ratio` 计算，让地面线恒在屏幕下方某比例处（默认 1/3，可配置）
- **自由镜头**：玩家可手动拖动/边缘滚动观察地图任意位置，自动跟随暂停
- **缩放看特写**：缩放是为了观察战场细节，缩放时不保持地面比例

#### 2.4.2 节点结构

```
CameraRig (Camera2D)
├── FollowController (Node)     ← 自动跟随（1/3 区域触发，仅 X 轴）
├── ManualControl (Node)        ← 手动控制（拖动 + 边缘滚动）
├── ZoomController (Node)       ← 缩放（1.0x ~ 2.0x，鼠标锚点）
└── ShakeRig (Node)             ← 震屏
```

#### 2.4.3 垂直位置计算

```
camera_y = ground_y - viewport_height / zoom * (0.5 - ground_ratio)
```

- `ground_y`：地图地面线 Y 坐标（世界坐标系），由 MapInstance 提供
- `ground_ratio`：地面占屏幕高度比例（默认 0.33，Inspector 可改，每张地图可不同，如室内可能 0.5）
- `zoom_level`：当前缩放（1.0~2.0）
- 推导：地面线应在屏幕下方 `ground_ratio` 处（距底部 `ground_ratio * vp_h / zoom`），即 `ground_y = camera_y + vp_h/zoom * (0.5 - ground_ratio)`

**验证各比例：**
- `ratio=0.33`：地面在屏幕下方 1/3 处 ✓
- `ratio=0.5`：camera_y = ground_y，地面在屏幕正中 ✓
- `ratio=1.0`：camera_y = ground_y + vp_h/2，地面在屏幕顶部（满屏地面）✓

**计算时机：**
- 地图加载/切换时：用初始 zoom_level 计算一次 `camera_y`
- 缩放变化时：**不重算 camera_y**（缩放是看特写，以鼠标位置为锚点，地面比例自然变化）
- `ground_ratio` 变化时（Inspector 调整）：重算 `camera_y`

#### 2.4.4 水平跟随算法（1/3 区域触发）

将屏幕水平三等分：

```
┌────────────┬────────────────┬────────────┐
│  左 1/3    │    中间 1/3     │   右 1/3   │
│ (触发跟随) │   (角色可游走)  │ (触发跟随) │
└────────────┴────────────────┴────────────┘
```

**规则：**
1. 角色在中间 1/3 → 相机不动
2. 角色进入左/右 1/3 → 相机跟随，直到角色回到中间 1/3
3. **手动控制激活时**（拖动 / 边缘滚动 / 小地图跳转）→ 自动跟随暂停
4. **手动控制结束** → 自动跟随恢复正常规则，**不主动拉回角色**（玩家想看角色需点居中按钮）
5. `centered_mode = true` → 强制角色到屏幕正中心，持续跟随（覆盖手动控制）

**边界约束：** 相机**视野边缘**不超出 `[map_left, map_right]`（不是中心点 clamp）：
```
half_vp_w = viewport_width / (2 * zoom)
cam_x_min = map_left + half_vp_w
cam_x_max = map_right - half_vp_w
若 cam_x_min > cam_x_max（地图比视野窄）→ 居中显示
```

#### 2.4.5 手动控制

| 操作 | 行为 | 模式限制 |
|------|------|---------|
| 鼠标左键拖动 | 平移视野（水平方向） | EXPLORE/INDOOR |
| 鼠标移到屏幕左/右边缘 5% | 以 `edge_scroll_speed`（默认 400 px/s）水平滚动 | EXPLORE/INDOOR |
| 鼠标滚轮上 | 放大（zoom +0.1，上限 2.0），以鼠标位置为锚点 | 全模式 |
| 鼠标滚轮下 | 缩小（zoom -0.1，下限 1.0），以鼠标位置为锚点 | 全模式 |
| "居中"按钮（GlobalHUD） | 切换 `centered_mode` | 全模式 |
| 小地图点击（§10.4） | 相机跳转到点击位置（RTS 式），暂停自动跟随 | EXPLORE/INDOOR |

**手动控制与自动跟随的关系：**
- 任何手动控制激活期间，自动跟随暂停
- 手动控制结束后，自动跟随恢复正常规则（不拉回角色）
- `centered_mode` 是唯一能让相机主动拉回角色的方式

#### 2.4.6 缩放

- `zoom_level` 范围：**1.0（默认最小）~ 2.0（最大迫近）**
- 缩放以鼠标位置为锚点（鼠标指向的世界点保持屏幕位置不变）
- 缩放时 `camera_y` 不重算，地面占比自然变化（可能整个屏幕看地面厮杀）
- 缩放变化触发 `ViewportRect`（小地图视野框）更新

#### 2.4.7 公共 API

```gdscript
set_follow_target(node: Node2D)         # 设跟随目标（不强制中心）
clear_follow_target()
set_ground_y(y: float)                  # 设地面线（地图加载时调用）
set_ground_ratio(ratio: float)          # 设地面占比（Inspector 可改）
set_map_bounds(left: float, right: float)  # 设水平边界
set_zoom(level: float)                  # 1.0~2.0
get_zoom() -> float
shake(intensity: float)                 # 震屏
is_shaking() -> bool
set_centered_mode(enabled: bool)        # 居中跟随开关
get_viewport_rect() -> Rect2            # 当前视野世界矩形（供小地图用）
```

---

## 三、地图与 Chunk 系统

### 3.1 地图类型分类

| 类型 | 用途 | 特征 | 进入方式 |
|------|------|------|---------|
| `VillageMap` | 村落/定居点 | 水平卷轴 + 垂直显示范围限定（ground_y 约束，详见 §2.4.3），重复地面纹理 | 战略图进入 / 快速旅行 / 步行 |
| `BattlefieldMap` | 战场 | 更长更空旷，两端各有建筑/阵地，同样垂直显示范围限定 | 触发战斗进入 |
| `RoadMap` | 村落间道路 | 水平卷轴 + 垂直显示范围限定，可亲自步行 | 步行走出村落末端 |
| `IndoorMap` | 小型室内（铁匠铺） | 挂载在建筑内，墙面透明化，独立 ground_y（可能地面占比更大） | 走进建筑交互区 |
| `MegaInteriorMap` | 大型建筑（宫殿） | 独立完整场景，独立 ground_y | 传送切换 |
| `SlopeMap` | 山坡/阶梯地形 | **独立逻辑**：相机可倾斜旋转、ground_y 按 X 变化、台阶处需工程队搭爬梯，不复用 VillageMap 卷轴算法 | 特定区域传送 |

**关键**：
- `VillageMap` 与 `BattlefieldMap` 共用同一套 Chunk 系统，区别仅在数据配置（chunk 数量、地形空旷度、建筑密度）
- **所有地图类型都遵循"水平卷轴 + 垂直显示范围限定"原则**：相机 Y 不跟随角色 Y，由 `ground_y` + `ground_ratio` 计算（详见 §2.4.3）；火柴人脚部锁定在 `ground_y`（详见 §7.1）
- 每张地图可在 Inspector 调整 `ground_ratio`（默认 0.33，室内可能 0.5）

**`SlopeMap` 独立原则（重要）**：
山坡/阶梯/倾斜地形走独立 `SlopeMap` 类型，**不污染通用 VillageMap 架构**：
- `SlopeMap` 不复用 CameraRig 的水平卷轴算法，有自己的 `SlopeCameraRig`（可旋转/倾斜跟随）
- `SlopeMap` 有自己的 `SlopeStickmanController`（Y 跟随地形高度查表，不能自由上下移动）
- 火柴人在 SlopeMap 中只能沿斜坡前进/后退
- **台阶断崖**：火柴人无法直接跨越，需工程队搭建"爬梯"建筑（一种特殊建筑类型 `bld_ladder`）才能通过
- 普通地图（VillageMap/BattlefieldMap/RoadMap/IndoorMap/MegaInteriorMap）**保持简单**，不为山坡让步
- 实施时机：山坡地图实际开发时再实现，P0 阶段不涉及

### 3.2 Chunk 流式加载

每张地图由若干 **Chunk**（固定宽度的可游玩片段）拼接：

```
Village_A 地图：
  Chunk[0] | Chunk[1] | Chunk[2] | ... | Chunk[N]
  ← 旧     当前激活   预加载         末端 →

玩家接近 Chunk[i] 末端触发器
  → SceneLoader 预加载 Chunk[i+1]
  → 玩家跨入 Chunk[i+1]
  → SceneLoader 卸载 Chunk[i-1]（保留过渡缓冲）
```

**Chunk 规格**：
- 一个 Chunk ≈ 1~2 屏宽（约 1920~3840px）
- 每段含地面段 + 该段的建筑/装饰/触发器
- **跨 Chunk 边界**：地面纹理连续，建筑不能跨边界
- **Chunk 末端出口**：地图末端 Chunk 有"出口触发器"，跨入后 → `SceneLoader` 加载相邻地图

### 3.3 地图数据 vs 场景实例

| 概念 | 文件 | 职责 |
|------|------|------|
| `MapDefinition` | `.tres` | 数据描述：哪些 chunk、每个 chunk 放什么建筑、地形参数 |
| `MapInstance` | 运行时对象 | 加载的节点、当前激活的实体、引用 State |
| `ChunkTemplate` | `.tscn` | Chunk 通用模板，由 MapDefinition 配置填充 |

```
config/maps/
├── village_north_pass.tres       # "北口村"地图定义
├── village_river_mouth.tres      # "河口村"
├── battlefield_north_gate.tres   # "北门战场"
├── road_north_to_river.tres      # "北口→河口 道路"
└── mega_palace_central.tres      # "中央宫殿"（大建筑）

scenes/chunk_templates/
├── village_chunk.tscn            # 村落 chunk 模板
├── battlefield_chunk.tscn        # 战场 chunk 模板
└── road_chunk.tscn               # 道路 chunk 模板
```

### 3.4 MapInstance 节点结构

```
MapInstance (Node2D)
├── PlacementGrid                 ← 占地网格（见 §4.2）
├── TerrainLayer                  ← 地面纹理重复渲染
│   └── GroundPolygon             ← 地面多边形（顶点从 ground_y 开始向下）
├── GroundLine (Marker2D)         ← 🆕 地面线标记（y = ground_y，供调试可视化）
├── DecorationLayer               ← 装饰物（草、石头、墙）
├── BuildingHost                  ← 当前激活的建筑节点容器（运行时所有建筑实例都在此）
├── TerrainBuildings (Node2D)     ← 🆕 地形建筑（只读，随场景打包，不可拆除，见 §4.5）
├── InitialBuildingsList (Node)   ← 🆕 初始建筑数据列表（def_id + cell_x + width，见 §4.5）
├── WalkBarrier (Node2D)          ← 🆕 地图级透明障碍容器（悬崖/高楼边缘，见 §7.1.3）
│   └── BarrierRect[] (Area2D)    # 矩形障碍区域，火柴人不可穿越
├── BuildMaskLayer (Node2D)       ← 🆕 不可放建筑区域（大石头/山坡阶梯处，见 §4.2）
│   └── BuildMaskRect[] (ColorRect) # 设计时绘制，运行时注册到 PlacementGrid.blockage_mask
├── ForegroundLayer (Node2D)      ← 🆕 前景层（z_index=10，室内支柱/天花板，火柴人经过被遮挡）
├── EntityHost                    ← 当前激活的火柴人节点容器
├── ChunkTriggers                 ← 末端触发器
└── BattleAnchor                  ← 战斗实例挂载点（城镇被袭时启用）
```

**z_index 层级约定**：
- TerrainLayer = 0（地面最底）
- DecorationLayer = 1
- BuildingHost = 2
- EntityHost = 3（火柴人）
- ForegroundLayer = 10（前景支柱最高）

#### 3.4.1 MapInstance 关键字段

```gdscript
# 所有 MapInstance（VillageMap/BattlefieldMap/RoadMap/IndoorMap/MegaInteriorMap）共用
@export var ground_y: float = 300.0       # 地面线 Y（世界坐标），火柴人可走区域顶部
@export var ground_ratio: float = 0.4     # 地面占屏幕高度比例（Inspector 可改，默认 2/5，室内可能 0.5）
@export var ground_bottom: float = 1024.0 # 地面底部 Y（火柴人可走区域底部）
@export var map_left: float = 0.0         # 地图左边界 X
@export var map_right: float = 2048.0     # 地图右边界 X
```

- `ground_y`：地面顶部，火柴人可走区域上边界（详见 §7.1.1）
- `ground_bottom`：地面底部，火柴人可走区域下边界（对应 GroundPolygon 底部）
- `ground_ratio`：相机计算用，让地面线在屏幕下方此比例处（详见 §2.4.3）
- `map_left`/`map_right`：相机水平边界 + 火柴人 X 活动范围

#### 3.4.2 公共方法

```gdscript
get_ground_y() -> float                  # 返回 ground_y
get_ground_ratio() -> float              # 返回 ground_ratio
get_camera_bounds() -> Vector2           # 返回 Vector2(map_left, map_right)
get_entity_walk_bounds() -> Vector2      # 返回火柴人 X 活动范围（通常同 map_left/map_right）
```

---

## 四、建筑系统

### 4.1 资产分层（型号 vs 等级）

**核心规则**：建筑**型号**决定 `.tscn`，**等级**决定 `.tres` 数据。同型号不同等级 = 同场景 + 不同 Resource。

```
assets/buildings/
├── components/                    # 通用组件素材
│   ├── walls/                     (墙、屋顶、门的贴图集)
│   └── props/                     (桌椅、火炉等装饰)
└── templates/                     # 一种型号一个 .tscn
    ├── house/house.tscn
    ├── blacksmith/blacksmith.tscn
    ├── blacksmith/blacksmith_interior.tscn   # 内部场景
    ├── barracks/barracks.tscn
    └── ...

config/buildings/                  # 数据驱动（已有 buildings.tres 基础上扩展）
├── house_tier1.tres               # 等级配置：贴图变体、占地格、容量、HP
├── house_tier2.tres
└── ...
```

**贴图尺寸 vs 占地尺寸（解耦）**：
- **占地尺寸**（`footprint_w`/`footprint_h`）= 格子数 × 32px，决定 PlacementGrid 占用
- **贴图尺寸**任意，可以比占地大或小
- 例：一棵树占地 1×1（32×32px，树根），贴图 96×192px（树冠很宽很高），Sprite2D 用 `offset = (-32, -160)` 让树冠向上向左展开
- 贴图超出占地范围不冲突，PlacementGrid 只校验占地格子

**程序化建筑扩展点（架构已就绪，P0 不实现）**：
建筑场景根节点可挂 `procedural_building.gd` 脚本，`_ready()` 中根据 `variant_seed` + `terrain_context`（地形类型、周围建筑、海拔）调用 `RandomNumGenerator` 选择 sprite 子集拼接。
- 建筑数据表（xlsx）新增字段：`procedural: bool`、`variant_seed: int`、`terrain_adaptive: bool`
- 地标建筑：地图设计时放置 `procedural=true` 实例，每个 `variant_seed` 不同 -> 每个地区天然不一样
- 玩家建筑：建造时传入 `terrain_context`，建筑脚本根据 context 生成不同造型
- 当前建筑场景文件设计为"可挂脚本"就为以后留好了口子，无需改架构

### 4.2 占地网格（32px 高精度）

```
modules/world/placement_grid/
├── placement_grid.gd              # 网格管理
├── grid_cell.gd                   # 单元格
└── placement_validator.gd         # 校验是否可建
```

每个 Map 持有一个 `PlacementGrid`：
- `cell_size = 32px`
- 每个建筑占据 N×M 个 cell（小屋 2×2、兵营 3×3、宫殿 8×6）
- **一块地只能建一个建筑** = cell 占用检查
- 选址预览 = ghost 建筑半透明 + 网格高亮（绿=可建/红=冲突）

**BuildMask 不可放建筑区域（地形限制）**：

PlacementGrid 新增 `blockage_mask: PackedByteArray`（与 grid 同尺寸），标记"地形上不可放建筑"的格子：

```gdscript
# PlacementGrid 新增字段
var blockage_mask: PackedByteArray  # 1=不可放建筑（大石头/山坡阶梯处/装饰物占地）

func is_blocked(x: int, y: int) -> bool:
    return blockage_mask[y * grid_width + x] == 1

func is_occupied(x: int, y: int) -> bool:
    # 原有建筑占用 OR blockage_mask = 1
    return _cells[y * grid_width + x] != "" or is_blocked(x, y)
```

**设计时操作**：地图场景的 `BuildMaskLayer` 节点下加 `ColorRect`（红色半透明，仅设计时可见），运行时 `_ready()` 读取这些 ColorRect 的位置尺寸，注册到 `PlacementGrid.blockage_mask`。

**与"路障建筑"方案对比（已否决）**：
- 路障建筑方案被否决，原因：污染建筑系统、调试显示乱、拆除逻辑需特殊处理
- BuildMask 是纯数据，不会误删、不会出现在建筑列表、与 PlacementValidator 自然集成
- 玩家尝试在 BuildMask 区域放建筑 -> PlacementValidator 返回冲突，提示与"建筑占用"相同的错误信息

**调试显示**：DebugOverlay 开启时，BuildMask 格子显示为红色半透明（详见 §10.5）。

### 4.3 建筑节点结构

```
Building (Node2D)                   ← 运行时实体
├── Footprint (CollisionShape2D)    ← 占地碰撞，对应 PlacementGrid 占用
├── Exterior (Node2D)               ← 屋顶 + 前墙（默认可见）
│   └── WallFront (Sprite2D)        ← 进入交互区时 modulate.a → 0.3
├── Interior (Node2D)               ← 内部空间 + NPC + 工作位
│   └── 默认 visible=false，进入交互区时 visible=true
├── InteractionZone (Area2D)        ← 玩家进入触发"室内模式"
├── PassageBarrier (Area2D)         ← 🆕 建筑级通行障碍（见 §7.1.3）
│   └── CollisionShape2D[]          # 矩形障碍区域，火柴人不可穿越
├── HealthComponent (Node)          ← HP/伤害
├── WorkSlot (Node2D)[]             ← 工作位（工人 AI 寻路目标）
└── CoverMarker (Node2D)[]          ← 掩体标记（战斗 AI 查询）
```

**建筑级 PassageBarrier（建筑本体不可通行区域）**：

建筑场景设计时，在 `PassageBarrier` 节点下加 `CollisionShape2D`（RectangleShape2D），划定建筑本体哪些区域火柴人不可穿越。与地图级 `WalkBarrier` 共用同一套碰撞查询（详见 §7.1.3）。

**典型用例**：

| 建筑类型 | 通行规则 | PassageBarrier 配置 |
|---------|---------|---------------------|
| 雕像（小） | 角色从上下两侧都可绕过 | 本体一小块矩形（仅雕像基座） |
| 房屋 | 角色只能从房屋下方通过 | 覆盖房屋上半部分（墙体），下方留空 |
| 兵营 | 角色可从下方过，正门交互进入 | 覆盖建筑上半部分 + 左右两侧 |
| 矿山（地形建筑） | 角色完全不能穿过 | 整个占地范围全覆盖 |
| 树木（装饰） | 角色可绕过 | 仅树根一小块 |

**设计原则**：
- PassageBarrier 形状可与占地不同（占地是水平条带宽度，PassageBarrier 是物理碰撞）
- 例：房屋占地 3 格宽（96px），PassageBarrier 只覆盖上半部分 96×64px，下方 96×32px 留给角色通行
- 设计时在建筑场景编辑器直接绘制 CollisionShape2D，所见即所得
- 调试显示：DebugOverlay 开启时，建筑 PassageBarrier 显示为紫色半透明（与地图级 WalkBarrier 蓝色区分，详见 §10.5）

### 4.4 村落差异化（抹除单调感）

每个 `VillageDefinition.tres`：

```
village_id: "north_pass"
biome: PLAINS                       # 决定环境基调
wall_height: 4                      # 城墙高度（影响防御）
ground_tint: Color(0.9, 0.85, 0.7)  # 地面色相微调
unique_buildings: ["windmill"]      # 独有建筑（独有贴图）
ground_props_density: 0.6           # 装饰物密度
chunk_count: 8                      # chunk 数
ambient_sfx: "village_plains"       # 环境音
```

七八个村落 = 七八个 `.tres`，**复用同一套 building templates**。独有建筑单独建 `.tscn`。

### 4.5 建筑三层架构（地形 / 初始 / 玩家）

游戏内有三种建筑来源，它们**和平共存**，通过同一套独立场景文件加载，但存储位置和可修改性不同。

#### 4.5.1 三层对比

| 层 | 存储格式 | 可破坏 | 加载时机 | 示例 |
|---|---|---|---|---|
| **地形建筑层** | 地图场景 `.tscn` 中 `TerrainBuildings` 节点 | 否 | 随场景实例化 | 矿山、遗迹、大石头 |
| **初始建筑层** | 地图场景 `.tscn` 中 `InitialBuildingsList` 列表（def_id + cell_x + width） | 是 | 地图加载后写入存档 | 关卡设计放的民居 |
| **玩家建筑层** | 存档 `.json` | 是 | 存档加载时实例化 | 玩家新建的农场 |

#### 4.5.2 为什么不会冲突

三者都使用**同一套建筑场景文件**（`modules/buildings/scenes/<building_id>.tscn`），只是加载时机不同。`PlacementGrid` 是统一占位登记表，三层建筑都向它注册，避免格子被重复占用。

```
地图加载流程：
1. SceneLoader 实例化 map.tscn
   └── TerrainBuildings 下的建筑自动随场景创建（静态，注册到 PlacementGrid，标记 is_terrain=true）
2. VillageMap._ready() 读取 InitialBuildingsList
   └── 首次进入该地图：将列表写入存档 JSON 的 buildings 数组
   └── 非首次：跳过（存档已有）
3. SaveManager 读取存档 JSON
   └── 对每个动态建筑实例（初始 + 玩家）：
       ├── 加载对应场景文件
       ├── 实例化后 add_child 到 BuildingHost
       └── 注册到 PlacementGrid
```

#### 4.5.3 拆除规则

- **地形建筑**：玩家不能拆除（PlacementGrid 标记 `is_terrain=true`，拆除按钮禁用）
- **初始建筑 + 玩家建筑**：可拆除，从存档 JSON 移除，释放 PlacementGrid 格子，退还部分资源

#### 4.5.4 存档 JSON 数据结构

存档中的 `buildings` 数组只记录动态建筑（初始 + 玩家），**不记录地形建筑**（地形建筑随地图场景打包，不需要存档）：

```json
{
  "version": "0.1.0",
  "save_id": "save_001",
  "maps": {
    "forest_01": {
      "buildings": [
        {
          "instance_id": "dyn_0001",
          "def_id": "bld_farm",
          "cell_x": 30,
          "width": 2,
          "health": 150,
          "is_initial": true,
          "built_at_time": 0.0
        },
        {
          "instance_id": "dyn_0002",
          "def_id": "bld_workshop",
          "cell_x": 35,
          "width": 3,
          "health": 300,
          "is_initial": false,
          "built_at_time": 12.5
        }
      ]
    }
  }
}
```

`is_initial` 字段区分初始建筑和玩家建筑，便于拆档调试和未来"关卡重置"功能。

---

## 五、室内与大建筑

### 5.1 小型建筑 — 墙面透明化（不传送）

铁匠铺、住宅等小型建筑采用双层结构：

```
默认状态：
  Exterior.visible = true  (墙、屋顶可见)
  Interior.visible = false (内部隐藏)
  WallFront.modulate.a = 1.0

玩家进入 InteractionZone：
  WallFront.modulate.a → 0.3 (前墙半透明)
  Interior.visible = true    (显示内部)
  InputDispatcher.set_mode(INDOOR)

玩家离开 InteractionZone：
  反向恢复
```

**优点**：
- 室外正常显示，玩家能看见"师傅在打铁"
- 进入是连续的，无传送感
- 多个建筑可同时透明化（玩家跨越多个交互区时）

### 5.2 大型建筑 — 传送切换（MegaInteriorMap）

宫殿、地下城、大型堡垒等：
- 进入触发器触发 `SceneLoader.load_map("mega_palace_xxx")`
- 显示一个简短过场（黑屏 + 文字"进入宫殿"）
- 内部是完整的 `MegaInteriorMap`，有独立的 Chunk 系统

**分类规则**：建筑数据 `.tres` 中 `interior_mode` 字段决定：
- `TRANSPARENT` → 透明化
- `TELEPORT` → 传送，需指定 `mega_interior_map_id`

---

## 六、旅行系统

### 6.1 三种旅行方式

| 方式 | 触发 | 实现 | 限制 |
|------|------|------|------|
| 步行 | 玩家走到村落末端出口 | 加载 RoadMap → 末端走出 → 加载下一村落 | 无限制 |
| 快速旅行 | 大世界地图选目标 | 显示"行进中"过场 + 消耗游戏内时间 → 加载目标 | 战斗中禁止 |
| 传送 | 大建筑进入触发器 | 切换到 MegaInteriorMap | 战斗中禁止 |

### 6.2 步行流程

```
Village_A 末端出口触发器
  → SceneLoader.load_map("road_A_to_B")
  → RoadMap 加载，玩家从左端进入
  → 玩家走完 RoadMap
  → 末端出口触发器
  → SceneLoader.load_map("village_B")
```

`RoadMap` 也支持 Chunk 流式加载，可长可短。后期可加入路上事件（伏击、商人、流民）。

### 6.3 快速旅行流程

```
玩家打开战略图 (Tab)
  → world_map 模块（战略图模块）显示已探索村落/城市
  → 玩家选择目标
  → 校验：当前不在战斗中？目标已探索？
  → UIRoot 显示"行进中"过场
  → WorldClock.advance(time_cost)  # 推进游戏内时间
  → SceneLoader.load_map(target_id)
  → 玩家出现在目标村落的入口
```

**设定合理性**："快速旅行"在游戏内解释为"主角走过去了"，只是省略了过程。消耗的游戏内时间按距离计算。

### 6.4 旅行封锁条件

- 当前 Map 挂载了 active `BattleInstance` → 禁止
- 玩家正在被攻击（HP < 阈值且最近受击 < 5s）→ 禁止
- 目标未探索 → 不可选

---

## 七、火柴人行为 AI

### 7.1 StickmanEntity 节点结构（在已有 StickmanRig 之外包一层）

```
StickmanEntity (CharacterBody2D)        ← 🆕 物理+碰撞
├── StickmanRig (Skeleton2D)            ← ✅ 已有，纯渲染骨架
├── Hitbox (Area2D)                     ← 受击判定
├── WeaponMount (Node2D)                ← 武器挂载
├── HealthComponent (Node)              ← HP/士气
├── AIController (Node)                 ← 🆕 决策大脑
│   └── BehaviorStateMachine
└── PossessionInterface (Node)          ← 🆕 玩家附身接口
```

**关键**：`StickmanRig` 不动（已实现的 IK/动画保留），外层包一个 `CharacterBody2D` 承担物理与移动。AI 通过 `rig.play("walk")` + `rig.set_anim_speed(...)` 驱动渲染，通过 `CharacterBody2D.velocity` 驱动物理。

#### 7.1.1 地面约束（Y 范围 + X 边界）

火柴人可在地面区域内上下左右自由移动（不是锁死在 ground_y 一条线）：

```gdscript
# StickmanEntity 字段
var ground_y: float = 300.0       # 地面顶部 Y（由 MapInstance.spawn_entity 注入）
var ground_bottom: float = 1024.0 # 地面底部 Y（火柴人可走区域底部）
var map_left: float = 0.0         # X 活动范围左边界
var map_right: float = 2048.0     # X 活动范围右边界
var foot_offset: float = 30.0     # 脚部到节点原点的偏移（CollisionShape2D 半高）

func _physics_process(delta):
    # ... 输入处理（WASD 上下左右）...
    move_and_slide()
    # Y 范围约束：脚部保持在 [ground_y, ground_bottom] 内
    var y_min: float = ground_y - foot_offset
    var y_max: float = ground_bottom - foot_offset
    global_position.y = clampf(global_position.y, y_min, y_max)
    # X 边界约束
    global_position.x = clampf(global_position.x, map_left, map_right)
```

**生成位置：** 火柴人生成 Y = `ground_y + (ground_bottom - ground_y) * 0.5`（地面垂直范围偏中心）。

**未来扩展（非 P0）：**
- 跳跃：`velocity.y` 临时非零，落地后重新受 Y 范围约束
- 飞行单位：`position.y` 可超出 `ground_y`（向上飞），但仍受 `ground_bottom` 约束
- 地形高度变化：地图不同 X 位置 `ground_y` 不同（查表），火柴人 Y 跟随地形

#### 7.1.2 通行障碍系统（WalkBarrier + PassageBarrier）

火柴人移动除了受 `ground_y`/`ground_bottom`/`map_left`/`map_right` 矩形约束外，还受**两级透明障碍**约束：

**地图级 WalkBarrier（悬崖/高楼边缘）**：
- 位于 `MapInstance.WalkBarrier` 节点下，含若干 `Area2D` + `CollisionShape2D`（矩形）
- 设计时在地图场景中绘制，覆盖悬崖边缘/高楼边界/断崖
- 运行时不可见，调试模式显示为**蓝色半透明矩形**（详见 §10.5）
- 适用场景：悬崖边缘、高楼堡垒边缘、断崖、任何"角色不能御空而行"的边界
- **不适用于山坡**（山坡走 `SlopeMap` 独立逻辑，详见 §3.1）

**建筑级 PassageBarrier（建筑本体不可通行）**：
- 位于每个建筑场景的 `PassageBarrier` 子节点下（详见 §4.3）
- 设计师在建筑场景中绘制，划定建筑本体哪些区域不可通行
- 运行时不可见，调试模式显示为**紫色半透明矩形**（与地图级蓝色区分）
- 典型用例：雕像上下可过、房屋只能从下方过、矿山完全不可过

**火柴人碰撞查询**：

```gdscript
# StickmanEntity._physics_process
var _last_valid_position: Vector2

func _physics_process(delta):
    # ... 输入处理 + move_and_slide() + Y/X 矩形约束 ...

    # 🆕 通行障碍检测：若进入任何 WalkBarrier / PassageBarrier 区域，回退到上一帧位置
    if _is_in_passage_barrier():
        global_position = _last_valid_position
        velocity = Vector2.ZERO  # 撞墙停止
    else:
        _last_valid_position = global_position

func _is_in_passage_barrier() -> bool:
    # 查询当前 MapInstance.WalkBarrier 下所有 Area2D
    # 查询 BuildingHost 下所有建筑的 PassageBarrier Area2D
    # 用 PhysicsDirectSpaceState2D.intersect_point 或 Area2D.overlaps_body 检测
    # 实现细节：见 modules/units/scripts/stickman_entity.gd
    ...
```

**与 SlopeMap 的关系**：
- WalkBarrier/PassageBarrier 是"矩形区域阻挡"的轻量方案，适用于 VillageMap 等水平卷轴地图
- SlopeMap 走独立逻辑，不用 WalkBarrier（坡面是连续的 Y 变化，不是矩形阻挡）

#### 7.1.3 附身接口

`PossessionInterface` 提供：
- `set_possessed(bool)`：切换玩家控制
- `is_possessed() -> bool`
- 附身时：读取 WASD 输入驱动 `velocity`，`AIController` 暂停
- 取消附身：`AIController` 恢复控制

### 7.2 行为状态机

```
modules/units/ai/
├── behavior_base.gd                 # 行为基类（enter/update/exit）
├── behavior_idle.gd                 # 闲置
├── behavior_move.gd                 # 移动（含简单寻路）
├── behavior_attack.gd               # 攻击（命中帧→伤害事件）
├── behavior_seek_cover.gd           # 找掩体
├── behavior_suppress.gd             # 火力压制
├── behavior_flank.gd                # 侧翼包抄
├── behavior_retreat.gd              # 撤退
├── behavior_work.gd                 # 工作（建造/采集/打铁）
├── behavior_flee.gd                 # 溃逃
└── behavior_state_machine.gd        # 状态机调度
```

每个行为是独立的 `Node`/`Resource`，状态机持有引用并通过 `travel(behavior_name)` 切换。

### 7.3 三层命令系统（决策来源）

```
┌──────────────────────────────────────┐
│ 1. 玩家/指挥链下达的指令              │ ← 高优先级
│    (tactical_orders → command_chain) │
├──────────────────────────────────────┤
│ 2. 编制默认战术（自主决策权限内）     │ ← 中优先级
│    (OrganizationState.autonomy_level)│
├──────────────────────────────────────┤
│ 3. 单位本能（受击反击、找掩体）       │ ← 低优先级
│    (behavior_xxx 内置触发)           │
└──────────────────────────────────────┘
```

- 高优先级指令覆盖中低优先级
- 中优先级在无指令时驱动默认行为
- 低优先级是生存本能，永远生效但被高优先级压制

`StickmanState` 已有 `autonomy_level` 字段，AIController 读取它决定能否自主行动。

### 7.4 小兵步枪式灵动性 — 两层实现

**第一层：行为层概率钩子**

每个战斗行为内置可配置概率：
```gdscript
# behavior_attack.gd 示意
@export var prob_aggressive_push: float = 0.05  # 擅自冲锋概率
@export var prob_hesitate: float = 0.03          # 犹豫概率

func update(delta):
    if _is_at_disadvantage() and randf() < prob_hesitate:
        _enter_hesitate_substate()
    elif _enemy_exposed() and randf() < prob_aggressive_push:
        _push_forward()
```

**第二层：战场导演情绪标签**

`battle_ai_director.gd` 周期性（每 2~5s）给单位打"情绪标签"：
- `HESITANT` — 犹豫（命中率-30%、移动减速）
- `EXCITED` — 亢奋（追击倾向+50%、忽视指令概率+10%）
- `PANICKED` — 恐慌（找掩体优先级最高、可能溃逃）
- `STEADY` — 稳定（默认）

情绪概率受：指挥官能力、部队士气、文化传统、自主决策权限影响。

### 7.5 玩家附身

`PossessionInterface`：
```gdscript
func possess(unit_id: int) -> void:
    # 1. 暂停该单位的 AIController
    # 2. InputDispatcher.set_mode(POSSESS)
    # 3. CameraRig.follow(entity)
    # 4. 路由 WASD/鼠标到该 entity 的 velocity/weapon

func release() -> void:
    # 反向恢复
```

附身时：
- WASD → `CharacterBody2D.velocity`
- 鼠标左键 → 攻击
- 鼠标右键 → 瞄准/格挡
- Tab → 打开该层级管理面板
- ESC → 退出附身

---

## 八、战斗系统

### 8.1 战斗实例 vs 战场场景（关键解耦）

```
battle_instance.gd (纯逻辑)         ← 战斗状态、参战双方、结果判定
   ↓ 拥有
battle_arena.tscn (战场场景)         ← 渲染战场、地形、掩体
   ↓ 引用
多个 StickmanEntity                  ← 参战单位
```

**为什么这样**：城镇被袭变战场时，**不切场景**——当前 `VillageMap` 挂载一个 `battle_instance` 即可，建筑继续显示（且可被破坏）。`battle_instance` 是纯数据+逻辑，挂在任何 Map 上都行。

`MapInstance.BattleAnchor` 节点就是 `battle_instance` 的挂载点。无战斗时为空。

### 8.2 模块结构

```
modules/combat/
├── api.gd
├── scripts/
│   ├── battle_manager.gd           # 多战场调度（同时打几场）
│   ├── battle_instance.gd          # 单场战斗逻辑（不依赖场景）
│   ├── formation_system.gd         # 编队/框选/分组
│   ├── tactical_orders.gd          # 预设号令（前进/冲刺/掩护/撤退）
│   ├── command_chain.gd            # 指挥链（逐层下达+延迟）
│   ├── morale_system.gd            # 士气（影响AI行为选择）
│   ├── cover_system.gd             # 掩体（查询接口供AI调用）
│   ├── suppression_system.gd       # 火力压制（区域debuff）
│   └── battle_ai_director.gd       # 战场导演（灵动性来源）
├── scenes/
│   ├── battlefield_chunk.tscn      # 战场chunk（用于BattlefieldMap）
│   └── battle_overlay.tscn         # 战斗UI层（指令面板、选中框）
└── data/
    └── tactical_presets.tres       # 预设号令配置
```

### 8.3 框选 → 编队 → 任命 → 下令 流程

```
1. 玩家框选 → selection_system 返回 unit_ids 数组
2. "编队"按钮 → formation_system.create_squad(unit_ids) → 返回 squad_id
3. "任命排长"→ organization_api.assign_commander(squad_id, leader_unit_id)
4. "全体前进"→ tactical_orders.issue(ORDER_ADVANCE_ALL, target_pos)
            → command_chain 逐层下达（带延迟）
            → 各单位 AIController 接收 → 切换到 behavior_move
5. "对排长发令"→ selection 排长 → tactical_orders.issue_to(squad_id, ORDER_*)
              → 仅该 squad 执行
```

**关键**：任命排长 = 创建 L1 组织节点，复用现有 `organization_state.gd`。这就是为什么战斗和组织高度耦合——必须一起设计。

### 8.4 预设号令清单（P0 范围）

| 号令 | 效果 | 适用层级 |
|------|------|---------|
| `ORDER_ADVANCE_ALL` | 全体向目标点前进 | L1-L2 |
| `ORDER_SPRINT` | 消耗体力加速冲刺 | L1 |
| `ORDER_HOLD_POSITION` | 原地坚守 | L1-L2 |
| `ORDER_RETREAT` | 有序后撤 | L1-L2 |
| `ORDER_TAKE_COVER` | 就近找掩体 | L1 |
| `ORDER_SUPPRESSING_FIRE` | 对指定区域压制射击 | L1 |
| `ORDER_FLANK_LEFT/RIGHT` | 侧翼包抄 | L1-L2 |
| `ORDER_RALLY` | 集结溃兵 | L2 |

### 8.5 指挥链延迟

命令从 L(n) 下达到 L(n-1) 有延迟，模拟传令时间：
- 延迟 = `base_delay × tier_diff × commander_efficiency_modifier`
- 默认 `base_delay = 2s`，每跨一层 +2s
- 指挥官能力高 → 延迟减半
- 玩家附身该层级指挥官时 → 延迟为 0（直接指挥）

---

## 九、定居点建设

### 9.1 模块结构

```
modules/construction/
├── api.gd
├── scripts/
│   ├── construction_manager.gd      # 总控
│   ├── build_queue.gd               # 队列（多项目并行）
│   ├── placement_system.gd          # 选址+对齐网格
│   ├── construction_project.gd      # 单个项目（复用 ProjectState）
│   └── work_crew_assigner.gd        # 派工（从组织拉施工队）
├── scenes/
│   ├── placement_preview.tscn       # 半透明 ghost 建筑预览
│   └── construction_site.tscn       # 施工中状态（脚手架+进度条）
```

### 9.2 建造流程（耦合点明示）

```
玩家选择"建造铁匠铺"
  ↓
placement_system 启动选址模式（InputDispatcher.set_mode(BUILD)）
  ↓
玩家移动 ghost 预览 → placement_grid.is_valid() 校验
  ↓
确认 → construction_project 创建（占用 grid cell）
  ↓
work_crew_assigner 从组织系统拉施工队
  ↓
工人 AIController 切换到 behavior_work（走过去+施工动画）
  ↓
进度推进 → 工程量 = base_work × difficulty / (worker_count × efficiency)
  ↓
完成 → 实例化 building.tscn 到 Map → 触发 building_completed 事件
```

### 9.3 工程量系统（非固定建造时间）

```gdscript
remaining_work = base_work  # 例如铁匠铺 = 100 工时

# 每帧推进
func _process(delta):
    var worker_count = _get_assigned_workers().size()
    var efficiency = _get_crew_efficiency()  # 受组织水平影响
    var work_done = worker_count * efficiency * delta
    remaining_work -= work_done
    if remaining_work <= 0:
        complete()
```

**与战斗的耦合**：工人被袭击 → `behavior_work` 中断 → 切到 `behavior_flee` 或 `behavior_attack`。施工暂停。这就是为什么必须一起设计。

---

## 十、UI 分层

### 10.1 三层 UI + 小地图

```
UIRoot (常驻)
├── GlobalHUD              ← 顶层常驻：时间速度、资源数、通知、居中模式按钮
├── ModePanel              ← 模式相关：整体替换
│   ├── VillagePanel       (建设菜单、村民管理、库存)
│   ├── BattlePanel        (框选信息、指令按钮、编制树)
│   └── PossessPanel       (附身时的角色控制 HUD)
├── ContextPanel           ← 上下文相关：选中什么显示什么
│   ├── BuildingInspector  (选中建筑)
│   ├── SquadInspector     (选中编制)
│   └── CommanderPanel     (附身指挥官时的管理面板)
├── Minimap                ← 🆕 小地图（屏幕正上方中央，详见 §10.4）
└── ModalOverlay           ← 弹窗（暂停菜单、组织架构总览、世界地图）
```

### 10.2 模式切换响应

切换城镇→战斗模式：
- `ModePanel` 整体 swap：`VillagePanel` → `BattlePanel`
- `ContextPanel` 清空（无选中）
- `GlobalHUD` 不变（时间/资源继续显示）
- `InputDispatcher.set_mode(BATTLE)`
- `BattlePanel` 显示当前 `battle_instance` 概要

### 10.3 模块结构

```
modules/ui/
├── api.gd
├── global_hud/
├── mode_panels/
│   ├── village_panel.tscn
│   ├── battle_panel.tscn
│   └── possess_panel.tscn
├── context_panels/
│   ├── building_inspector.tscn
│   ├── squad_inspector.tscn
│   └── commander_panel.tscn
├── modals/
│   ├── pause_menu.tscn
│   ├── org_tree_overview.tscn
│   └── strategic_map_modal.tscn   # 战略图模态窗口
├── minimap/                    # 🆕 小地图（详见 §10.4）
│   ├── minimap.gd
│   ├── minimap.tscn
│   └── minimap_renderer.gd     # 缩略图生成
└── widgets/                    # 通用控件（按钮、进度条、列表）
```

### 10.4 Minimap 小地图系统

#### 10.4.1 位置与尺寸

- **位置**：屏幕正上方中央（`anchor_top = 0`，水平居中）
- **尺寸**：默认 `240x80`（可配置），按地图长宽比自适应
- **层级**：`UIRoot` 子节点，`z_index` 高于游戏画面，低于 `ModalOverlay`

#### 10.4.2 节点结构

```
Minimap (Control)                      ← 屏幕正上方中央
├── Background (Panel)                 ← 背景框
├── MapThumbnail (TextureRect)         ← 整图缩略图（程序生成或预渲染）
├── ViewportRect (ColorRect)           ← 当前屏幕视野框（红色边框）
├── PlayerDot (ColorRect)              ← 角色位置点（绿色）
└── BuildingLayer (Node2D)             ← 建筑物图标容器
    └── BuildingIcon[] (ColorRect)     # 每个建筑一个小图标
```

#### 10.4.3 显示内容

| 元素 | 来源 | 更新频率 |
|------|------|---------|
| 地图缩略图 | `minimap_renderer` 生成（地图加载时一次） | 地图切换时重生成 |
| 视野框 | `CameraRig.get_viewport_rect()` 映射到缩略图坐标 | 每帧（或 10Hz 节流） |
| 角色点 | 玩家实体 `global_position.x` 映射；Y 固定在 `ground_y` 对应位置 | 每帧 |
| 建筑图标 | `BuildingHost` 子节点，按 `building_type` 着色 | 建筑增减时增量更新 |

#### 10.4.4 坐标映射

```
缩略图坐标 = (世界坐标 - map_left) * (缩略图宽 / 地图宽)
```

- Y 轴：地图垂直范围 `[sky_top, ground_y + depth]` 映射到缩略图高度
- 角色点 Y 固定在 `ground_y` 对应的缩略图位置（约下方 1/3）

#### 10.4.5 交互

| 操作 | 行为 |
|------|------|
| 左键点击小地图 | 相机跳转到点击位置（RTS 式观察），暂停自动跟随（详见 §2.4.5） |
| 左键拖动小地图 | 相机持续跟随拖动位置 |
| 鼠标悬停（可选，P0 不做） | 显示该位置坐标/建筑名提示 |

**点击跳转实现：**
```gdscript
func _on_gui_input(event):
    if event is InputEventMouseButton and event.pressed and event.button_index == MOUSE_BUTTON_LEFT:
        var world_x := (event.position.x / size.x) * (map_right - map_left) + map_left
        camera_rig.jump_to_x(world_x)  # 手动控制，暂停自动跟随
```

#### 10.4.6 公共 API

```gdscript
# Minimap
set_map_info(map_left, map_right, ground_y, ground_ratio)  # 地图加载时调用
update_player_position(x: float)                           # 每帧调用
update_viewport_rect(rect: Rect2)                          # 每帧调用（来自 CameraRig）
refresh_buildings(buildings: Array)                        # 建筑变化时调用
```

#### 10.4.7 缩略图生成策略

`minimap_renderer.gd` 负责生成缩略图，三种策略（按优先级）：

1. **预渲染纹理**（首选）：地图配置里指定 `thumbnail.png`，直接使用
2. **程序生成**（兜底）：读取 `TerrainLayer.GroundPolygon` 顶点 + `DecorationLayer` 装饰物，用 `Viewport` 离屏渲染缩略图
3. **纯色填充**（最小可用）：地面绿色矩形 + 天空蓝色矩形，无装饰

P0 阶段先用策略 3（纯色填充），后续迭代到策略 1/2。

### 10.5 DebugOverlay 调试覆盖层

调试覆盖层用于可视化所有运行时不可见的标记（占地格/障碍/地面线/触发器等），是后续开发的必备工具。

#### 10.5.1 模块结构

```
modules/debug/                        ← 🆕 新建
├── api.gd                            # DebugApi：注册绘制器、切换可见性
├── scripts/
│   ├── debug_overlay.gd              # 主控（CanvasLayer，F3 切换）
│   ├── debug_legend.gd               # 启动图例常驻显示（小字）
│   └── debug_drawers/                # 各类调试绘制器
│       ├── grid_drawer.gd            # PlacementGrid 占用（绿）+ BuildMask（红）
│       ├── barrier_drawer.gd         # WalkBarrier（蓝）+ PassageBarrier（紫）
│       ├── building_drawer.gd        # 建筑边界框（白）
│       ├── ground_line_drawer.gd     # ground_y 线（黄）+ ground_bottom 线（青）
│       ├── chunk_trigger_drawer.gd   # Chunk 触发器范围（紫矩形）
│       └── entity_state_drawer.gd    # 火柴人状态文字
└── scenes/
    └── debug_overlay.tscn
```

#### 10.5.2 启动图例（常驻小字）

游戏启动时在屏幕左上角显示**小字号**图例，**不淡出**，常驻显示（直到玩家按 F3 关闭调试模式）。图例内容：

```
[F3] 调试
绿格=占地  红格=不可建  蓝区=地图障碍  紫区=建筑障碍
黄线=地面线  青线=地面底  白框=建筑  紫框=Chunk触发
```

图例字号小（约 10pt），半透明（alpha=0.6），不遮挡游戏画面。F3 切换调试模式时图例同步显示/隐藏。

#### 10.5.3 显示内容明细

| 元素 | 颜色 | 来源 | 说明 |
|------|------|------|------|
| PlacementGrid 占用格 | 绿色半透明 | `PlacementGrid._cells` | 已被建筑占用的格子 |
| BuildMask 不可放建筑格 | 红色半透明 | `PlacementGrid.blockage_mask` | 地形限制不可放建筑 |
| WalkBarrier 地图障碍 | 蓝色半透明 | `MapInstance.WalkBarrier` 下 Area2D | 悬崖/高楼边缘，火柴人不可穿越 |
| PassageBarrier 建筑障碍 | 紫色半透明 | 各建筑 `PassageBarrier` Area2D | 建筑本体不可通行区域 |
| ground_y 地面线 | 黄色线 | `MapInstance.ground_y` | 火柴人可走区域顶部 |
| ground_bottom 地面底线 | 青色线 | `MapInstance.ground_bottom` | 火柴人可走区域底部 |
| 建筑边界框 | 白色边框 | `BuildingHost` 子节点 | 每个建筑的 Footprint 矩形 |
| Chunk 触发器范围 | 紫色矩形边框 | `MapInstance.ChunkTriggers` | 流式加载触发区域 |
| 火柴人状态文字 | 白色文字 | `EntityHost` 下 StickmanEntity | 速度/动画/朝向/坐标 |
| FPS / 实体数 | 白色文字 | 引擎 | 屏幕左下角 |

#### 10.5.4 交互

| 按键 | 行为 |
|------|------|
| F3 | 切换调试覆盖层显示/隐藏（记忆到 `settings.cfg`） |
| F1（可选） | 仅切换图例显示 |

#### 10.5.5 实现要点

- `DebugOverlay` 是 `CanvasLayer`，`z_index` 最高（高于游戏画面和 UI）
- 用 `_draw()` 在 Control 上绘制（不是 Node2D，避免相机移动影响坐标）
- 调试数据通过 `DebugApi` 收集，各模块注册自己的调试信息提供者
- 启动时调用 `debug_legend.show_legend()`，图例常驻不淡出
- 性能：调试绘制器按需启用，关闭调试模式时所有 `_draw` 跳过
- 世界坐标到屏幕坐标的转换：`world_to_screen(pos) = (pos - camera.global_position) * effective_zoom + viewport_size * 0.5`

#### 10.5.6 公共 API

```gdscript
# DebugApi（autoload 单例）
register_drawer(name: String, drawer: Callable)   # 各模块注册绘制器
unregister_drawer(name: String)
toggle_visibility()                                # F3 切换
is_visible() -> bool
show_legend()                                      # 显示图例（常驻）
hide_legend()
```

#### 10.5.7 各模块注册绘制器

| 模块 | 注册的绘制器 | 触发时机 |
|------|------------|---------|
| `world` | grid_drawer / barrier_drawer / ground_line_drawer / chunk_trigger_drawer | 地图加载时 |
| `buildings` | building_drawer | 建筑增减时 |
| `units` | entity_state_drawer | 单位生成时 |

---

## 十一、环境系统

### 11.1 模块结构

```
modules/environment/
├── api.gd
├── sky_system.gd              # 天空：极光、星星、太阳/月亮
├── weather_system.gd          # 天气：雨/雪/沙尘暴
├── ground_system.gd           # 地面：纹理重复、震动摇晃
├── lighting_system.gd         # 光照：随时间变化
└── biome_system.gd            # 生物群落：地理位置决定基调
```

### 11.2 跨场景保持

环境系统是 `GameRoot` 的常驻子节点。地图切换时只是切换 `WorldChunkHost` 的子节点，**环境继续工作**。

环境参数（天气、时间、地理位置）挂在 `WorldState`（Autoload）上，不挂场景。

### 11.3 关键技术点

| 效果 | 实现方式 |
|------|---------|
| 极光 | `Shader` + 噪波纹理 + 时间动画 |
| 星星 | `GPUParticles2D` 或 Shader 顶点 |
| 下雨 | `GPUParticles2D` + 风向参数 |
| 沙尘暴 | `GPUParticles2D` + 屏幕色染 + 能见度衰减 |
| 地面震动 | `GroundShakeRig` 父节点 `position` 抖动（参考相机抖动） |
| 光照变化 | `CanvasModulate.color` 按 `time_of_day` 插值 |
| 生物群落基调 | `biome_system` 提供参数，`sky`/`weather`/`ground` 各自读取 |

---

## 十二、模块依赖图（防循环依赖）

```
                    ┌──────────────┐
                    │  GameRoot    │ (常驻容器)
                    └──────┬───────┘
                           │
        ┌──────────┬───────┼────────┬──────────┐
        ▼          ▼       ▼        ▼          ▼
   Environment  Camera  SceneLoader  InputDispatcher  UIRoot
                           │
                           ▼
                    ┌──────────────┐
                    │  MapInstance │ (村落/战场/室内)
                    │  ├─PlacementGrid
                    │  ├─Buildings
                    │  └─StickmanEntities
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   construction        combat             organization
        │                  │                  │
        └──────────┬───────┴──────────────────┘
                   ▼
                units (StickmanEntity + AI)
                   │
                   ▼
               StickmanRig (已有渲染)
```

**单向依赖规则**：
- 上层依赖下层，禁止反向
- `units` 不直接调 `combat`，由 `combat` 通过 `units/api.gd` 拉数据+下令
- `combat` 不直接调 `construction`，通过 EventBus 通知（如建筑被破坏）
- 跨模块引用 → 走 `api.gd` 或 EventBus

---

## 十三、目录结构提案

```
stick-world/
├── core/                              # ✅ 不动，已有
├── modules/
│   ├── units/                         # 扩展：加 ai/ + stickman_entity
│   │   ├── scripts/
│   │   │   ├── stickman_rig.gd        # ✅ 已有
│   │   │   ├── stickman_entity.gd     # 🆕
│   │   │   └── ai/                    # 🆕
│   │   ├── scenes/
│   │   │   └── stickman_entity.tscn   # 🆕
│   │   └── api.gd
│   ├── combat/                        # 🆕 实现
│   ├── construction/                  # 扩展实现
│   ├── organization/                  # 扩展实现
│   ├── world_map/                     # ✅ 已有，战略图模块（鸟瞰多边形领土，玩家不在其中）
│   ├── environment/                   # 🆕 新建
│   ├── world/                         # 🆕 新建：Map/Chunk/PlacementGrid
│   ├── buildings/                     # 🆕 新建：建筑运行时
│   ├── player_control/                # 🆕 新建：选择/编队/指令/附身
│   └── ui/                            # 🆕 新建：UI 分层
├── scenes/                            # 🆕 新建
│   ├── game_root.tscn
│   └── chunk_templates/
├── assets/buildings/                  # 🆕 新建：建筑素材资产
├── config/maps/                       # 🆕 新建：地图定义
└── ...
```

---

## 十四、EventBus 新增信号

在 `core/autoload/event_bus.gd` 已有信号基础上，新增以下信号：

### 14.1 场景/地图事件

| 信号 | 参数 | 发射方 | 接收方 | 触发条件 |
|------|------|--------|--------|---------|
| `map_loaded` | map_id, map_type | SceneLoader | UI、Environment | 地图加载完成 |
| `map_unloaded` | map_id | SceneLoader | UI | 地图卸载 |
| `chunk_loaded` | chunk_idx | SceneLoader | MapInstance | Chunk 加载完成 |
| `chunk_unloaded` | chunk_idx | SceneLoader | MapInstance | Chunk 卸载 |
| `travel_started` | from_id, to_id, mode | SceneLoader | UI、WorldClock | 开始旅行 |
| `travel_completed` | to_id | SceneLoader | UI | 旅行完成 |

### 14.2 输入模式事件

| 信号 | 参数 | 发射方 | 接收方 | 触发条件 |
|------|------|--------|--------|---------|
| `input_mode_changed` | old_mode, new_mode | InputDispatcher | UI、Units | 模式切换 |
| `possession_started` | unit_id | PossessionInterface | UI、Units | 玩家附身 |
| `possession_ended` | unit_id | PossessionInterface | UI、Units | 退出附身 |

### 14.3 建筑事件（扩展）

| 信号 | 参数 | 发射方 | 接收方 | 触发条件 |
|------|------|--------|--------|---------|
| `building_placement_started` | building_def_id | PlacementSystem | UI | 进入选址模式 |
| `building_placement_confirmed` | building_def_id, cell_pos | PlacementSystem | Construction | 选址确认 |
| `building_placement_cancelled` | — | PlacementSystem | UI | 取消选址 |
| `interior_entered` | building_id | Building | InputDispatcher、UI | 进入室内交互区 |
| `interior_exited` | building_id | Building | InputDispatcher、UI | 离开室内交互区 |

### 14.4 战斗编队事件

| 信号 | 参数 | 发射方 | 接收方 | 触发条件 |
|------|------|--------|--------|---------|
| `selection_changed` | unit_ids: Array | SelectionSystem | UI | 框选/选择变化 |
| `squad_created` | squad_id, unit_ids | FormationSystem | UI、Organization | 编队创建 |
| `order_issued` | order_type, target_squad_id, issuer_unit_id | TacticalOrders | UI、Units | 下达号令 |
| `commander_assigned` | squad_id, unit_id | Organization | UI | 任命指挥官 |

### 14.5 环境事件

| 信号 | 参数 | 发射方 | 接收方 | 触发条件 |
|------|------|--------|--------|---------|
| `weather_changed` | old_weather, new_weather | WeatherSystem | UI、Audio | 天气切换 |
| `time_of_day_changed` | old_t, new_t | WorldClock | Environment | 时间推进 |
| `ground_shake` | intensity, duration | 任意 | Environment、Camera | 地面震动 |

---

## 十五、P0 实施顺序

P0 = 验证"层级管理→好玩"的关键假设。按以下顺序施工，每步可独立验证：

### 阶段 0.1 - GameRoot 骨架 ✅
- [x] `game_root.tscn` 主场景搭建
- [x] `WorldClock`、`CameraRig`、`SceneLoader`、`InputDispatcher` 基础实现
- [x] `EnvironmentSystem` 简版（仅时间->光照变化，无天气）
- [x] `UIRoot` 三层容器骨架

### 阶段 0.2 - 单张村落地图 ✅
- [x] `VillageMap` + 单个 Chunk（先不做流式，硬编码一张完整地图）
- [x] `PlacementGrid` 实现
- [x] 地面重复纹理渲染（`GroundPolygon`，顶点从 `ground_y` 开始，草地噪波材质）
- [x] `ground_y` / `ground_ratio` / `map_left` / `map_right` 字段实现
- [x] `CameraRig` 重构：水平卷轴 + 1/4 区域跟随 + 垂直显示范围限定（详见 §2.4）
- [x] 玩家 `StickmanEntity` 控制移动（WASD），脚部锁定 `ground_y`，X 限界
- [x] `CameraRig.set_ground_y` / `set_map_bounds` / `set_follow_target` 接入
- [x] DESIGN_HEIGHT=1080 三层缩放系统（base_zoom / user_zoom / effective_zoom）
- [x] 相机拖动延迟弹回（5 秒冷却）+ 居中模式（松手即弹回，禁边缘滚动）
- [x] 居中模式按钮（GlobalHUD）
- [x] 🆕 `DebugOverlay` 调试覆盖层（F3 切换 + 启动图例，详见 §10.5）
- [x] 🆕 建筑三层架构改造（TerrainBuildings / InitialBuildingsList / 存档，详见 §4.5）
- [x] 🆕 WalkBarrier 透明障碍 + PassageBarrier 建筑障碍（详见 §7.1.2）
- [x] 🆕 BuildMask 不可放建筑区域（详见 §4.2）
- [x] 🆕 ForegroundLayer 前景层（详见 §3.4）

### 阶段 0.3 - 火柴人行为 AI 基础
- [x] `StickmanEntity` 节点结构搭建（AIController 子节点）
- [x] `AIController` + `BehaviorStateMachine`
- [x] `behavior_idle`、`behavior_move` 实现（随机游走）
- [x] `behavior_work` 实现（与阶段 0.4 建设系统耦合，一起做）
- [x] 测试：村民在村里自主走动（idle ↔ move 循环）

### 阶段 0.4 — 定居点建设
- [x] `Building` 节点结构
- [x] `placement_system`（选址+ghost预览）✅ P0 仅实现选址 API，ghost 预览留到阶段 0.6
- [x] `construction_project` + `work_crew_assigner`
- [x] 测试：选址->派工->建造->完成循环（test_stage_04 10/10 通过）

### 阶段 0.5 - 小队级战斗 ✅
- [x] `StickmanEntity` 添加 `Hitbox`、`HealthComponent`、`WeaponMount`
- [x] `behavior_attack`、`behavior_seek_cover`、`behavior_retreat`
- [x] `battle_instance` 挂载到 `VillageMap.BattleAnchor`
- [x] `BattleDirector` + `battle_ai_director`（情绪标签）
- [x] 测试：5v5 战斗，能看见掩体利用、压制、溃逃（test_stage_05 8/8 通过，观察到 attack/seek_cover/retreat 行为切换）

### 阶段 0.6 — 编队与指挥 + 小地图
- [ ] `selection_system`（框选）
- [ ] `formation_system`（编队）
- [ ] `tactical_orders` + `command_chain`
- [ ] `BattlePanel` UI
- [ ] `Minimap` 实现（详见 §10.4）：缩略图 + 视野框 + 角色点 + 建筑图标 + 点击跳转
- [ ] `CameraRig` 手动控制完善：拖动 + 边缘滚动 + 缩放（1.0~2.0）+ 居中模式按钮
- [ ] 测试：框选10人→编队→任命排长→下令前进；小地图点击跳转观察战场

### 阶段 0.7 — 玩家附身
- [ ] `PossessionInterface`
- [ ] `PossessPanel` UI
- [ ] 测试：附身排长亲自操控战斗

### 阶段 0.8 — 多场景衔接（可延后）
- [ ] Chunk 流式加载
- [ ] `RoadMap` 实现
- [ ] 战略图（world_map）从快速旅行到进入聚落
- [ ] 测试：村落A→道路→村落B 完整链路

### 阶段 0.9 — 室内（可延后）
- [ ] 建筑透明化实现
- [ ] `IndoorMap` 流程
- [ ] 测试：进入铁匠铺看打铁

---

## 十六、待补充清单

### 设计层（需创始人决策）
- [ ] 兵种类型与武器属性（影响 Hitbox、伤害计算）
- [ ] 各村落的 `VillageDefinition.tres` 内容
- [ ] 大建筑（宫殿）内部场景设计
- [ ] 道路上随机事件机制
- [ ] 战场地图的两端建筑模式（预设阵地 vs 现场部署）

### 数值层（待原型后填）
- [ ] 工程量基础值（每种建筑 base_work）
- [ ] 工人效率（受组织水平影响的具体公式）
- [ ] 指挥链延迟系数（base_delay、tier_diff 倍率）
- [ ] 战场导演情绪触发概率基线
- [ ] 士气阈值（溃逃/罢工条件）

### 技术债（P0 阶段可接受）
- [ ] LOD 系统（远距离单位简化 AI）— P0 全部完整 AI
- [ ] 多战场并行性能优化 — P0 只单战场
- [ ] 天气 Shader 精细化 — P0 仅基础光照变化

---

*本文档为 P0 阶段的工程架构基线。后续阶段（P1 物流/扩张、P2 法律/成就）将基于此架构扩展。*
