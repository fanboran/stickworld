# 待办事项

> **⏳ 当前进行中**：兵种 AI 复刻批次，**计划与执行口径见 [`AI复刻执行计划.md`](AI复刻执行计划.md)**。2026-09-03 P5/P6/P7 完成（P7 = 祭司 Meric 兵种+治疗），**覆盖率 53%→55%（含近似 57%→59%）**，详见计划 §三。同日三条并行开发线收线合并回 main：配置线（BalanceConfig 自装载）、战略图线（聚落 tooltip/粒度指示/视图互斥）、UI 线（设置项真实生效+通知堆叠 feed），合并后全量测试绿。下一项 P8（批次 7a 巨人）；观察场缺陷余 9h/9j/9k/9s，见计划 §二 P1。

### AI 复刻覆盖率审计 → 新批次（2026-09-01 审计立项，方案详表见 [`AI复刻执行计划.md §三`](AI复刻执行计划.md)，按 P1~P13 顺序执行）

- [x] **11b Formation 列队形**（✅ 2026-09-01）：槽位制 row/col 阵列（`UNITS_PER_COLUMN/ROW_GAP`）+ `FormationPositionIsStable` 落点稳定 + `UpdateCatchingUpToFormation` 追赶收盾疾跑 + 贪心换位/掉员补位；覆盖率 44%→51%
- [x] **P5 批次 2 数值校准**（✅ 2026-09-02）：SWL wiki Normal 真值落 `stickmen.tres`（6 兵种 HP/伤害/冷却/价格）+ `WEAPON_DEF_ID` 消费链 + 9d per-weapon 射速与冷却≥命中帧检查（顺手修"冷却≤命中帧重挥自打断"边界 bug）+ 11d `MissingArrowsTolerance`/`NextGaussian` 三参版；覆盖率 51%→53%
- [x] **P6 批次 7c+11c TeamAi 逐函数直译**（✅ 2026-09-03）：21 函数直译（17✅ + 4 桩◐）+ 三态姿态机 + 号令映射器 + 手动号令保护期 + 9i+ 五项增强（开关默认关零回归）+ 3 套件 30 用例；覆盖率 53%→62%
- [x] **P7 批次 7b 祭司 Meric 兵种+治疗**（✅ 2026-09-03）：MericAi 3 函数直译（Update/CanAttack/UpdateTarget 无桩）+ WeaponMount 治疗 3 方法 + StatusEffects HEAL 类型 + _apply_hot 正向结算 + find_weakest_ally + 后排站位 + kite 撤而不打 + 施法停移 + L2 降级通用动画（`[meric].txt` 缺失）+ 12 用例套件 + battle_sim 2 场景；覆盖率 53%→55%（MericAi 0%→100%）；**待 6.2 观察场人工验收**
- [x] **P9 批次 10 物品栏系统**（✅ 2026-09-06，分支 `agent/inventory-equipment`）：modules/inventory 新模块——ItemDef/ItemStack/ItemDB 数据层（内置 GDScript 注册表，16 种物品）+ PlayerInventory（24 格背包 + 5 装备槽：头/胸/腿/主手/副手，事务性装备/回滚）+ InventoryService（装备→附身实体桥接，附身记录恢复，背包跟玩家走）+ E 键模态背包界面（点击/光标堆/右键智能穿脱）+ 底部 Hotbar 三组布局（主副手|左键右键 / 物品组 1-4 / 动作组 F交互 H脱困 E背包，格子下标注按键）；WeaponType 加 NONE（徒手不可攻击）；右键按住举盾；盾双轨制（兵种默认盾 shield_enabled / 装备盾 equipped_shield）；护甲三件减伤（DamagePipeline 单入口，封顶 0.6）+ 移速乘子；键位变更：E=背包（原交互）、F=交互（原空挥）、G=空挥、1-4=Hotbar 物品；删 1-5 直切与 T/B 调试键、WeaponPanel 退役。15 用例单元套件 + 全量 33 套件零回归。设计文档：`docs/设计/系统/背包与装备系统.md`
  - [ ] **武器 stats 数据化应用**：ItemDef.stats 的 attack_mult/speed_mult/armor_penetration 只入数据未应用（战斗数值由 BalanceConfig 校准管，避免打破 battle_sim），待 P5 数据化批次接线
  - [ ] **拾取/掉落**：战利品实体 + 地面物品 + 拾取交互
  - [ ] **商店购买**：原版 InGameShop 形态，经济系统接线时做
  - [ ] **背包存档接线**：to_dict/from_dict 已就绪，SaveManager 玩家数据段挂链验证
  - [ ] **背包拖拽交互 + 整理按钮**：UI 打磨批次
- [ ] **11e 玩法依赖簇挂总账缓**：ZombieAi（Pounce/Kai 召唤）、StatueAi/BarricadeAi、CastleArcher 簇、MinerAi 路障/互殴——依赖对应玩法实装
- [ ] **9 余项**（见计划 §二 P1）：9h 卡死复验 / 9j 脱战回血（MOBA 延迟血条）/ 9k 群体避让+空挥保真（含 9s）
- [ ] **P7 祭司翻译缺口与已知限制**（2026-09-03 登记）：
  - [x] **battle_sim 治疗动画报错已修**（✅ 2026-09-05）：根因 = heal_meric_1/2.tres 从未烘焙（bake_anims 清单缺条目）且 AnimationTree 状态机无对应节点，每次施法 travel 报 "No such node"；已补两个手写施法动画（高举法杖 1.2s / 胸前咏唱 1.3s，heal_cooldown=3.0 硬约束内）+ 状态机接线（照攻击动画同款 AT_START 切入/回 idle），battle_sim 零报错。⚠️ 重烘焙必须重跑 inject_anim_events（bake 整体重写 tres 会冲掉 Hit 事件元数据，bake_anims.gd 头部有警示）
  - [ ] **MericHeal / MericDeath 特效资源名**（P10 消费）：本批治疗表现 = 动画 + 血条回升，特效资源名待 P10 特效扩容时补
  - [ ] **meric-staff 贴图裁剪工具链**：`extract_weapons.gd` 未覆盖 meric 图集 → 首版复用 Magicstaff 贴图占位，待裁剪工具链扩展后换真贴图
  - [ ] **祭司 wiki 面板值补真值**：P5 同源渠道 namu wiki Normal 面板值，一次性校准 `heal_amount`/`heal_cooldown`/`heal_range`/HP/价格/站位参数（当前全为语义推断初值，标注待实测校准）
  - **已知限制（非缺陷）**：bone3~6 装饰骨保持默认姿势（L2 降级无 Spine 源数据，维持不映射）；治疗冷却与 HOT 现实秒混源（暂停期继续流动，与既有 DOT 同源特性）

### SWL 全模块对账 → 补充立项（2026-09-01，全景统计见 [`AI复刻执行计划.md §四`](AI复刻执行计划.md)）

> SWL 游戏代码 363 类型 / 35 命名空间，游戏性模块 14 个。除 AI（§三）外的对账结论与立项：

- [ ] **12 特效扩容**（新批次，~1 轮）：fx 模块对照原版 Effects 16 类扩容——**DirectionalBlood 方向血溅 / ExplosionScorch 爆炸焦痕（兼召唤/爆炸地面标记）/ GroundSlam 震地 / Rain+Cloud 天气**；战斗观感大头
- [ ] **Spell 基类先行**（并入计划 4）：原版 15 法术 = Spell 基类（冷却/耗金/施放动画/效果统一结构）+ 14 具体法术；ArrowVolley/HealSpell 挂基类实现，其余按需
- [ ] **InGameShop/HudInventory 并入批次 10 物品栏**：原版战场商店+物品条 UI 形态参考
- [ ] **模式变体低成本低挂**（Alpha 后）：EndlessDeads 无尽尸潮 / SuperDeathMatch 超级乱斗 → 观察场变体
- [ ] **缺失兵种实体决策**：CastleArchers（城堡弓手）/ Statue+MainStatue（雕像=SWL 胜利目标，可抽象为"战争目标建筑"）/ King / GiantBoss / ConversionChannel——依赖大世界玩法定位，需创始人拍板
- [ ] **不复刻**：平台服务（IAP/广告/云同步/成就/AB 测试 ≈40 类型）、Levels 关卡制（战略图路线替代）、UserInterface 159 类（UX 流程按需参考）

> **项目级待办追踪**。按优先级排序，完成即删除。
>
> **唯一待办源声明**：本文档是项目级待办的**唯一入口**——需要创始人决策的事项、
> 工程流程决策、跨模块协调都在这里记；各专项总账（审计、缺口对账等）追踪各自领域的
> 明细工作项，经下方职责边界表挂载到这里，不另设平级待办文档。

---

## 职责边界（与其他文档解耦）

本文档只记录**项目级**待办：需要创始人决策的设计待定项、工程流程决策、跨模块协调。
**不重复**以下文档已追踪的内容，仅指向：

| 职责 | 文档位置 | 内容 |
|------|---------|------|
| 代码级技术债（bug/隐患） | [`技术/架构/场景与战斗架构.md`](../技术/架构/场景与战斗架构.md) §六 | P0-1~P0-11（PossessionInterface 装配、Chunk 空壳、EnterTrigger 缺失等） |
| **模块依赖/实现状态真相** | [`技术/架构/模块依赖关系.md`](../技术/架构/模块依赖关系.md) | GDD↔代码对照、断链点索引、战斗链路特写 |
| 架构级待补充（设计/数值/实现） | 同上 §五 | 兵种/村落/建筑/数值/存档等待补全项 |
| P0 阶段实施顺序与完成状态 | 同上 §四 | 阶段 0.1~0.9 的子任务清单 |
| 战略图工作项 | [`技术/架构/世界地图数据流.md`](../技术/架构/世界地图数据流.md) §八 | 生成端/导入端/消费端待实现清单 |
| **复刻缺口总账**（专项总账挂载） | [`审计/总账/翻译缺口总账.md`](../审计/总账/翻译缺口总账.md) | 动画/骨骼/事件/皮肤四层对账 + 机制/操作层缺口，新缺口先补总账再立项 |
| 阶段里程碑规划 | [`项目/路线图.md`](路线图.md) | 阶段 0~4 的目标与验收标准 |
| 开发进度追踪 | [`项目/项目进度.md`](项目进度.md) | 当前完成到哪 |

---

## 当前状态

- **2026-09-04 待办全量对账**：对全部未勾选条目逐条 grep/文件核对——勾销 15 条（T1-T13 批次修复回写：存档 SQL 参数化与 schema 版本化、combat⇄units 契约化、texture_gen 回归测试、战略图 UI 三件套、存档面板/读档兜底等；实测过时：HealthComponent 出生即死、数据存储方案选择题、科技状态字段漂移；世界生成 L1 链与 0.9a 产物核实已落地）；Excel 导出管线条目刷新为现状（9 文件 15 sheet 已映射、导出器已硬化）；其余条目按"仍有效/待创始人决策"保持 `[ ]`。

- **全大陆 L1 地块蒙版已实现**（程序化世界生成后续步骤）：`tools/worldgen/l1/l1_world_split.py` 在 L3 层把整块大陆划分为 389 个 L1 地块（城市点 = 均匀网格 + 细微扰动，每岛至少 1 点；岛×L2 分组细胞质膨胀，细胞不跨海/不跨湾/不跨 L2、半岛不共地块，边缘城市向陆内膨胀不吃亏；面积下限合并 + 极小岛豁免；同 L2 地区相似色蒙版），M 键 L3 战略图按 V 键可叠加查看（`l1_overlay.gd` + `test_l1_overlay` 集成测试绿），详见 [`08-程序化世界生成.md §二十一 L3-L`](../设计/系统/08-程序化世界生成.md)。
- **城市细分层已实现**：`tools/worldgen/l1/city_split.py` 在 L1 地块之下细分出 3203 个城市（城市点 jittered grid + 每 L1≥1 点；按 L1 分组细胞质膨胀，城市不跨 L1/海/湾；面积下限合并；同 L1 相似色），产物 `output/l1/city_*`，详见 [`08-程序化世界生成.md §二十一 L3-M`](../设计/系统/08-程序化世界生成.md)。
- **出生 L1 已定案**：游戏内 L3 地图 `#28bd72` 色（L2 地区 13）内最东侧 L1 地块 = **老 L1 label 69**（region_013 的 3 号老 L1 地块）；Tab 战略图数据源 = 出生 L1 的城市划分（8 城市 / MST 道路，城市暂无可玩地图）。下一步：为出生 L1 城市生成可玩地图（SettlementGenerator），打通"双击城市进入"。详见 [`08-程序化世界生成.md §L3-O`](../设计/系统/08-程序化世界生成.md)。

P0 原型阶段：阶段 0.1~0.8 已全部完成，P0 收口执行计划已执行完毕（数据清理/EnterTrigger/资源接入/数据驱动）。
- **队伍类型编制系统已实现**（原型循环"编队"环节阻塞解除）：编制预设（战斗班/建造队/劳工队/运输队）+ 职责范围可调整 + 编制管理窗口（FormationPanel），详见 [`CHANGELOG.md`](../CHANGELOG.md)
- **带队出征已实现**（原型循环"战斗"环节阻塞解除）：跨图携带编队 + 战场重建 + 遭遇战队伍参战（`test_squad_travel` 4 用例绿），详见 [`CHANGELOG.md`](../CHANGELOG.md)
- P0 测试链稳定化（S1-4）+ 战斗链路确认（S2-1）+ 测试体系重设计（T0，数字标号全清除）+ 文档校准（T1）完成（`bash tests/run_all.sh` 一键运行）
- S1-4 修复：02/03/07 挂死（脚本解析错误 + TestHelpers class_name 未注册）、存档污染（`save_0.db` 自动读档）、resource_node 信号参数、03 AI 移动抖动、06 HBox/Label 类型错
- S2-1：`TacticalOrders→CommandChain→AIController.set_order` 链路确认已装配
- 技术债清理：P0-3 废弃 WorldMapController 删除、P0-4 IK 日志噪音、P0-1 Chunk stub 防静默告警
- T1 文档校准：架构 §四/§六（P0-1~P0-17 统一编号）、自动加载依赖、路线图、GDD差异分析、归档声明 全部对齐实测
- **P0 范围**：0.1~0.8 + 新 0.9 基础战略图（L1 单层）——8 城邦 + 随机地块边界 + 道路 + 卫星图底图 + 边界线着色（P 社索引图机制），点击聚落进出场景图。聚落模型见 [08-程序化世界生成.md §0.19](../设计/系统/08-程序化世界生成.md)（1 个 L1 地块 = 1 个聚落或空聚落；1 聚落 = 1 张主场景图）。室内系统归阶段 1/2
- **待创始人亲自跑一遍原型循环**（建造→编队→任命→下令→战斗→附身→跨图→战略图）确认"层级管理->好玩"假设成立后，从路线图阶段 1 启动 Alpha
- 详见 [`场景与战斗架构.md`](../技术/架构/场景与战斗架构.md) §四、§六

---

## 高优先级（需尽快处理）

### 世界视觉重绘（2026-09-06 统筹立项，用户口述规划）
> 总纲：场景图（卷轴地图）的自然资源与地面全部从贴图/色块迁移到**程序化手绘笔触**渲染，
> 与已定稿的毛线团树（TreePainting/TreeYarnBall，分支 `agent/tree-yarn-ball`）同一语言。
> 顺序沿用第一会话定调：石头/铁矿（同毛线团架构）→ 地面重绘 → 背景 biome → 极光 → 云 → 草坪。

- [ ] **树系统收尾**（毛线团架构已实现在 `agent/tree-yarn-ball` 分支待合入）：干/枝算法直绘 + 冠=连续波浪轮廓毛线团 + 每秒翻动；含林区梯度（距硬化区一屏净空→一屏渐密→2×密度森林）
- [ ] **地面笔触重绘**：草地与硬化地面（土路）分别用**不同笔触风格**重绘——草地有机弧线笔（跟随毛线团语言）、硬化地面直线/硬边笔触（人造感）；需先定地面着色入口（village_map._update_ground_polygon 一带）
- [ ] **砍树反馈两件套**：①砍到一半（储量约 50%）树缩为**树桩**（保留 TreePainting 干的下半段+顶部断口弧线）；②储量砍空时播放**树倒塌动画**（整体绕根旋转倒下+叶团散落笔触，倒地后消失给资源）
- [ ] **石头/铁矿同架构改造**：沿用 TreeYarnBall 模式（连续波浪轮廓+内部笔触堆叠+每秒翻动），笔触语言换矿物系（石=灰蓝粗弧、铁=棕底+亮橙矿脉线）；现有 Python 烘焙贴图退役
- [ ] **背景序列**（第一会话顺序）：背景 biome 变体 → 极光 → 大世界云（SketchCloud 已定稿动漫体积系，接入村庄背景）→ 草坪/草丛笔触（2026-09-06 对账刷新：**极光已随泰拉瑞亚夜空复刻落地**——AuroraSky.cs 直译，见下方完成记录；背景 biome 变体/大世界云/草坪仍有效）
- [x] **泰拉瑞亚夜空复刻**（✅ 2026-09-06，分支 `agent/night-sky-terraria`）：夜晚天空整体对齐 Terraria（数值依据见 [`天空与水体逆向笔记.md`](../技术/参考逆向/天空与水体逆向笔记.md) §七~§九）。①**双轨昼夜色彩**（`modules/environment/api.gd`）：天空底色 SKY_BG_KEYFRAMES 每帧写 `RenderingServer.set_default_clear_color`（Terraria Main.SetBackColor 的 bgColor 直译：夜 (35,35,35)→(5,5,5)+月相 floor→黎明前 (25,35,35)→白→日落粉峰 (235,120,170)→入夜灰黑，时间轴对齐本项目太阳 5..21 时）与环境光照 LIGHT_KEYFRAMES→CanvasModulate（夜晚压暗至 Terraria 剪影量级，午夜≈(14,14,19)）分离；②**发光体除法补偿** `EnvironmentAPI.unmodulate`：星/月/极光/流星/萤火虫绘制色÷当前 CanvasModulate 补偿后屏显≈目标色（泰拉瑞亚星星 alpha 独立于天色手算的同构）；③星星对齐 Star.cs（`modules/world/scripts/map/sky_stars.gd`：twinkle 反弹式往返 [0.6,1.0] 同时驱动大小+透明度、1/40 大星尺寸×2 闪烁减半带旋转四芒、天空亮度门控——粉天/白天天空星灭）；④极光对齐 AuroraSky.cs（同在 sky_stars.gd：HSL 色相随游戏时间轮转 AURORA_HUE_SPEED 0.004/s（绿/青/紫三带 hue_off 0.40/0.46/0.72）+ 带内 cos 调制 + ADD 混合子节点，视觉验收调参后定稿）；⑤月亮普通夜月面纯白 + 月相阴影圆取当前天空底色（新月时与天空无缝）；⑥CampLight 默认关闭（`environment_system.gd` `camp_light_enabled=false`——泰拉瑞亚夜晚无随身光源，节点保留供调试面板读取，P2 逐光源布点时可再启用）；⑦后处理顶部天光夜晚压到 0.02 残辉（`post_process_layer.gd`）

### 河流/水体系统重做（2026-09-05 封印，用户指示）
- [ ] **水体全线封印已落地**：`MapBase.enable_water` 默认 false（battlefield 显式关闭），Pond（K2C 镜像倒影+滚动白沫）在所有地图不再挂载
- [ ] **重做方向（用户已定调）**：K2C 布局需要**地面抬高**让水带落在画面底部（battlefield ground_y=432 时水带会横在屏幕中央），且必须适配缩放（zoom 0.5~2 下水线锚定不漂移）；地面构图定稿后按 docs/技术/参考逆向/天空与水体逆向笔记.md §六 重做
- [ ] 诊断脚本 `tests/dev/diag_pond.gd` 保留可复用

### 光影系统（2026-09-05 用户需求登记）
- [ ] **方向**：类似**八方旅人（Octopath Traveler）**的 HD-2D 光影质感 + **泰拉瑞亚部分 mod** 的光照风格（参考：tModLoader 的 Lighting 权重光照/阴影系统）
- [ ] 现有基础：单 pass 六层后处理（暖色分级/太阳炫光/渐晕/色差/颗粒）+ CanvasModulate 昼夜染色；炫光已在暂停（菜单）时淡出（post_process_layer.gd `_flare_fade`）
- [ ] 候选路径：Light2D/PointLight2D 逐光源（营地光 CampLight 雏形已有）→ 2D 光照 buffer（CanvasItemMaterial normal_map 或 CompositorEffect）→ 昼夜色 LUT

### 战斗性能后续（2026-09-05 一轮优化后，96v96 模拟已满速）
- [ ] **渲染侧是下一瓶颈**：模拟 30/30 满速，渲染在 Intel 核显+调试版二进制下 2~16 FPS（随机器并发负载波动）；候选：MSAA 2D 关闭试测（Line2D 密集场景核显开销大）、单位渲染架构升级（笔触贴图 Sprite 替代每单位 30 条 Line2D 矢量肢体 / MultiMesh）、发布版基准复测（release 模板 GDScript 快 3~10 倍）
- [x] **物理 tick 之谜已解**（✅ 2026-09-05）：根因 = 本机 Godot 4.7.2 的 project.godot 解析器**静默吞掉紧跟在 `#` 中文注释行之后的键行**（最小启动 + ConfigFile 复现确认：注释前键正常、注释后键 MISSING、更后面的节不受影响）；键挪到注释之前即生效（实测 `Engine.physics_ticks_per_second=30`），TimeManager 代码兜底已删。**教训：project.godot 里注释不要插在键与键之间**
- [ ] 基准工具 `tests/dev/battle_perf.gd`（渲染 FPS + headless tick 速率 + 瓶颈二分开关 --anim-off/--rig-hidden/--bodies-ghost/--freeze-entities）；同机对比：改动前 1.4 FPS → 优化后 16.4 FPS（安静负载窗口）
- [ ] **天空小项**（村庄背景视觉验收 7/10 遗留）：云在可视区占比偏疏（可调云池密度）；树线层顶部树尖齐平（Terraria 贴图固有，重提取裁切可解）

### 大乱斗观察场（2026-08-31 专项审计 → 8 轮修复，全部完成 ✅）

审计与 8 轮修复全记录已归档：[大乱斗观察场审计-2026-08-31.md](归档/大乱斗观察场审计-2026-08-31.md)。
完成项摘要：死亡收口/hitstop 节流/分离限幅/血条变体池（Death×10+Hit×12 直译）/相机 0.55/
抛物线箭矢（射程 1400）/兵种档案化（kite/瞄准/召唤/移速）/盾姿态/状态效果底座/仿真闭环
battle_sim/队列间距 90-110/y 排序遮挡。遗留后续全部并入 [AI复刻执行计划.md](AI复刻执行计划.md)
（盾姿态分层/跟队/掷矛/数值校准/城堡弓手等）。

### 代码阻塞项
- （无）P0 阻塞项已全部清除

### 武器验收链发现（2026-08-30 武器持握重直译，详见各 weapon_*.tscn 头注释）
- [ ] **实体在独立场景下全身渲染横躺 ~90°**：stickman_entity 的骨骼 `global_transform` 打印为直立（播 idle/不播动画/关 IK 三配置均同），渲染却整体旋转（伴随 Godot "Using transform rotation for bone angle" 刷屏，疑似 Bone2D rest 与运行时 transform 分歧）；武器画面验收已绕开（`tools/baking/render_weapon_check.gd` 用裸骨架 `build_from_scratch` 直出）。待查根因，修复后验收截图可切回实体管线
- [x] **HealthComponent.hp 默认 0 + BalanceConfig 缺 `units.stickmen.stm_plain_001.base_hp`**（✅ 2026-09-04 实测过时：`health_component.gd` `_ready` 即置 `hp = max_hp`（默认 100，并非 0 出生）；`config/units/stickmen.tres` 已含 `stm_plain_001.base_hp=100` 等 def 行（P5 批次另补 6 兵种 SWL 校准值），BalanceConfig autoload 自装载——"缺 base_hp / 出生即尸体"两个前提均不成立）

### 架构审计修复（2026-08，全量只读审计后）

**本轮已修复（详见 CHANGELOG）**：GlobalHUD 速度/时间标签路径、AIController 职责门控（WORK_HAUL）、OrganizationManager insert_tier 层级不变量与 disband 泄漏、BattleInstance/Director 结束清理与 battle 信号接线、SaveManager 事务与读档兜底、建造扣减回滚、units→construction 编译期依赖、texture_gen 反依赖资产、run_all.sh Windows Git Bash 兼容。

**遗留（转入本节跟踪）**：
- [ ] units→player_control 的 api.gd 级反向边（`stickman_entity.gd` 经 `PlayerControlAPI` 查 InputDispatcher）：api.gd 注释声明为有意妥协，待阶段 1 输入系统重构时评估是否改为注入
- [ ] 代码建 UI 迁移：PossessPanel/SavePanel/SettingsMenuPanel/ResourceBar/ZoomBar/DebugOverlay 从 `Control.new()` 迁回 `.tscn`（场景是布局唯一真相源）（2026-09-04 对账刷新：五面板实测已无 `Control.new()` 根——代码建根改走 UIKit `full_rect()`/`widget()` 合规出口路线，`.tscn` 迁移被助手方案取代；余 `debug_overlay.gd` 4 处 `Control.new()` 根待迁）
- [ ] EventBus 死信号二次清理：`map_unloaded/chunk_loaded/chunk_unloaded/travel_completed/strategic_map_opened` 等仍零订户；`balance_changed` 零发零订；模块 api 状态信号（building_started/resource_not_enough/org_*）确认消费方
- [ ] `stickman_entity.gd`（1332 行，2026-09-04 复核）按职责拆分（状态/渲染/武器/AI 接口）（2026-08-30 审计长期治理；2026-09-04 已做协议级收口——IncomingThreatLedger 账本外置，全量拆分仍有效）
- [x] `texture_gen` 补渲染回归测试（✅ 2026-09-04：`test_texture_gen_api` 8 用例入 unit 批量，含 seed 确定性像素断言；同批另补 `test_fx` 7 用例与 `test_debug_api` 4 用例冒烟）（2026-08-30 审计长期治理）
- [x] `test_combat_feedback` 全量并行偶发超时（✅ 2026-08-22 审计第三轮修复：静态分离固定 0.3s 等待改条件轮询 + `run_all.sh` 并行 6→4/套件超时 45→90 校准，全量 24/24 绿可复现；2026-09-03 顺序依赖审计复核标注"已改负载免疫"）
- [ ] `construction_manager` 拆上帝对象 + 32/390/96 等魔法数收编到 PlacementGrid/地图常量
- [ ] texture_gen/reference 下残留 `.import` 源路径仍指向 `res://modules/building_gen/materials/...` 的历史文件，下次资源清理时重导或删除
- [ ] 测试进程退出泄漏（103 ObjectDB / 13 resources）统一 teardown
- [ ] `test_menu_navigation` 既有失败（"调试区含地图选择按钮"/"动态生成目的地含步行出口" 2 断言挂），改动前后均复现，与本轮 rest/描边修复无关
- [x] world_map_panel.gd 归档（✅ 2026-09-04 结项：全工程 find 0 命中，文件已随 world_map/archive 死代码一并删除）
- [x] SaveManager legacy_modules(JSON) 与新 SQLite 表双轨存储合并（2026-08-22：WorldState 迁移到 game_saving/game_loaded 信号直写 world_state 表，legacy_modules 机制删除；旧档读档自动回退旧表）
- [x] 文档漂移（部分）：`modules/README` §3/§8、地图制作教程、建筑与定居点的存档格式 .json→SQLite `buildings` 表 / `save_<slot>.db`（2026-08-22 已修）；科技状态字段待查
- [x] 文档漂移：科技状态字段（✅ 2026-09-04 结项：`technology_state.gd` 死代码删除后全 docs 扫描无残留——核心实体与状态机/模块API契约/GDD差异分析统一为"科技模块待重建、征服获得即拥有、无状态机"表述）

### 架构审计整改（2026-08-15，第二轮全量审计后）

**2026-08-22 第三批+第四批执行记录**：
- ✅ resources 供需定价引擎落地（`update_price/tick_supply_demand`，api 驱动 5s tick + `price_changed`；模型：均衡库存稀缺度^0.5 × 基准价 × (1+税率)，单次限幅 ±15%）
- ✅ construction 升级/修理实现（即时制简化：升级=0.5×成本+20%血上限+`upgrade_level`；修理=缺损比例×0.3×成本回满；新增 `building_upgraded/building_repaired` 信号经 api 转发）
- ✅ AudioManager 事件框架（SFX_EVENTS 语义表 + play_event 静默跳过缺失资产 + EventBus 战斗/存档接线 + StickKit 按钮统一点击音 + 建造完工经 SystemSetup 接线）
- ✅ BalanceConfig F9 热重载（debug_overlay 触发 reload + toast）
- ✅ CombatApi.issue_order 实现（委托 TacticalOrders，SystemSetup 注入）；契约注释修正（possess_commander 归 player_control、initiate_battle 由 start_battle 承接）
- ✅ WorldState/organization/entities 冻结标注（决策：不删不接，README §8 + 类头注明）
- ✅ WorldZ 世界空间 z 序常量化（6 地图场景去硬编码，MapBase 统一应用；指示物 OVERLAY_* 收编）
- ✅ fx 模块：粒子池（FxPool 组查找 + FxLibrary 三效果纯代码配置）+ 建造尘土/打击火花已接线
- ✅ environment 夜间压暗 + CampLight 视口跟随暖光（夜间渐亮）
- ✅ thatch.gdshader 红色占位 → 程序化草束材质
- ✅ barracks/warehouse 脱离 placeholder 外壳（_post_build 差异化挂件：军旗盾饰 / 货箱麻袋）
- ✅ map_camera.focus_on SM-1 实现（renderer.get_tile_centroid + Tween 缓动）
- ✅ 双份暂停回归测试（StickScreen×UIModalStack 恢复权归属钉死）
- ⚠️ export_excel.py 输出路径漂移未跑真导出：会写中文孤儿目录（config/制造 等）且 formations/presets.tres 与实际消费的 formation_presets.tres 不一致——修管线映射表后再导

#### PLACEHOLDER 素材替换清单（Demo 占位 → 自制资产）

| # | 文件 | 现状 | 替换方向 |
|---|---|---|---|
| P1 | `modules/fx/scripts/fx_library.gd` | 运行时生成的软圆点贴图 + 手调粒子参数 | 手绘粒子贴图（尘土蓬松/木屑形状）、逐效果调参 |
| P2 | `environment_system.gd::_ensure_camp_light` | GradientTexture2D 径向光斑跟随视口 | 逐光源布点：火把/建筑窗光挂 PointLight2D 到场景节点，本光退化为调试用 |
| P3 | `thatch.gdshader` | 程序化草束（参数可用级） | 美术调参或换手绘屋顶贴图混合程序化细节 |
| P4 | `barracks.gd/_post_build` | 几何军旗+八边形盾饰 | 军旗手绘贴图+摆动动画、纹章盾徽 |
| P5 | `warehouse.gd/_post_build` | 几何货箱堆+椭圆麻袋 | 木板纹货箱贴图、麻袋手绘、货物随库存变化 |
| P6 | `audio_manager.gd::SFX_EVENTS` | 纯路径约定表（零音频文件） | 录制/采购 ogg 放入 `res://assets/audio/sfx/` 即生效 |
| P7 | `modules/fx/assets/demo/`（水晶簇/岩壁带/墙纹/接触阴影） | 从药工解包资源与实机截屏提取的**本地演示占位**（fx_gif authentic 舞台用，游戏内资源点未引用） | 自制手绘水晶簇与岩壁纹理替换；发布导出须排除该目录 |

**本轮已修复（详见 git 提交 8a8f85d / 0de8bf9 / ad5f15c / 4783424 / 34e40e5）**：
- 🔴 存档 schema 缺 `material_progress` 列致未完工项目丢失：建表补列 + `_migrate_schema` 旧库幂等迁移 + 回归测试 `test_save_roundtrip`（真实 SaveManager 全链路，覆盖新库/旧库两条路径）
- 存档外层 BEGIN/COMMIT 与 godot-sqlite helper 内部事务冲突（实测每次存档刷 2 条 ERROR 且外层事务被提前提交）：移除并注释说明
- SaveHandler 直调 construction 内部私有方法：新增 ConstructionAPI `save_to_db/load_from_db/refresh_city_terrain_mask` 契约
- EventBus 编队三信号补参数类型（squad_created/order_issued/commander_assigned）
- 仓库卫生：Godot 4.7.1 UID 重生成全量提交、building_gen/archive 与 world_map/archive 死资产删除、.gitignore 追加 `stick-world/Godot/`
- 文档漂移：EventBus 信号清单（残留 `ui_toggle_pause_requested` 删除、30 vs 32 修正）、P0-2/P0-6/P0-17 状态校准、自动加载依赖补 WorldState→SaveManager 与 SaveManager→ConfigManager 边、CONTRIBUTING 过时清单、AGENTS.md/tests/README 分层描述

**本轮已修复（2026-08 审计第三轮，详见 git 提交 023e166 / cc1d00a / ea1f0ac）**：
- 🔴 测试超时误调：`run_all.sh` 默认并行 6→4、`DEFAULT_TIMEOUT` 45→90、全套件超时表按串行实测校准；`test_l2_strategic_map` 索引图扫描改分步采样 + 漏标兜底；`test_combat_feedback` 静态分离固定 0.3s 等待改条件轮询 → 全量 `run_all.sh` 实测 24/24 绿可复现
- 🔴 `BalanceConfig.reload()` 激活：`system_setup.setup()` 装配起点调用，数据驱动层从"零调用/空字典"变为可读（`units.stickmen` 3 兵种已装载）
- StickmanEntity 数据驱动消费：`stickman_def_id` + 读 `base_hp/base_attack` 覆盖 HealthComponent/WeaponMount，未命中回退硬编码（默认平原步兵行为零回归）；`base_speed` 映射留待兵种设计
- `technology_state.gd` 死代码删除（全库 0 引用）
- 拆 world⇄combat 硬引用：`battle_director` 的 `WorldAPI.PATH_*` 改为注入路径字段（`battle_anchor_path`/`building_host_path`），装配层注入；`模块依赖关系.md` 标注"装配根/有意环"
- 仓库卫生：根 `modules/` 空壳删除、`tools/godot_ai_repo/.git` 嵌套仓库删除、`tools/worldgen/output/**` 可再生产物 gitignore（保留 `locked/` 与运行时 `config/strategic_map`，从索引移除 224 个文件）

**本轮已修复（2026-09-03 系统性审计批次，详见 git 提交）**：
- 6 处资源加载静默失败补 `push_warning`（building_gen/api、visual_controller、behavior_attack、building_catalog、l1_world_data、stickman_weapon）
- Building 契约收口：`building_gen/api.gd` 声明 Building 为对外公共类型（仿 combat TargetFinder 先例），construction 31 处 is/as 引用转合规
- world_map/data 旧数据层死代码簇删除（region_definitions/continent_data/region_data/landmark_ref，全工程零引用）
- `test_balance_config.gd` 补入 batch_runner 批量清单（第 26 个 unit 套件此前游离在外）
- 文档契约回写批次：核心实体与状态机对齐代码、EventBus 目录补 2 信号 + 删 safe_emit 准则、模块API契约补 6 模块章节 + 修签名、架构文档变更记录迁 CHANGELOG、10 处死链修复（详见 CHANGELOG「架构契约回写与已知问题清理」节）

**遗留（转入本节跟踪）**：
- [ ] 装配根越界（C2 剩余）：`system_setup.gd` 仍 preload 34 个跨模块内部脚本（2026-09-04 复核：ui_global 9 / combat 8 / world_map 3 / construction·player_control·resources 各 3 / organization·debug_gui 各 2 / fx·world 各 1）并直挂 `_root`；world 持 ConstructionManager/ResourceManager/OrganizationManager 强引用——内部类装配收敛到各模块自建出入口（阶段 1 装配重构）。注：C1 已拆 `battle_director→WorldAPI` 硬引用并文档标注 world⇄combat 为有意环
- [x] `combat ⇄ units` 双向实体级耦合（✅ 2026-09-04 T5 契约化收口：`combat/api.gd` 头部声明"编队职责查询契约"——FormationSystem 为 combat 内部类、units 侧禁 class_name/preload，duck 调用 + `has_method` 防御；units/ai_controller 持 WorkType 本地常量副本，weapon_mount/behavior_* 补 audit-exempt 标记对齐先例）
- [x] player_control/api.gd 职责声明与代码错位（✅ 2026-09-04 结项）：SelectionSystem/FormationSystem/TacticalOrders 实际在 combat/scripts/command/，三处文档（api.gd 头注释/README §1/模块依赖关系.md）已统一归属；全 docs 扫描无剩余归属错位，同批修正场景与战斗架构.md 目录树、开发指南.md 目录树两处 player_control 注释与 EventBus信号契约.md `commander_assigned` 发射方（Organization→FormationSystem，唯一发射点 formation_system.gd）
- [x] `world_state.gd` God-object：序列化职责已拆 `world_state_serializer.gd`（core/entities/；2026-09-03 核实 world_state.gd 已降至 237 行）
- [x] 信号连接生命周期防护（✅ 2026-09-03 审计结项）：全工程 151 处 `.connect(` 逐一复核——25 处已带 `is_connected`/`disconnect` 防护（construction 系 4 文件、world_map l2/l3 控制器、weapon_mount/visual_controller 换武器重载、team_ai 号令订阅、building 交互区、game_root/save_handler 换图路径、stick_window 等，为登记后各批次陆续修复，"唯一例外 save_handler:131" 已不成立）；其余 126 处均为一次性生命周期（`_ready` / `new()` 临时对象 / `SystemSetup.setup` 单次装配链），二次调用路径不存在，按克制原则不修。边界项备忘：travel_handler/save_handler/system_setup 对常驻 EventBus 的连接当前仅单次调用，若未来装配重构改成可重入需补防护；`audio_manager._wire_event_bus` 4 处 lambda 连接无法用 is_connected 防护（每次 Callable 身份不同），依赖 autoload `_ready` 一次性
- [x] world/api.gd 仅 22 个 PATH_* 路径常量，debug_GUI 经其遍历地图节点树（✅ 2026-09-03 审计结项）：登记已过时——debug_gui 全模块 0 处 WorldAPI 代码引用，绘制器（debug_drawers.gd `_map_child`）与信息面板（debug_info_panel.gd）均经 `DebugApi.set_ctx_extra("map_paths", ...)` 注入的路径表查节点（system_setup.register_debug_drawers 注入，注释明确"不反向 import WorldAPI"）；残余 PATH_* 引用仅为 world 模块内部自用、combat/battle_director 注释 2 处（实际值经注入字段）与测试断言，无运行时跨模块耦合
- [x] 测试顺序依赖与裸帧等待（✅ 2026-09-03 修复）：combat_control 用例自清理（结束还原 BATTLE 模式/取消附身/解散小队，开局经 helper `reset_units` 重置瞬态——此前 `_test_follow_moves` 隐式依赖上一用例的 possessed 玩家与"随行队"）+ combat_test_setup 补 `reset_units` 用例边界重置设施；construction_cycle 用例自清理（不收尾项目 `cancel` 防 PLANNED 残留劫持 try_assign——曾致 building_registry 实际建的是残留项目、工人用毕注销、测试实体移除）+ building_registry 自证断言（派工须落在本用例项目且本项目完工）。注：`test_combat_feedback` 静态分离与 `test_l2_strategic_map` 索引扫描已改为负载免疫
- [x] 存档事务返回值不检查（insert/delete 失败静默）+ 6 处 SQL 字符串拼接 + schema 无版本化迁移（✅ 2026-09-04 T3 存档层加固：18 处字符串拼接全改 `?` 绑定参数查询、insert/delete/update 返回值检查失败 push_error 不再静默、PRAGMA `user_version` 门禁 + 迁移占位）
- [ ] CI 未启用（ci.yml.disabled 模板）；`AGENTS.md` 与 `.trae/rules/rule.md` 双规则文件漂移（116 行差异）
- [x] 命名违规（✅ 2026-09-04 修复）：目录已改名 `modules/debug_gui/`（project.godot autoload 路径同步），`UIKit.gd` 已改名 `uikit.gd`（git mv 连同 .uid）
- [x] 文档漂移（2026-09-03 修复）：模块API契约.md 补齐 units/world/player_control/environment/fx/debug_GUI 六章并修 world_map `initialize` 签名矛盾；世界地图数据流.md 头部状态改为"部分实装"现状（§2.1 桥接 JSON「待实现」属生成端工作项，保留原标注非漂移）


### 世界生成管线（L3/L2 已定稿，L3 视图已实装，L1 已实装）

> **2026-08 进度**：L3 大陆生成（fractal_continent.py）→ L2 地区划分（region_split.py watershed 沿地形切分 + 创始人 HTML 工具三轮人工调整：7 组合并/2 处地峡切开/22 微型岛就近合并）→ **定稿 13 个地区**。L2 图包素材（蒙版/底图裁切/高程/索引图/元数据）已由 `export_l2_packs.py` 导出至 `tools/worldgen/output/l2_packs/`。生成工具链与产物格式见 [08-程序化世界生成.md §二十一](../设计/系统/08-程序化世界生成.md)、[世界地图数据流.md §2.1.1](../技术/架构/世界地图数据流.md)。
> **L3 大世界战略图已实装（M 键）**：13 地区分块（地面+海洋一起分：陆地沿用实际分界线、隔海质心连线直线延长）、hover 描边高亮、与 Tab 的 L1 地图互斥独立（CanvasLayer 100/101）。素材由 `export_l3_view.py` 导出（label 直编索引图），运行时见 `modules/world_map/`（l3_world_data/l3_map_renderer/l3_map_controller/strategic_map_l3.tscn），集成测试 `test_l3_strategic_map`。
> **L3 → L2 下钻已实装**：M 图单击地区进入该 L2 详细视图（L1 地块分块 + hover 灰色描边），ESC 返回 L3（CanvasLayer 102，位于 L3 之上）；M 关闭/重开保留相机状态与当前 L2 视图。素材由 `l2_export/export_l2_view_packs.py` 导出（8192 级索引图 + 共享网格多边形 + **烘焙 `.bin`**，label 与 L3 独立命名空间），渲染为纯矢量（ArrayMesh 静态几何缓存）。运行时见 `modules/world_map/`（l2_world_data/l2_map_renderer/l2_map_controller/strategic_map_l2.tscn），集成测试 `test_l2_strategic_map`。
> **L2 描边/补丁感已修复（同源重烘焙）**：填充三角剖分与描边**共用同一份金标准多边形**（`mesh_extract.simplify_mesh` 产物，Hausdorff=0.0px）；形状处理 Chaikin×3 平滑像素台阶（保留 ≥3px 真实尖角）+ DP(tol=0.2) 降顶点。详见 [`CHANGELOG.md`](../CHANGELOG.md)。

- [x] **L1 地块划分**（✅ 2026-09-04 结项，方法演进：`tools/worldgen/l1/l1_world_split.py` 改用均匀网格+扰动方案（替代原设想的裁切高程+watershed），全大陆 389 个 L1 地块，M 键 L3 图按 V 键叠加查看，集成测试绿；详见 08-程序化世界生成.md §二十一 L3-L）
- [ ] **政权/聚落生成**：L1 地块内聚落定位（靠近地块中心）+ 规模分桶 + 政权分配（§0.19 八城邦模型，独立无首领）
- [x] **战略图数据接入**（✅ 2026-09-04 结项：L2 图包已接入运行时——modules/world_map 的 l2_world_data/l2_map_renderer/l2_map_controller，8192 级索引图+共享网格烘焙 `.bin` 纯矢量渲染，集成测试 `test_l2_strategic_map`；L3→L2 下钻 M 图实装）

### P0 新 0.9：基础战略图（L1 单层）

> **定位**：P0 原型循环的"跨图"环节从按钮列表（world_map_panel 占位）升级为真正的战略图。只做 L1 单层（玩家第一阶段世界图 = 8 城邦），不做 L3/L2 多级缩放。聚落模型见 [08-程序化世界生成.md §0.19](../设计/系统/08-程序化世界生成.md)、技术方案见 [战略图架构.md §10 SM-1'](../技术/架构/战略图架构.md)。

- [x] **0.9a L1 世界生成器**（✅ 2026-09-04 结项：产物 `config/strategic_map/l1_world.json + l1_base.png + l1_mask.png` 已在用——tiles/states（8 政权）/spawn_settlement_id + MST 道路，出生 L1 = Tab 战略图数据源；生成链演进为大陆全域 l1_world_split/city_split（389 L1 地块/3203 城市），种子参数由 l1_data.json `seed` 字段承载）
- [ ] **0.9b 聚落地图生成器**：8 城邦主场景图程序化生成（seed+biome+级别+产业 → MapDefinition → VillageMap，喂现有 SceneLoader；1 聚落 = 1 张主场景图，空聚落不可进入）。验收：8 张图可进出、初始建筑正确
- [x] **0.9c world_map L1-only 重写**：单层数据容器 + MapRenderer（卫星图底图 + 边界索引图 `_draw` 描边不填充 + 道路/图标/文字）+ query_at_screen 索引图像素解码（P 社 provinces.bmp 机制）。验收：strategic_map.tscn 显示 8 城邦+道路+地块边界线
- [x] **0.9d 接线**：`api.initialize`/`close_strategic_map` 补齐，替换 `world_map_panel.gd` 占位（system_setup），Tab 开关 + 点击聚落进入场景图 + ESC 退出恢复场景图状态。验收：开图→点聚落→进入→退出恢复
- [x] **0.9e 集成测试** `test_strategic_map_p0`：索引图命中、边界描边、空聚落、进出链路。验收：全链路绿

### 需创始人决策
- （无当前阻塞项）

---

## 中优先级（P0 后期 / P1 前期）

### 阶段 1/2 指挥系统深化（从 P0-1 附身后移）

> **已完成（2026-08）**：队伍类型编制系统（编制预设/职责范围/编制管理窗口 FormationPanel）——原型循环"编队"环节阻塞解除，详见 CHANGELOG。

- [ ] **运输队职责消费（WORK_TRANSPORT）**：编制预设已有"运输队"（fp_transport_convoy，LOGISTICS 标签），但跨城市运输/物资运往前线的实际行为待物流系统（[`../设计/系统/06-物流系统.md`](../设计/系统/06-物流系统.md)）实装时接入；场景图小队的"运输队"与组织层级模板的 L2"运输队"（`config/formations/presets.tres` tier_transport_convoy）为同一概念的两层投影，挂接关系届时一并设计
- [x] **编队预设对齐组织语义**（✅ 2026-09-06）："工人队"改名"劳工队"（id 不变）；新增"运输队"预设 + Tag.LOGISTICS 组织标签；搬运（WORK_HAUL）定为全员基础能力——`is_work_allowed` 对搬运恒放行，职责范围只限制专业分工（战斗/建造/采集/运输），战斗班也能搬运

- [ ] **附身系统完整版**：多层级指挥官附身 / 附身时 AI 协奏 / 指挥链延迟=0 / 自主决策权限切换
      - 定位：附身是指挥系统的"亲自下场"模式（详见 [`../设计/系统/02-战斗系统.md`](../设计/系统/02-战斗系统.md) §1、[`../技术/架构/场景与战斗/战斗与AI.md`](../技术/架构/场景与战斗/战斗与AI.md) §7.5/§8.5）
      - 阶段：阶段 1 "核心系统（Alpha）"或阶段 2 "深度扩展（Beta）·完整指挥链设计"子任务，详见 [`路线图.md`](路线图.md)

### 工程流程决策
- [x] **WorldState 容器决策（集中 vs 模块自持）→ 已决策 A（集中制），2026-08 organization 先行落地**：OrganizationManager 值升级为 OrganizationState 对象、经 set_world 同步注册 WorldState 容器、序列化格式与 WorldState 统一（存档由 WorldState 统一落盘，模块不再自注册 SaveManager）；单测含容器同步用例（15 用例全绿）。**剩余**：resources 的 ResourceState 是"品类定义"（价格/重量）而非"库存台账"（区域嵌套 stocks），需先扩展状态类设计（或新增 ResourceLedgerState）再迁移；technology 为空壳，重建时按 A 方案实施
- [ ] **地图编辑器方案决策**：三种方案待选（直接改 .tscn / 独立编辑器 / Godot 插件），见下方"地图编辑器方案探讨"
- [x] **数据存储方案确认**（✅ 2026-09-04 实测过时：存档已统一 SQLite `save_<slot>.db`——建筑落 `buildings` 表、WorldState 经信号直写 `world_state` 表、schema `user_version` 版本化；原"建筑存档 JSON vs Resource vs 混合"选择题已被实现超越，无待决事项）

### 行为树演进（详见 [P0收口执行计划-2026-08.md](归档/P0收口执行计划-2026-08.md) §13）
- [ ] [短期] **寻路避障系统**：火柴人直线移动遇障碍硬弹回导致卡死，需实现局部避障或 A* 网格寻路（调研报告见 [`../技术/架构/寻路避障调研.md`](../技术/架构/寻路避障调研.md)）
- [ ] [中期] WorkCrewAssigner 扩展为任务板（搬运/建造任务分发），决策与执行解耦（参考监狱建造师/环世界 Job 系统）
- [ ] [中期] AIController 决策优先级数据驱动化（.tres 配置），把 `_make_decision` 硬编码优先级抽成资源
- [ ] [长期] 可编辑行为树评估：LimboAI（Godot 成熟 BT 插件）vs 自研 HBT，`BehaviorBase` 适配为 BTNode

### 代码债治理（2026-09-03 系统性审计挂载）

- [x] **胖文件拆分·weapon_mount**（✅ 2026-09-04）：弹道解算+高斯散布拆 `arrow_ballistics.gd`、格挡判定拆 `block_resolver.gd`、顿帧触发（含全局节流状态）拆 `hitstop_controller.gd`（三个 RefCounted 子组件，weapon_mount 改薄委托）
- [x] **胖文件拆分·stickman_entity**（✅ 2026-09-04 协议级收口）：新建 `IncomingThreatLedger` 账本承载在飞箭矢伤害估计与威胁时刻（含 register/settle/mark/is_threatened 语义方法），实体侧两个裸字段改为 property 委托——WeaponMount/arrow_projectile/behavior/team_ai 的 duck-typing 协议零改动，账本独立可测（test_incoming_threat_ledger 5 用例入 unit 批量）
- [ ] **胖文件拆分·其余**：formation_system/game_root/system_setup/construction_manager 等 700+ 行文件内部已分区，随改随拆
- [x] **战斗/编队手感数值入数据管线**（✅ 2026-09-04）：18 个手感变量（弹道 3/格挡 4/HITSTOP 3/击退 1/移动 3/缩放 1/编队 4）落 Excel 平衡变量表 → `config/balance/variables.tres`（31 行）；三消费方改 var + BalanceConfig 校准（weapon_mount._apply_global_tuning / stickman_entity._apply_movement_tuning / formation_system.setup→_apply_balance_tuning），行缺失回退代码默认零回归
- [x] **world⇄ui_global 解耦**（✅ 2026-09-04 核实：伪问题）：`tools/audit_deps.py` 实测 ui_global →（无出向依赖）、全工程 0 依赖环——原审计的"26 处互指"为注释行与 duck-typing 调用（面板已是弱类型 Node + setup 注入），代码级仅 main_menu/loading_screen 2 处 .tscn 场景路径常量（切场景合法入口）。无需改动
- [ ] **Excel 导出管线映射收尾**（2026-09-04 对账刷新）：元数据行机制（首行 `#output_dir: <目录>` + sheet 英文名）已铺开——12 张 Excel 中 11 张带映射，其中 9 张文件 15 个 sheet 命中现有英文名产物（平衡→balance/variables、建筑→buildings、战斗战术→combat/tactics、扩张×3→expansion、资源→resources、单位×3→units（stickmen/weapons/armors）、制造→manufacturing、法律→law、运输×3→logistics）；导出器已硬化（导出保留目标 .tres uid 头 / 空 id 行硬错误阻断导出 / `--yes` 非交互确认 / 强制 LF 幂等）。**剩余**：① 编制预设.xlsx 无映射（config/formations 下 presets.tres 与 formation_presets.tres 双产物归属待拍板）；② 法术科技/管理科技.xlsx 元数据指向 `technology` 目录但现有产物为 `config/tech/techs.tres` 且科技模块待重建（tech 双表方案待拍板）；③ buildings/regions/resources/stickmen 4 表仓库版 .tres 为陈旧格式，重导会产生纯格式 diff，受「真实导出冻结」约束待拍板

### 设计待定（GDD 标注 [待补充]，详见架构文档 §十六）
- [ ] 兵种类型与武器属性（影响 Hitbox、伤害计算）
- [ ] 各村落 `VillageDefinition.tres` 内容
- [ ] 大建筑（宫殿）内部场景设计
- [ ] 道路上随机事件机制
- [ ] 战场地图两端建筑模式（预设阵地 vs 现场部署）
- [ ] 加工品/奢侈品具体资源列表（GDD §2.2 `[待补充]`）

### 科技系统（阶段 1 重建）
- [ ] **科技模块重建**：原 modules/technology 空壳（研究制契约）已删除；阶段 1 按 [04-科技系统.md](../设计/系统/04-科技系统.md)「征服获得」策略重建——征服解锁 + 事件获取，获得即拥有，无研究状态机；重建时同步 modules/README 速查表、模块API契约.md 与 EventBus 信号（按当时契约声明，不预先占位）

### 火柴人个体系统（创始人确认，参考《世界盒子》）
- [ ] **个体属性标签系统**：每个火柴人有独立属性（力量/智力/敏捷/工艺/指挥），影响行为树和适合的工作
- [ ] **装备系统**：武器/护甲/工具影响属性和行为
- [ ] **迭代式复杂化**：P0 先做基础属性 + 简单行为，后续逐步增加（世界盒子至今仍在完善）
- [ ] 详见 [战斗与AI.md §7.2.1/§7.2.2](../技术/架构/场景与战斗/战斗与AI.md) + [竞品分析.md §4.12](../商业/竞品分析.md)

### 地图系统（创始人确认）
- [ ] **城镇朝向决定道路方向**：大世界地图上城镇的随机朝向决定从场景图左/右边界走出分别到达哪些道路（依赖战略图完成）
- [ ] **战场垂直跨度**：战场地图地面带大幅加宽（类似《米拉奇战纪》），方便排兵布阵
- [ ] **道路遭遇战**：行军途中遭遇敌方触发战斗（低自由度全面战争式行军，依赖战略图 + 行军系统）
- [ ] **城镇地面贴图差异化**：不同类型城镇使用不同硬化地面贴图（矿业/农业/军事等）
- [ ] 详见 [地图与场景图.md §5.5.5-§5.5.8](../技术/架构/场景与战斗/地图与场景图.md)

### 地图系统架构重构（创始人原始诉求，待重构）
- [ ] **城镇动态扩张**：地图大小随城镇规模增长，`map_left`/`map_right` 可变
- [ ] **城墙改为 Building 类型**：可建造/破坏/穿越，不在地图边界，可能有多层（旧城墙保留+新城墙新建）
- [ ] **城外环境**：城墙外约 100 格的自然环境（森林/草原/绿洲），城外地面贴图与城内不同
- [ ] **出城机制重构**：走到边界附近 20 格弹"打开大世界地图"按钮，或顶边界走几秒触发，不再用 ChunkTrigger
- [ ] **战场类型扩展**：遭遇战/攻城战/守城战三种，攻城守城利用城墙阵地
- [ ] **战场 spawn 动态**：我方位置根据行军方向决定（从南往北走 -> 右侧/南面），不固定左边
- [ ] **战场持续性**：玩家走回撤退点返回城镇后战场战斗不停止，由 AI 自动指挥
- [ ] **工程战/攻城战**：我方阵地变为城墙（如果有城墙），需攻破城门入城
- [ ] 详见 [地图与场景图.md §5.6](../技术/架构/场景与战斗/地图与场景图.md) + P0计划地图架构重构需求说明
- [ ] **后续**：基于以上诉求重新设计地图系统架构（方案待定）

### P1 工程
- [x] **UI 模态栈**（✅ 已落地）：`UIModalStack` 建成（层键字典 + 逐层 pop 替代 `GameRoot._handle_escape` 特判，输入屏蔽随栈统一）；`LayerOrder` 层号常量与 `SystemOverlay` 系统层就绪；模态栈/ESC 链路有集成测试覆盖（`test_modal_stack` / `test_esc_key_input`）
- [ ] **UI 做做样子项清理（2026-08 系统审计）**：① 通知 feed 仍是 GlobalHUD 单条 Label（04 篇规划堆叠 feed 未做）；② 游戏内快捷栏缺失（仅 hud_template 演示，未接模式/指令）；③ 设置项部分"存了不生效"——video/window_mode、audio 音量、video/show_fps 写了 ConfigManager 但无消费方应用；④ 小地图/缩放条样式未统一（未走 StickTheme）。模板场景（template_*/workspace/gallery）为演示性质，不在正式范围（2026-09-04 对账刷新：①③ 已完成——通知升级左下堆叠 feed（`notification_feed.gd` + `test_notification_feed`）、设置项真实生效（音量接 AudioServer 总线/FPS 计数修复/重启恢复 round-trip + `test_settings_apply`）；②④ 仍有效保持跟踪）
- [ ] **UI 体系落地（部分完成，2026-08）**：主菜单/载入屏/暂停菜单/设置面板（分类切换+落盘）/存档面板已接入；分层重构完成（ModalOverlay 盖住全部 UI、模态暂停+遮罩消费鼠标+相机输入兜底、ESC 统一 `_handle_escape`）；4 种弹窗行为模板（StickScreen=MODAL / StickWindow=FLOATING/DOCK/POPOVER，编制菜单已归 FLOATING）；空面板通达（暂停菜单帝国功能 + K/O/J/L 快捷键）；战略图懒加载（启动 20.8s→3.2s）+ 世界加载覆盖（无死灰屏）；统一层号 `LayerOrder` + `SystemOverlay` 系统层；顶栏/小地图/缩放条/时钟布局调整。**剩余**：业务面板（Village/Battle/Possess/BuildMenu 等）向 4 模板 + StickTheme 迁移，详见 [`docs/设计/UI/README.md`](../设计/UI/README.md)
- [x] **主题变体/层级皮肤（石头→羊皮纸→全息）已删除**（✅ 2026-09-04 结项：2026-08 决策已落地，主题变体/层级皮肤方案删除，UI 统一黑玻璃单一风格，无遗留动作），见 `docs/设计/UI/` 相关篇目
- [x] **启动读档链路兜底已修**（✅ 2026-09-04 结项：`save_handler.gd` L118 存档缺地图信息（空档/损坏档）回退新游戏开局、L180 恢复后无玩家实体生成默认玩家兜底，均为带 push_warning 的在用代码路径；回归验证走 `tests/dev/ui_shots.tscn` 截图自检 05/07）
- [x] 手动存档 / 读档界面（✅ 2026-09-04 结项：`save_panel.gd` 多槽位列表 + 逐槽保存/读档按钮（读档经 `SaveHandler.load_game_from_slot` 转发，主菜单只读模式），经模态栈 SAVE_PANEL 层键接入；quick_save/quick_load 框架既有）
- [ ] 读档完整性：建筑/工地/派工/资源/编队状态全量恢复

### UI 系统技术债（2026-08 审计遗留，见 modules/ui_global 重构记录）
- [ ] **PossessPanel 每帧刷新改信号驱动**：`_process` 每帧字符串拼接 + 反射查询 HP/士气/冷却，改订阅单位属性变化事件（当前收益低，P1 随战斗 UI 深化做）
- [ ] **hover_indicator 悬停检测移出 `_process`**：每帧全量遍历实体做包围盒检测，可改 `_unhandled_input` 鼠标移动事件驱动
- [x] **strategic_map 补充 UI**（✅ 2026-09-04 结项：战略图线三件套已接线——聚落 tooltip（hover 跟随）、粒度指示器（L1/L2/L3 层级+地块号+ESC 语义）、L1/L3 视图互斥，集成测试 `test_strategic_map_ui`；原 world_map/ui 面板已归档至 archive/，重建时参考）

### P1 系统设计
- [ ] 确定完整科技树结构
- [ ] 确定法律分支树
- [ ] 各文明的初始条件和差异化
- [ ] 上古魔方获取剧情
- [ ] 主角力量来源设定
- [ ] 两个帮手设定

---

## 低优先级（P2+）

- [x] **发布导出排除战略图 json**（2026-08-31 完成，前提已被修正）：原条目假设"玩家运行时永远命中同名紧凑 bin"——**实测该假设不成立**。Godot 不把 `.bin` 识别为资源，`export_filter="all_resources"` 下 bin 从不进包，只有 json 进，玩家端实际一直在走 json 慢路径（此前"数据紧致化"的性能收益在发布版本完全失效）。
  正确做法分两步，已写入 `stick-world/export_presets.cfg`：
  1. `include_filter` 显式纳入 `config/strategic_map/**/*.bin` —— 让 bin 真正进包（运行时主路径，原样序列化 `var_to_bytes`，比 json 解析快）；
  2. `exclude_filter` 只排除**有同名 bin 兜底**的 json（l1_world/l3_world/l3_city/l3_l1、l1_packs/\*\*、l2_packs/\*\*）；
  `color_map.json` 与 `l1_data.json` **无对应 bin，是运行时唯一数据源，必须保留**。
  另排除 `assets/_raw/*`（AI 生成原图）、`tools/*`、`tests/*`、材质 `reference/*`。
  实测：pck 由 120MB 降至 75.7MB，bin 100 个全部在包内。
  ⚠️ 注意 Godot 的 `**` 不匹配目录直接子级，根目录文件须单独列出。
- [ ] **战斗结束判定（战斗收敛）**：胜负收束能力——溃散判定（全员溃逃即结束）、尸体处理（尸体不应成为阻挡，美术表现后续单独设计）、溃兵回场策略。P0 战斗基础已可用（全灭即结束），收敛能力依赖战斗与碰撞系统稳定后实施，不在 P0 范围
- [ ] 成就图章完整列表
- [ ] 定价策略确定
- [ ] 宣发计划
- [ ] 音效/音乐方案
- [ ] 实体周边可能性调研

### 代码卫生（2026-09-03 系统性审计挂载）

- [x] **重复代码下沉**（✅ 2026-09-04）：texture_gen 四面板提公共基类 `MaterialDebugPanel`（thatch 保留本地滑杆并注记语义差异）；building_gen 调色板数据化——`BuildingPalette` Resource + 3 个 .tres 场景注入（基类保留 `_get_palette()` 字典适配器）；world_map 提 `MapControllerUtil.find_child/find_sibling` 原语，三控制器复用
- [x] **命名统一**（✅ 2026-09-04，两处一并勾销）：uikit.gd 改名（实际引用面仅 diag_real_panels preload + UI.md 链接）；debug_GUI→debug_gui 目录改名（project.godot + system_setup 2 处 preload + 模块内 4 处自引用同步）；TextureGenApi→TextureGenAPI（4 文件）；DebugApi 保留（autoload 单例名，与 SaveManager/EventBus 同类不在该约定内）
- [x] **测试补缺**（✅ 2026-09-04）：新增 `test_texture_gen_api`（8 用例，含 seed 确定性像素断言）+ `test_fx`（7 用例，FxLibrary 配置/FxPool 池化语义）入 unit 批量（28 套）、`test_debug_api`（4 用例冒烟，含持久化保护）入 integration 套件表与 -Changed 映射
- [x] **Control.new() 灰区迁移**（✅ 2026-09-04）：UIKit 新增 `widget(script, name)` 助手（角落 HUD 部件合规出口，与 full_rect 分工见头注释）；system_setup 六处 HUD 部件迁 widget()、game_root 加载覆盖层与 ui_root F3 检查器两处全屏根迁 full_rect()

---

## 已完成 / 已迁移（清理记录）

| 原条目 | 状态 | 去向 |
|--------|------|------|
| 启动核心原型开发（阶段 2 部落首领核心循环） | ✅ 已推进到阶段 0.8 | 详见架构文档 §十五 |
| 验证"管理学概念->好玩"的关键假设 | 🔄 P0 原型验证中 | 详见路线图.md 阶段 0 |
| 确认无缝缩放具体技术方案 | ✅ 已实现 | CameraRig 三层缩放（base_zoom/user_zoom/effective_zoom） |
| 确认数据存储方案（Resource vs JSON） | 📐 已设计待实现 | 架构文档 §4.5.4 存档 JSON 结构 |
| LOD 系统原型 | 📋 迁移 | 架构文档 §十六 技术债（P0 全部完整 AI，暂不需要 LOD） |

---

## 地图编辑器方案探讨（待决策）

> 触发原因：当前地图用 Godot 场景（.tscn）存储，手动编辑效率低，需要专门工具。
> 关联架构文档：[`场景与战斗架构.md`](../技术/架构/场景与战斗架构.md) §三 地图系统

**核心问题：地图用什么格式存储？编辑器如何工作？**

### 待选方案

1. **直接修改场景文件**（当前方式）
   - 优点：Godot 原生支持，所见即所得
   - 缺点：手动编辑繁琐，无法批量生成，易出错

2. **独立编辑器程序**（@tool 脚本 / 独立 Godot 项目）
   - 编辑器输出结构化数据（JSON/自定义格式），游戏运行时转译为场景
   - 优点：可控性强，支持程序化生成参数
   - 缺点：开发成本高，需维护转译层

3. **Godot 编辑器插件**（@tool + EditorPlugin）
   - 在 Godot 编辑器内提供自定义 Dock，辅助放置地形/装饰/建筑点位
   - 输出 .tscn 或 .json
   - 优点：复用 Godot 编辑器能力，无需独立项目
   - 缺点：插件 API 学习成本

### 待决策问题

- 地图数据是否应该从 .tscn 迁移到 JSON/Resource 驱动（与 xlsx 数据驱动一致）？
- 地形/装饰/建筑点位如何参数化存储？
- Chunk 流式加载（阶段 0.8，实际为地图间切换）与编辑器输出格式的兼容性？
- 是否需要支持程序化生成（如噪波地形）与手工编辑混合？

### 决策时机

P0 阶段现有 6 张场景图（village_a / village_b / road_a_b / battlefield / forest_zone / mega_interior），直接编辑 .tscn 足够。
当地图数量超过 10 张或需要程序化生成时，再启动编辑器方案设计。**不阻塞当前开发**。

---

## 输入/战斗表现小问题（2026-08-30 演练场验收时发现，均低优先）

- [x] **battle_director._battles 积累已释放引用**（✅ 2026-09-03 修复）：BattleInstance 新增 `battle_finished` 信号（`_end` 内 queue_free 前 emit，全灭/胜负/平局三路径均汇入），director 在 `start_battle_at` 订阅并及时 `_battles.erase` 注销；原 `_process` 帧裁剪保留为异常路径兜底（外部直接 free/场景树整体卸载），`is_instance_valid` 防护不动，战斗逻辑零改动
- [x] **BalanceConfig 警告路径格式**（✅ 2026-09-03 修复 + 诊断勘误）：原诊断"路径解析与 tres 实际结构不匹配"有误——路径解析没坏，真根因是 `reload()` 手动调用时机之外，autoload 阶段无自装载（启动时数据字典为空，警告只是空数据的表现）；已改为 autoload `_ready` 自装载数据、空数据时给精确警告，单位数值真正吃配置表（并行配置线 312563f，随合并入库，含 `test_balance_config` 套件）
- [ ] **全套测试偶发 26/28**：并行池下集成测试偶发一例抖动，重跑即恢复（本轮观察到一次，
  未定位到具体用例，连续两次重跑均 27/28）。若复现频率上升再排查。
- [x] **test_melee_combat 冷却用例并行池间歇失败**（✅ 2026-09-04 修复）：
  **真根因（DIAG 插桩实证，修正早前的"动画打断窗口"推测）是用例间击退位移泄漏**——
  并行负载下挥砍用例的命中帧结算拖进冷却用例的 pending 等待窗口，击退把防御者推出
  剑长（diag: first/second 均 reason="out_of_range" 且 cd=0，冷却门未参与）。
  修复：冷却用例在 pending 落地后**再摆位**（重设 def 位置 + 清残余速度），
  并行 12+ 轮稳定通过。诊断 print 已清理，断言消息保留 actual= 数据供未来排查

## 测试演练场（2026-08-30 新增，入口：主菜单「测试场景」→ 仅 debug 构建显示）

- 已有：大乱斗观察场（tests/dev/battle_arena.tscn，battlefield 空旷地图，双方各 12
  混编剑/矛/弓/杖自动互殴，相机跟随战场质心，ESC 返回/R 重开/空格暂停）
- 可扩展（加场景只需在 main_menu.gd 的 TEST_SCENES 清单登记一行）：
  - 单挑观察场（1v1 各武器对位，看站姿/挥砍细节）
  - 编队配置化（当前编队硬编码 COMPOSITION 常量，可改 UI 调兵）
  - 箭雨/法术演示场（依赖法术系统立项）

## 夜空复刻验收遗留（2026-09-06 视觉验收后，均不阻塞）

- [ ] **水面夜间倒影补偿**（潜伏问题）：`Pond`（village_map `enable_water`，当前全图
  默认 false 未挂载）的水面输出整体会被 CanvasModulate 乘暗——深夜倒影（反射的是
  未染色的清屏色天空）会一起被压到近黑，呈"死水"。启用水面时需在
  pond_reflection.gdshader 给倒影项乘 1/cm 补偿矩阵（与 `EnvironmentAPI.unmodulate`
  同构；水体底色/岸沫仍保留 CM 染色）。
- [ ] **天体层与云的遮挡关系**：星/月/极光现画在背景贴图层之上
  （sky_decor.gd `_build_stars` 设 `z_index=1`，因背景贴图 86%+ 不透明且铺满
  天空区，画在其下会被完全盖住）。代价：天体也会叠在最深档云的剪影上
  （泰拉瑞亚原版星在云后）。若要云遮星，需把云 pass 与天体层按深度分层。
- [ ] **夜空"露出真天空"的取舍**（待决策）：清屏色夜空只在山脊上缘的窄条可见，
  大部分"天空"实为背景山体贴图的剪影（BIOME_SETS 各层 `top` 值把贴图铺满了
  天空区）。若想要泰拉瑞亚式大视野天空，需整体下调各层 `top`——会改变
  已定稿的日间构图，需用户拍板后再动。
