# 建筑与美术升级：进度与交接

> **用途**：跨会话交接单一入口。新会话恢复本任务从本文件开始读，不重新摸底。
> **任务**：建筑结构件管线升级——铁匠铺落地、石头建筑结构件化、多材质家族、双/三层建筑、室内内饰、茅草笔触美术。愿景锚点：[`docs/设计/系统/12-小镇生活与美术.md`](../../设计/系统/12-小镇生活与美术.md) §四（创始人原始诉求+《王国两位君主》标杆）。
> **工作区约定**：worktree `.temp/building-art`（分支 `agent/building-art`，已建好）。**首跑测试前先 `godot --headless --path stick-world --import` 连跑两遍**（否则 fx 类单测假红）。
> **与图标管线线的关系**：隔壁图标三渲二管线（`main` 分支）只提供美术规范（一面一色/cel 分档/1px 描边）；建筑贴图实际产线是 `modules/texture_gen/`（CPU 笔迹 + GPU stone shader），**不要把 icon_pipeline 当纹理产线**。

---

## 侦察结论（2026-09-09 已完成，勿重复摸底）

- **组合模式**：建筑 = 场景(.tscn) + 专属脚本(@tool BuildingExterior 子类) + 调色板(.tres BuildingPalette)。`modules/building_gen/api.gd:33-39` `_BUILDING_SCENE_PATHS` 注册表（placeholder/wall_tier1-3/wall_gate/barracks/warehouse/smithy_lv1 共 8 def）。
- **外观装配**：`buildings/building_exterior.gd`（217 行）`_build_exterior()` 分层拼装 L1_BackWall→L5_Roof，逐件调 `TextureGenAPI.make_straw_thatch/make_wood_pillar`；**拉伸模型**（左组合体锚左、右锚右、中间拉伸+按间距补柱）；子类 `_post_build` 挂件差异化（warehouse 货箱、barracks 军旗——几何 PLACEHOLDER）。
- **调色板**：`building_palette.gd` 茅草 3 + 木作 4 + 石作 3（`C_STONE_MAIN/DARK/JOINT`，批次 2 扩；旧 .tres 缺键时取脚本默认值，向后兼容）。
- **石头底子**：`modules/texture_gen/` 有 GPU Shader `stone_wall/stone_band/stone_window` + `shaders/lib/stone_lib.gdshaderinc`（SDF/笔触/配色原语）+ CPU `StoneBrickGen`（make_wall/gate/crenellated，批次 2 起色板可参数化并新增石带/拱洞/角石原语）+ CPU `stone_dark/metal_iron`；石作消费路径受渲染环境限制（见下节，仅 Sprite2D 可靠），BuildingExterior 已有石作装配函数。
- **铁匠铺**：参考图 `modules/building_gen/buildings/reference/smithy_lv1..4.png`（创始人手绘目标效果图，Lv1 另有 thatch 拆解图）；`config/buildings/buildings.tres` 已有 `smithy_lv1..4` 数据 def——批次 1 后已注册场景、**width 已从 2 改 16**（Excel 源 `建筑数据.xlsx` + 重导出，2 格=64px 塞不下参考图的宽棚三工位布局）。
- **多层/内饰**：`docs/技术/架构/建筑模块化设计.md` 有 Layers[0..N]/楼梯模块 B3 图纸（:551）与 stone 材质表（:87,158,178,430 checklist 未勾）——**零实现**；building.gd 契约已有 `Interior(Floor/Props/WorkSlots)` 节点位与 `interior_mode`（全部=0）；`mega_interior.tscn` 地图存在但与 def 未打通。
- **城墙类未外壳化**：wall_tier1-3/wall_gate 是场景内手绘单色 Polygon2D 耦合模式。
- **文档漂移**：building_gen/README.md 的 archive 漂移已修（批次 1）；`docs/技术/架构/场景与战斗/建筑与定居点.md` §4.3.4 规定新增建筑走 BuildingDef+BuildingGen 模块化路径。

## ⚠️ 渲染环境关键发现（批次 2/3/5 必读）

- **Polygon2D 的 uv 纹理采样在本环境坍缩为纹理平均色**（整面平涂、无笔触）。已排除：uv 越界、seed、filter、纹理内容、4/6 点几何、新建节点——均无效；`texture=null` 显示 vertex color、`make_solid` 显示纯色（即 uv 常数化）。Sprite2D 路径采样完全正常。**坍缩含 alpha 平均**：straw_thatch 稀疏笔触纹理平均后 alpha 极低（后墙几乎隐形）。
- **材质路径六格矩阵实测（批次 2，实验脚本留档 `tools/baking/render_stone_probe.gd`，复跑可重现）**：① Polygon2D + 纹理 uv 全 0..1 → 平涂（格内亮度 std=0.0000）；② Polygon2D + uv 收窄开区间 (0.005,0.995) → **同样平涂**；③ Sprite2D + CPU 纹理 → 有笔触（std=0.16）；④ Sprite2D + stone_wall ShaderMaterial → 有笔触（std=0.07，输出偏暗）；⑤ Polygon2D + ShaderMaterial → 平涂；⑥ Sprite2D + region 平铺 ×2 → 有笔触。**结论：Sprite2D 是唯一可靠显示路径；石作定案 CPU 纹理（StoneBrickGen）+ Sprite2D，GPU stone shader 仅作 Sprite2D 载体的备选。**
- **BackWallTop 平涂已修（批次 4）**：批次 1 遗留的「uv 收窄法无效」问题，批次 4 已把三栋标准外壳的 BackWallTop 统一换 thatch_layered + 提亮乘色 `_backwall_tint()`（见 building_exterior.gd）。
- **绕过方案（已落地于 smithy_lv1）**：`smithy_lv1.gd` 的 `_make_slope_thatch_tex()`——按坡面梯形/平行四边形逐行裁剪 layered 茅草图、坡外透明，Sprite2D 显示；`RoofMain/RoofLeftGroup1` 已 visible=false 由 RoofRightSlope/RoofLeftSlope 顶替。**坡面 mask 边界必须与原 Polygon2D 几何一致**，否则盖住烟囱前桁架（第 3 轮评审的回归教训：底行右缘收进后桁架才可见）。
- **茅草纹理用 `TextureGenAPI.make_thatch_layered(w,h,seed)`**（层叠茅草束，自带金色系配色），不要用 `make_straw_thatch`（笔触太弱近似平涂）；seed 决定行色序列。原色偏暗，Polygon2D 大面使用需 >1 的提亮 self_modulate 才可读。
- **`--script` 模式 `_ready` 时序陷阱（批次 4 踩坑）**：`SceneTree._initialize()` 上下文里 root 尚未进树，`add_child` 的子节点 `_ready`（@tool 程序化装配）延迟到首帧；此时同步 `call()` 依赖装配结果的方法会全部落空（症状：首栋渲染裸壳 + `Tween started with no Tweeners`）。修复：add_child 后先 `await process_frame` 再驱动状态（见 render_interior.gd 注释；render_smithy.gd 未踩坑是因为它 call 的 set_state 不依赖 _ready 产物）。
- **程序化建节点必须刷新基类成员引用**：`Building._lookup_children` 在 `_ready` 先跑，程序化 Interior 建立在其后——基类 `_interior` 成员若不手动刷新，`_set_transparent` 里 `Interior.visible` 切换被 null 守卫静默跳过（症状：进屋态 Interior 永不可见）。building_exterior.gd `_build_interior()` 已做 `_interior = interior` 刷新。
- **16 格标准外壳的后墙被左右屋顶坡完全遮挡**（外视角）——后墙纹理只有 smithy 这类异形坡面才露出；进屋态渲染图的"黑背景"是屋脊间空隙（夜景氛围），Interior 家具对比反而突出，不按缺陷处理。
- **视觉验收渲染工具**：`tools/baking/render_smithy.gd`（单栋外观）与 `tools/baking/render_interior.gd`（批次 4，四栋进屋态：`set_state(2)` + `_set_transparent(true)`，输出 `user://interior_render_<def_id>.png`）。`godot --path stick-world --script res://tools/baking/<脚本>.gd`，非 headless。注意：① 场景默认 PLANNED 态 alpha=0.3，验收图须 `set_state(2)` 切落成态；② `--script` 模式下别引用全局类名（class_name 解析不可靠，用 duck typing）；③ 改完 .gd 先 `--import` 再渲染，否则卡死。
- **测试编排**：`run_all.sh -Match` 是**子串匹配**不是正则，多目标要循环单跑。
- **【批次 6 补】GameRoot 启动默认深夜，观感验收渲染须推到正午**：EnvironmentSystem 的 CanvasModulate 会把地表压成剪影（星月/极光夜景），截图验收前须 `env.set_seconds_per_day(100000)` 冻结昼夜 + `env.set_time_of_day(12.0)` 推正午，再等约 2.5s 让 `_night` lerp 收敛（render_village.gd 已内置）。
- **【批次 6 补】村庄级验收渲染用「真加载」而非 --script**：`tools/baking/render_village.gd` 走 .tscn 入口正常启动（autoload 在场），实例化 game_root.tscn 完整走 map_loaded → 初始建筑 → 资源点 → NPC 全链路；GameRoot 每次启动都是新游戏（不自动读档，game_root.gd:472），无存档污染，`--script` 模式拿不到这套环境。全景图用临时 Camera2D（zoom 0.5）+ Image 裁掉上下露白。
- **【批次 6 补】出生点 ±20px 是建筑放置禁区**：game_root 先 spawn 玩家（X=0，y=945 落在建筑体 Y 判定带 [baseline-390, baseline] 内）再放初始建筑，`construction_manager._entity_blocking` 会对 X∈[left-20, right+20] 覆盖 0 的选址拒绝——铁匠铺右缘须 ≤ cell -2、宅邸左缘须 ≥ cell 1。
- **【批次 6 补】资源点净空带天然覆盖村庄布局区**：resource_gen `_forest_rate` 对土路带（cell -40..40）外 30 格（960px）净空不长树，布局最远建筑（cell -51）也在净空内，无需清树处理。
- **【批次 6 补】expand_map 的 WALKABLE_MARGIN=128 格会把数据边界推到 ±6144**（64 格粒度向上取整）：突破 town_siege 收窄值 ±2160，但 SiegeWall 物理墙挡住玩家，无实际逃逸（既有行为，未恶化处理逻辑）。
- **【批次 6 补】`-Match siege` 在本分支实际只跑 unit 批量**：tests 套件清单无 siege 命名的 integration 套件（siege_wall_showcase/snapshot_siege 是 tests/dev 工具）；TownSiege 出口序列完好性由 diff 复查（挂载位零改动）+ test_cross_map_travel/test_village_map 绿共同背书。
- **批次 3 补充**：① `--check-only --script` 检查单脚本时 `EventBus` 等 autoload 不存在，报 `Compile Error: Identifier not found` 属**假阳性**（真实工程加载时 autoload 在场），只认 Parse Error；② GDScript 类型推断坑：`var cx := (xs[i] + xs[i+1]) * 0.5`（xs 为无类型 Array）直接 **Parse Error**，数组元素参与推断须显式 `var cx: float = (float(xs[i]) + float(xs[i+1])) * 0.5`；③ 多层结构件沿用 Sprite2D+CPU 纹理路径**零新坑**（灰泥噪点图/茅草坡面裁剪/纯色块几何全部正常显示），批次 5 可放心复用同一套装配函数。
- **Excel 管线漂移**：`export_excel.py` 是全量重导（18 表），仓库里 `config/formations/presets.tres` 存在手写旧版 vs Excel 管线版的漂移（重导必连带刷新它，批次 1/4 均还原不背书，合并后同样处理）——待数据表负责人收口。
- **模块 API 转发层也是签名真相源（批次 5 踩坑）**：给 `ProceduralMaterials.make_thatch_layered` 加可选参数后忘改 `texture_gen/api.gd` 的转发签名，`building_exterior.gd` 经 `TextureGenAPI` 调 4 参报 `Too many arguments` Parse Error——并**级联**使所有 `extends BuildingExterior` 的子类全部 `Could not resolve class` 挂掉（症状像全局类缓存损坏，实际是父类解析失败）。排查看 `--check-only --script` 逐脚本报错即可定位真凶。改静态函数签名时，模块 api.gd 转发层必须同步。

## 任务分级调度（Pro / Flash × 会话批次）

> 通用验收：`check_godot_errors` 干净 + `run_all.sh` 全绿（construction/village_map 组必跑）+ 回填本档 + 中文提交。
> **美术观感验收一律派有视觉的 Subagent**（读渲染 PNG 对照参考图/《王国两位君主》标杆三判据：剪影可读/色块克制/远近景成立），主会话无视觉只做指挥与代码审查。
> 新会话开场白：「继续 建筑与美术升级 批次 N」。

| 批次 | 级别 | 任务 | 依赖 | 验收门 | 新会话必读 |
|---|---|---|---|---|---|
| **1** | Pro | **铁匠铺落地**：照 smithy_lv1 参考图做铁匠铺场景（BuildingExterior 石基+木作+茅草混合外观；炉火/铁砧/烟囱结构件）+ `_BUILDING_SCENE_PATHS` 注册 smithy_lv1 → 可建造可进；Interior WorkSlots 摆铁砧工位（为 C 线打铁行为供位）；顺手修 README archive 漂移 | 无 | 建造→施工→落成全链路可玩（手动/集成测试）+ 视觉 Subagent 对照参考图评审 | 本档侦察结论；`reference/smithy_lv1.png`（视觉 Subagent 读图）；`building_exterior.gd` 全文 |
| **2** | Pro | **石头结构件化**：BuildingPalette 扩石色槽；BuildingExterior 石作装配函数（石墙段/石带/石窗/角石——接线 texture_gen stone shader 现成原语）；落一个纯石头建筑 def（如石造仓库/瞭望塔）+ 砌缝/风化笔触调优 | 批次 1（沿用装配模式） | 石头建筑程序化生成、宽度可拉伸（复用拉伸模型）+ 视觉验收 | `texture_gen/` stone shader 与 stone_lib；`建筑模块化设计.md` 材质表 |
| **3** | Pro | **多层建筑（B3 图纸落地）**：Layers[0..N] 二层框架（阳台/悬挑/楼梯模块）+ 层间通行（Interior.Floor 打通 or 外部楼梯可见即可 P0）+ 天际线变体 | 批次 2 | 二层/三层建筑程序化生成 + 进出可玩 + 视觉验收（天际线观感） | `建筑模块化设计.md` :30-55, :551；`building.gd:10-31` 节点契约 |
| **4** | Pro | **室内内饰系统**：interior_mode>0 打通（进屋 → 家具 props 程序化生成：桌椅/床/货架/工作台，按建筑类型配比）+ mega_interior 接线评估 | 批次 1 | 至少 3 类建筑有可进入的美观内饰 + 视觉验收 | `building.gd` Interior 契约；`mega_interior.tscn` |
| **5** | Flash×N | **多材质家族扩充**：木骨石基(T2)/混合精修(地标) 变体 + wall_tier 系列外壳化改造（从耦合 Polygon2D 迁到 BuildingExterior）+ 茅草笔触迭代（thatch v11 管线参数调优） | 批次 2 | 每变体视觉验收过 + 建造测试绿 | — |
| **6** | Flash | **village_a 初始村美化**（创始人点名）：手写村换成有血肉的初始小镇——铁匠铺+仓库+若干民居（多层/多材质配比）+ 出口序列复查（TownSiege 挂载位不变） | 1/3 | 进村观感视觉验收 + siege_gate/menu 测试绿 | `village_a.tscn`；`initial_content.gd` |

**依赖图**：1 → 2 → 3；4 依赖 1；5 依赖 2；6 依赖 1+3。B 线（城镇生成）消费 2/3/5 的结构件能力。

## 进度记录（每批完成后回填）

| 批次 | 状态 | 提交 | 备注 |
|---|---|---|---|
| 1 | ✅ 完成（待创始人观感验收） | 37d68f15 | 铁匠铺 Lv1 落地：smithy_lv1.gd/.tscn/smithy_palette.tres + api 注册 + Interior/WorkSlots 两工位（铁砧/炉前）+ Excel width 2→16 + README archive 漂移修复 + render_smithy.gd 验收渲染工具。视觉 Subagent 4 轮评审 PASS（对比 reference/smithy_lv1.png）。**遗留 MINOR 打磨**（不阻塞）：桁架内衬偏亮、檐口垂挂须未做、两坡束形不一致。**发现 Polygon2D uv 采样坍缩环境 bug**——见上方「渲染环境关键发现」，批次 2/3/5 必读。 |
| 2 | ✅ 完成（待创始人观感验收） | 1f1f1564 | 石头结构件化：BuildingPalette 石色三槽（向后兼容）+ StoneBrickGen 色板参数化、新增石带/拱洞/角石原语 + BuildingExterior 石作装配函数（`_stone_wall/_stone_crenellated/_stone_band` 等，静态纹理缓存）+ 石造仓库 `stone_warehouse` 落地（Excel 加行重导出 + 场景/脚本/调色板 + api 注册）。构图=双层城垛+拱窗按间距均布+拱形木大门+角石列+垛后旗杆；16 格全宽与 8 格窄版渲染验证拉伸模型（窗随宽自动增减）。视觉评审 2 轮 PASS（第 1 轮修石带过蓝：self_modulate 压暗）。验收渲染工具 `tools/baking/render_stone_warehouse.gd`，输出 `user://stone_render.png`。**遗留 MINOR**（不阻塞）：角石列观感偏弱、后墙垛口压顶偏亮。**注意**：export_excel.py 全量重导出会重写 `config/formations/presets.tres`（编制预设格式大改、数据 id 等价）——非本批次范围未提交（工作区已还原），待编制专线确认格式迁移。 |
| 3 | ✅ 完成（待创始人观感验收） | 9882bbdb | 多层建筑（B3 图纸落地）：**宅邸 `manor`** 二层半木悬挑建筑落地——一层暖石墙（拱门+拱窗 carve）+ 悬挑楼板（jettying 左右各出挑 36px+托架斜撑）+ 二层半木框架灰泥墙（竖柱/横梁/交替斜撑+直棂窗）+ 大坡茅草双坡（前坡梯形+后坡露头条贴脊）+ 穿坡烟囱 + **外部楼梯**（12 级实心剖面+斜侧梁+扶手，贴正面爬升对接阳台，P0 层间通行可见）+ 阳台（栏杆+托架）；`Interior/Floor` 场景节点预留（批次 4 内饰接线）。BuildingExterior 新增**通用多层装配件** `_plaster_tex/_timber_frame/_jetty_slab/_balcony/_exterior_stairs/_mullion_window`，smithy 坡面茅草裁剪法提升为基类 `_slope_thatch_tex`（子类改委托，无逻辑变化）；Excel 加行 manor（14 格 tier3 民居）+ api 注册；拉伸模型同批次 2（墙面整张生成+构件按间距铺，10 格窄版渲染验证）。验收渲染 `tools/baking/render_manor.gd`（`user://multi_story_render.png`）+ `render_skyline.gd`（`user://skyline_render.png` 四栋同框天际线对比）。视觉评审 3 轮 PASS（修烟囱悬空/后坡露头漂浮/窄版窗溢出/画布溢出）；测试 construction/village/building 全绿。**遗留 MINOR**（不阻塞）：茅草纹理偏鳞瓦感（批次 5 茅草迭代范围）、楼梯扶手与阳台栏杆衔接略重叠、二层左段墙面留白偏多。 |
| 4 | ✅ 完成（已合并） | 68dcc6bf | 室内内饰系统（分支 agent/building-art-interior，合并 4ed99871←…←68dcc6bf）：①Excel `interior_mode=1` 开通 placeholder/barracks/warehouse/smithy_lv1 四类 def 并重导出；②`interior_props.gd` 家具工厂库（床/通铺/桌凳/货架/武器架/火盆/油灯/木桶/淬火桶/麻袋/箱堆/墙挂件等 20+ 件，Sprite2D 纹理+Polygon2D 纯色安全路径）；③BuildingExterior 基类装配 Interior(Floor/Props)/InteractionZone（Area2D mask=2 玩家层，宽度自适应）/`_set_transparent` 覆盖（L4/L5 前景淡出等价 WallFront）；④四栋差异化内饰布局+进屋态渲染验收（`render_interior.gd`）；⑤行为层：building.gd interior_mode 门控 + 修复 `_complete()` 建造路径不应用 def 的断点（project.building_def 注入）。mega_interior 评估结论见下节。**独立复审修复轮**：复审 FAIL 四硬伤已修——兵营军旗 L5→L4 右柱顶（淡出后贴柱消除黑背景悬浮物）、兵营外观盾饰移贴前柱身（原半透明浮块无依托）、民居辣椒串移到横梁清晰可见段、民居储物箱放大提亮加板缝/包角/锁扣+凳子加大（原不可读）；顺带 MINOR：铁匠铺灰蓝煤斗串换梁下暖光挂灯。主会话已复检四图硬伤消失。 |
| 5 | ✅ 完成（待创始人观感验收） | eda6b01a | 多材质家族扩充（本会话无 Agent 工具，视觉评审为主会话自行读图完成，已注明）：①**wall_tier 系列外壳化**——新增 `wall_segment.gd`（@tool extends BuildingExterior，`wall_kind` 1/2/3 + `gate_mode` 场景注入，一脚本管四段），wall_tier1/2/3/wall_gate 四场景从手绘单色 Polygon2D 迁到「子类+调色板」模式（新增 wall_earth/wall_stone/wall_stone_dark 三块 palette），石作装配复用（StoneBrickGen+Sprite2D）；tier1 夯土大板块+草皮帽、tier2 垛口+箭缝窗、tier3 垛口+双石带+双箭窗、gate 拱洞+拱圈石+木门板；**PassageBarrier/StandPlatform 碰撞几何逐字节保留，wall_tier/can_stand_on/is_gate 字段与阶段 F 逻辑未动**；改造前后留底 `user://wall_render_before/after.png`（工具 `tools/baking/render_walls.gd`）。注意 `make_crenellated` 豁口周期 112px 按宽墙设计，窄墙段需手挖豁口（wall_segment.gd 已内置）。②**新变体×2**——`timber_cottage` 木骨民居（10 格 T2：石基+窄悬挑+半木+金茅草坡+烟囱，interior_mode=1，内饰=床/桌凳/油灯/火盆/货架/地毯/挂串）、`grand_hall` 议事厅（16 格 T3 地标：石砌角石基层+宽悬挑半木+外梯阳台+茅草大坡+**脊上钟楼**（塔身/钟窗/尖锥/旗），interior_mode=1，内饰=长桌/长凳/双火盆/武器架/货架/地毯）；Excel `建筑数据.xlsx` 加 2 行重导出（presets.tres 已还原），api 已注册。③**茅草笔触迭代 v12**——`make_thatch_layered` 加可选 `opts`（slant/stretch/slender/taper_min），**缺省时与 v11 逐像素一致（既有调用方零影响）**；推荐组合 `{"slant":0.42,"stretch":1.9,"slender":1.55,"taper_min":0.45}`，鳞瓦感消除；对比图 `user://thatch_compare_render.png`（工具 `render_thatch_compare.gd`）；`_slope_thatch_tex` 同步加 opts 透传。④**新基类通用件 `_thatch_roof`**（茅草双坡：后坡露头条收窄藏进脊边+对称脊线+脊梁+檐口+阴影线，opts 透传 v12）——村庄配比可直接复用；`render_interior.gd` TARGETS 已加两新变体。视觉评审 4 轮 PASS（画布裁切→露头条悬空→tint 两轮压橘）；construction/village/siege/building 测试全绿。**遗留 MINOR**（不阻塞）：茅草乘色仍偏暖橘（layered 原色红棕行主导，tint 已到边界；根治需动 v11 调色板默认色，与向后兼容红线冲突，待创始人定夺）；tier1 夯土板块在 1 格宽下纹理感弱；manor 既有后坡露头漂浮（未迁移通用件，批次 3 遗留）。 |
| 6 | ✅ 完成（已合并） | b2909176 | village_a 初始村换血肉（分支 agent/building-art-village）：InitialBuildingsList 注入 4 栋（西村口民居 cell -51/石造仓库 -34/铁匠铺 -17/宅邸地标 1）+ spawn_initial_warehouse 挪 cell 17 充东侧民居兼搬运取货点，五栋衔接成主街（巷道 2 格）；NPC 生成点 1050→1100 避障；TownSiege 挂载位零改动。渲染验收 `tools/baking/render_village.gd`（真加载 GameRoot，输出 `user://village_render_entry/overview.png`）。测试 siege/village/menu/construction 全绿 + smoke×2 绿 + check_godot_errors 干净。视觉评审 2 轮 PASS（第 1 轮夜景版 FAIL → 推正午重截）；主会话已复检两图。**遗留**：民居两栋同用 placeholder 无差异（timber_cottage 可收口）；全景图顶部少量天空露白（SkyDecor 视差上限，仅渲染工具观感）；`-Match siege` 无真实 integration 套件背书（靠 diff+cross_map_travel 组合验证，建议后续补守城链路测试）。 |

## 关键决策速查

| 决策 | 结论 |
|---|---|
| 石头路径 | 走 texture_gen 现成 stone shader 结构件化，不另起 3D 管线 |
| 石作材质路径 | **CPU StoneBrickGen + Sprite2D**（批次 2 六格矩阵实验定案）；GPU stone shader 仅作 Sprite2D 载体备选 |
| 铁匠铺基准 | 创始人手绘参考图 smithy_lv1..4.png 为目标效果图 |
| 美术先放 | 图标管线仍在迭代时，结构件先程序化上（创始人允许），手绘替换留后 |
| 多层通行 | P0 外部楼梯可见 + Interior.Floor 预留，层间室内连通待实测定 |
| 多层装配件 | 收口在 BuildingExterior 通用函数（`_timber_frame/_jetty_slab/_balcony/_exterior_stairs/_mullion_window`），manor 为消费范例，村庄配比与 B 线城镇生成可直接复用 |
| 视觉验收 | 主会话无视觉，一律派视觉 Subagent 对照参考图评审 |

## 任务收口状态（2026-09-09，批次 1-6 全部完成）

- **分支 `agent/building-art` 最终提交**：`3b9122e3`（批次 1-6 全部合入）。`agent/building-art-interior`、`agent/building-art-village` 已合并删除，worktree 已收线。
- **回归**：`--import`/`check_godot_errors` 干净；construction/village/siege/menu 组 + smoke 全绿（见下方风险项）。
- **待办**：①创始人观感验收（六批全部渲染图在 `user://`：smithy/stone/multi_story/skyline/interior_*/wall_*/thatch_compare/variant/village_render_*）；②验收通过后本档移入 `docs/项目/交接/归档/`。
- **⚠️ 性能风险（合并后发现）**：`test_new_game_smoke` 耗时涨到 ~138s（超时线 150s）——村庄初始注入 5 栋程序化建筑，每栋 CPU 纹理逐像素生成（石墙/茅草 layered/坡面裁剪）叠加所致。静态纹理缓存只对同参数纹理生效，不同宽度仍各自生成。建议后续优化：装配异步化/纹理缓存按内容哈希共享/或调高该套件超时，勿静默放过。
- **跨批次遗留汇总**（均 MINOR 不阻塞，详见各批次行）：茅草乘色偏暖橘（根治需动 v11 默认调色板，待创始人定夺）；檐口垂挂须/桁架内衬偏亮/两坡束形不一致；manor 后坡露头漂浮+二层左段留白；民居两栋同款（timber_cottage 可入村替换）；`-Match siege` 无真实 integration 套件；mega_interior TELEPORT 未实现（评估见下节）；presets.tres 手写版 vs Excel 管线版漂移待数据表负责人收口。

## mega_interior 接线评估（批次 4 结论：本批次不实现）

`mega_interior.tscn`（`modules/world/scenes/maps/`）实为**空 VillageMap 骨架**：`village_map.gd` 脚本 + 全套空容器（PlacementGrid/BuildingHost/EntityHost 等）+ ExitTrigger（`mega_interior_exit_trigger.gd`），无任何室内内容。

**打通 interior_mode=2（TELEPORT）的链路现状**：
- 触发侧已实现：建筑 EnterTrigger → `building.gd _on_enter_trigger_body_entered` → `EventBus.mega_interior_entered`（EnterTrigger 节点场景件仍未建，机制同 InteractionZone 可程序化补）。
- 消费侧骨架已有：`travel_handler.gd` 订阅 mega_interior_entered/exited（game_root.gd:766 注释）。
- 缺口：①map_id → 场景的注册/查找路径；②mega_interior 地图的室内内容生成（对应建筑类型的布景+工位+出口）——**主要工作量在此**；③出入口回传坐标约定。

**结论**：不实现。interior_mode=1（TRANSPARENT）已满足"进屋可见内饰"P0 观感验收；TELEPORT 依赖旅行切换 + 室内地图内容管线，建议挂靠城镇生成线（B 线，可直接复用批次 4 的 `InteriorProps` 家具库布景）或单独开一个 Pro 批次。机制层约 70% 已存在，剩余估 1 个 Pro 批次量级。
