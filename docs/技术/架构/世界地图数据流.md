# 世界地图数据流：从程序化产物到战略图

> **状态**：架构基线契约。管线两端部分已实装——L3/L2 视图包已导出并接入运行时战略图（M 键 L3 / 下钻 L2）、L1 单层战略图已实装（Tab 键，消费端现状见 §8.4）；L1 程序化生成与 §2.1 标注「待实现」的桥接 JSON 仍为生成端工作项。各节未实装部分以文中「待实现」标注为准。
> **关联**：
> - [`../../设计/系统/08-程序化世界生成.md`](../../设计/系统/08-程序化世界生成.md)（生成端）
> - [`战略图架构.md`](战略图架构.md)（消费端，配套文档）
> - [`场景与战斗架构.md`](场景与战斗架构.md) §三（场景图侧的地图系统）
> - [`数据流.md`](数据流全景.md) §3.2（静态配置 .tres 约定）
> - [`模块API.md`](模块API契约.md)（region_id 类型约定）

---

## 〇、本文档要解决的问题

程序化世界生成（Python 工具链）和战略图模块（`modules/world_map/`）分别独立设计，在「产物交付」这一步出现断点：

- 生成端产出的是 PNG 掩码 + numpy 高度场 + 将来的多边形 JSON
- 消费端期望的是 `region_mask_texture`（像素 RGB 编码 region_id 的索引图）+ 三级粒度数据模型（`ContinentData`/`RegionData`/`TileData`，见 §六）

本文档定义两者之间的**转换链路、数据契约、导入器接口**，作为后续编码的真相源。

---

## 一、术语对照（先统一语言）

### 1.1 两类地图的本质区分

游戏内同时存在两类「地图」，过去文档混用「地图」一词导致歧义。本文档明确区分：

| 术语 | 英文 | 定义 | 模块 | 玩家是否在其中 |
|------|------|------|------|--------------|
| **战略图** | Strategic Map | 鸟瞰多边形领土的「看地图」，用于指挥、缩放、外交、查看版图 | `modules/world_map/` | **否**，玩家在场景图里，战略图是「右上角小地图」放大版 |
| **场景图** | Scene Map | 玩家实际所在的卷轴/独立场景，火柴人在其中真实移动 | `modules/world/` 等 | **是**，玩家操控的单位在场景图内 |

> **类比**：P社游戏中「政治地图模式」（战略图）vs「打开一个省份看战斗」（场景图）。战略图永远不挂载 StickmanEntity，只显示色块/图标/战线。

### 1.2 GDD §6.7 的六类地图全部是场景图

GDD §6.7 列举的村落/战场/道路/室内/大建筑/山坡六类，**全部是场景图**。战略图不在该表中，是独立的第七类（或者说，是与六类正交的「元视图」）。

### 1.3 三级层级与 GDD 五层级的对应

程序化生成文档的 L3/L2/L1 与 GDD §4.2 的五层级管理深度是同一套层级的两种视角：

| 程序化文档术语 | GDD §4.2 术语 | 行政语境 | 战略图缩放级别 | 对应战略图数据粒度 |
|--------------|--------------|----------|--------------|------------------|
| **L3 大世界** | 第5层 帝国级 | 整个已知世界 | 最远（看全大陆） | `ContinentData`：大陆轮廓 + 地区多边形 |
| **L2 地区** | 第4层 行省级 | 区域/行省 | 中（看一个地区） | `RegionData`：地区多边形 + 地块多边形 |
| **L1 地块** | 第3层 城市/郡县级 | 城市/郡县 | 近（看一个地块） | `TileData`：地块多边形 + 聚落点位 |
| —（场景图） | 第2层 镇/村级 | 镇/村 | 进入聚落即切到场景图 | `VillageMap` 等六类场景图 |
| —（玩家小队） | 第1层 个体/班组 | 排/班 | 场景图内附身操控 | `StickmanEntity` |

> **关键**：战略图只覆盖 GDD 第5/4/3 层级。第2层（聚落）和第1层（玩家小队）属于场景图范畴，不在战略图模块内。从战略图「点击聚落」即触发 `SceneLoader.load_map()` 切换到场景图。

---

## 二、生成端产物清单

来自 [`程序化世界生成.md`](../../设计/系统/08-程序化世界生成.md) §十二的数据流，按交付阶段分类。

### 2.1 阶段 A：世界模板（开发期生成，随包发布，永不再变）

| 产物 | 格式 | 来源 | 内容 |
|------|------|------|------|
| `locked_continent_8192.png` | PNG（L 模式二值） | `fractal_continent.py` | 大陆掩码，1=陆地 0=海洋，8192×8192 |
| `locked_continent_8192.png.npy` | numpy uint8 | 同上 | 同上的数组形式，便于 Python 处理 |
| `fractal_heightmap_8192.npy` | numpy float32 | 同上 | 高度场，值域 0-1，含外海/内湖/山脉 |
| `preview_fractal.png` | PNG RGB | 同上 | 高度场彩色预览（8 色分级），人眼查看用 |
| `world_map_l3.png` | PNG RGB | `world_map.py` | L3 群系+河流渲染图（当前阶段产物） |
| **`continent_data.json`** | JSON | **待实现** | L3 大陆元数据：大陆名、地区列表、地区多边形顶点、地区邻接图 |
| **`regions_data.json`** | JSON | **待实现** | L2 地区元数据：每个地区内的地块多边形、地块邻接图、地块群系 |
| **`tiles_data.json`** | JSON | **待实现** | L1 地块元数据：每个 L1 地块的聚落（**1 个 L1 地块 = 1 个聚落或空聚落**，§0.19）、资源禀赋、河流裁剪 |
| **`political_data.json`** | JSON | **待实现** | 政权数据：政权/联盟/归属/都城（程序化文档 §15） |

> 标注「待实现」的三份 JSON 是桥接文档要求生成端补产的，目前 `fractal_continent.py` 只产出 PNG/npy，未产出结构化多边形数据。**这是生成端的工作项，不是消费端的工作项**。

### 2.1.1 实际产出（2026-08）：L2 地区划分与图包素材

L2 地区划分已由 `region_split.py`（watershed 沿地形切分）+ 创始人人工调整（HTML 工具合并/画线切口）定稿为 **13 个地区**。当前实际产出（位于 `tools/worldgen/output/`）：

| 产物 | 路径 | 内容 |
|------|------|------|
| 地区标签图 | `regions/region_labels.npy` | 2048² int32，每像素地区 ID（0=海洋，1..13=地区） |
| 地区元数据 | `regions/region_data.json` | 每地区：类型（continent/island）/面积/邻接/多边形（2048 坐标） |
| 地区预览图 | `regions/region_preview.png` | 唯一色预览 |
| **L2 图包素材** | `l2_packs/` | 见下方逐文件说明 |
| **L2/L3 视图包** | `l2_view_packs/` + `l3_view/` | 运行时战略图素材（拷贝至 `stick-world/config/strategic_map/`） |
| **全大陆老 L1 + 城市层** | `l1_v2/` | L3 层老 L1 划分（69 块）+ 之下城市细分（1040 城，8192 级）：`legacy_l1_labels_8192.npy` / `city_labels_8192.npy` / `city_data.json`，工具 `l1/city_split_v2.py`（--res 8192，按老 L1 分组膨胀 + 面积下限） |
| 画线切口计划 | `output/region_cut_plan.json` | 用户涂鸦切割线（工具 `isthmus_cut_tool.html` 导出） |

**L2 图包素材（`l2_packs/`，由 `export_l2_packs.py` 生成）**：

```
l2_packs/
├── color_map.json          # 索引图颜色 -> 地区 label
├── regions_meta.json       # 全部地区元数据汇总（bbox_8192/邻接/文件清单）
├── README.md               # 格式与坐标约定
└── region_XXX/             # 每地区一个目录
    ├── mask_8192_full.png  # 8192 全图蒙版（海岸线 8192 级：定稿划分放大 + 8K 大陆蒙版裁切）
    ├── mask_8192.png       # 8192 裁切蒙版（与 base/heightmap 对齐）
    ├── base_8192.png       # L3 底图裁切（preview_fractal.png，bbox 外扩 80px）
    ├── heightmap_8192.npy  # 高程裁切（float32，与 base 对齐）
    ├── heightmap_8192.png  # 高程可视化
    ├── tiles_8192.npy      # L1 地块标签图（8192 级，人工定稿状态）
    ├── tiles.json          # 地块元数据（8192 级共享网格 polygons/holes/color/centroid）
    └── info.json           # 该地区定位元数据
```

**坐标约定**：裁切文件（mask/base/heightmap）像素一一对齐；全局 8192 坐标 ↔ 2048 坐标换算为 ×4 / ÷4。**海岸线精度**：mask 系列海岸线为 8192 级（8K 大陆蒙版裁切），地区内部划分语义保持 2048 定稿。

**消费用途**：L2 地区图底图、L2 内部 L1 地块细分（用裁切高程 + watershed）、战略图地区级下钻（渲染几何取自 `tiles_8192`，无需索引图）。生成工具链：`region_split.py` → 人工调整（合并/画线切口，工具在 `archive/`）→ `export_l2_packs.py`（图包导出）→ `export_l2_maps.py`（L1 地块分块）→ `update_tiles_coastline.py`（按 8K 蒙版裁切海岸线）→ `export_l2_view_packs.py`/`export_l3_view.py`（运行时视图包），详见 [`程序化世界生成.md §二十一`](../../设计/系统/08-程序化世界生成.md)。

**运行时视图包（`config/strategic_map/`，L3→L2 下钻用）**：

```
config/strategic_map/
├── l3_world.json / l3_world.bin / l3_partition_2048.png   # L3：8192 级共享网格 + 2048 索引图（hover 查询）
├── l3_terrain.png                            # L3：程序着色地形底图（B2，2048；TERRAIN 模式异步加载）
├── l1_world.json / l1_world.bin / l1_base.png / l1_mask.png # L1（Tab）：出生老 L1 上下文（城市细分）
├── l1_packs/l1_%03d/                         # L1（L2→L1 下钻）：全部 69 个老 L1 上下文，结构同左
│   └── l1_world.json + l1_world.bin + l1_base.png + l1_mask.png   # parent_l1_label 标识所属老 L1
└── l2_packs/region_XXX/                      # L2：每地区一个视图包（懒加载）
    ├── l2_world.json / l2_world.bin          # 8192 级 polygons/holes/color/centroid + global_l1_label
    ├── l2_geom.bin                           # 烘焙 mesh（earcut 剖分 + 同源描边，运行时零剖分）
    ├── l2_terrain.png                        # 程序着色地形底图（B2，8192 级 context 裁切，虚空透明）
    └── l2_tiles_index.png                    # 8192 级地块索引图（label 直编，hover 查询）
```

> **道路数据（E1，`road_generate.py`）**：各 `l1_world.json` 的 roads 从 `{from,to}` 直线对扩展为
> `{from,to,tier,length_px,polyline[]}`（tier：DIRT 土路 / PAVED 官道=两端 T3+ / HIGHWAY 预留；
> polyline 为该包 context 本地坐标 `[x,y]`，端点与聚落 `position_px` 对齐）。边集 = L1 内 MST 骨架
> + 相邻老 L1 最近聚落对 + T3+ 重镇 K 近邻；A* on 1024 降采样高度场（cost=坡度+河湖惩罚，海洋
> 不可通行），山口豁免 = 连线剖面鞍点邻近格坡度减免；Chaikin×2 + DP 平滑。参数外置
> `tools/worldgen/l1/road_params.json`，密度验收看 `output/roads_preview_2048.png`。群岛跨海边
> 无陆路不产 polyline（渲染回退直线、旅行判不可达）。另产 `config/strategic_map/roads_global.json`
> （8192 全局坐标全大陆路网，含跨 L1 边）= E3 快速旅行连通性 / E4 道路场景数据源。运行时
> `l1_world_data.gd` 现只读 from/to 画直线（向后兼容），polyline 渲染接线属 E2。

> **数据紧致化（2026-08）**：各 `l*_world.json` 的同名 `.bin` 为紧凑二进制（magic `LWDB` + ver + `var_to_bytes`，由 `tools/worldgen/l_world_bake.gd` 在素材阶段烘焙，`l3_l1.json/l3_city.json` 同法）。运行时 data 类 **bin 优先**、bin 缺失/格式不符（如 Godot 升级）**自动回退 JSON**。两种坐标语义（各自保持现状，避免动渲染逻辑）：
> - **L2/L3**：json 顶点 `[y,x]` → bin `PackedVector2Array(Vector2(x,y))`，省逐点 Array 构造 + 体积省 ~32%（L3 打开 162ms → 33ms）
> - **L1**：json 顶点 `[x,y]`，bin 原样序列化（polygon 保持 Array，构造逻辑不变，加载省 ~15-30ms）
> json 保留为生成器源码与回退；新增/重跑生成器后需重跑 `l_world_bake.gd` 刷新 bin。

> **L2→L1 下钻（2026-08）**：L2 单击任一 L1 地块 → 打开该老 L1 的 Tab 视图（`api.open_l1` 加载 `l1_packs/l1_%03d/`，ESC 返回 L2）。定位依据 = `l2_world.json` 每地块的 `global_l1_label`（`export_l2_view_packs.py` 将 `tiles_8192` 与 `legacy_l1_labels_8192` 逐像素对齐、多数投票：局部 label → 老 L1 全局 label，如 region_013 局部 1/2/3 ↔ 全局 67/68/69）。`l1_packs/` 由 `export_l1_view_context.py --out-dir` 批量导出（69 个老 L1 全覆盖）。

> **L2 烘焙（2026-08）**：`l2_geom.bin` 由 `l2_export/l2_bake.py` 在素材阶段生成——earcut 三角剖分 + 描边**同源**（填充与描边共用 `mesh_extract.simplify_mesh` 的金标准多边形，Hausdorff=0.0px，无补丁感）；形状处理为 Chaikin×3 平滑像素台阶（保留 ≥3px 真实尖角）+ DP(tol=0.2) 降顶点。运行时 `l2_map_renderer.gd` 直接组装 ArrayMesh，零运行时剖分。

### 2.2 阶段 B：每局运行时扰动（仅规模）

| 产物 | 格式 | 来源 | 内容 |
|------|------|------|------|
| `run_data.tres` | Godot Resource | `RunPerturbator`（运行时） | 仅聚落规模 ±15% 扰动，其他全部不变 |

### 2.3 阶段 C：按需可玩地图（玩家进入聚落时）

不属于本数据流——这是场景图侧的事，由 `SettlementGenerator` 产 `MapDefinition.tres` 喂 `SceneLoader`。详见 [`场景与战斗架构.md`](场景与战斗架构.md) §三。

> **1 个聚落 = 1 张主场景图**（见 [`程序化世界生成.md §0.19`](../../设计/系统/08-程序化世界生成.md)）。空聚落（有 ID/位置、无人无建筑）无可玩地图、不可进入。

---

## 三、消费端输入清单

`modules/world_map/` 的战略图渲染需要以下输入（来自 [`战略图架构.md`](战略图架构.md) 的重新设计）。

### 3.1 三级缩放各自的输入

战略图按缩放级别加载不同粒度的**独立地图包**，每包含地形底图（程序着色）+ 矢量数据：

| 缩放级别 | 地图包 | 包内容 | 用途 |
|---------|--------|--------|------|
| **L3 大世界** | `l3_world.json/.bin` + 纹理 | 程序着色底图 l3_terrain.png(2048 起) + 河流矢量 + 矢量多边形 | 看全大陆，地形图/政治图双模式 |
| **L2 地区** | `region_<id>` 视图包 | l2_terrain.png（同管线裁切）+ 矢量多边形 + 地标 | 看一个地区，底图 + 城市色块/blob |
| **L1 地块** | `l1_world.json/.bin` + 底图 | 矢量多边形 + 聚落数据 + 道路 + 湖泊 | 看一个地块，**纯矢量 + 城市 blob** + 河流 polyline |

> **关键**：每个层级的地图包是**独立的**，粒度切换 = 卸载旧包 + 加载新包。不是同一张图的不同着色逻辑。详见 [`战略图架构.md`](战略图架构.md) §4.2。

### 3.2 region_mask 索引图规范（P社 provinces.bmp 同款）

战略图模块沿用 P社 Clausewitz 引擎的省份索引图机制（[`战略图架构.md`](战略图架构.md) §三详解）：

- **格式**：PNG RGB，每像素的 R/G/B 三通道编码一个 `region_id`
- **编码**：`region_id = (R << 16) | (G << 8) | B`，最大支持 1677 万个地块
- **ID 分配规则**：
  - L3 级别：每个地区一个 `region_id`，从 1 开始连续编号
  - L2 级别：每个地块一个 `region_id`，**与 L3 的地区 ID 独立命名空间**（避免冲突，前缀区分）
  - L1 级别：每个聚落一个 `region_id`（**1 个 L1 地块 = 1 个聚落或空聚落**，§0.19），聚落 ID 在 L1 地图内唯一（配合 L1 地图 id 唯一确定；空 L1 地块 = RGB(0,0,0)）
- **背景色**：`RGB(0,0,0)` = 海洋/无地块/空 L1 地块，渲染时跳过

### 3.3 WorldMapData 结构（运行时数据容器）

战略图模块的运行时数据容器（替代现有的 `WorldMapData` 单一粒度设计），按三级粒度分层：

```gdscript
# 详见 战略图架构.md §四
class_name StrategicMapData extends Resource

# L3 大世界
var continent: ContinentData
# L2 地区（按 region_id 索引，懒加载）
var regions: Dictionary  # {region_id: RegionData}
# L1 地块（按 tile_id 索引，懒加载）
var tiles: Dictionary  # {tile_id: TileData}
# 当前缩放级别
var current_granularity: int  # 0=L3, 1=L2, 2=L1
# 当前聚焦的父级 ID（L2 时是 region_id，L1 时是 tile_id）
var focused_parent_id: String
```

---

## 四、转换链路总览

```
┌─────────────────────────────────────────────────────────────┐
│  生成端（开发期 Python 工具）                                │
│  tools/worldgen/                                            │
│                                                             │
│  fractal_continent.py → 8192 掩码 + 高度场 + 河流           │
│  region_generator.py   → continent_data.json（L3 地区多边形）│
│  tile_generator.py     → regions_data.json（L2 地块多边形）  │
│  settlement_placer.py  → tiles_data.json（L1 聚落布局）      │
│  state_expander.py     → political_data.json（政权/联盟）    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼  阶段 A 产物（8192 原始数据 + JSON）
┌─────────────────────────────────────────────────────────────┐
│  地形底图渲染器（开发期 Python 工具，全程序化着色）           │
│  tools/worldgen/l3/biome_generate.py + terrain_render.py     │
│                                                             │
│  输入：8192 高度场 + 大陆掩码 + 河流/湖泊 mask（无贴图素材）  │
│  产物：                                                      │
│    ├─ biome_labels_2048.npy（Whittaker 温湿矩阵七群系标签）   │
│    ├─ l3_terrain.png（2048 起，不足升 4096；群系基色×高度明度 │
│    │   +hillshade+水体+海岸线，整大陆程序着色）               │
│    ├─ l2_packs/region_XXX/l2_terrain.png（每地区裁切）        │
│    └─ 河流/湖泊矢量随视图包导出（L1 运行时 draw_polyline）    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼  地形底图 + 群系/河流数据
┌─────────────────────────────────────────────────────────────┐
│  打包器（开发期 Python 工具，一次性）                         │
│  tools/worldgen/l2_export/                                   │
│                                                             │
│  输入：原始 JSON + 地形底图 + 河流/湖泊矢量                   │
│  产物：                                                      │
│    ├─ config/strategic_map/l3_world.json/.bin + l3_terrain   │
│    ├─ config/strategic_map/l2_packs/region_XXX/              │
│    ├─ config/strategic_map/l1_world.json/.bin + l1_packs/    │
│    └─ 河流矢量随视图包导出（L1 运行时叠加）                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼  随包发布的 Godot 资源（多层级独立地图包）
┌─────────────────────────────────────────────────────────────┐
│  消费端（运行时 GDScript）                                   │
│  modules/world_map/                                         │
│                                                             │
│  StrategicMapData.load_layer(granularity, parent_id)        │
│    → 加载对应层级的地图包（.tres + 纹理 PNG）                │
│  MapRenderer 按层级差异化渲染：                              │
│    · L3：预渲染底图 + Shader 叠加边界/政治色                 │
│    · L2：预渲染底图 + Shader + 动态地标图标                  │
│    · L1：地形底图 + 动态聚落建筑群 + 道路 + 资源             │
│  玩家缩放/双击 → 切换地图包 → 卸载旧包 + 加载新包            │
│  跨地区作战 → 拼接预览模式（多包拼接显示）                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、导入器接口设计

### 5.1 底图渲染与打包定位

两者都是**开发期 Python 工具**，不是运行时 GDScript。地形底图渲染器把生成端的 JSON + PNG + numpy 高度场**程序化着色**成底图 PNG（群系基色×高度明度 + hillshade + 水体 + 海岸线，零贴图素材）；打包器把底图 + JSON 转成 Godot 可直接 `load()` 的资源随包发布。一次性运行。

**位置**：`tools/worldgen/l3/biome_generate.py`（群系标签）+ `tools/worldgen/l3/terrain_render.py`（程序着色底图）+ `tools/worldgen/l2_export/`（视图包导出）

**与生成器的关系**：底图渲染在生成器之后运行，输入是生成器的产物（高度场/掩码/群系标签/河流矢量）。

### 5.2 渲染与打包 CLI

```
# 步骤 1：群系标签生成（Whittaker 温湿矩阵）
python tools/worldgen/l3/biome_generate.py

# 步骤 2：程序着色底图（L3 全图 + L2 每地区裁切）
python tools/worldgen/l3/terrain_render.py

# 步骤 3：打包成 Godot 资源（JSON + PNG 到 config/strategic_map/）
python tools/worldgen/l2_export/export_l2_view_packs.py
```

### 5.3 程序着色底图生成（terrain_render.py）

把"原始数据（高度场/群系标签/河流湖泊 mask）"着色成"玩家看到的地形底图"，算法与产出对齐总体设计 §5.3：

**输入**：
- 8192 大陆掩码 + 高度场
- `biome_labels_2048.npy` 七群系标签（biome_generate.py 产物）
- 河流/湖泊 mask + 河流矢量（流量）

**输出**：

```
output/
├── l3_terrain.png                       # 2048 起（不足升 4096），整大陆程序着色
├── l2_packs/region_XXX/l2_terrain.png   # 每地区按 world_bounds 裁切
└── 河流矢量 JSON 随视图包导出（L1 运行时叠加）
```

### 5.4 程序着色算法

> 全程序化着色，**无贴图素材**（素材库路线已作废，总体设计 §二/§5.3）。

```
1. 群系基色：每像素按群系标签取基色（草原绿/冰原白/荒漠土黄/森林深绿…，
   色板对齐 UI 语义色体系）；超自然炎热大陆按 biome_hot_zone falloff 乘暖色调
2. 高度调制：群系基色 × 高度明度曲线（山地上层偏灰白岩石、山顶雪线）
3. 山体阴影（hillshade）：高度场梯度光照（西北 45°），multiply 0.85-1.15
4. 水体：河流按宽度画深浅蓝折线（宽度=四次根流量×k），湖泊浅蓝，海洋离岸渐变深蓝
5. 海岸线：EDT 等值线 1px 深色描边
6. L2 按地区 world_bounds 从同一管线裁切输出（同源同观感）
```

> **L1 无底图**：L1 是纯矢量图（多边形填充 + 描边 + 城市 blob + 河流 `draw_polyline`），
> 不做地形底图。聚落规模变化由 blob 覆盖表达（总体设计 §5.7），建筑群仅存在于场景图
> （进城后的横版卷轴）。

### 5.5 边界索引图生成

边界索引图（用于运行时 Shader 着色和点击查询）用 **NEAREST 采样**保持边界锐利：

```
L3 边界索引图：
  · 从 continent_data.json 的地区多边形栅格化
  · 每地区一个唯一 RGB（region_id 编码）
  · 海洋 = RGB(0,0,0)
  · 尺寸同 L3 底图

L2 边界索引图：
  · 从 regions_data.json 的地块多边形栅格化
  · 每地块一个唯一 RGB（tile_id 编码）
  · 尺寸同 L2 底图

L1 边界索引图：
  · 从 tiles_data.json 的聚落领地多边形栅格化（**1 个 L1 地块 = 1 个聚落或空聚落**，§0.19）
  · 每聚落一个唯一 RGB（settlement_id 编码）
  · 无聚落区域（空 L1 地块/海洋）= RGB(0,0,0)
  · 尺寸同 L1 底图
```

### 5.6 分辨率策略

按总体设计 §5.3 的「全屏观看无像素块」原则：

| 层级 | 底图 | 理由 |
|------|------|------|
| L3 大世界 | 程序着色 `l3_terrain.png` 2048 起，不足升 4096 | 整大陆全屏观看 |
| L2 地区 | 同管线按地区 `world_bounds` 裁切 | 与 L3 同源同观感 |
| L1 地块 | **无底图**（纯矢量：多边形填充+描边+blob+河流 polyline） | 2048 坐标矢量在 L1 视口缩放下无像素问题 |

### 5.7 打包成 Godot 资源（import_to_godot.py）

```
1. 读取 l3_world.json + 地形底图 l3_terrain.png
   ├─ 生成 config/strategic_map/continent.tres（ContinentData）
   │   · 含 style_base_texture 引用
   │   · 含 boundary_mask_texture 引用
   │   · 含 world_bounds、regions 列表
   └─ 复制 PNG 到 assets/strategic_map/l3/

2. 读取 regions_data.json
   对每个地区 region_id：
   ├─ 生成 config/strategic_map/regions/region_<id>.tres（RegionData）
   │   · 含 style_base_texture、boundary_mask_texture、world_bounds、neighbors
   └─ 复制 PNG 到 assets/strategic_map/l2/

3. 读取 tiles_data.json
   对每个地块 tile_id：
   ├─ 生成 config/strategic_map/tiles/tile_<id>.tres（TileData）
   │   · 含 style_base_texture、boundary_mask_texture、world_bounds、neighbors
   │   · 含 settlements、resources、rivers、roads
   └─ 复制 PNG 到 assets/strategic_map/l1/

4. 读取 political_data.json
   └─ 生成 config/strategic_map/political.tres（PoliticalData）

5. 生成 manifest.tres（索引清单，列出所有 .tres 和 .png 路径）
```

### 5.8 拼接预览的边缘对齐约定

地图包之间的边缘必须对齐，拼接时无缝（见 [`战略图架构.md`](战略图架构.md) §5）：

- 每个地图包记录其在世界坐标系中的**地理范围**（`world_bounds: Rect2`，0-8192 坐标系）
- 拼接时按 `world_bounds` 计算偏移，相邻包的边缘像素天然连续（因为都源自同一张 8192 原始数据裁剪）
- 边界索引图在包边缘有 1-2 像素的重叠区（`edge_overlap`），避免拼接缝隙
- 邻接关系（`neighbors`）从程序化生成的邻接图直接导入

---

## 六、数据结构映射：程序化 TileData ↔ 战略图数据模型

这是 [`程序化世界生成.md`](../../设计/系统/08-程序化世界生成.md) §7.2 的 `TileData` 与战略图消费端数据模型的字段映射。

### 6.1 单一粒度模型的问题

程序化侧的 `TileData` 是单一粒度的「地块」定义，混入了：
- **L1 地块级属性**（resource_types / stickman_types / tech_unlocks）
- **L3/L2 政治属性**（initial_owner / adjacent_region_ids）
- **渲染属性**（center_position / outline_points）

战略图数据模型按三级粒度拆分接收。

### 6.2 新数据模型（详见 战略图架构.md §四）

#### ContinentData（L3 大世界）

| 字段 | 类型 | 来源 | 说明 |
|------|------|------|------|
| `continent_id` | String | 固定 `"continent_main"` | 唯一大陆 |
| `seed` | int | `locked_continent` 的 seed | 锁定种子，用于按需生成 |
| `size` | Vector2i | 8192×8192 | 原始数据尺寸 |
| `regions` | Array[String] | `continent_data.json` 的 region_id 列表 | 该大陆所有地区 ID |
| `ocean_mask` | Image | `locked_continent_8192.png` | 海洋掩码（用于河流裁剪、海岸线绘制） |

#### RegionData（L2 地区）

| 字段 | 类型 | 来源 | 对应程序化文档字段 |
|------|------|------|------------------|
| `region_id` | String | `"region_001"` | §5.5 地区种子 |
| `name` | String | `continent_data.json` | 地区名 |
| `polygon` | PackedVector2Array | `continent_data.json` | §5.5 地区多边形 |
| `adjacent_regions` | Array[String] | `continent_data.json` | §5.5 邻接图 |
| `dominant_biome` | int | `regions_data.json` | §0.4 主导气候 |
| `tiles` | Array[String] | `regions_data.json` | 该地区所有地块 ID |
| `political_owner` | String | `political_data.json` | 当前归属政权（可运行时改） |
| `alliance` | String | `political_data.json` | 所属联盟（可空） |

#### TileData（L1 地块）

| 字段 | 类型 | 来源 | 对应程序化文档字段 |
|------|------|------|------------------|
| `tile_id` | String | `"tile_001"` | §7.2 tile_id |
| `parent_region_id` | String | `regions_data.json` | 所属地区 |
| `polygon` | PackedVector2Array | `regions_data.json` | §7.2 polygon |
| `biome` | int | `regions_data.json` | §7.2 biome |
| `settlements` | Array[SettlementRef] | `tiles_data.json` | §7.2 settlements |
| `resources` | Array[ResourceDeposit] | `tiles_data.json` | §7.2 resources |
| `rivers` | Array[PackedVector2Array] | `tiles_data.json` | §7.2 rivers（裁剪自 L3） |
| `central_city_id` | String | `tiles_data.json` | §7.2 central_city_id |
| `harshness` | float | `tiles_data.json` | §3.2 harshness（影响聚落密度） |

#### SettlementRef（聚落引用，轻量）

```gdscript
class_name SettlementRef extends Resource
var settlement_id: String       # "settlement_tile_001_03"
var name: String
var level: int                  # 1=村落 T1, 2=镇 T2, 3=城市 T3, 4=中心城市 T4, 5=首都 T5
var industry: Array[String]     # 支柱产业标签 ["mining", "trade"]
var position: Vector2           # 在地块多边形内的归一化坐标（0-1）
var population_score: float     # 规模分数（运行时可被 ±15% 扰动）
var map_id: String              # 对应场景图的 map_id（点击进入时 load_map 用）
```

> **关键**：`SettlementRef` 只存战略图需要的展示信息，不存场景图内部的建筑/装饰数据。点击聚落进入场景图后，由 `SettlementGenerator` 从 `settlement_seed` 按需生成 `MapDefinition.tres`。

### 6.3 单一粒度字段的去向

单一粒度地块定义（RegionDefinition 形态）的字段在新模型中的去向：

| 旧字段 | 新去向 | 说明 |
|--------|--------|------|
| `id: int` | `tile_id: String`（TileData） | 类型改 String |
| `name` | `SettlementRef.name`（中心城市）或 `RegionData.name`（地区名） | 按粒度拆分 |
| `type: int`（陆地/海洋/湖泊/荒原） | `biome: int`（TileData） | 改用群系枚举 |
| `is_coastal` | `TileData` 派生属性 | 从 polygon 与海洋掩码计算 |
| `resource_types` | `TileData.resources` | 改为 ResourceDeposit 数组（含量级） |
| `stickman_types` | 删除 | 不属于战略图关注，移到场景图侧的 `VillageDefinition` |
| `tech_unlocks` | 删除 | 不属于战略图关注，移到科技模块的 `tech_unlocks_by_region` |
| `initial_owner` | `RegionData.political_owner` | 上移到地区级 |
| `adjacent_region_ids` | `RegionData.adjacent_regions` | 上移到地区级 |
| `center_position` | `SettlementRef.position`（聚落）或 `RegionData.polygon` 质心 | 按粒度拆分 |
| `outline_points` | `RegionData.polygon` / `TileData.polygon` | 直接用多边形 |

---

## 七、运行时数据流

### 7.1 战略图启动流程

```
1. GameRoot 启动 → SceneLoader 加载初始场景图（玩家所在村落）
2. 玩家按 Tab 打开战略图 → UIRoot 显示 ModalOverlay
3. StrategicMapController._ready()
   ├─ 加载 config/strategic_map/manifest.tres
   ├─ 加载 continent.tres（L3，必加载）
   └─ 默认 granularity = L3，渲染全大陆
4. 玩家滚轮缩放 → 触发 granularity 切换
   ├─ L3 → L2：根据点击的 region_id 加载 region_<id>.tres + region_mask_l2_<id>.png
   ├─ L2 → L1：根据点击的 tile_id 加载 tile_<id>.tres + region_mask_l1_<id>.png
   └─ L1 → 场景图：点击聚落 → 关闭战略图 → SceneLoader.load_map(settlement.map_id)
```

### 7.2 懒加载策略

- L3 大陆地图包**启动时必加载**（1 份，~10-50MB 含纹理）
- L2 地区地图包**双击地区时懒加载**（每地区 ~2-5MB，全大陆约十来个地区）
- L1 地块地图包**双击地块时懒加载**（每地块 ~0.5-2MB，全大陆数十到数百个地块）
- 同一时刻最多加载：1 个 L3 + 1 个 L2 + 1 个 L1（拼接预览模式除外，见 [`战略图架构.md`](战略图架构.md) §5）
- 拼接预览模式：1 个中心包 + 4-8 个相邻包（降采样）+ 远邻矢量描边

### 7.3 与场景图的切换契约

战略图 ↔ 场景图的切换通过 `SceneLoader` + EventBus 信号：

| 事件 | 触发 | 数据流 |
|------|------|--------|
| 玩家在场景图按 Tab | `InputDispatcher` | → 打开战略图 ModalOverlay，记录 `return_map_id` |
| 玩家在战略图双击聚落 | `StrategicMapController` | → `EventBus.travel_requested(settlement.map_id)` → `SceneLoader.load_map()` |
| 玩家在战略图按 ESC | `StrategicMapController` | → 关闭 ModalOverlay，回到 `return_map_id` 场景图 |
| 场景图内聚落规模变化 | construction 模块 | → `EventBus.settlement_updated(settlement_id)` → L1 地图包重新渲染该聚落建筑群 |
| 场景图内归属变化 | expansion 模块 | → `EventBus.region_owner_changed(region_id)` → 战略图刷新政治着色 |

---

## 八、未实现项与工作项清单

### 8.1 生成端待补（程序化生成侧）

- [ ] `region_generator.py`：从 8192 掩码生成 `continent_data.json`（L3 地区多边形 + 邻接图）
- [x] ~~`tile_generator.py`（全局 L1 蒙版部分）~~ **已由 `l1/l1_world_split.py` 实现**（L3 层全大陆 L1 蒙版：城市点 + 受限 Voronoi，产出 `output/l1/`，见 [程序化文档 §二十一 L3-L](../../设计/系统/08-程序化世界生成.md)）
- [ ] `settlement_placer.py`：从地块生成 `tiles_data.json`（L1 聚落布局 + 资源 + 河流裁剪 + 道路矢量）
- [ ] `state_expander.py`：生成 `political_data.json`（政权/联盟/归属）
- [ ] **关键**：生成端产出的多边形必须是**受限 Voronoi 裁剪后的精确多边形**（程序化文档 §八），不能是像素近似的边界

### 8.2 程序着色底图待实现（对齐总体设计 §5.3）

- [x] `tools/worldgen/l3/biome_generate.py`：高度场 + 掩码 → Whittaker 七群系标签 `biome_labels_2048.npy`（✅ B1 已完成，含超自然炎热大陆）
- [x] `tools/worldgen/l3/terrain_render.py`：高度场 + 群系标签 + 河流/湖泊 mask → `l3_terrain.png`（2048）+ L2 每地区裁切（✅ B2 已完成）
  - [x] 群系基色 × 高度明度 + 山体阴影（hillshade，西北 45°，multiply 0.85-1.15）
  - [x] 水体着色（河流宽度=四次根流量×k，经 river_mask 掩码继承）+ 海岸线 EDT 描边
  - [x] 超自然炎热大陆暖色调（biome_hot_zone falloff 蒙版，R+42/B−18）
- [ ] 河流矢量随 L2/L1 视图包导出（B3；L3/L2 底图内河流已预渲染）

### 8.3 打包器待实现（本桥接文档定义）

- [ ] `tools/worldgen/l2_export/`：JSON + 地形底图 → `config/strategic_map/` 视图包
- [ ] 验证每层级分辨率是否满足"全屏观看无像素块"（见 §5.6）

### 8.4 消费端现状（2026-08 已实装，原"待重构"清单作废）

> 原 8.4 的"消费端待重构"清单已过时：L1/L2/L3 战略图已实装并接入（Tab 键 L1、M 键 L3/L2 下钻），
> 旧三级粒度框架文件（granularity_manager/stitched_preview_controller/settlement_renderer/map_mode_*/settlement_tooltip/strategic_map_data）已归档至 `world_map/archive/`。
> 现状：

- [x] L1 单层：`strategic_map.tscn`（controller + map_renderer + map_camera + api），索引图像素解码查询（P 社机制）
- [x] L3/L2 视图：`strategic_map_l3.tscn` / `strategic_map_l2.tscn`，烘焙几何（l2_geom.bin）+ 8K 底图
- [x] `api.gd` 重构完成：`initialize(l1_json, config_dir)` / `query_at_screen` / `enter_settlement` / `close_strategic_map`
- [x] 集成测试：`test_strategic_map_p0` / `test_l2_strategic_map` / `test_l3_strategic_map`
- [ ] L1 地块划分（tools/worldgen 生成管线）与政权/聚落数据生成（待办事项 0.9a/0.9b）
- [ ] Shader 叠加（region_overlay/border_outline）：当前边界为 CPU `_draw` 描边，Shader 版未做

### 8.5 与其他模块的对接

- [ ] `modules/expansion/api.gd`：地块归属变化时通过 EventBus 通知战略图刷新缓存
- [ ] `modules/organization/api.gd`：组织树按 `region_id` / `tile_id` 过滤显示
- [ ] `modules/combat/api.gd`：战斗发生时在战略图上叠加战线图标
- [ ] `modules/construction/api.gd`：聚落规模变化时通知 L1 重新渲染聚落建筑群
- [ ] `SceneLoader`：增加 `load_strategic_map()` 和 `close_strategic_map()` 方法

---

## 九、决策点状态

| 决策 | 状态 | 备注 |
|------|------|------|
| 战略图 vs 场景图术语 | **已敲定** | 本文 §1.1 |
| L3/L2/L1 ↔ GDD 第5/4/3 层级 | **已敲定** | 本文 §1.3 |
| region_id 类型统一为 String | **已敲定** | 对齐模块API契约.md |
| 数据模型按三级粒度拆分 | **已敲定** | 本文 §6.2 |
| **多层级独立地图包** | **已敲定** | 本文 §3.1、§4：8192 裁剪派生，每层级独立包 |
| **分层级渲染策略** | **已敲定** | 战略图架构 §4.1：L3 预渲染为主，L1 重度程序化 |
| **地形底图由 Python 工具程序化着色** | **已敲定** | 本文 §5.4（素材库路线作废，总体设计 §二/§5.3） |
| **L1 动态聚落建筑群** | **已敲定** | 战略图架构 §4.6：聚落规模随玩家建设实时变化 |
| **地图拼接预览模式** | **已敲定** | 战略图架构 §5：跨地区作战多包拼接 |
| **边界索引图 NEAREST 采样** | **已敲定** | 战略图架构 §4.8：P社做法 |
| **每层级独立分辨率** | **已敲定** | 本文 §5.6：L3 程序着色 2048 起（不足升 4096），L2 同管线裁切，L1 无底图纯矢量 |
| 导入器为开发期 Python 工具 | **已敲定** | 本文 §5.1 |
| 懒加载策略（L3 必加载，L2/L1 懒加载） | **已敲定** | 本文 §7.2 |

---

*本文档为世界地图数据流的契约真相源。生成端、导入端、消费端三方按此文档对齐。*
