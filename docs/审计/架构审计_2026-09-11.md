# 架构审计：全工程依赖与卫生（2026-09-11）

> **性质**：一次性审计快照——**AR 系列（`项目/待办事项.md`）全部执行完毕后删除本文件**，不进归档。
> 审计方式：`python tools/audit_deps.py` 全量扫描（class_name + preload 双口径，剥注释字符串）+ 定向脚本实测。
> 本文只记录**实测数据与证据**；工作项定义、方案、状态以待办 AR 节为唯一跟踪处。

---

## 一、实测数据

### 1.1 模块规模（GDScript 行数，`find modules -name '*.gd' | xargs wc -l`）

| 模块 | 行数 | | 模块 | 行数 |
|------|-----:|---|------|-----:|
| units | 10,296 | | texture_gen | 1,723 |
| world | 9,396 | | debug_gui | 1,695 |
| ui_global | 8,074 | | inventory | 1,563 |
| world_map | 7,373 | | fx | 1,297 |
| combat | 4,369 | | building_gen | 882 |
| construction | 3,010 | | player_control | 676 |
| | | | organization | 673 |
| | | | resources | 386 |
| | | | environment | 326 |

全工程 modules+core 合计 54,191 行。胖文件 TOP（>900 行）：stickman_entity 1477 / weapon_mount 1258 /
map_renderer 1110 / formation_system 1029 / system_setup 939 / game_root 926。

### 1.2 依赖图（audit_deps.py：class_name 引用 + preload 路径）

```
零出向：core, environment, fx, organization, resources
texture_gen    → ui_global
player_control → ui_global
building_gen   → texture_gen
construction   → building_gen, player_control
inventory      → ui_global, units
combat         → fx, player_control, ui_global
debug_gui      → fx, ui_global
units          → combat(audit-exempt×3), fx, player_control
world_map      → ui_global, world（经全局类 WorldAPI 消费枚举/路径常量）
world          → 全部 13 模块（组装根 system_setup）
```

**编译期环 1 个：world ⇄ world_map**（world_map 消费 WorldAPI 的 MapType/TravelMode/EntrySide/PATH_*
共享词汇，world 又装配 world_map；preload 层面 world→world_map 仅装配器 4 处，环来自 class_name）。
既有文档已声明的 world⇄combat 有意环在当前实测中不成环（combat→world 边已拆为注入路径）。

### 1.3 装配根 preload 分布（system_setup.gd，重测 `grep -oP 'preload\("res://modules/\K[^/]+' … | sort | uniq -c`）

共 41 处跨模块 preload，覆盖 12 模块：ui_global 9 / combat 8 / world_map 4 / inventory 4 /
player_control 3 / construction 3 / world 2 / resources 2 / organization 2 / debug_gui 2 / units 1 / fx 1。
趋势：09-04 审计 34 处 → 09-09 复核 38 处 → 本审计 41 处（inventory 接入 +4 主因）。
另：非装配器跨模块 preload 内部文件 8 处未豁免（pond/siege_director/siege_wall/sky_decor/demo_quest×4）+
已豁免 4 处（units→combat target_finder×3、game_root→ui_global overlay×1）。

### 1.4 EventBus 信号健康（34 信号，重测脚本见 git 历史）

- 死发射（有 emit 零订阅）：`balance_changed`（热重载预留，豁免）、`team_ai_stance_changed`、`chunk_loaded`、`chunk_unloaded`
- 死订阅（有 connect 零 emit）：`settlement_updated`
- 零发射零订阅：无

### 1.5 Autoload 与分支

- project.godot autoload 9 个 = 8 正式 + **PerfProbe（tests/dev/perf_probe.gd，性能审计临时探针残留）**
- 未合并分支 9（agent/building-art、conquest-loop、organization-deepening、perf-pareto、stickman-visual、
  town-gen、town-life、world-map-rework、perf/battle-30fps——后者待办已记"实际落地"，合并方式非 ancestry）；
  `.temp/` 下 worktree 10 个。**分支卫生创始人指示搁置，本项数据仅存档。**

### 1.6 文档对照发现

- 模块依赖关系.md §一依赖图与实测有出入：缺 inventory 模块、world 层级画法过时（已加实测注，AR-2 执行时刷新）。
- modules/README.md §2 依赖图同样未含 inventory。
- `docs/技术/架构/` 原无目录 README（已补，2026-09-11）。

## 二、发现 → 工作项映射

| 发现 | 工作项（待办 AR 节） |
|------|------|
| 装配越界 41 preload | AR-1 装配下沉 + AR-6 红线守护 |
| world⇄world_map 环 | AR-2 共享词汇下沉 |
| 胖文件 6 个 | AR-3 登记与随改随拆 |
| 死信号 + PerfProbe 残留 | AR-4 信号与探针卫生 |
| 分支/worktree 堆积 | AR-5（搁置，创始人指示） |
