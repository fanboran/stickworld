# 迭代交接文档（temp/，新上下文会话第一步必读）

> 本文件夹是**临时迭代记录**，不属于正式文档体系（docs/ 才是）。
> 配套三件套：`开发目标.md`（求职定位+原始诉求+当前目标）/ `开发设计.md`（决策与参数）/ `进度日志.md`（Round 记录）。
> 最后更新：2026-09-05 11:30（Round 12 收口 + 文档三拆，**阶段结束**）。分支 `agent/playable-demo`。
> **新会话第一件事：mona-3 笔触建筑零件重绘（开发目标.md §五）。**

## 〇、用户指令状态（2026-09-05）

1. ~~音频搬运（私有演示可搬，登记替换清单）~~ ✅ 13 件 Terraria 提取接入+登记
2. ~~云重做（笔触管线）~~ ✅ gen_cloud_stroke.py 3 变体，stroke_cloud.gd 已删
3. ~~抹"求职Demo"痕迹~~ ✅ 双文档改名+全代码清理（残留仅 temp/ 过程文档内的历史引用）
4. ~~主菜单副标题删除~~ ✅（金色 badge 一并删）
5. 声音时机学 Terraria ✅（重触发停旧+音高抖动+变体随机）；**剩余 7 件程序合成音效（战斗胜负/鸟鸣/雨声）Terraria 无对应源，留 CC0/自研**
6. ~~素材替换清单~~ ✅ 已建并逐条登记
7. 测试平时跑局部，全量只在收尾 ✅（Round 12 收口全量 31+1flake→复跑双绿）
8. ~~RWR 士兵 AI 三项~~ ✅（散布热度/点射停顿/飞行时间衰减，换算见开发设计.md §四）
9. **mona-3 笔触建筑重绘（零件拆分）未启动**——新会话第一件事
10. 工作方式：**杂活交 Subagent 分流**（Round 12 实践过：音效填充零越界完成）；文档三件套职责分离，目标/设计不写历史

## 坑与红线（会再踩的）

- **版权**：音频提取件仅私有演示，公开前按《素材替换清单》全替换；贴图/代码资产不搬（只学机制）
- .gdshader 改后必须 --import（SPIR-V 缓存）
- headless 方窗(zoom1.78) ≠ 真渲染(1920x1080 zoom1.0)——取景诊断要同源
- 地面多边形 z=0 不透明盖低层；水挂 DECORATION(z=1)
- Write 工具正斜杠盘符路径会静默不落盘（用反斜杠）
- XNB 解包：头部有 uint32 文件总长字段（提取器已是固定布局解析）
- stroke_paint 白底抠图会误抠白云——用 mask_path；参考图必须有颜色结构（纯渐变→detail 层 0 笔收敛）
- AudioManager._event_players 存弱引用对象，取回必须 is_instance_valid 后再访问

## 快速命令

```bash
cd F:\VSCode\game-2\stick-world
# 报错自检（须退出码 0）
bash tools/check_godot_errors.sh
# 局部验证：天空/水/雨
godot --path . res://tests/dev/verify_parallax.tscn --headless
# 快照（真渲染，别 --headless）
godot --path . res://tests/dev/snapshot_demo.tscn -- --out=F:/tmp/day.png
# 音效提取（固定布局解析，用法：xnb wav 成对）
python tools/ai/extract_terraria_sfx.py <xnb> <wav>
# 笔触云再生成
python tools/ai/gen_cloud_stroke.py assets/sky
# 全量（仅收尾）
bash tests/run_all.sh
```
