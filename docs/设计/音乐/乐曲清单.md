# 乐曲清单（Cue Sheet）

> 本文是音乐资产的**主键表**：一个 cue（乐曲）对应一个游戏状态，一个 `cue_id`
> 对应磁盘上的一个目录。设计意图见 [音乐设计文档.md](音乐设计文档.md)。
>
> 曲名与 cue_id 的对应关系、分层与循环点**由代码生成**（`tools/music/compose/cues.py`
> 与交付的 `music_manifest.json`），本文是它们的可读版本。**改谱面请改代码，
> 不要只手改本文**——两者不一致时以代码与清单为准。

## 一、总表

| cue_id | 曲名（工作名） | 触发 | 类型 | 调性 | BPM | 小节 | 循环时长 | 层数 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `menu_title` | 原野·序 | 标题画面 / 启动 | 循环 | D 大调 | 66 | 32 | 116.4s | 2 |
| `field_day` | 原野·昼 | `map_loaded` 户外·白天 | 循环 | D 大调 | 72 | 32 | 106.7s | 4 |
| `field_night` | 原野·夜 | `map_loaded` 户外·夜晚 | 循环 | B 小调 | 63 | 32 | 121.9s | 3 |
| `village` | 小镇 | `map_loaded(VILLAGE)` | 循环 | G 大调 | 84 | 32 | 91.4s | 4 |
| `interior` | 灯下 | `interior_entered` / 室内地图 | 循环 | F 大调 | 58 | 32 | 132.4s | 1 |
| `strategic` | 远望 | `strategic_map_opened` | 循环 | C 大调 | 64 | 32 | 120.0s | 4 |
| `battle` | 出征 | `battle_started` | 循环 | D 小调 | 104 | 32 | 73.8s | 4 |
| `sting_victory` | 凯旋 | `battle_ended(victory=true)` | 一次性 | D 大调 | 72 | 2 | — | 3 |
| `sting_defeat` | 折戟 | `battle_ended(victory=false)` | 一次性 | B 小调 | 63 | 2 | — | 2 |

循环时长 = 小节数 × 拍数 ÷ BPM × 60。所有循环曲目**循环体恰好 32 小节**
（整数小节是硬约束，否则每次循环相位漂移）。

## 二、分层表（tier = 强度档位）

运行时按游戏强度逐层淡入：tier 0 常驻，tier 越大越"热闹"。
同一 cue 的全部层**等长、同循环点、同和声进行**，所以任意组合都能叠加。

| cue_id | tier 0（地基） | tier 1（常规） | tier 2（点亮） |
| --- | --- | --- | --- |
| `menu_title` | piano | strings | — |
| `field_day` | piano | strings | harp, bells |
| `field_night` | piano | strings | bells |
| `village` | piano | guitar | marimba, winds |
| `interior` | piano | — | — |
| `strategic` | pad | vibraphone, piano | bells |
| `battle` | piano | strings, perc | winds |
| `sting_victory` | （单文件混音） | — | — |
| `sting_defeat` | （单文件混音） | — | — |

## 三、编制与音色

| 层名 | 乐器 | 音源 | GM 音色号 | 角色 |
| --- | --- | --- | --- | --- |
| `piano` | 三角钢琴 | Salamander Grand Piano V3（sfizz） | — | 全作主角，旋律与伴奏 |
| `strings` | 弦乐合奏 / 慢弦乐 | GM SoundFont（fluidsynth） | 48 / 51 | 长音和声垫（弱奏） |
| `pad` | 温暖 Pad | GM SoundFont | 89 | 战略图的地基长音 |
| `harp` | 竖琴 | GM SoundFont | 46 | 跨八度分解，"空气感" |
| `bells` | 钟琴 / 八音盒 / 钢片琴 | GM SoundFont | 9 / 10 / 8 | 点状色彩音（"光"） |
| `vibraphone` | 颤音琴 | GM SoundFont | 11 | 战略图的慢速分解 |
| `marimba` | 马林巴 | GM SoundFont | 12 | 小镇的木质点状音 |
| `guitar` | 尼龙弦吉他 | GM SoundFont | 24 | 小镇的指弹伴奏 |
| `winds` | 长笛 / 双簧管 | GM SoundFont | 73 / 68 | 独奏木管（每曲只用一件） |
| `perc` | 定音鼓 | GM SoundFont | 47 | 战斗的强拍（克制，不用鼓组） |

## 四、自适应设计

### 4.1 纵向混音（vertical remixing）

引擎加载同一 cue 的全部层，同时开始播放，用**层音量**表达强度。
层的进入顺序固定为 **钢琴 → +弦乐 → +钟琴**，退出顺序相反。
底层设计保证：**任何一层被静音，其余层仍是完整的音乐**——
这是"层"能自由开关的前提，也是"骨相好"的检验标准。

### 4.2 状态切换（horizontal re-sequencing）

状态切换以**量化到小节**的交叉淡化为默认（详见引擎侧设计）。
循环体之间可直接硬接（和声在 32 小节处闭合，接得回开头），
跨调性/跨速度的切换用交叉淡化。

### 4.3 昼夜

昼夜不换曲，而是切换到**同主题不同调式**的另一首（`field_day` ↔ `field_night`）。
两者共享同一串音阶级数，玩家听到的是同一段旋律换了心情。

## 五、交付规范

| 项 | 规格 |
| --- | --- |
| 采样率 | 48000 Hz |
| 声道 | 立体声 |
| 格式（游戏内） | OGG Vorbis，`libvorbis -q:a 6`（约 190kbps） |
| 格式（存档） | 母带 WAV 24bit/48kHz，放 `tools/music/out/master/`（不进版本库） |
| 响度 | 音乐 **-15 ±0.5 LUFS**；sting **-14**（文件按流媒体量级交付，游戏内位置由音量滑条决定） |
| 真峰值 | ≤ -1.0 dBTP |
| 循环信息 | 写在 `music_manifest.json`，加载时赋值给 `AudioStreamOggVorbis` |
| 路径 | `stick-world/assets/audio/bgm/<cue_id>/<layer>.ogg` |
| 清单 | `stick-world/assets/audio/bgm/music_manifest.json` |

`music_manifest.json` 的每条 cue 含：
`title / bpm / bar_beats / bars / beat_count / loop / loop_offset / key /
duration_s / loudness_lufs / layers[]`，其中每个 layer 含
`name / file / tier / db`。

## 六、待办

- [ ] 曲名定为最终对玩家展示的标题（当前为工作名，**提案/待定**）
- [ ] 天气变体（雨/雪）：仅环境音层，**是否要音乐变体待定**
- [ ] 领地主题（玩家组织专属主题）：**提案/待定**
- [ ] 抗疲劳的"无旋律变奏"交替播放：**提案/待定**
