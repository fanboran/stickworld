# 出征与领地循环：进度与交接

> **用途**：跨会话交接的单一入口。新会话恢复本任务时**从本文件开始读**，不重新摸底。
> **真相源**：[`docs/技术/架构/出征与领地架构.md`](../../技术/架构/出征与领地架构.md)（模块结构/数据契约/信号契约/C1~C7 详表/决策记录全在那边）；玩法动机见 [`docs/设计/系统/03-扩张系统.md`](../../设计/系统/03-扩张系统.md) §六。本文件只记进度、批次拆分与会话指引。

---

## 快速恢复（新会话从这里开始）

- **分支**：`agent/conquest-loop`（独立分支，2026-09-07 从 main `080f5859` 拉出）
- **工作区**：`F:\VSCode\game-2\.temp\conquest-loop`（git worktree；**本任务全部会话都在此工作区干活**，主工作区归其他并行任务）
- **必读**：架构文档 §二（数据契约）、§三（数据流总图）、§四（战斗侧改造）、§八（工作项 C1~C7）、§9.1（P 社化预留——C1 实装时必须遵守的语义）
- **自检**：`bash stick-world/tools/check_godot_errors.sh`（退出码 1 必须修复）；测试 `bash stick-world/tests/run_all.sh`
- **当前阶段**：**设计定稿，实现未开工**——6 个会话批次（下表），每批一个新上下文

## 任务分级调度（Pro / Flash × 会话批次）

> 分级标准沿用项目约定（[世界地图档 §任务分级调度](世界地图系统完善-进度与交接.md)）：**Pro** = 需设计判断 / 跨模块契约改动 / 算法建模；**Flash** = 规格明确、组件与模式现成、单模块机械实现。
> **通用验收（两级同规）**：`check_godot_errors` 干净 + `run_all.sh` 全绿（含既有测试零回归）+ 完成后回填本档「进度记录」+ 中文提交。
> **新会话开场白**：「继续 出征与领地循环 批次 N」——该会话先读本档 + 架构文档指定章节，再动手。

| 批次 | 级别 | 任务 | 依赖 | 验收门 | 新会话必读 |
|---|---|---|---|---|---|
| **1** | Flash | **C1 领地数据与契约**：`config/expansion/territories.tres`（首版手写，见下方决策）+ `TerritoryRegistry`（加载/查询/序列化）+ `WorldState.territories` 域（含 start_new_run 重置 + 存档回传，模式照 `visited_settlements`）+ EventBus 三新信号 + `modules/expansion/api.gd` 骨架 | 无（地基） | 单测：配置加载/状态查询/存档往返；三信号按 `@warning_ignore("unused_signal")` 惯例声明 | 架构文档 §一/§二/§五/§9.1 |
| **2** | Pro | **C2+C3 战斗侧改造**：C2 victory 语义修正（BattleInstance 加 `player_faction`，`battle_ended` 的 victory=玩家阵营胜；三监听方 demo_quest/global_hud/audio_manager 适配；battle_lifecycle 测试更新）+ C3 敌将撤仗（TeamAi 战役评估：伤亡率/战损比/相持超时 → ROUT 姿态 → `OrderType.RETREAT` 全军有序后撤；单位行至地图边缘记 `departed`，`_count_alive` 计非存活；**据点战才启用评估，普通战斗维持全灭判定**） | 无（与批次 1 并行可行） | 既有战斗测试更新后全绿；新集成测试：伤亡率拉满→敌军撤离→`battle_ended(胜)`；另一测试：无 commander 配置的战斗仍走全灭 | 架构文档 §四全文；`team_ai.gd` 头注释（注册制闸门）、`battle_instance.gd:389-439`、`tactical_orders.gd:25`、`demo_quest.gd:227-236` |
| **3** | Flash | **C4 据点图与守军**：改造 `l1_settlement_02/03/04.tscn` 挂 `ConquestAnchor`（GarrisonSlots 守军位组 + CommanderSlot 敌将位 + RallyX 集结线，契约见架构文档 §2.3）+ `GarrisonSpawner`（按 Anchor 布阵刷军，数量 = 配置 − `garrison_losses` 车轮战扣减；刷单位照 `initial_content.gd` 先例走 UnitsApi）+ **顺带**：battlefield 退役（`game_root.gd:637` 专属刷敌逻辑摘除，保留注册与 `tests/dev/verify_battle.gd` 直达供 dev） | 批次 1 | 进图按配置刷军；打掉部分守军离场再进，数量减少；battlefield 进图不再自动开战 | 架构文档 §2.3/§4.3；`initial_content.gd:88-126`、`game_root.gd:401-426` |
| **4** | Pro | **C5 占领与收益**：`ConquestManager` 状态机（SystemSetup 装配照 SaveHandler 先例；监听 `map_loaded` 匹配据点 → 调 GarrisonSpawner → `CombatApi.start_battle(玩家侧为攻方, player_faction 传入)` → 监听 `battle_ended` → HOSTILE→CAPTURED → 奖励发放 ResourcesApi + `unlock_granted` → `territory_state_changed` + `region_owner_changed(tile级id)` → 全部占领发 `conquest_completed`）+ **战略图染色接收端**（world_map L1 政治模式按 region_owner_changed 刷新聚落/地块色，挂 P7 政权版 states 体系上） | 批次 1+2+3 | 集成测试：打完一场→WorldState 状态变+资源入账+战略图色变；再进据点图不刷军 | 架构文档 §三/§五/§六；`system_setup.gd:193-226`（战斗装配先例）、world_map P7 states 装载链 |
| **5** | Flash | **C6 出征入口与演示改写**：`siege_gate_prompt.gd` 出城选项加「⚔ 征伐 <据点名>（守军N/已臣服）」动态项（照动态道路项 `:130-140` 先例，数据来自 `api.list_targets()`）+ `demo_quest.gd` 第四阶段改写（完成判定换 `territory_state_changed(任意id, CAPTURED)`；文案/箭头/台词按架构文档 §七 对照表修正）+ 通关结算（`conquest_completed` → victory_overlay 复用，统计占领数/用时/伤亡） | 批次 4 | 演示四阶段手动全链路可玩（采集→建造→编队→征伐→结算）；旧"战场"目标不残留 | 架构文档 §七；`siege_gate_prompt.gd`、`demo_quest.gd`、`victory_overlay.gd` |
| **6** | Pro | **C7 端到端 smoke 测试**：一条测试跑全循环——新开局 → api 骨架就绪 → 征伐第 1 座（触发战斗、敌将撤仗或全灭）→ 断言占领/染色/奖励 → 再征第 2 座（车轮战守军扣减生效）→ 全部占领 → `conquest_completed` → 通关结算弹出 | 批次 1~5 | `bash stick-world/tests/run_all.sh -Match conquest` 绿——**循环自此被测试锁死** | 架构文档 §八 C7 行；`tests/` 现有 integration 模式（test_battle_lifecycle / test_squad_travel） |

**依赖图**：批次 1 与批次 2 相互独立（可两个会话并行）；3 依赖 1；4 依赖 1+2+3；5 依赖 4；6 收口。

## 批次 1 开工前裁决（已定，Flash 会话直接执行）

- **territories 首版手写 `.tres`**（不走 Excel 管线）：格式照 `config/expansion/regions.tres` 的 balance_resource 形态，`_meta` 标注 `"source": "handwritten"`。理由：首版数值是 AI 提案待实测定稿，定稿后再 Excel 化（届时向 `config/excel/扩张数据.xlsx` 加 territories sheet 重导出）。字段表 = 架构文档 §2.1，另加 §9.1 要求的 `tile_key` 预留字段位。
- 3 座据点挂 `l1_settlement_02/03/04`，`map_id` 写法 `"l1_settlement_02"` 等（game_root 已注册）；守军 3/5/8 递增，兵种档案从 `config/combat/` 现有 profiles 挑（批次 3 刷军时要能按 profile spawn）。

## 进度记录（每批完成后回填）

| 批次 | 状态 | 提交 | 备注 |
|---|---|---|---|
| 1 | ⬜ 未开工 | — | |
| 2 | ⬜ 未开工 | — | |
| 3 | ⬜ 未开工 | — | |
| 4 | ⬜ 未开工 | — | |
| 5 | ⬜ 未开工 | — | |
| 6 | ⬜ 未开工 | — | |

## 关键决策速查（详表见架构文档 §十）

| 决策 | 结论 |
|---|---|
| 循环正轴 | 主动出征（合战忍者村式），守城为 P1 反扑支线 |
| victory 语义 | 破坏性改玩家阵营胜，三监听方一次改齐（批次 2） |
| 敌将撤仗 | TeamAi 战役评估→RETREAT 有序后撤；据点战启用、普通战斗不启用 |
| 守军持久化 | 车轮战扣减 `garrison_losses`，玩家败仗不判负 |
| 归属语义 | `region_owner_changed` 发 tile 级 id（§9.1 P 社化预留），territories 配置带 `tile_key` |
| 遭遇战 battlefield | 退役为 dev 验证图（批次 3 顺带） |
