# 旧代码清理——审计与交接

> **一句话**：全面 HD-2D 化（第七~九轮）后，对仓库里残留的旧 2D 时代代码做全量审计与清退。
> **2026-09-16 创始人裁决并已执行**：守城/森林/道路全部走 HD-2D 化（森林由城外资源图承担、道路待 HD-2D 重做）、室内由建筑管线 v3 内景承担、测试开机图不保留——A/B/C 三级全量清退完毕，D 级悬置测试保持重建口径。
> ⚠ 主工作区多会话并行，提交一律按路径精确 add，不 `git add -A` 全仓。

## 一、A 级：已删除（先期批次）

| 对象 | 原用途 | 替代 |
|---|---|---|
| `modules/world/scenes/maps/village_a.tscn` | 旧 2D 出生村主场景 | hd2d_street |
| `modules/world/scenes/maps/village_b.tscn` | 2D 村B | hd2d_village_b（算法村） |
| `stick-world/tools/building_pipeline/`（整目录） | v2 Godot 2D 建筑管线 | v3 = tools/blender_buildings |
| `tools/baking/render_village.gd` | 拍旧村全景 | proto_hd2d --shots |
| `tests/dev/snapshot_town.gd` | 拍旧村村民 | 同上 |
| `tests/dev/proto_hd2d/proto_hd2d_ground.tscn` | --save-scene 探索产物（9.9MB） | 运行时构建 |

## 二、B 级：已删除（2026-09-16 同批清退）

`tests/dev/snapshot_demo`、`snapshot_harvest`、孤儿 `snapshot_town.tscn`、`tools/terrain_viewer/`（整目录）、`tools/baking/render_manor.gd`、`render_interior.gd`——删前逐个 grep 确认零生产引用；`snapshot_menu/night/professions/rhythm` 主体系统仍在役，保留。

## 三、C 级：已全量清退（2026-09-16，原「随迁移退役」清单）

**删除**：

- 旧 2D 图场景 13 张：`battlefield`（旧壳）、`road_a_b`、`siege_battlefield`、`forest_zone`、`mega_interior`、`l1_settlement_00~07`
- 2D 图脚本链：`village_map.gd`、`road_map.gd`、`siege_field/siege_wall/siege_gate_prompt/siege_director/town_siege.gd`、`terrain_renderer.gd`、`initial_buildings_list.gd`（IBL 链含 `spawn_initial_buildings` 与 api 路径常量）
- 2D 环境效果：`sky_decor/fireflies/sky_stars/sky_birds/pond/weather/sky_aurora/ambient_motes.gd`（EnvironmentSystem 的 game_time 时钟保留，为 HD-2D 昼夜数据源）
- 道路/室内附属：`road_map_generator.gd`（含 scene_loader 道路懒生成）、`mega_interior_exit_trigger.gd`（含 travel_handler 大建筑传送链与 game_root 返回字段）

**切链要点**：game_root 注册表与出口表（主街西缘/战场右缘步行出口随链关闭，城外资源图走城门选项框）、street/battlefield `_exit_specs`、settings 调试地图清单、main_menu dev 场景清单、closure_probe 预热清单、world/api.gd 路径常量（TerrainLayer/InitialBuildingsList）。

**保留的共用基础设施**（HD-2D/建造在用，勿删）：`chunk_trigger.gd`、`city_gen.gd`、`map_base.gd`、`resource_gen.gd`、`resource_node.gd`、`placement_grid.gd`、`conquest_anchor.gd`、`environment_system.gd`、`decorations/`、`assets/resources/*_paint_*` 贴图池。

## 四、D 级：挂起测试（重建而非删除）

`tests/integration/test_{town_life_harvest,town_life_worksite,recruit_flow,construction_cycle,save_roundtrip,garrison_spawner,village_map}.tscn.suspended` + 同名 .gd——玩法设施 HD-2D 化后在生产图上重建断言，**不要删 .gd**。
另随本批删除两个 unit：`test_road_walk`（道路链退役）、`test_settlement_entry`（断言旧 l1 图结构，待聚落线按 HD-2D 薄实例 layout_name/city_tier 契约重建）。

## 五、创始人裁决记录（2026-09-16，原「待裁决」节）

1. 守城：走 HD-2D（旧 siege 链已删，重建时按出征系统架构实现）。
2. 森林：走 HD-2D，城外资源区即其形态；战场右出步行链关闭。
3. 室内：建筑管线 v3 内景承担（29 套内景已入库），旧 mega_interior 传送链删除。
4. l1_settlement 旧 2D 图：随聚落等级链切 HD-2D 后删除（hd2d_settlement_00~07 已注册，map_id 不变）。
5. 测试开机图（旧 battlefield_2d）：不保留，战斗套件直接改挂 HD-2D 战场 `battlefield`。

## 六、明确保留（易误删清单）

- `tools/blender_buildings/` 全部（v3 主力）+ `tools/baking/` 动画/武器烘焙（bake_anims/extract_weapons/inject_anim_events/boot_timer 等）
- `tools/worldgen/`（战略图生成端）+ `check_godot_errors.sh` + `diff_png.py`
- `tests/dev/` 的火柴人 probe（crowd/bar/weapon）、`verify_hd2d_*`、`snapshot_menu/night/professions/rhythm`
- 上文「保留的共用基础设施」全部

## 七、执行状态

- 报错自检干净（编辑器启动模拟无 ERROR/Parse Error）。
- 本清理直接负责的测试全绿：cross_map_travel 15/15（重写）、squad_travel 4/4、menu_navigation 5/5、battle_retreat 4/4、unit 批量 60/61。
- 与本清理无关的既有红（HEAD 基线即红，worktree 对照实验证实非本清理回归）：`test_suppression`（units 在途改动）、`test_selection_formation/test_melee_combat/test_combat_feedback/test_battle_ui`（战斗行为，HEAD 基线同样失败）、`test_conquest_flow/test_conquest_e2e`（聚落 HD-2D 切换在途影响）——归各自主线收口。
