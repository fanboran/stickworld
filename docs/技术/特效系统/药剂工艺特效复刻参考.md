# 药剂工艺特效复刻参考（Potion Craft FX Replication Reference）

> 创建：2026-08-22
> 目的：把《药剂工艺》(Potion Craft) 反编译分析、资源包破解方法、真实特效参数、
> 以及本项目的复刻实现，全部沉淀为一份可追溯的知识文档。
> 后续任何特效开发/调参/继续深挖，先读本文。

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [药剂工艺技术栈全景（反编译结论）](#2-药剂工艺技术栈全景反编译结论)
3. [三大特效系统机制详解](#3-三大特效系统机制详解)
4. [资源包破解方法（大端 bundle 解包）](#4-资源包破解方法大端-bundle-解包)
5. [CrystalSparks 真实参数（出土数据）](#5-crystalsparks-真实参数出土数据)
6. [Godot 复刻实现映射](#6-godot-复刻实现映射)
7. [原创 vs 复刻 vs 占位清单](#7-原创-vs-复刻-vs-占位清单)
8. [调试与截图验证流程](#8-调试与截图验证流程)
9. [后续深挖清单](#9-后续深挖清单)
10. [附：本次会话修复记录](#10-附本次会话修复记录)

---

## 1. 背景与目标

用户要求将《药剂工艺》(Potion Craft，Steam 游戏) 的视觉特效"完美复刻"到本项目
（火柴人帝国模拟，Godot 4.7）。最初用"贴图近似 + 粒子参数脑补"的方式交付，
被指出与药工原版不符。随后改为：
1. 反编译药工 C# 源码，确认**系统机制**
2. 破解其资源包，提取**真实参数**
3. 在 Godot 中按原版机制逐条翻译实现

**核心结论先行**：药工的"布灵布灵"（水晶/岩壁闪光）不是贴图效果，也不是复杂
shader——是**一个参数精确的粒子系统 + 脚本驱动的精灵特效**。复刻的关键是
拿到参数，而不是重画贴图。

---

## 2. 药剂工艺技术栈全景（反编译结论）

来源：`PotionCraft.Scripts.dll` 反编译（1698 个类）+ 资源包解析。

| 类别 | 结论 |
|---|---|
| 引擎 | Unity **2023.1.13f1**，纯 2D，DirectX11 |
| 核心库 | DOTween（动画）、Addressables（按需加载）、TextMeshPro（SDF 文字）、InputSystem、Facepunch.Steamworks、Newtonsoft.Json、QFSW Quantum Console |
| 架构 | **Manager 单例**（`Managers.cs` 一个节点挂 26 个 Manager），ScriptableObject 配置，UnityEvents 事件驱动 |
| 叠加层 | `SpriteSortingLayers.cs`：**117 个排序层枚举**一处定义（背景/特效/GUI/提示/光标全共用） |
| 模态 | `DarkScreenSystem`：按层索引的字典，`DeactivateAll(deactivateTilLayer)` 逐层 pop；输入屏蔽用碰撞体；暗屏分 Lower/Middle/Upper 三层 |
| 文字 | TextMeshPro SDF（Vollkorn/Trirong/NotoSerif 多字体） |
| 全屏效果 | Sepia（夜晚/老纸色调）、Dissolve（窗口溶解过渡）、MapRt（地图 RenderTexture）、CircularBarGradientTexture（刻度条） |
| 动画气泡 | AlchemyMachineBubbles（3 层气泡贴图不同速度滚动） |

---

## 3. 三大特效系统机制详解

药工的视觉特效分三套独立系统，**用途完全不同**，复刻时必须先分清是哪一套。

### 3.1 IngredientVisualEffectSystem —— 交互蹦闪（采集/投掷/碰撞）

文件：`IngredientVisualEffectSystem/IngredientVisualEffect.cs` + `IngredientVisualEffectController.cs`

- 数据脚本（IngredientVisualEffect）：**多张 Sprite 帧集合**（List\<Sprite\>）、
  fadeOutAfter/fadeOutTime（Vector2 随机范围）、canBeMirrored、color、
  randomSpawnRotation、randomRotationDirection、randomSpawnRotationSpeed（范围）、
  rotationAcceleration、accelerateRotationSpeedToZero、gravityAcceleration、
  speedSlowdownCommon/Explosion、spawnAreaRadiusCommon/Explosion
- 执行器（Controller）：**单 SpriteRenderer + 手写 Update 物理循环**：
  - `speed += gravityAcceleration * deltaTime * Vector3.down`（重力）
  - `speed = Vector3.MoveTowards(speed, zero, slowdown * deltaTime)`（衰减）
  - 旋转速度先加速/减速（`accelerateRotationSpeedToZero` → `sign * abs(accel) * delta`）
  - 位移 `position += speed * deltaTime`
  - 淡出 `ShaderEffect().SetEffectValue(value)`（原版用 shader 控制 alpha）
- 池化：静态 `Queue<Controller> effectsPool`，`TakeFromPool/ReturnToPool`，
  预建 N 个（settings.maxVisualEffectsCount）
- 触发：`StackVisualEffects.SpawnEffectsExplosion`（拿起/扔出爆 5 个）、
  移动速度 >20 时按冷却概率持续冒（Common 型）
- **关键**：这是"蹦出来的碎屑闪光"，有旋转有位移，不是墙壁闪烁

### 3.2 CrystalSparks + GlobalMeshParticleSystem —— 岩壁/水晶持续闪光 ✅ 用户要的"布灵布灵"

文件：`ParticleSystemFX/`（GlobalParticleSystemsPool、ParticleSystemsGroup、
MeshParticleSystemEntry）+ `SpriteParticleApplier` + `ParticleAreaMeshCollection`

- `SpriteParticleApplier`（挂在岩壁/水晶 SpriteRenderer 上）：精灵变化时查
  `ParticleAreaMeshCollection`（美术在编辑器手绘的多边形路径表）→
  `SpriteParticleMeshGenerator.CreateMesh()` 把路径转 Mesh → 粒子系统以
  **Mesh 为发射形状**
- `ParticleSystemsGroup.UpdateGroupEmission()`：
  `rateOverTime = areaFactor × mesh表面积 × 全局SpawnRate`（**面积驱动密度**）
- CrystalSparks 是**持续发射的 Unity ParticleSystem**（非 one_shot），
  粒子原地出生、原地闪烁（见第 5 节真实参数）
- 池化：GlobalParticleSystemsPool（按 prefab 分池）
- 全局开关：可访问性"闪光"开关 → `GlobalMeshParticleSystemSettings.particlesEnabled`

### 3.3 结论：用户要的是 3.2，不是 3.1

本项目最初错误地把 3.1（交互蹦闪）当成了墙壁闪光复刻 → 完全不像。
正确路径是 3.2 的机制 + 真实参数。

---

## 4. 资源包破解方法（大端 bundle 解包）

### 4.1 现象

`StreamingAssets/aa/StandaloneWindows64/defaultlocalgroup_assets_all_*.bundle`
（734MB）用 UnityPy 加载报 `MemoryError`。

### 4.2 根因

**该游戏所有 bundle 的头部整数是大端序（BE）**（UnityPy 按小端读导致字段全乱）：
- rooms 包（128MB）头部验证：ver=8、size=128,456,964 均 BE 精确匹配
- 大包头部：ver=8、size=734,472,611 与文件大小精确一致（BE）
- flags BE = 0x243（LZ4HC + BlocksAndDirectoryInfoCombined + padding）
- blocksInfo 内容（blockCount/块尺寸/目录项）同样 BE

UnityPy 小端读 BE 头 → cbsize 读到 3.8GB → `read_bytes` 吃到文件尾 →
解压 1.7GB 假数据 → MemoryError。rooms 包能读是因为它小，join 没爆内存，
但内部字段同样是 BE（UnityPy 的 EndianBinaryReader 对 bundle 默认大端读取，
恰好匹配！—— 详见 4.4 注意事项）。

### 4.3 破解脚本（本仓库 tools/ 外，位于临时目录，路径见下）

脚本位置：`C:\Users\fanbo\AppData\Local\Temp\opencode\potioncraft\`
- `normalize_bundle.py`：大端头 → 小端归一化（曾用于 UnityPy 直接读，后弃用）
- `stream_extract.py`：**流式解压**（最终方案）

最终方案 `stream_extract.py` 流程：
1. 按 BE 读头部：sig(8B) + ver(4B) + "5.x.x\0" + "2023.1.13f1\0" + size(8B) + cbsize/ucbsize/flags(各4B)
2. blocksInfo 位置 = 头部结束后 16 对齐（50 → 64）
3. `lz4.block.decompress(blocksInfo, uncompressed_size=ucbsize)`（BE 字段）
4. 解析 blockCount(12610) → 每块 (uncomp, comp, flags) → 目录项 (offset,size,flags,path)
5. **逐块 seek + 解压 + 写盘**（不 join！12610×128KB ≈ 1.65GB 流式落盘）
6. 输出 `F:\MyMy\decompressed_cab.bin`，前 303MB 为内层序列化文件
7. `dd` 前 303MB → `cab_main.bin` → UnityPy.load 直接可读

### 4.4 注意事项（踩坑记录）

| 坑 | 教训 |
|---|---|
| 大端头 | 所有整数（ver/size/cbsize/ucbsize/flags/blocksInfo 内）都是 BE；字符串/字节正常 |
| LZ4 重压缩 | `lz4.block.compress(store_size=True)` 会带长度头，UnityPy 按无头解压 → 垃圾；必须 `store_size=False` |
| 16 对齐 | blocksInfo 和数据区都在 64 对齐处；归一化写入前必须先补 padding |
| 内存 | 1.65GB 数据绝不能 `b"".join`，流式写盘 |
| .resS | 纹理等大资源在 `.resS` sidecar（1.29GB），UnityPy 需同目录同名文件才能解析 |
| console 编码 | 中文输出在 GBK 控制台乱码，写 JSON 文件再读 |

### 4.5 临时文件（F:\MyMy\，可删）

- `normalized.bundle`（734MB，归一化版，可留作后续深挖）
- `decompressed_cab.bin`（1.65GB，**建议删除**）
- `cab_main.bin`（303MB，内层序列化文件，深挖时保留）
- `图片/Work/药剂工艺原版闪光贴图_*.png`（若提取成功）

---

## 5. CrystalSparks 真实参数（出土数据）

来源：`cab_main.bin` 内 283 个 `CrystalSparks*` GameObject 的 ParticleSystem
typetree 反序列化。**参数级真实值**：

| 参数 | 原版值 | 说明 |
|---|---|---|
| rateOverTime | **8.0/s 恒定** | 再由 ParticleSystemsGroup 按 表面积×areaFactor 缩放 |
| startLifetime | **0.72s 恒定** | |
| startSize | **0.13 单位 ≈13px**（@100PPU） | 小而精 |
| startColor | **渐变：出生冷白蓝(0.77,0.88,0.91)不透明 → 渐隐透明** | 快亮慢暗=闪烁本体 |
| gravityModifier | **0** | 不飘落 |
| 初速度 | **0** | 原地出生原地闪 |
| 旋转 | **关**（RotationModule.enabled=false） | |
| 外部力/碰撞/拖尾 | 全部 false | |
| ShapeModule | **type=20（Mesh）** | 运行时塞精灵网格 |
| maxNumParticles | 1000 | |
| 材质 | **Unity 内置 Default-ParticleSystem**（软白点） | 非定制美术！ |
| UVModule | tilesX=1,tilesY=1（整贴图，非翻页动画） | |
| 颜色变体 | maxGradient 后段含橙金/紫/青/粉（a=0，变体储备） | "CrystalSparks - 1..5"、"Goblet1Color1" 等 283 个变体 |

**视觉结论**：药工水晶闪光 = 冷白蓝小点、原地出生、靠颜色渐隐快速亮起缓慢消失。
没有位移、没有金色（金色是我之前脑补的）。

---

## 6. Godot 复刻实现映射

| 药工组件 | 本项目实现 | 对应关系 |
|---|---|---|
| CrystalSparksBase（挂 5 个 -1..-5 强度变体粒子系统） | `crystal_sparkles.gd`：`attach_to()` 循环创建 5 个 CPUParticles2D 子节点，areaFactor [6.0,4.5,2.25,1.5,0.75]，preprocess 按 index 错相 | ✅ 5 子系统结构对齐；一簇一色（同主题色，每粒子白↔主题随机） |
| ParticleSystemsGroup.UpdateGroupEmission（面积驱动速率） | `compute_rate(tier, area, areaFactor, jitter)` = tier × AF × mesh_area × SpawnRate × jitter；mesh_area = clamp(轮廓面积×0.12, 0.008, 0.35) | ✅ 公式对齐原版 UpdateGroupEmission；jitter 按子系统构建时各取一次等价于每帧随机 |
| SpriteParticleApplier（精灵上挂粒子） | `AmbientSparkleSpawner`（每 0.5s 扫描 `resource_node` 组，视野内补挂 CrystalSparkles） | 资源点=水晶洞对应物；增加视野门控防全图数百节点同时挂载 |
| shape.mesh（区域发射） | `emission_shape = POINTS` + `sample_host_points()` 轮廓点集（Sprite2D 按 alpha / Polygon2D 三角化内采样 / ColorRect 网格；空点集回退 RECTANGLE）+ `measure_host()` 面积测量（shoelace） | Mesh→POINTS 点集等价（药工是美术手绘多边形，粒子只出生在轮廓内）；5 子系统共享同一份点集 = 原版共享同一 mesh |
| startColor RandomBetweenTwoGradients（白↔主题色） | `color_initial_ramp`（Gradient 两键 a=1） | ✅ 等价；全程不透明无渐隐 |
| Default-ParticleSystem 混合模式：AlphaBlend + Standard Unlit（2026-08-29 解包实证 _SrcBlend=5/_DstBlend=10） | `CanvasItemMaterial`：BLEND_MODE_MIX + LIGHT_MODE_UNSHADED | ✅ 扁平无泛光；unlit=不受昼夜压暗（B7/B11） |
| SizeModule 脉冲曲线（Hermite 0→0.97→0.42） | `_build_size_curve()` 七点采样 → `scale_amount_curve`（Curve）× 0.203（贴图 scale） | ✅ 还原"长出→缩回→硬切"脉动 |
| startSize 0.13 单位 | 64px 贴图 × scale_curve 峰值 ≈13px | 一致 |
| 无重力无速度 / prewarm+looping | direction/gravity 全零；preprocess=lifetime（tier20 除外）；one_shot=false | 一致 |
| 池化（GlobalParticleSystemsPool） | `FxPool`（group "fx_pool" + 数组复用） | 一致 |
| IngredientVisualEffect（交互蹦闪，采集/投掷用） | `modules/fx/scripts/ingredient_visual_effect.gd` + `ingredient_visual_effect_controller.gd` | 逐字段逐行翻译——**保留未接入玩法**，待采集系统 |
| 全局闪光开关 | 待接（可参考设置面板"闪光"开关） | |

关键 Godot API 注意（2026-08-29 起水晶闪光用 **CPUParticles2D**，原因见附录 B 表"附带"行）：
- CPUParticles2D 的 `color_initial_ramp` / `scale_amount_curve` 直接吃 **Gradient / Curve**（GPU 版才要 GradientTexture1D / CurveTexture 包装）
- **Godot 4.4+ 把 GPUParticles2D 的点集发射改成了 `emission_point_texture`**：位置编码进纹理像素、配 `emission_point_count` 使用；旧的 `emission_points` 数组属性已删，赋数组直接报 Invalid assignment。CPUParticles2D 的 `emission_points`（PackedVector2Array）不受影响
- CPUParticles2D **没有 rate_over_time 属性**，连续速率 = amount/lifetime（与 GPU 版同）
- CPUParticles2D 默认 `gravity = (0, 980)`，原地闪烁粒子必须显式置零；`direction/spread/initial_velocity` 同理
- `measure_host()` 是公开静态方法，AmbientSparkleSpawner 直接复用避免重复测量逻辑
- 宿主面积→mesh 面积做了上下限截断（MESH_AREA_MIN/MAX），防止巨型宿主把速率顶到失控
  （药工的发射 mesh 是美术手绘的小块区域，大面积宿主在原版里是多挂几个小块而非一个大块）

---

## 7. 原创 vs 复刻 vs 占位清单

| 项 | 类型 | 说明 |
|---|---|---|
| 水晶闪光 5 子系统 + 原版速率公式（crystal_sparkles.gd） | ✅ 参数级复刻 | 5 子系统 areaFactor [6,4.5,2.25,1.5,0.75] + 原版 UpdateGroupEmission 公式 + SizeModule 脉冲曲线 + RandomBetweenTwoGradients 配色 + 轮廓点集发射（B5）；AlphaBlend 实证（B11）+ UNSHADED 保色（B7）+ 一簇一色（B8）+ 烘焙实测密度标定（B9）+ 子系统错相（B10） |
| 演员特效系统（ingredient_visual_effect*.gd） | ✅ 机制级复刻 | 逐字段翻译，未接入玩法 |
| 建造尘土/打击火花/采集飘屑 | ❌ 原创 | 药工无建造系统；参数自调 |
| 夜间跟随灯光（CampLight） | ❌ 原创 | 药工无玩家跟随光 |
| 星点/圆点/星芒贴图 | ⚠️ 占位 | 程序化生成；原版即 Unity 默认软点，形态已接近；P1 可换手绘 |
| 截图工具舞台（水晶/石墩） | ⚠️ 占位 | 程序化多边形替身，非游戏内美术 |

### 已完成修复项（2026-08-29）

| # | 差异 | 修复内容 | 验证 |
|---|---|---|---|
| B1 | 单系统 → 5 系统 | attach_to 循环 5 次，areaFactor [6,4.5,2.25,1.5,0.75]，配色 [主色,白,金,青,粉] | 测试断言 5 子系统 + 截图多色可见（五色配色后由 B8 修正为一簇一色） |
| B2 | 速率公式缺 areaFactor | compute_rate = tier × AF × mesh_area × SpawnRate × jitter | 测试断言公式精确匹配 |
| B3 | color_ramp 渐隐误用 | 删除 color_ramp，全程 a=1（color_initial_ramp 白↔主题色） | 代码审查 ✓ |
| B4 | 无 size 脉冲 | scale_curve 七点 Hermite 还原（0→峰0.97→缩回0.42→硬切） | 截图可见脉动闪烁 |
| B5 | 发射区 BOX（矩形均匀） | `sample_host_points()` 轮廓点集 → `EMISSION_SHAPE_POINTS`；Sprite2D 按 alpha 网格采样、Polygon2D 三角化面积加权内采样（确定性 seed）、ColorRect 网格点；空点集回退矩形 | 截图：粒子全部贴合水晶轮廓，无外溢；单测 4 用例（点在轮廓/多边形/矩形内 + 透明回退） |
| 附带 | GPUParticles2D → CPUParticles2D | Godot 4.4+ 把 GPU 版点集改为 `emission_point_texture`（位置编码进纹理像素，语义晦涩）；CPU 版 `emission_points` 仍是 PackedVector2Array 一一对应，且每子系统在世仅 2~6 颗，CPU 成本可忽略 | 单测 11/11 + 截图 7 张 + 报错自检干净 |
| 新增 | 宿主面积测量不全 | measure_host() 支持 Sprite2D / Polygon2D(shoelace) / ColorRect 三种节点类型 | 测试断言三种面积精确 |
| 新增 | tier 缺失键兜底错误 | TIER_TABLE.get(tier, TIER_TABLE[DEFAULT_TIER]) 替代 .get(tier, 8.0)（后者把 8 当寿命） | 测试断言兜底寿命正确 |
| 新增 | 大型宿主速率失控 | mesh_area clamp [MESH_AREA_MIN, MESH_AREA_MAX] = [0.008, 0.35] | 420px 水晶从 149→39 颗粒子 |

---

## 8. 调试与截图验证流程

### 8.1 F3 调试工具面板（调试入口）

- F3 展开 → 底部勾选「调试工具面板」→ 特效页签：
  - 视口中心试放按钮（建造尘土/采集飘屑/打击火花）
  - 点放模式（点世界任意处）
  - 压力测试（3 效果 ×10 连发）
  - 「挂载水晶闪光：全部资源点」按钮（手动触发挂载 + toast 计数）
- 环境页签：时间轴滑杆（调昼夜看闪光对比）+ CampLight 能量读数
- 市场页签：资源价格实时表 + 增减库存 + 强制结算
- 建筑页签：升级/修理即时操作

### 8.2 截图/动图交付工具（用户不用开游戏看效果）

```bash
godot --path stick-world --script tools/fx_screenshot.gd
# 弹窗约 20 秒自动退出，输出 7 张 PNG 到 F:\MyMy\图片\Work\
godot --path stick-world --script tools/fx_gif.gd
# 弹窗约 15 秒自动退出，双场景各 90 帧 PNG（30fps × 3s，帧对齐采样+采集后统一落盘）
# day_* = 原版水晶洞风格（羊皮纸亮底）；night_* = 游戏内夜间
# 再用 PIL 拼动图（30fps）：
python -c "from PIL import Image; import glob; fs=[Image.open(f).convert('RGB').quantize(256) for f in sorted(glob.glob('F:/MyMy/图片/Work/fx_gif_frames/day_*.png'))]; fs[0].save('F:/MyMy/图片/Work/水晶闪光_原版风格_日间.gif', save_all=True, append_images=fs[1:], duration=33, loop=0)"
```
- 截图：01 水晶白天 / 02 水晶夜间+灯光 / 02b 密度特写 / 03 星芒特写 / 04 尘土 / 05 飘屑 / 06 火花
- GIF 双场景：day=原版羊皮纸洞风格（一簇一色/多对象密度），night=游戏内夜间（USHADED 保色）；
  **闪烁效果以 GIF 为准**，静态 PNG 只能验证布局/配色/轮廓贴合，验证不了脉动
- ⚠️ 采集期 save_png 会阻塞主线程造成慢动作/掉帧——帧必须先驻留内存采集结束后统一落盘；
  12fps 采样对 0.72s 寿命粒子只有 ~9 帧/周期，观感频闪，30fps 起步
- 工具无 autoload 依赖，任何状态可跑；改完特效重跑一次即可自验
- ⚠️ 工具内舞台坐标系必须按实际窗口视口 1920×1080 画背景，输出再缩放——
  背景画小了会露出 Godot 默认灰底（曾踩：背景 1280×720 → 截图四周灰边）

---

## 9. 后续深挖清单

1. **CrystalSparks 变体参数**：283 个变体（-1..-5 强度、Goblet1Color1 等颜色主题）
   的差异（areaFactor 在 ParticleSystemsGroup MonoBehaviour 里）
2. **其它药工特效参数**：药水气泡、采集碎屑、炼金机器气泡（AlchemyMachineBubbles shader
   参数已在 rooms 包提取过属性名）；SubModule 已考古为空壳（见附录 A5），无需深挖
3. **ParticleAreaMeshCollection 的 Mesh 路径**：美术手绘路径→Mesh 的生成器逻辑
   （`SpriteParticleMeshGenerator.CreateMesh` 反编译可读，路径转三角形化）
4. **P1 替换**：星点贴图换手绘帧（药工原版是 Unity 默认软点，可考虑直接用
   Godot 内置点纹理或自绘）
5. **交互蹦闪接入**：采集/投掷时调 `IngredientVisualEffectController.spawn`

---

## 10. 附：本次会话修复记录

### 10.1 会话内踩过的 Godot 编译/运行坑（全项目排查后清零）

| 坑 | 修复 |
|---|---|
| GDScript 禁止 static/instance 方法同名 | `FxPool.burst`(实例) vs `spawn_burst`(静态) |
| `color_ramp` 要 GradientTexture1D | 封装 `_ramp_tex()`（GPU 版；CPU 版直接 Gradient） |
| `scale_curve` 要 CurveTexture | 同 |
| **Godot 4.4+ GPUParticles2D 点集发射改为 `emission_point_texture`（位置编码进纹理像素）**，`emission_points` 数组属性已删，赋 PackedVector2Array/Array/Array[Vector2] 全部 Invalid assignment | 水晶闪光改用 CPUParticles2D（`emission_points` 仍是 PackedVector2Array） |
| 测试脚本缺 `signal test_done(code: int)` 声明 | batch 模式下 batch_runner 等不到信号报 Invalid access；每个 batch 测试脚本必须声明 |
| `var x := node.method()` 从无类型返回值推断失败 | 显式 `: float` 等 |
| 重复定义函数（get_building_node 审计旧版 + 新版） | 合并为一份 |
| `slider.dragging` 不存在 | 用 drag_started/drag_ended 信号打标 |
| SQL "database is locked" | `PRAGMA busy_timeout=5000` + save/load 入口关残留连接 |
| GPUParticles2D 无 rate_over_time | amount/lifetime 换算 |

### 10.2 药工复刻专属坑

| 坑 | 修复 |
|---|---|
| 大端 bundle 头 | 见第 4 节 |
| 误把交互蹦闪当墙壁闪光 | 分清 3.1/3.2，参数出土后修正 |

---

*文档维护：新增特效参数/破解进展/调参记录请追加到对应章节，勿覆盖历史。*

---

## ⚡ 交接附录（2026-08-26 深挖终版 · 供下一位 AI/开发者接手）

> 本轮完成了资源包全面解包，以下是最终出土数据、当前实现与原版的逐项差异、
> "还是不像"的疑似根因排序、以及接手者下一步的精确操作清单。

### A. 最终出土数据（本轮新挖，覆盖 §5）

**A1. areaFactor 终于拿到**（在 CrystalSparksBase GameObject 的第 4 组件
ParticleSystemsGroup MonoBehaviour 里，不在粒子系统上）：

每个 CrystalSparksBase 挂 5 个粒子系统（对应 "- 1" ~ "- 5" 强度变体），areaFactor：

| 变体 | areaFactorForEmission | useAreaFactor |
|---|---|---|
| - 1 | 6.0 | true |
| - 2 | 4.5 | true |
| - 3 | 2.25 | true |
| - 4 | 1.5 | true |
| - 5 | 0.75 | true |

最终速率公式（原版 UpdateGroupEmission）：

```
rateOverTime = areaFactorForEmission × mesh表面积(单位²) × 全局SpawnRate
每次更新再随机 ×0.5~2
```

一个墙面同时挂 5 个系统（5 色 × 5 档组合出 283 个预制变体）——密度是单系统的 5 倍以上。

**A2. 完整字段真相（修正此前两处误读）**：

| 字段 | 真值 | 此前误读 |
|---|---|---|
| maxGradient alpha | key0 a=1 → key1(t=1.0) a=1，全程不透明；key2~7 全部 t=0（未用储备） | "快亮慢暗渐隐" ❌ |
| 闪烁本体 | SizeModule.enabled=true（曲线 0→峰值≈0.97→0.42→硬切，Hermite 端点斜率 -4.6633）= 长出→缩回→啪没 | 无 size 曲线 ❌ |
| SizeModule.size.maxCurve | key(0, 0.0, outSlope=0) → key(1, 0.4154, inSlope=-4.6633) | |
| minGradient | 纯白 a=1 恒定（RandomBetweenTwoGradients 的另一端） | 忽略了 ❌ |
| 主模块 | looping=true, prewarm=true, playOnAwake=true, startSpeed=0, startRotation=0, lengthInSec=1.0 | prewarm 漏了 |
| SubModule | enabled=true（子发射器，作用未深挖） | |
| 速率×寿命绑定 | {1:1.72s, 2:1.6s, 3:1.08s, 6:0.96s, 8:0.72s, 20:0.1s+maxP25+prewarm=false} | |
| 实际挂载速率分布 | 0.008~0.62/s（烘焙实例实测；面积缩放后非常稀疏，靠多系统×多对象叠加） | |

**A3. 真实贴图**：`F:/MyMy/图片/Work/药剂工艺闪光贴图_Default-ParticleSystem.png`
（64×64 高斯软点）。已复制进项目：`modules/fx/assets/sparkle_dot_default_particle.png`
（运行时 load，导入前回退程序化软点）。

**A4. 完整数据文件**（交接用，在 `C:\Users\fanbo\AppData\Local\Temp\opencode\potioncraft\`，
⚠️ 临时目录可能已被清理；若缺失按 §4 流程重解包，逻辑完整）：
- `cs_full_dump.json` —— CrystalSparks - 1 全字段 typetree（50KB）
- `cs_gameobjects.json` —— 283 个对象 (pid/name/comps)
- `cs_variants.json` —— 全变体 rate/lifetime/size/颜色三键
- `cs_group_mb.json` —— CrystalSparksBase 的 ParticleSystemsGroup（areaFactor 表）
- `ps_names.json` —— 全部 290 个粒子系统名

**A5. 材质与子发射器考古（2026-08-29，cab_main.bin 实测）**：
- 283 个 CrystalSparks 变体的 Renderer 全部指向同一材质 **Default-ParticleSystem**（无任何 Crystal/Spark 专属材质）；shader=Particles/Standard Unlit，_SrcBlend=5(SrcAlpha)、_DstBlend=10(OneMinusSrcAlpha)、_Mode=2(Fade)、_ZWrite=0 → **AlphaBlend**（曾误判 Additive，见附录 B #11）
- SubModule：enabled=true 但 subEmitters 的 emitter 引用为 NULL（全文件 290 个粒子系统同模式）——序列化占位空壳，死亡不触发任何子发射，复刻无需实现
- 考古过程数据：F:/MyMy/图片/Work/fx_audit_findings.md

### B. 当前实现与原版的逐项差异审计（"还是不像"的根因排序）

**2026-08-29 更新：B1~B4 已全部修复并验证通过。剩余差异仅 B5（发射区精度）→ B5 也已于同日修复，见上表。**

| # | 差异 | 影响 | 状态 |
|---|---|---|---|
| 1 | ~~单系统 vs 5 系统~~ | ~~密度和色彩丰富度差 5 倍~~ | ✅ 已修：attach_to 循环 5 子系统 + areaFactor [6,4.5,2.25,1.5,0.75] |
| 2 | ~~速率公式缺 areaFactor~~ | ~~速率偏低~~ | ✅ 已修：compute_rate = tier × AF × mesh_area × SpawnRate × jitter |
| 3 | ~~color_ramp 渐隐误用~~ | ~~亮度观感~~ | ✅ 已修：全程 a=1，仅 color_initial_ramp 白↔主题色 |
| 4 | ~~无 size 脉冲~~ | ~~脉动感缺失~~ | ✅ 已修：scale_curve 七点 Hermite 还原 |
| 5 | ~~发射区 BOX（矩形均匀）vs 原版 Mesh（精灵轮廓多边形）~~ | ~~粒子落在透明区域/边角~~ | ✅ 已修（2026-08-29）：`sample_host_points()` 采样轮廓点集 → `EMISSION_SHAPE_POINTS`（Sprite2D 按 alpha、Polygon2D 三角化内采样、ColorRect 网格；空点集回退矩形） |
| 6 | ~~混合模式 alpha blend~~ | ~~粒子是"贴白点"不发光，没有亮晶晶感（静态截图不明显的另一根因）~~ | ✅ 已修（2026-08-29）：`CanvasItemMaterial.BLEND_MODE_ADD` 发光叠加——Unity 新建 ParticleSystem 的默认材质即 Additive，暗背景重叠处越来越亮 |
| 7 | ~~夜间配色全部丢成白点~~ | ~~CanvasModulate 压暗 × Additive 把主题色乘暗后≈不可见~~ | ✅ 已修（2026-08-29）：`light_mode = UNSHADED`（对齐 Unity Particles/Standard Unlit，粒子不受昼夜压暗/Light2D 影响）；另注意原版主体即冷白蓝 (0.77,0.88,0.91)，白点占一半是 RandomBetweenTwoGradients 机制 |
| 8 | 每粒子随机白↔主题色 | 等价 ✓ 无需动 | — |
| 9 | 主模块 prewarm/looping | 已实现（preprocess=lifetime）✓ | — |
| 10 | ~~5 子系统五彩配色~~（B1 引入的发明） | 原版一簇一色：5 强度变体同色，仅白↔主题随机（2026-08-29 原版截图调研确认） | ✅ 已修（B8）：删除 variant_palette，全部子系统同 theme_key |
| 11 | ~~Additive 混合~~（B6 误判） | 解包实证 Default-ParticleSystem=SrcAlpha/OneMinusSrcAlpha **AlphaBlend**；原版扁平手绘无泛光 | ✅ 已修（B11）：回退 BLEND_MODE_MIX（UNSHADED 保留，unlit 正确） |
| 12 | 密度 10~30× 于原版 | mesh clamp 上限 0.35 vs 烘焙实测速率反推 ≈0.013 单位²（0.62/s ÷ tier8×AF6） | ✅ 已修（B9）：MESH_AREA_MAX=0.013、MAX_RATE=2.0；注 CPUParticles2D 连续发射下限 1/lifetime（每宿主最低 ~5 颗在世，原版靠多对象叠加总密度） |
| 13 | 整簇同步闪烁/死场帧 | 5 系统同寿命同 preprocess 相位锁死，相位谷重叠即整簇熄灭 | ✅ 已修（B10）：preprocess = index × lifetime/5 错相（原版 5 系统寿命各异天然错相） |
| 14 | GIF 12fps 频闪 + 采集掉帧 | 采样率低 + save_png 逐帧阻塞主线程（模拟慢动作） | ✅ 已修：30fps 帧对齐采样 + 帧驻留内存采集后统一落盘 |

结论：B1~B11 全部闭环（B6 为误判由 B11 回退）。**验证以动图为准**（tools/fx_gif.gd 出帧序列 → PIL 拼 GIF）：
闪烁是时间维度效果（size 脉冲 + Additive 叠加），静态截图恰好抓到粒子小尺寸阶段，
"怎么看怎么不像"多数是静态图的天然局限。无遗留差异项。

### C. 疑似根因的验证方法

1. 修完 B1~B11 后跑 tools/fx_gif.gd 出双场景动图（tools/fx_screenshot.gd 出静态图）：
   应出现每颗水晶 5 色 5 档混合的多彩闪烁（白/主题/粉/青/金同屏），
   且闪光点全部落在水晶轮廓内部（B5 验收点：轮廓外无散粒）
2. 和药工实机对照：打开药工水晶洞存档截图，并排比较密度/颜色/大小
3. 若密度仍不足：查全局 SpawnRate——原版 particlesSpawnRate 默认值在
   GlobalMeshParticleSystemSettings 的 settings 资产里（cab_main.bin 内 MonoBehaviour）

### D. 接手者操作清单（按顺序执行）

**2026-08-29 状态：全部完成 ✅**

| # | 操作 | 状态 | 验证 |
|---|---|---|---|
| 1 | 读本文 §3 → §5 → 附录 A | ✅ | — |
| 2a | attach_to 循环 5 次 + areaFactor [6,4.5,2.25,1.5,0.75] + 5 色配色去重 | ✅ | 测试断言 5 子系统 |
| 2b | compute_rate = tier × AF × mesh_area × SpawnRate × jitter | ✅ | 测试断言公式精确匹配 |
| 2c | 删 color_ramp 渐隐，全程 a=1 | ✅ | 代码审查 ✓ |
| 2d | scale_curve 七点 Hermite 脉冲还原 | ✅ | 截图可见脉动闪烁 |
| 3 | 单元测试同步（7 用例 / 31 断言） | ✅ | 全量 unit 通过 |
| 4 | fx_screenshot.gd 重跑（含新增 02b 密度特写） | ✅ | 7 张 PNG 均正常；420px 水晶 39 颗粒子 |
| 5 | 更新 §6 映射表 + §7 清单 + 附录 B/D | ✅ | 本文档已更新 |
| 6 | **B5：发射区 BOX → POINTS 轮廓点集**（2026-08-29 第二轮） | ✅ | 单测 11/11（新增 4 用例）+ 截图粒子全部贴合轮廓 + 报错自检干净 |
| 6a | `sample_host_points()`：Sprite2D alpha 网格采样 / Polygon2D 三角化面积加权 / ColorRect 网格，空点集回退矩形 | ✅ | 单测 4 用例 |
| 6b | GPUParticles2D → CPUParticles2D（4.4+ GPU 版点集已纹理化，CPU 版保留数组 API；粒子量小成本可忽略） | ✅ | 单测 + 截图 |
| 7 | **B6：Additive 发光混合**（2026-08-29 第三轮，用户反馈"怎么看怎么不像"后排查） | ✅ | 夜间 GIF 亮斑发光明显 |
| 7a | `CanvasItemMaterial.BLEND_MODE_ADD` —— Unity 默认粒子材质即 Additive，此前漏译是"不亮"的根因 | ✅ | 对比修复前后夜间图 |
| 7b | tools/fx_gif.gd 帧序列工具 + PIL 拼 GIF（闪烁是时间维度效果，静态图验收不了脉动） | ✅ | 水晶闪光_亮晶晶.gif |
| 8 | **B7：UNSHADED 保色**（2026-08-29 第三轮，用户指出"全是纯白小点"） | ✅ | 夜间 GIF 三色（紫/绿/金）可辨 |
| 8a | `CanvasItemMaterial.light_mode = LIGHT_MODE_UNSHADED`：CanvasModulate 压暗曾把彩色乘没（Additive 下暗色不可见） | ✅ | 修复前 gold 主题全白 → 修复后金点明显 |
| 8b | GIF 舞台主题换 violet/mint/gold 强对比组合，右水晶 tier 3→6 增加粒子量 | ✅ | 同上（后由第 4 轮双场景舞台取代） |
| 9 | **B8 一簇一色**（第 4 轮 2026-08-29：三路子代理审计——我方 GIF 视觉分析 / 原版截图调研 / 解包数据考古） | ✅ | 单测 _test_single_theme + 视觉验收无跨簇串色 |
| 10 | **B11 AlphaBlend 回退**：解包实证 283 变体全用 Default-ParticleSystem（Particles/Standard Unlit，_Mode=2 Fade） | ✅ | 视觉验收：无过曝糊斑、重叠不变亮（additive 曾致灯笼区糊斑） |
| 11 | **B9 密度标定**：MESH_AREA_MAX 0.35→0.013（0.62/s 反推）、MAX_RATE 60→2.0 | ✅ | 单测上下限断言；视觉密度对齐"每簇 5~15 亮点" |
| 12 | **B10 相位错开**：preprocess=index×lifetime/5 | ✅ | 单测错相断言；夜间 10 帧终验 13~18 点/帧、无死场、三簇永不为零 |
| 13 | GIF 工具 30fps 帧对齐 + 延迟落盘 + 双场景（day 羊皮纸原版风/night 游戏夜间） | ✅ | 水晶闪光_原版风格_日间.gif + 水晶闪光_夜间.gif |

**B5 轮次额外修复（发现于实施过程）：**
- 测试脚本缺 `signal test_done(code: int)` 声明（batch 模式必需，缺失时 batch_runner 报 Invalid access）
- 发射点集坐标系：粒子系统节点挂在轮廓中心 `meas["center"]`，点集必须减去中心转为相对坐标（否则双重偏移）

**额外修复（发现于实施过程）：**
- 宿主面积测量增加 ColorRect 支持（资源节点当前是色块占位）
- tier 缺失键兜底从错误的 8.0s 寿命改为 DEFAULT_TIER=8 的 0.72s
- 大型宿主速率失控：mesh_area clamp [0.008, 0.35]（药工发射 mesh 是小块区域）
- AmbientSparkleSpawner 增加视野门控（避免全图数百节点同时挂载）
- fx_screenshot.gd 相机 make_current 时序错误修复

### E. 决策记录（历史决策与原因，防止被无谓推翻）

| 决策 | 原因 |
|---|---|
| 岩壁闪光用 GPUParticles2D（原版即 ParticleSystem） | 等价物正确；交互蹦闪是精灵脚本循环，已单独翻译成 ingredient_visual_effect*.gd |
| 贴图直接用提取的原版 PNG（P1 再换自制） | 用户明确允许素材照搬当占位；原版即 Unity 默认软点 |
| 速率=档位×面积缩放 | 原版 UpdateGroupEmission 公式，烘焙实例验证 |
| 每材料一色（THEMES 16 色） | 283 变体颜色主题统计 |
| 稀疏挂载（AREA_SCALE） | 烘焙实例分布；需结合 B1 多系统叠加修正总体密度 |
| F3 面板开关调试工具（非 F4） | 用户指定 |
| 截图交付到 F:\MyMy\图片\Work | 用户指定 |
| 清理项：analytics/iap 空目录、dotnet 段、文档漂移 | 第一批清理（用户批准） |
| WorldState 冻结标注（不删不接） | 用户决策 |
| BalanceConfig F9 热重载 | 用户决策 |
| AudioManager 只建框架 | 用户决策（零音频资产） |
| God Object 拆分推迟 | 用户决策（第五批，未做） |
| 存档双轨合并、safe_emit 废除、UI 框架合并 | 第二批架构债（已完成） |

---

## 附录 F：原版视觉参考调研（2026-08-29，子代理对 Steam 官方截图）

> 三路审计之一：下载 Steam 官方水晶洞截图逐像素分析（参考图存 F:/MyMy/图片/Work/ref/）。
> 该调研直接驱动了 B8（一簇一色）、B11（AlphaBlend 回退）、B9（密度）三项修正。

**关键结论**：
1. **一簇一色**：洞内多个单色簇（钢蓝/透白/玫瑰红/琥珀橙/紫），闪光白色为主带极轻色偏，**不是一颗水晶上五彩**
2. **闪光分层**：主体是白色**四角星形**（+ 与 45°× 两种，8-16px）+ 晶面 1-3px 微尘白点。
   星形疑为水晶贴图自带的美术层（粒子材质是柔点贴图，无法产生星形）——复刻 CrystalSparks
   只到"柔点微尘"层；若要星形需美术给水晶加星芒贴图（程序化星芒已有 FxLibrary._star4 可作 P1）
3. **扁平手绘风**：白点是实心矢量感，无光晕/bloom/泛光，重叠不变亮（与 AlphaBlend 实证互证）
4. **底色不暗**：洞底浅褐羊皮纸、暗部仅中灰矿石环带与围岩——闪光处于中等对比，不是黑底高对比
5. **密度靠多对象**：单簇 5~15 亮点 + 微尘层，密度来自"多个子对象各挂系统"叠加

**验收清单**（30fps GIF 可核对）：
1. 白点主体+极轻色偏；一簇一色无串色
2. 每簇 5~15 亮点，无满屏常亮、无整簇归零、无死场帧
3. 点小而锐（亮核 1~5px），无大光晕无过曝，重叠不变亮
4. 此起彼伏：连续帧可见单点"长大→缩小→熄灭"，相位错开
5. 日间中等对比（羊皮纸底+灰带），清晰不刺眼

**来源**：Steam 商店官方截图（水晶洞 2.0 / 蘑菇洞 / 店内水晶）：
- https://store.steampowered.com/app/1210320/Potion_Craft_Alchemist_Simulator/
- https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/1210320/ss_99c1bf0b394493258e03824b05061d298a5a7079.1920x1080.jpg
- https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/1210320/ss_09892f4ef7797fe97d17bf6b4431e2a94218e385.1920x1080.jpg

**当前验证状态**：日间场景清单 5/5 PASS（金簇密度曾 FAIL，补簇内对象数后复检 PASS）；
夜间场景 USHADED 保色可辨、10 帧无死场（相位错开修复前存在整簇同步熄灭）。
