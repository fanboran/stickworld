# 场景与战斗架构

> **P0 原型阶段的工程架构方案。**
> 解决火柴人行为 AI、小队级战斗、定居点建设三大高度耦合系统的解耦设计。
> 关联文档：[`../../设计/GDD.md`](../../设计/游戏设计文档.md)、[`../../设计/系统/02-战斗系统.md`](../../设计/系统/02-战斗系统.md)、[`../../设计/系统/01-组织系统.md`](../../设计/系统/01-组织系统.md)、[`../../设计/系统/03-定居点与建筑.md`](../../设计/系统/03-定居点与建筑.md)、[`../../设计/核心循环.md`](../../设计/核心循环.md)、[`世界地图数据流.md`](世界地图数据流.md)、[`战略图架构.md`](战略图架构.md)、[`建筑模块化设计.md`](建筑模块化设计.md)

---

## 〇、文档目的

GDD 定义了"做什么"，本文档定义"怎么搭才不变屎山"。

P0 阶段三件事必须同时上线：
1. 火柴人行为 AI（特别是战斗 AI）
2. 小队级战斗（10-30 人）
3. 定居点建设基础逻辑

这三者高度耦合：工人建造时被袭击要切战斗 AI、战斗需要建筑作为掩体、建设需要建筑网格、战斗的编队直接复用组织系统的 L1 节点。本文档定义统一的场景骨架与模块边界，让三者各自开发但接口对齐。

---

## 一、设计原则（三条铁律）

### 1.1 逻辑层 ≠ 表现层

所有系统状态放在 `core/entities/` 的 State 对象里，**场景节点只是 State 的视图**。

- 火柴人 HP/位置/动画状态 → `StickmanState`
- 建筑血量/等级/占地 → `BuildingState`（新建）
- 战斗进度/参战双方 → `BattleState`（已有）
- 编制关系/指挥链 → `OrganizationState`（已有）

**场景卸载时 State 不丢**，挂载到新场景就能继续。这是支持"城镇就地变战场"、"快速旅行后状态延续"的基础。

### 1.2 常驻宿主 + 动态子场景

`GameRoot` 主场景常驻，村落/战场/室内都是动态加载的子场景。

- 环境、相机、UI、时间、输入分发跨场景保持
- 地图切换 = 替换 `WorldChunkHost` 的子节点

### 1.3 接口契约

模块间通信一律走 `api.gd` 或 EventBus。**任何 `get_node("../...")` 跨模块引用视为 Bug**。

依赖方向严格单向（见 §二 模块依赖图）。

### 1.4 子文档索引

本文档已按子系统拆分为以下独立文件，详情见各子文档：

| 文档 | 内容范围 | 来源章节 |
|------|----------|----------|
| [场景与战斗/场景宿主架构.md](场景与战斗/场景宿主架构.md) | GameRoot 主场景骨架、CameraRig 详细设计、启动流程 | §二 |
| [场景与战斗/地图与场景图.md](场景与战斗/地图与场景图.md) | 地图类型、Chunk 流式加载、MapInstance、室内系统、旅行系统 | §三、§五、§六 |
| [场景与战斗/建筑与定居点.md](场景与战斗/建筑与定居点.md) | 建筑系统(分层/网格/节点结构)、定居点建设、工程量、资源系统 | §四、§九 |
| [场景与战斗/战斗与AI.md](场景与战斗/战斗与AI.md) | StickmanEntity 与行为 AI、战斗实例、编队指挥 | §七、§八 |
| [场景与战斗/UI.md](场景与战斗/UI.md) | UI 分层、Minimap、DebugOverlay、建造交互、UI 布局原则 | §十 |
| [场景与战斗/EventBus信号契约.md](场景与战斗/EventBus信号契约.md) | 场景/地图/输入/建筑/战斗/环境 新增信号 | §十四 |

---

## 二、模块依赖图（防循环依赖）

```
                    ┌──────────────┐
                    │  GameRoot    │ (常驻容器)
                    └──────┬───────┘
                           │
        ┌──────────┬───────┼────────┬──────────┐
        ▼          ▼       ▼        ▼          ▼
   Environment  Camera  SceneLoader  InputDispatcher  UIRoot
                           │
                           ▼
                    ┌──────────────┐
                    │  MapInstance │ (村落/战场/室内)
                    │  ├─PlacementGrid
                    │  ├─Buildings
                    │  └─StickmanEntities
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   construction        combat             organization
        │                  │                  │
        └──────────┬───────┴──────────────────┘
                   ▼
                units (StickmanEntity + AI)
                   │
                   ▼
               StickmanRig (已有渲染)
```

**单向依赖规则**：
- 上层依赖下层，禁止反向
- `units` 不直接调 `combat`，由 `combat` 通过 `units/api.gd` 拉数据+下令
- `combat` 不直接调 `construction`，通过 EventBus 通知（如建筑被破坏）
- 跨模块引用 → 走 `api.gd` 或 EventBus

---

## 三、目录结构提案

```
stick-world/
├── core/                              # ✅ 不动，已有
├── modules/
│   ├── units/                         # 扩展：加 ai/ + stickman_entity
│   │   ├── scripts/
│   │   │   ├── stickman_rig.gd        # ✅ 已有
│   │   │   ├── stickman_entity.gd
│   │   │   └── ai/
│   │   ├── scenes/
│   │   │   └── stickman_entity.tscn
│   │   └── api.gd
│   ├── combat/                        # 实现
│   ├── construction/                  # 扩展实现
│   ├── organization/                  # 扩展实现
│   ├── world_map/                     # ✅ 已有，战略图模块（鸟瞰多边形领土，玩家不在其中）
│   ├── environment/                   # 新建
│   ├── world/                         # 新建：Map/Chunk/PlacementGrid
│   ├── building_gen/                   # 新建：建筑运行时（含程序化生成）
│   ├── player_control/                # 新建：选择/编队/指令/附身
│   └── ui/                            # 新建：UI 分层
├── scenes/                            # 新建
│   ├── game_root.tscn
│   └── chunk_templates/
├── assets/buildings/                  # 新建：建筑素材资产
├── config/maps/                       # 新建：地图定义
└── ...
```

---

## 四、P0 实施顺序

P0 = 验证"层级管理→好玩"的关键假设。按以下顺序施工，每步可独立验证：

### 阶段 0.1 - GameRoot 骨架 ✅
- [x] `game_root.tscn` 主场景搭建
- [x] `WorldClock`、`CameraRig`、`SceneLoader`、`InputDispatcher` 基础实现
- [x] `EnvironmentSystem` 简版（仅时间->光照变化，无天气）
- [x] `UIRoot` 三层容器骨架

### 阶段 0.2 - 单张村落地图 ✅
- [x] `VillageMap` + 单个 Chunk（先不做流式，硬编码一张完整地图）
- [x] `PlacementGrid` 实现
- [x] 地面重复纹理渲染（`GroundPolygon`，顶点从 `ground_y` 开始，草地噪波材质）
- [x] `ground_y` / `ground_ratio` / `map_left` / `map_right` 字段实现
- [x] `CameraRig` 重构：水平卷轴 + 1/4 区域跟随 + 垂直显示范围限定（详见 [场景与战斗/场景宿主架构.md](场景与战斗/场景宿主架构.md) §2.4）
- [x] 玩家 `StickmanEntity` 控制移动（WASD），脚部锁定 `ground_y`，X 限界
- [x] `CameraRig.set_ground_y` / `set_map_bounds` / `set_follow_target` 接入
- [x] DESIGN_HEIGHT=1080 三层缩放系统（base_zoom / user_zoom / effective_zoom）
- [x] 相机拖动延迟弹回（5 秒冷却）+ 居中模式（松手即弹回，禁边缘滚动）
- [x] 居中模式按钮（GlobalHUD）
- [x] `DebugOverlay` 调试覆盖层（F3 切换 + 启动图例，详见 [场景与战斗/UI.md](场景与战斗/UI.md) §10.5）
- [x] 建筑三层架构改造（TerrainBuildings / InitialBuildingsList / 存档，详见 [场景与战斗/建筑与定居点.md](场景与战斗/建筑与定居点.md) §4.5）
- [x] WalkBarrier 透明障碍 + PassageBarrier 建筑障碍（详见 [场景与战斗/战斗与AI.md](场景与战斗/战斗与AI.md) §7.1.2）
- [x] BuildMask 不可放建筑区域（详见 [场景与战斗/建筑与定居点.md](场景与战斗/建筑与定居点.md) §4.2）
- [x] ForegroundLayer 前景层（详见 [场景与战斗/地图与场景图.md](场景与战斗/地图与场景图.md) §3.4）

### 阶段 0.3 - 火柴人行为 AI 基础
- [x] `StickmanEntity` 节点结构搭建（AIController 子节点）
- [x] `AIController` + `BehaviorStateMachine`
- [x] `behavior_idle`、`behavior_wander` 实现（随机游走）
- [x] `behavior_work` 实现（与阶段 0.4 建设系统耦合，一起做）
- [x] 测试：村民在村里自主走动（idle ↔ move 循环）

### 阶段 0.4 — 定居点建设
- [x] `Building` 节点结构
- [x] `placement_system`（选址+ghost预览）✅ P0 仅实现选址 API，ghost 预览留到阶段 0.6
- [x] `construction_project` + `work_crew_assigner`
- [x] 测试：选址->派工->建造->完成循环（无独立 tests/integration/test_construction_cycle 文件，验证入口在 GameRoot 演示建造 + tests/integration/test_battle_lifecycle 装配间接验证）

### 阶段 0.5 - 小队级战斗 ✅
- [x] `StickmanEntity` 添加 `Hitbox`、`HealthComponent`、`WeaponMount`
- [x] `behavior_attack`、`behavior_seek_cover`、`behavior_retreat`
- [x] `battle_instance` 挂载到 `VillageMap.BattleAnchor`
- [x] `BattleDirector` + `battle_ai_director`（情绪标签）
- [ ] 战斗结束判定（胜负收束/溃散判定/尸体处理）：**规划延后**——依赖战斗与碰撞系统稳定后实施，见 待办事项.md 低优先级
- [x] 测试：5v5 战斗，掩体利用、压制、溃逃、伤亡、行为切换（tests/integration/test_battle_lifecycle）

### 阶段 0.6 - 编队与指挥 + 小地图
- [x] `selection_system`（框选）
- [x] `formation_system`（编队）
- [x] `tactical_orders` + `command_chain`
- [x] `BattlePanel` UI
- [x] `Minimap` 实现（详见 [场景与战斗/UI.md](场景与战斗/UI.md) §10.4）：缩略图 + 视野框 + 角色点 + 建筑图标 + 点击跳转显示
- [x] `CameraRig` 手动控制完善：拖动 + 边缘滚动 + 缩放（1.0~2.0）+ 居中模式按钮
- [x] 测试：框选→编队→任命排长→下令前进；小地图点击跳转观察战场（tests/integration/test_selection_formation 29/29 通过）

### 阶段 0.7 - 玩家附身 ✅ 已装配（16/16）
- [x] `PossessionInterface` 代码（附身接口 + POSSESS 模式 handler + ESC 退出 + 时间降速）
- [x] `PossessPanel` UI 代码（HP/士气/武器/行为/坐标 + 退出附身按钮）
- [x] `BattlePanel` 添加"附身选中单位"按钮
- [x] `StickmanEntity` 鼠标左键攻击（_player_attack + _find_nearest_enemy_in_range）
- [x] EventBus 新增 `possession_started` / `possession_ended` 信号
- [x] 测试：附身排长亲自操控战斗（tests/integration/test_possession **16/16 通过）

> **装配补全**：`_setup_possession_interface()` / `get_possession_interface()` / `_setup_possess_panel()` / `get_possess_panel()` 已实现（[game_root.gd:363-399](../../../stick-world/modules/world/scripts/game_root.gd)）；`InputDispatcher` 已注册 POSSESS handler（[game_root.gd:370-371](../../../stick-world/modules/world/scripts/game_root.gd)）。

> **阶段定位**：附身是指挥系统的"亲自下场"模式
> （详见 [`场景与战斗/战斗与AI.md`](场景与战斗/战斗与AI.md) §7.5/§8.5、[`../../设计/系统/02-战斗系统.md`](../../设计/系统/02-战斗系统.md) §1）。
> P0 阶段实现最小可用版本（基本附身/释放/ESC 退出），完整版（多层级指挥官附身/附身时 AI 协奏/
> 指挥链延迟=0/自主决策权限切换）在 [`../../项目/路线图.md`](../../项目/路线图.md) 阶段 1
> "核心系统（Alpha）"或阶段 2 "深度扩展（Beta）·完整指挥链设计"中深化。

### 阶段 0.8 - 多场景衔接 ✅
- [x] **地图间切换**（SceneLoader.travel_to_map + ChunkTrigger 出口触发器 + EventBus 信号转发）
- [x] `RoadMap` 实现（road_map.gd + test_road_map.tscn，双向出口触发器）
- [x] 战略图（world_map）从快速旅行到进入聚落（enter_settlement + EventBus.travel_requested）
- [x] 测试：村落A->道路->村落B 完整链路（tests/smoke/test_cross_map_travel 23/23 通过）

> **⚠️ 遗留问题**：战略图闭环（`close_strategic_map`|`api.initialize`）和 Chunk 流式加载仍为 TODO，详见 §六 P0-1/P0-2。`WorldMapController`（旧版）已废弃，见 §六 P0-3。

### 阶段 0.9 - 基础战略图（L1 单层）🔄 实施中

> 阶段 0.9 = **基础战略图**：玩家第一阶段世界图（8 城邦），只做 L1 单层，不做 L3/L2 多级缩放。室内系统归阶段 1/2（见 [路线图.md](../../项目/路线图.md) 阶段 1 第 8 项）。
> 聚落模型见 [08-程序化世界生成.md §0.19](../../设计/系统/08-程序化世界生成.md)：**1 个 L1 地块 = 1 个聚落（或空聚落）**；8 聚落整体 = "1 中心城市 + 若干次级聚落"（标准样本 1 城 + 2 镇 + 5 部落）；**1 聚落 = 1 张主场景图**。
> 技术方案见 [战略图架构.md §10 SM-1'](战略图架构.md)；实施条目见 [待办事项.md](../../项目/待办事项.md) 高优先级。

- [ ] **0.9a L1 世界生成器**（`tools/worldgen/` Python）：随机地块（Voronoi+扰动）→ 8 聚落定位（L1 地块内靠近中心）→ MST 道路 → 8 政权（独立无首领、全敌对）→ 卫星图底图 + 边界索引图（P 社 provinces.bmp 机制，NEAREST 采样）
- [ ] **0.9b 聚落地图生成器**：8 城邦主场景图（seed+biome+级别+产业 → MapDefinition → VillageMap；空聚落不可进入）
- [ ] **0.9c world_map L1-only 重写**：单层数据容器 + MapRenderer（卫星图底图 + 索引图 `_draw` 描边不填充 + 道路/图标/文字）+ query_at_screen 像素解码
- [ ] **0.9d 接线**：`api.initialize`/`close_strategic_map` 补齐，替换 `world_map_panel.gd` 占位，Tab 开关 + 进出聚落 + 退出恢复
- [ ] **0.9e 集成测试** `test_strategic_map_p0`：索引图命中、边界描边、空聚落、进出链路

> **室内系统（阶段 1/2）**：透明化/INDOOR/传送代码骨架已接通（[building.gd](../../../stick-world/modules/building_gen/scripts/building.gd) `_lookup_children()`、`interior_mode` 数据驱动、`workshop.tscn` EnterTrigger），完整化（室内内容 + 测试）依赖建筑模块化 B 系列。

### 阶段 B0~B4 - 建筑模块化重构（详见 [建筑模块化设计.md](建筑模块化设计.md)）

> B 系列归入阶段 1/2 实施（P0 使用 placeholder 预制场景即可），随室内系统一并深化。
>
> 把"建筑 = 预制场景"重构为"建筑 = BuildingDef 配方 + BuildingGen 程序化生成"。
> 4 种材质（thatch/wood/stone/brick）+ N 个模块（锻造作坊/住所/仓库/马厩/市集摊位）+ 开口系统。

#### B0 最小显示通 ⚠️ 部分开展（阶段 1 续做）
- [~] 建 `modules/building_gen/` 目录 —— ✅ 已建
- [~] 4 个 Resource 类（`BuildingDef` / `ModuleDef` / `MaterialDef` / `RoofShape`）
- [~] 材质 `thatch` 完整定义（柱贴图数组、屋顶贴图、`RoofShape`）—— ✅ thatch 材质调试场景已存在
- [~] 模块 `forge_workshop`（纯色方块占位）—— ✅ `pg_smithy_lv1` 程序化铁匠铺雏形已存在
- [ ] `BuildingGen.generate()` 跑通：`thatch 1×1` / `thatch 3×1 empty` / `thatch 3×1 + forge` 正确显示
- [ ] `building_showcase.tscn` 测试场景并排展示各种配置供肉眼检查

#### B1 完整 thatch + wood + 模块挖洞
- [ ] `wood` 材质完整（前后墙 + 屋顶）
- [ ] 模块挖洞流程（前景墙跳过模块格）
- [ ] 适配现有 `_set_transparent` 路径（遍历所有层 `FrontLayer.Walls_Front`）
- [ ] 用 `BuildingDef` 数据驱动替换 `workshop.tscn`

#### B2 材质补齐
- [ ] `stone` 材质
- [ ] `brick` 材质
- [ ] 4 套 `RoofShape`

#### B3 模块系统完整
- [ ] 3-5 个基础模块（锻造作坊 / 住所 / 仓库 / 马厩 / 市集摊位）
- [ ] 模块自动布局 + 手动摆放约束校验
- [ ] 模块后期插入 API
- [ ] 装饰开口（DECORATION）系统

#### B4 多材质混合 + 建材解锁
- [ ] 多材质混合（每层不同材质，接口早已留好）
- [ ] 建材解锁 / 贸易系统接入

---

## 五、待补充清单

### 设计层（需创始人决策）
- [x] 兵种类型与武器属性（影响 Hitbox、伤害计算）
- [~] 各村落的 `VillageDefinition.tres` 内容（`config/villages/` 目录不存在，尚未创建）
- [x] 大建筑（宫殿）内部场景设计
- [x] 道路上随机事件机制
- [x] 战场地图的两端建筑模式（预设阵地 vs 现场部署）

### 数值层（待原型后填）
- [x] 工程量基础值（每种建筑 base_work）
- [x] 工人效率（受组织水平影响的具体公式）
- [x] 指挥链延迟系数（base_delay、tier_diff 倍率）
- [x] 战场导演情绪触发概率基线
- [x] 士气阈值（溃逃/罢工条件）

### 实现层（待补全功能）
- [x] 存档 JSON 读写（[场景与战斗/建筑与定居点.md](场景与战斗/建筑与定居点.md) §4.5.2 流程第 2-3 步，关联 P0-2）
- [x] 建筑数据表接入（：`config/buildings/buildings.tres` 已读取，P0-6 ✅）
- [~] 建筑等级升级 / 修复（`ConstructionManager.upgrade_building` / `repair_building` 仍返回"P0 未实现"，P1 再补）
- [x] Chunk 流式加载实现（`preload_chunk` / `unload_chunk`，关联 P0-1，保持 P0 暂缓）
- [x] 战略图 `close_strategic_map` 状态恢复（关联 P0-2，见 §四 0.9）
- [x] 战略图 `api.initialize(manifest_path)` 数据加载（关联 P0-2，见 §四 0.9）
- [x] 室内场景透明化与 `IndoorMap` 骨架（透明化 + EnterTrigger + 数据驱动均已接通；完整化归阶段 1/2，见 §四 0.9 注）

### 技术债（P0 阶段可接受，详见 §六）
- [x] LOD 系统（远距离单位简化 AI）- P0 全部完整 AI
- [x] 多战场并行性能优化 - P0 只单战场
- [x] 天气 Shader 精细化 - P0 仅基础光照变化
- [x] 其他技术债见 §六「已知问题与技术债」P0-1 ~ P0-17

---

## 六、已知问题与技术债

> 本章节由对照代码实现的审计产生，按严重程度排序。每项含：现象、根因、影响、修复方案。
> 审计方法：grep 关键装配点 + 读取核心脚本 + 检查测试结果文件时效性。
> 修复后保留记录但标记 ✅；新增项在末尾追加（P0-10 起）。
> **编号规则**：以本文档为准；其他文档引用时指向本文档编号（历史版本中 P0-1/P0-11 曾在 GDD差异分析/待办事项 中指代不同问题，已统一）。

### P0-1 🟡 Chunk 流式加载为空壳（中等，标题误导）

- **现象**：`SceneLoader.preload_chunk()` / `unload_chunk()` 只有 TODO 注释，直接 emit 信号不实际加载（见 [scene_loader.gd:236-246](../../../stick-world/modules/world/scripts/loading/scene_loader.gd)）。
- **根因**：阶段 0.8 实际实现的是"地图间出口触发器切换"（`ChunkTrigger.target_map_id` -> `travel_to_map`），而非"地图内 Chunk 分段加载"。两者被混为一谈。
- **影响**：当前每张地图是整张一次性加载。P0 阶段单张地图足够小，性能可接受。但文档标题"Chunk 流式加载"有误导性，让人以为已实现。
- **修复方案**：
  - 文档已在本 v7 中修正措辞（[场景与战斗/地图与场景图.md](场景与战斗/地图与场景图.md) §3.2、阶段 0.8）
  - 代码暂不改，等真正需要大地图时再补全 `preload_chunk` / `unload_chunk`
  - 可考虑把 `preload_chunk` / `unload_chunk` 改名为 `_stub_preload_chunk` 或加 `push_warning("Chunk 流式加载未实现")` 避免静默失败

### P0-2 ✅ 已实现 战略图闭环（close_strategic_map / initialize）

- **现象（原始）**：[world_map/api.gd](../../../stick-world/modules/world_map/api.gd) 的 `close_strategic_map()` 注释写"TODO: 完整实现需要恢复场景图的 CameraRig 和 InputDispatcher"，当前仅做 ModalOverlay 隐藏。`api.initialize(manifest_path)` 也是 TODO 占位。
- **现状**：`close_strategic_map()` 委托控制器 close（控制器缺失时兜底发射 `strategic_map_closed`），`initialize(json_path, base_dir)` 已完整加载 L1WorldData 并注入渲染器/相机。消费端现状见 [世界地图数据流.md](世界地图数据流.md) §8.4。

### P0-3 ✅ 已删除 WorldMapController 废弃文件（轻微，代码清理）

- **现象（原始）**：[world_map_controller.gd](../../../stick-world/modules/world_map/scripts/strategic_map_controller.gd) 文件头注释"已废弃"，但仍存在于代码库。
- **影响**：可能误导后续开发。无功能影响（未被引用）。
- **修复方案**：删除文件，或移到 `prototypes/` 目录留档。

### P0-4 ✅ 已修复 StickmanRig IK 日志噪音（轻微，体验问题）

- **现象**：[stickman_rig.gd](../../../stick-world/modules/units/scripts/rig/stickman_rig.gd) 每次实例化打印约 18 行 IK 修改器详细信息（`[IK] stack.enabled = true...`）。5 个 NPC + 1 玩家 = 108 行日志。`stickman_entity.gd:171` 还有 `print("[StickmanEntity] foot_offset = ...")` 残留。
- **影响**：日志噪音严重，掩盖真正有用的输出。无法通过日志级别关闭（是 `print()` 非 `push_warning`）。
- **现状**：日志已改为 `push_warning`（不再静默刷屏）；`foot_offset` print 残留已删除。

### P0-5 ✅ 已修复 开场 IK workaround 是 hack（轻微，技术债）

- **现象**：[stickman_entity.gd:176-193](../../../stick-world/modules/units/scripts/stickman_entity.gd) 前 0.25s 模拟向右移动触发 IK 解算，然后重置位置。
- **根因**：Skeleton2D + IK 修改器在 `_ready()` 后首帧未正确解算，需要"骗"一下 IK 让它工作。
- **影响**：功能正常但设计不优雅。若 IK 系统升级可能失效。
- **现状**：workaround 已删除——改为 IK 修改器 deferred 启用（`stack.enabled=false` 后 `call_deferred` 开启），不再需要开场模拟移动来欺骗 IK 初始化。

### P0-6 🟡 部分修复 ConstructionManager 硬编码数值

- **现象（原始）**：[construction_manager.gd](../../../stick-world/modules/construction/scripts/construction_manager.gd) 中 `width=2`、`total_work=10.0` 硬编码，注释写"将来从建筑配置表读取"。
- **现状**：footprint_w / base_work / cost 已从 `config/buildings/buildings.tres` 按 `def_id` 读取；`total_work` 仍硬编码 `8.0`（construction_manager.gd:213，后续由 Excel build_time 驱动）。

### P0-7 🟢 GdUnit4 未引入（规范冲突，决策已定）

- **现象**：[workspace rule.md](../../../.trae/rules/rule.md) 强制要求"生成的任何功能代码，必须附带相应的 GdUnit4 单元测试"，但项目使用自研 `TestRunner`。
- **决策**：P0 稳定期**暂不迁移** GdUnit4（迁移成本高、扰动未稳代码）；自研 TestRunner 已加固（零断言守卫 + async 内建 + 断言扩充，见 [tests/README.md](../../../stick-world/tests/README.md)）。GdUnit4 列为 P1 引入项，待稳定后一次性迁移。**待办**：更新 rule.md 措辞为"建议"而非"必须"。

### P0-8 🟢 RefCounted 持有 Node 引用的生命周期风险（轻微，架构隐患）

- **现象**：[battle_ai_director.gd](../../../stick-world/modules/combat/scripts/battle/battle_ai_director.gd) `extends RefCounted` 但持有 `BattleInstance`(Node) 引用。
- **核实**：`organization_manager.gd` 虽 `extends RefCounted` 但被正确使用（game_root 用 `.new()` 创建，未当 Node 挂树），不再列为问题。
- **影响**：RefCounted 生命周期不与 Node 绑定，节点 `queue_free` 后仍调用会访问已释放节点。当前靠 `is_instance_valid()` 检查规避。
- **修复方案**：长期应改为 Node 挂到场景树，或用弱引用（WeakRef）。P0 阶段 `is_instance_valid` 检查足够。

### P0-9 ✅ 已修复 资源系统未接入

- **现象（原始）**：`modules/resources/` 代码完整但完全孤立：GameRoot 无装配、无消费方、无 UI。
- **现状**：GameRoot 已装配 `_setup_resources_system()`（资源发放 + ConstructionManager 注入 + ResourceBar UI 已接），建造扣资源已通；`base_price` 已改名 `initial_price`，`res_food` 已删除。剩余 TODO：运输损耗、动态价格、价格更新（经济系统 P1 范围）。

### P0-10 ✅ 已修复 workshop 缺 EnterTrigger

- **现象（原始）**：`workshop.tscn` 缺 `EnterTrigger` 子节点。
- **现状**：已补节点。（室内系统完整化见 §四 0.9 注）

### P0-11 ✅ 已修复 资源字段命名与清理

- **现象（原始）**：`base_price` 字段名未重命名、`res_food` 未删除。
- **现状**：已改名 `initial_price`（[resource_state.gd](../../../stick-world/core/entities/resource_state.gd)）；`res_food` 已从 `resources.tres` 删除（P0 只接入 4 种基础资源）。

### P0-12 🟢 延后 战斗结束判定（规划延后，不在 P0 范围）

- **现象**：战斗以全灭即结束，无胜负收束能力（溃散判定/尸体处理/溃兵回场）。
- **现状**：P0 战斗基础已可用，收敛能力依赖战斗与碰撞系统稳定后实施，列为低优先级待办（见 [待办事项.md](../../项目/待办事项.md)）。

### P0-13 ✅ 已关闭 headless 相机关闭测试（方案撤销）

- **原方案**：headless 模式自动关闭相机相关逻辑。
- **现状**：方案已撤销不实施（headless 测试环境无此需求）。

### P0-14 ✅ 已修复 启动受旧存档影响

- **现象**：启动时自动读取旧存档，玩家出生位置/地图状态异常（旧存档与新代码不兼容），甚至灰屏。
- **现状**：原型阶段每次启动都是新游戏（不自动读档），旧存档不再影响启动；手动存档/读档（SaveManager / SavePanel / quick_save / quick_load）保留。

### P0-15 ✅ 已修复 放置校验（选址范围内有实体则拒绝）

- **现象**：玩家站在建筑选址上时，工地/建筑生成后玩家被困内部无法移动。
- **修复（源头杜绝）**：`start_construction_at`/`spawn_operational_building` 的 `_entity_blocking`——选址 ±40px 内有实体（玩家/NPC）时拒绝放置，玩家不会被罩进建筑。
- **补充**：曾尝试"生成后主动把玩家推出"（push_out_of_rect），会把玩家弹进隔壁建筑造成二次卡死，已废除。

### P0-16 ✅ 已修复 读档恢复缺陷 + 敲击锁定

1. **敲击时可移动**：`_handle_player_input` 不检查 `_player_build_timer` → 锁定 1.8s 内禁止移动。
2. **读档工地无碰撞箱**：`_restore_project_from_row` 补 `_create_barrier()` 重建障碍。
3. **读档工地材料满**：存档记录 `material_progress` 真实值（旧简化强制 1.0 已废除）。
4. **读档 NPC 不帮忙**：恢复项目补注册派工池 `_assigner.add_project`。

### P0-17 ✅ 已修复 脱困机制 + 工人站位

1. **脱困**：H 键 / HUD"脱困(H)"按钮——随机传送到附近空旷地带（不依赖附身系统，直接作用于当前玩家实体）。
2. **工人站位**：`behavior_work.WORK_OFFSET_Y = 40.0`（units/scripts/ai/behavior_work.gd:26）——工人站在建筑基线下方 40px 处敲击，不再"隔空"施工。

---

## 七、实现亮点（比文档更详细的地方）

> 审计中也发现部分实现比文档描述更完善，记录于此，避免后续迭代时误判。

| 模块 | 亮点 | 代码位置 |
|------|------|---------|
| VillageMap 草地纹理 | Stochastic Tiling + FBM 噪波 + 双层错缝混合 + 边缘距离加权混合的 GLSL Shader，远超文档"重复地面纹理"的描述 | [village_map.gd](../../../stick-world/modules/world/scripts/map/village_map.gd) `_load_ground_texture()` / `_setup_ground_shader()` |
| BehaviorWander | 基于 Reynolds Steering 的漫游行为，含卡住检测（0.2s 移动<3px 视为卡住）+ 120~240°掉头恢复 + 冷却防抽搐 + 边界规避力 | [behavior_wander.gd](../../../stick-world/modules/units/scripts/ai/behavior_wander.gd) |
| CameraRig 弹回过渡 | 1.5 秒缓动过渡（`RETURN_SMOOTHING=2.5, RETURN_TRANSITION_TIME=1.5`），比文档"不拉回角色"的描述更平滑 | [camera_rig.gd](../../../stick-world/modules/world/scripts/camera/camera_rig.gd) `_return_camera()` |
| DebugOverlay | 6 个绘制器 + 图例面板 + FPS/实体数显示 + F3 切换，实现比文档 [场景与战斗/UI.md](场景与战斗/UI.md) §10.5 描述更完整 | [debug_drawers.gd](../../../stick-world/modules/debug_GUI/scripts/debug_drawers.gd) |
| 图片格式自动检测 | VillageMap 加载图片时不依赖扩展名，按文件头魔术字节判断 JPEG/PNG/WebP | [village_map.gd](../../../stick-world/modules/world/scripts/map/village_map.gd) `_load_ground_texture()` |

---

*本文档为 P0 阶段的工程架构基线。后续阶段（P1 物流/扩张、P2 法律/成就）将基于此架构扩展。*