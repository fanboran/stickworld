# 面试防御手册

> 配套《简历-游戏客户端实习-范博然.md》使用：简历每条 bullet 在这里对应「30 秒答案 + 代码坐标 + 追问预案」。
> 所有内容来自你自己的仓库（代码行号、架构文档），可放心引用，无需背"标准答案"。
> 标注 **[TODO]** 的是还没验证/没补课的部分，补完再面试。

## 一、战斗与 AI

### 命令三级裁决（玩家 > 编队 > 单位本能）

**30 秒答案**：编队是常态归属，玩家命令是临时租借——手动号令执行期间编队自动化冻结，执行完自动归还编制。裁决发生在每次 AI 决策入口的优先级瀑布最顶端。例外是溃逃：士气崩了的单位连玩家命令都不听，"溃兵不可战"。

- 代码：`stick-world/modules/units/scripts/ai/ai_controller.gd` 212-233 行（命令覆盖层）、361 行（手动号令期间编队姿态调整不生效）
- 追问预案：**[TODO]** 玩家命令的目标失效（障碍物被抢挖走）算不算"执行完"？去代码里确认命令失败/中止是否释放控制权

### 在飞伤害账本

**30 秒答案**：弓手出弓瞬间，箭矢的预估满伤害登记到目标身上的计数器；箭矢终态（命中或插地落空）时扣减，一箭只结算一次。其他弓手开火前先看这个数——在飞伤害已足以杀死目标就不再放箭。顺带记录"最后一次被瞄准时刻"供受击规避用。

- 代码：`stick-world/modules/units/scripts/entity/incoming_threat_ledger.gd`（全文仅 35 行，面试前读一遍）
- 面试话术："**我在试玩里注意到弓手集火浪费箭，定向让 AI 实现了登记-结算机制，review 后合入**"——发现问题是人的功劳，AI 是打字员。**绝不要说"AI 顺手就做了"**
- 背景知识：过度击杀（overkill）是 RTS 经典问题，《最高指挥官》等做过系统级方案——被追问时可以提

### 命中帧结算

**30 秒答案**：每个武器的动画标记了命中帧时间点，数据存在兵种档案表里；战斗模块订阅动画事件，播放位置越过时间点的那一帧才结算伤害。表现侧和战斗侧读同一张表，换武器时命中帧跟着重新解析——避免"播矛的动作、按剑的命中帧结算"这种错配。副产品是可打断窗口：命中帧之后的尾段按比例可被打断，前摇被打断则伤害不发生。

- 代码：`stick-world/modules/units/scripts/entity/weapon_mount.gd` 17-22、113、160 行；`stickman_entity.gd` 36-37、1246 行
- 设计动机（被问"为什么不按下就掉血"）：打击感的真实性——按下就掉血玩家看得出假；命中帧延迟带来可打断窗口，是取消/霸体/弹反类机制的根基

### 兵种行为档案

**[TODO]** 读 `behavior_profiles.gd` 后补 30 秒答案（侧翼包抄、风筝保距等策略的参数化实现）

## 二、UI 系统

### 为什么自建槽位系统（而不是用 Godot 原生布局）

**30 秒答案**（bug → 规则 → 架构）：起点是个 bug——建造按钮在玩家那里消失了，日志零报错。查下来是代码回退路径用 `Control.new()` 建控件，丢了场景里的 anchor，按钮被摆进 0 尺寸容器的负坐标，静默不可见。修复后把它升华成规则：**场景是布局唯一真相源，代码建全屏 UI 根一律走 `UIKit.full_rect()`，所有部件挂具名槽（HudOverlay 等），槽提供确定坐标系，模块间禁止跨模块 get_node**。规则写进了项目规范，再没复发。

- 文档：`docs/技术/架构/场景与战斗/UI.md` §10.7、§10.7.4；`AGENTS.md` 核心行为指令 #5
- ⚠️ **红线**：绝不要说"Godot 原生槽位美术自由度低"——Godot 没有原生"槽位"概念，会被当场戳穿

### 最头疼的 bug（行为面试题）

用上面的建造按钮故事，按 STAR 填空：现象（UI 无声消失、零报错）→ 定位（节点在树里、打坐标断点发现负坐标）→ 根因（回退路径 `Control.new()` 丢 anchor）→ 修复（当场：统一 full_rect 工厂；防复发：立规则进规范）→ 教训（回退路径平时跑不到、一出事就静默，代码创建 UI 必须和场景创建走同一套锚定逻辑）

## 三、动画与渲染

### 命中与音效由动画事件驱动

见上文"命中帧结算"，同一个故事的两面。

### 物理帧 / 渲染帧平滑

**[TODO]** 未验证。线索：原版简历写过"速度采样在物理帧做差分、渲染帧读指数平滑值，消除帧率不匹配导致的抖动"——去 `modules/units/scripts/rig/` 相关代码确认后，按此模板补：为什么需要（物理帧与渲染帧频率不一致，渲染帧直接读物理量会抖）→ 怎么做（物理帧差分、渲染帧指数平滑）→ 效果（消除抖动）

## 四、工程化

### 测试体系

**事实卡**：60+ 脚本、单元/集成/冒烟三层、单元层 2 秒跑完、pre-commit 强制、支持按 git 改动增量执行。命令：`bash stick-world/tests/run_all.sh`（全量）/ `-Changed`（增量）。

**合法话术**："测试规范是我定的，AI 按规范写，我跑红绿把关合入"——**前提是至少能讲出一个具体测试**。**[TODO]** 面试前跑一次 `run_all.sh`，从输出里挑一个测试文件读懂（测什么、断言什么、为什么红过），补进这里

### SQLite 存档

**事实卡**（来自原版简历，去 `core/autoload/save_manager.gd` 核对后转正）：`PRAGMA user_version` 管理 schema 版本；版本高于当前游戏的存档拒绝读取、低于的走迁移分支；存档器经事件总线广播、各模块在回调里写自己的表，存档代码不 import 任何业务模块。

## 五、尖锐题预案

### "这项目多少是 AI 写的？"

**参考答案**："代码产出大概一半来自 AI 协作，但主导权在我：玩法设计、架构决策、模块边界、验收标准是我的，AI 是执行工具。我用项目规则文件约束它的行为边界，每个功能过了 60 多个测试脚本才允许合入。类比的话，我扮演的是定规范、做验收的角色。"——核心姿态：**人握主导权，AI 是工具**。绝不说"AI 顺手做的/我也看不懂"

### "没有团队协作经验？"

**参考答案**："个人项目里我搭建了完整的协作基础设施——模块 API 契约、规范文档、约定式提交、测试门禁，这些是为'别人能接手我的代码'准备的。另外我和 AI 的协作本身就是一套完整流程：任务拆分、下发、review、合入——我认为这是协作流程的现代形态。"（如被追问高中/大学团队经历：物理竞赛获奖的学习能力故事在这里讲）

### "资产是哪来的？版权怎么办？"

**参考答案**："Demo 阶段我执行明确的资产政策——机制可学、资产可搬：参考游戏的机制通过反编译研读后用自研代码落地，机制本身不受版权保护；音效有 30 多个来自 Terraria 和 Stick War 的提取件作占位，每一件都登记在仓库的素材替换清单里，正式版前全量替换成自研合成或 CC0 素材，替换 checklist 也写好了。这其实就是商业开发的常见流程——占位资产先行、正式资产后换，区别是我把登记和替换做成了流程而不是口头承诺。"

### "为什么用 Godot 不用 Unity？我们公司是 Unity"

**参考答案**："个人项目选 Godot 是因为它轻量、开源、上手快，适合一个人快速验证玩法。引擎的核心概念是相通的——场景树对应 Unity 的 GameObject 层级、信号对应事件订阅、动画状态机两边都有对应物。我的项目经历证明的是'我能用引擎把游戏做出来'，切 Unity 主要是在换 API，我的 C++/C# 基础也在那。"

## 六、线下作业清单（按优先级）

1. `git log --oneline | grep -i "fix\|修复"` 滚一遍，挑 3 个有印象的读 diff，凑 3 个 STAR 故事
2. 读 `incoming_threat_ledger.gd` 全文（35 行，五分钟）
3. 读 `ai_controller.gd` 212-260 行段（命令覆盖与优先级瀑布）
4. 跑一次 `bash stick-world/tests/run_all.sh`，挑一个测试读懂（补手册 §四）
5. 对照自我介绍骨架练 3 遍（60-90 秒，录音回听）
6. 本手册所有 **[TODO]** 补完

## 七、BOSS 直聘打招呼语

**标准版**（设为默认）：

> 您好！2027 届计算机本科，求游戏客户端开发实习，随时到岗、可实习 6 个月。个人游戏 Demo：独立开发 Godot 4 策略战争游戏，2 个月每天 4 小时投入，完整代码在 GitHub，README 有实机图片；有算法基础，C++ 蓝桥杯国赛三等奖。

**针对性版**（认真投的岗位手动改）：

> 您好！看到贵司【职位名】在招，三个匹配点：① 做过完整游戏——独立开发 Godot 4 策略战争游戏，2 个月每天 4 小时，完整代码在 GitHub；② 有算法基础——C++ 蓝桥杯国赛三等奖；③ 工程习惯——模块化 + 60+ 测试脚本挂 pre-commit。2027 届，随时到岗、可实习 6 个月。

**短版**（字数受限场景）：

> 2027 届求游戏客户端实习：独立完成 Godot 4 策略战争游戏 Demo（代码在 GitHub），有算法基础，蓝桥杯国赛三等奖，随时到岗、可实习 6 个月。

**使用规则**：

1. "随时到岗、6 个月"是 HR 第一死门，永远放显眼位置
2. 提交数、可查这类细节不进招呼语——招呼语只负责让人点开附件，"700+ 提交"的证据由简历和 README 承担
3. JD 指明 Unity 时，针对性版加第 ④ 点："引擎概念相通、有 C# 基础，可快速转 Unity"
4. 已读不回隔 2-3 天跟一句："之前投递的客户端实习岗还在招吗？随时可到岗"
5. 链接不放消息正文（平台对外链有风控），GitHub 入口写在简历头部
